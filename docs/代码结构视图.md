# 代码结构视图（2026-04-13 最新）

> 基于三层架构规则：`presentation` → `service` → `data`，层间依赖只允许从上到下。
> 所有层均可引用 `shared`。同层内部互引允许。
> **tsc 全量编译 0 ERROR，架构检查 0 违规。**

---

## 一、全局概览

| 层级 | 文件数 | 代码行数 | 占比 |
|------|:------:|:-------:|:----:|
| **shared** | 12 | 1,410 | 3.8% |
| **data** | 8 | 794 | 2.1% |
| **service** | 14 | 13,992 | 37.8% |
| **presentation** | 31 | 20,772 | 56.2% |
| **入口 (index.ts)** | 1 | 85 | 0.2% |
| **合计** | **66** | **37,053** | 100% |

---

## 二、文件树（按层级 + 职责分组）

```
src/
├── index.ts                          (85行)  应用真入口，按层级顺序导入所有模块
│
├── shared/                           ── 纯函数/常量/类型，任何层均可引用 ──
│   ├── constants.ts                  (46行)  全局常量（ID前缀、弹窗ID、Toast分类枚举）
│   ├── data-constants.ts             (46行)  数据层常量（存储键名、隔离码归一化工具）
│   ├── defaults.ts                   (35行)  默认设置值（TS）
│   ├── defaults-json.js              (302行) 默认设置值（JSON大对象，含表格模板/剧情默认）
│   ├── env.ts                        (41行)  运行环境变量（topLevelWindow、storage策略）
│   ├── host-api.ts                   (15行)  宿主平台API引用（SillyTavern/jQuery/toastr）
│   ├── html-helpers.ts               (19行)  HTML工具函数（escapeHtml）
│   ├── idb-import-temp.ts            (107行) IndexedDB导入暂存（跨层共用的存储工具）
│   ├── json-helpers.ts               (23行)  JSON安全解析/序列化
│   ├── template-preset-utils.ts      (85行)  模板预设纯函数工具
│   ├── text-optimization.ts          (262行) 文本优化算法（去标点/关键词提取/段落匹配）
│   └── utils.ts                      (429行) 通用工具函数集（日志/深合并/颜色/正则/校验）
│
├── data/                             ── 数据存取，不含业务逻辑 ──
│   ├── models/                       类型定义（纯接口，无运行时代码）
│   │   ├── settings-model.ts         (59行)  Settings_ACU / PlotSettings_ACU 接口
│   │   ├── table-data.ts             (86行)  SheetUpdateConfig / TableData 接口
│   │   └── template-model.ts         (66行)  TemplatePresetsStore / TemplatePresetEntry 接口
│   ├── repositories/                 数据仓库（纯 CRUD，不持有业务状态）
│   │   ├── profile-repo.ts           (96行)  配置档案读写（全局元数据/隔离档案）
│   │   └── isolation-repo.ts         (71行)  数据隔离历史管理
│   └── storage/                      存储后端（底层 I/O）
│       ├── tavern-storage.ts         (297行) 酒馆桥接 + IDB缓存 + 配置存储统一入口
│       ├── config-storage.ts         (36行)  配置持久化（persistSettingsToStorage）
│       └── chat-history.ts           (83行)  聊天历史数据读取工具
│
├── service/                          ── 业务逻辑编排（读数据→处理→写数据）──
│   ├── ai/                           AI调用相关
│   │   ├── api-call.ts               (143行) AI API调用编排（流式/自定义/酒馆模式）
│   │   └── prompt-builder.ts         (1,629行) 提示词构建器（消息组装/模板渲染/变量替换）
│   ├── optimization/
│   │   └── content-optimization.ts   (705行) 正文优化逻辑（AI调优/解析重试/缓存）
│   ├── plot/                         剧情推进
│   │   ├── plot-state.ts             (89行)  剧情运行时状态 + 纯逻辑（构建默认提示词组）
│   │   └── plot-logic.ts             (776行) 剧情核心逻辑（预设管理/任务归一化/标签规则）
│   ├── runtime/                      运行时基础设施
│   │   ├── state-manager.ts          (234行) 全局状态管理（settings/currentData/API引用）
│   │   └── helpers-remaining.ts      (4,262行) 待拆分的大型辅助函数集（填表/更新/规划）
│   ├── settings/
│   │   └── settings-service.ts       (548行) 设置读写服务（加载/保存/迁移/作用域应用）
│   ├── summary/
│   │   └── merge-logic.ts            (309行) 摘要合并逻辑（触发判定/批次准备/执行/收尾）
│   ├── table/
│   │   └── table-service.ts          (280行) 表格数据操作（加载/保存/首次初始化检测）
│   ├── template/
│   │   ├── chat-scope.ts             (1,409行) 聊天作用域模板管理（隔离/快照/合并）
│   │   └── template-preset-service.ts(369行) 模板预设CRUD服务
│   └── worldbook/
│       ├── pipeline.ts               (958行) 世界书数据管线（合并/完整性修复/通知）
│       └── injection-engine.ts       (2,281行) 世界书注入引擎（条目解析/放置计算/注入）
│
├── presentation/                     ── DOM操作/UI渲染/用户交互 ──
│   ├── bootstrap/                    启动与注册
│   │   ├── init.ts                   (503行) 主初始化流程（事件监听/API桥接/首次加载）
│   │   ├── api-registry.ts           (2,040行) API注册表（暴露给酒馆前端的全部方法）
│   │   └── startup.ts                (59行)  启动入口（延迟加载/版本检测）
│   ├── state/
│   │   └── ui-refs.ts                (114行) UI jQuery元素引用集中管理
│   ├── window/                       窗口系统
│   │   ├── window-system.ts          (472行) ACU窗口管理器（创建/关闭/拖拽/缩放）
│   │   └── window-styles.ts          (434行) 窗口主题样式（注入CSS/暗色切换）
│   ├── theme/
│   │   └── toast.ts                  (287行) Toast通知系统（样式注入/去重/过滤/分类）
│   ├── components/                   可复用UI组件
│   │   ├── optimization-ui.ts        (1,356行) 正文优化UI（对话/差异/应用/预设编辑）
│   │   ├── worldbook-selector.ts     (699行) 世界书选择器UI（懒加载/搜索/多选）
│   │   ├── plot-editors.ts           (501行) 剧情编辑器UI（提示词段/任务列表/排序）
│   │   ├── template-preset-ui.ts     (264行) 模板预设UI（选择器/保存/删除/导入导出）
│   │   ├── update-status-display.ts  (251行) 更新状态面板UI
│   │   ├── import-status-ui.ts       (198行) 导入状态UI（文件读取/拆分/进度）
│   │   ├── table-selector.ts         (188行) 表格选择器UI（手动/导入两种模式）
│   │   ├── status-display.ts         (154行) 设置同步到UI（全量刷新/填表状态）
│   │   ├── plot-planning-ui.ts       (126行) 剧情规划UI封装（进度toast/中止按钮）
│   │   ├── pipeline-ui-helpers.ts    (52行)  数据管线UI包装（刷新后自动更新UI）
│   │   └── settings-ui-helpers.ts    (32行)  设置UI便捷函数（加载刷新/保存通知）
│   ├── pages/                        页面级UI
│   │   ├── popup-bindings.ts         (2,747行) 主弹窗事件绑定（全部按钮/输入框/切换）
│   │   ├── main-popup.ts             (2,503行) 主弹窗HTML生成（完整DOM结构）
│   │   ├── visualizer.ts             (1,473行) 可视化编辑器入口（创建窗口/数据绑定）
│   │   ├── visualizer-main.ts        (1,151行) 可视化编辑器主区域（表格渲染/编辑/保存）
│   │   ├── popup-helpers.ts          (575行) 弹窗辅助函数（剧情设置读写/预设操作）
│   │   └── visualizer-sidebar.ts     (191行) 可视化编辑器侧边栏（表格列表/排序/操作）
│   └── triggers/                     用户操作触发器
│       ├── settings-ui-sync.ts       (1,313行) 设置UI同步（API配置/模型加载/预设管理）
│       ├── update-trigger.ts         (796行) 更新触发器（手动/自动更新编排）
│       ├── data-admin-ui.ts          (723行) 数据管理UI操作（导出/导入/重置/模板）
│       ├── update-process.ts         (716行) 更新流程（单表/批量/流程控制）
│       ├── import-process.ts         (481行) 导入流程（TXT拆分/注入/世界书操作）
│       ├── auto-loop.ts              (250行) 自动循环控制（启动/停止/重试/结束回调）
│       └── admin-ui.ts               (123行) 管理操作（导入导出配置/清理数据）
```

---

## 三、依赖关系矩阵

```
引用方向    → shared   → data     → service  → presentation
──────────────────────────────────────────────────────────────
shared      ✅ 同层     ✖         ✖         ✖
data        ✅ 允许     ✅ 同层    ✖         ✖
service     ✅ 允许     ✅ 允许    ✅ 同层    ✖
presentation✅ 允许     ✖ 禁止    ✅ 允许    ✅ 同层
```

### 实际依赖流向（已验证合规）

| 来源层 | 目标层 | 引用路径示例 | 合规 |
|--------|--------|-------------|:----:|
| data → shared | `profile-repo` → `shared/utils` | ✅ |
| data → data | `config-storage` → `tavern-storage` | ✅ |
| service → shared | `state-manager` → `shared/host-api` | ✅ |
| service → data | `settings-service` → `data/storage/config-storage` | ✅ |
| service → service | `plot-logic` → `settings-service` | ✅ |
| presentation → shared | `toast` → `shared/constants` | ✅ |
| presentation → service | `init` → `service/runtime/state-manager` | ✅ |
| presentation → presentation | `popup-bindings` → `components/optimization-ui` | ✅ |

**违规路径：0 条**

---

## 四、各层职责说明

### shared 层（纯函数 / 常量 / 类型）
- **零副作用**：所有文件只包含纯函数、常量定义和类型声明
- **零依赖**：不 import 任何其他层（仅同层互引）
- `host-api.ts` 特殊：声明宿主API引用变量（`SillyTavern_API_ACU` 等），由 `state-manager.ts` 在运行时赋值

### data 层（数据存取 CRUD）
- **不含业务逻辑**：只做读/写/删/格式化
- **不持有业务状态**：需要 settings 的函数通过参数接收（如 `saveCurrentProfileTemplate_ACU(tpl, settings)`），不通过 DI 或回调间接获取
- `tavern-storage.ts`：统一存储入口（酒馆设置桥接 → IDB缓存 → localStorage 降级）
- `models/`：纯 TypeScript 接口，全项目共用的类型定义

### service 层（业务逻辑编排）
- **不操作 DOM**：所有 UI 操作由 presentation 层完成
- `helpers-remaining.ts`（4,262行）：待拆分的大型辅助函数集，包含填表逻辑、更新编排、规划执行
- `state-manager.ts`：全局运行时状态（settings、当前数据、API引用），通过 `_set_` 函数提供可控赋值

### presentation 层（UI渲染 / DOM操作 / 用户交互）
- **不直接访问 data 层**：全部通过 service 层间接操作
- `ui-refs.ts`：集中管理所有 jQuery 元素引用，通过 `_assignUIPlaceholders_ACU` 统一赋值
- `api-registry.ts`（2,040行）：暴露给酒馆前端的完整API注册表
- `main-popup.ts`（2,503行）+ `popup-bindings.ts`（2,747行）：主弹窗的 HTML 生成和事件绑定

---

## 五、已知的改善空间

### 5.1 大文件待拆分
| 文件 | 行数 | 建议 |
|------|:----:|------|
| `service/runtime/helpers-remaining.ts` | 4,262 | 按职责拆分为独立 service 模块 |
| `presentation/pages/popup-bindings.ts` | 2,747 | 按功能区域拆分绑定 |
| `presentation/pages/main-popup.ts` | 2,503 | 按UI区域拆分HTML生成 |
| `presentation/bootstrap/api-registry.ts` | 2,040 | 按领域拆分API注册 |
| `service/worldbook/injection-engine.ts` | 2,281 | 按阶段拆分注入流程 |

### 5.2 类型安全（HINT 级）
- ~300+ 个 implicit any 参数（从 JS 迁移 TS 的历史遗留）
- ~50 个 unused import / unused variable
- ~40 个 JSDoc 类型待迁移到 TS 签名
- 这些不影响运行时，适合渐进式改善

### 5.3 代码健康指标
| 指标 | 当前状态 |
|------|---------|
| tsc ERROR | **0** |
| 架构违规 | **0** |
| 空壳/门面/孤儿文件 | **0** |
| 重复 re-export | **0** |
| 无消费者的死代码 | **0** |
| DI 反模式 | **0** |
| `@ts-ignore` | **0** |
| `if(true)` / `if(false)` | **0** |

---

*文档生成时间：2026-04-13 23:02*
*基于 commit: 001-three-layer-refactor 分支*
