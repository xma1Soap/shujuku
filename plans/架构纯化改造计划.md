# 架构纯化改造计划

> 前置文档：`docs/架构违规报告.md`（84 条违规 import）  
> 目标：消除全部违规，达到严格单向 `presentation → service → data → shared`  
> 分支：从 `001-three-layer-refactor` 拉出 `002-arch-purify`  
> 约束：每个阶段结束必须 `npm run build` 通过 + 功能等价

---

## 改造原则（从违规报告第六节搬过来，执行时对照）

```
presentation  只做 UI 编排，调 service 拿结果后自己更新 DOM
service       纯逻辑 + 返回值，绝不 import presentation
data          纯 CRUD + 参数化，绝不 import service / presentation
shared        无状态工具/常量/类型/宿主 API 引用，被所有层引用
```

---

## 阶段 P0：基础设施（不改任何业务代码）

> 在动任何业务文件之前，先把"屋子"搭好。

- [ ] P0-01 创建 `shared/host-api.ts`：导出宿主 API 引用（`SillyTavern_API`、`TavernHelper_API`、`jQuery_API`、`toastr_API`）和初始化 setter。这些目前在 `service/runtime/state-manager.ts` 里，要移到 shared 层，因为三层都可能需要（presentation 用 jQuery 操作 DOM，service 可能用 SillyTavern_API 读数据，data 用宿主存储 API）。
- [ ] P0-02 创建 `service/state/app-state.ts`：导出业务状态（`settings_ACU`、`currentJsonTableData_ACU`、`currentChatFileIdentifier_ACU`、`coreApisAreReady_ACU`、`allChatMessages_ACU`、`lastTotalAiMessages_ACU`、`independentTableStates_ACU`、`getCurrentIsolationKey_ACU`）和对应 setter。从 `state-manager.ts` 搬过来。
- [ ] P0-03 创建 `presentation/state/ui-refs.ts`：导出所有 `$xxx_ACU` jQuery 引用（50+ 个）和 `_assignUIPlaceholders_ACU` 函数。从 `state-manager.ts` 搬过来。
- [ ] P0-04 清空 `service/runtime/state-manager.ts`：它现在只做 re-export 门面（从 `shared/host-api`、`service/state/app-state`、`presentation/state/ui-refs` re-export 全部符号）。这样所有旧的 `import { ... } from 'state-manager'` 暂时不用改。
- [ ] P0-05 创建 `scripts/check-arch.sh`：lint 脚本，检查 import 违规（`service/ → presentation/`、`data/ → service/`、`data/ → presentation/`、`presentation/ → data/`），输出违规数。后续每阶段结束跑一次。
- [ ] P0-06 构建验证 + `check-arch.sh` 基线（记录改造前违规数）

检查点：构建通过，state-manager 拆分完成，check-arch.sh 可用

---

## 阶段 P1：data 层纯化（消除 data → service / data → presentation 共 14 条）

> 优先级最高。data 层最底层，它干净了上层才能干净。

### P1-A：data 层参数化（消除 data → state-manager 的 7 条）

策略：data 层函数不再 import `app-state.ts`，改为通过参数接收 `settings`、`isolationKey` 等。

- [ ] P1-01 `data/storage/config-storage.ts`：`settings_ACU` → 参数传入
- [ ] P1-02 `data/models/table-data.ts`：`currentJsonTableData_ACU` → 参数传入
- [ ] P1-03 `data/repositories/character-settings-repo.ts`：`settings_ACU`, `currentChatFileIdentifier_ACU` → 参数传入
- [ ] P1-04 `data/repositories/isolation-repo.ts`：`settings_ACU` → 参数传入
- [ ] P1-05 `data/repositories/profile-repo.ts`：`settings_ACU` → 参数传入
- [ ] P1-06 `data/repositories/template-preset-repo.ts`：`settings_ACU`, `SillyTavern_API_ACU`, `TavernHelper_API_ACU` → 参数传入（宿主 API 改为从 `shared/host-api` import）
- [ ] P1-07 修改所有调用方（service 层和 presentation 层），在调用 data 函数时传入参数

### P1-B：data/repositories/table-repo.ts 拆分（消除 5 条 service 依赖 + 1 条 presentation 依赖）

这是 data 层最重的违规文件（6 条）。根因：`loadOrCreateJsonTableFromChatHistory` 里塞了编排逻辑。

- [ ] P1-08 将 `table-repo.ts` 中的编排逻辑（模板应用、世界书刷新、独立表合并）提取到 `service/table/table-orchestrator.ts`。`table-repo.ts` 只保留纯 CRUD（save/load/delete JSON table）。
- [ ] P1-09 `table-repo.ts` 中 `showToastr_ACU`（presentation 依赖）→ 改为 throw 或返回错误码，由 service 层/调用方处理。

### P1-C：其余 data 层违规

- [ ] P1-10 `data/models/defaults-json.js`：`entryName` 从 `service/worldbook/injection-engine` import → 把 `entryName` 常量移到 `shared/constants.ts`（它本质上是个字符串常量）
- [ ] P1-11 `data/repositories/template-preset-repo.ts`：`getTemplatePreset_ACU` 从 presentation import → 将 `getTemplatePreset` 的纯数据逻辑部分下沉到 `data/repositories/template-preset-repo.ts` 自身或 `service/template/`，UI 部分留在 presentation

### P1-D：验证

- [ ] P1-12 构建通过 + `check-arch.sh` 报告 data 层 0 条违规
- [ ] P1-13 功能等价验证（手动测试：配置导入导出、数据隔离切换、表格读写）

检查点：data 层完全干净：0 条 service 依赖，0 条 presentation 依赖

---

## 阶段 P2：错放函数归位（消除 service → presentation 反向依赖的根因之一）

> 很多 service → presentation 的反向 import 是因为业务逻辑函数放在了 presentation 里。先把它们搬到正确的层。

### P2-A：optimization-ui.ts 中的纯逻辑下沉（约 17 个函数）

- [ ] P2-01 创建 `service/plot/plot-logic.ts`，从 `presentation/components/optimization-ui.ts` 迁出以下纯计算函数（不操作 DOM 的）：
  - `ensurePlotTasksCompat`, `ensurePlotPresetBindingsStore`, `ensureLoopPromptsArray`, `ensurePlotPromptsArray`
  - `normalizePlotPresetSelectionValue`, `normalizePlotTask`, `normalizePlotTasks`
  - `findPlotPresetByName`, `getPlotPresetBindingForChat`, `clearPlotPresetBindingForChat`
  - `getCurrentRuntimePlotPresetName`, `isDefaultPlotPresetSelection`
  - `getPlotGlobalRevision`, `getPlotPromptContentByIdFromSettings`, `setPlotPromptContentByIdForSettings`
  - `getPlotFinalDirectiveFromSource`
  - `applyPlotPresetToSettings`, `replaceCurrentPlotSettingsWithSnapshot`, `resetPlotSettingsToDefault`
  - `syncCurrentEditablePlotPresetState`, `switchCurrentChatPlotPreset`
  - `ensureTagRulesCompat`, `normalizePlotPresetExcludeRules`
  - `markPlotIntercept`, `shouldSkipPlotIntercept`
- [ ] P2-02 更新 `optimization-ui.ts` 中剩余 UI 函数对这些逻辑函数的 import（改为从 `service/plot/plot-logic` import）
- [ ] P2-03 更新 service 层中所有对这些函数的 import（从 `presentation/components/optimization-ui` → `service/plot/plot-logic`）

### P2-B：template-preset-ui.ts 中的纯逻辑下沉

- [ ] P2-04 创建 `service/template/template-preset-service.ts`，从 `presentation/components/template-preset-ui.ts` 迁出：
  - `getTemplatePreset`, `getDefaultTemplateSnapshot`, `upsertTemplatePreset`
  - `getTemplatePresetDisplayName`, `listTemplatePresetNames`
  - `normalizeTemplateOperationScope`, `parseImportedTemplateData`
  - `persistTemplateScopeSelectionState`
- [ ] P2-05 更新所有 import

### P2-C：plot-editors.ts 中的业务状态下沉

- [ ] P2-06 将以下状态变量从 `presentation/components/plot-editors.ts` 移到 `service/state/app-state.ts`：
  - `isAutoUpdatingCard`, `wasStoppedByUser`, `manualExtraHint`
  - `currentAbortController`, `newMessageDebounceTimer`
  - `currentPlotTaskEditorId`
  及对应 setter
- [ ] P2-07 更新所有 import

### P2-D：其余零散迁移

- [ ] P2-08 `presentation/components/import-status-ui.ts` 中的 `IMPORTED_ENTRY_PREFIX_ACU`、`getImportJsonStorageComment_ACU`、`getImportBatchPrefix_ACU`、`getImportStablePrefix_ACU` → 移到 `shared/constants.ts` 或 `service/import/import-constants.ts`（这些是字符串常量/纯函数，不是 UI）
- [ ] P2-09 `presentation/components/worldbook-selector.ts` 中的 `isEntryBlocked_ACU` → 纯判断逻辑，移到 `service/worldbook/pipeline.ts` 或新建 `service/worldbook/worldbook-utils.ts`
- [ ] P2-10 `presentation/pages/popup-helpers.ts` 中的 `formatPlotScopeUpdatedAt_ACU` → 纯格式化函数，移到 `shared/utils.ts`

### P2-E：验证

- [ ] P2-11 构建通过 + `check-arch.sh`（预期 service → presentation 大幅减少）
- [ ] P2-12 功能等价验证

检查点：错放的函数全部归位，service → presentation 的违规从 48 条降到 ~20 条

---

## 阶段 P3：service 层纯化 — 消除全部 service → presentation 依赖

> 这是最大最难的阶段。每个 service 文件都要改造为"纯逻辑 + 返回值"。

策略：对于每个 service 文件中的 presentation import，分析具体调用场景，选择对应改造方式：
- **toast 调用** → 删除，改为返回错误/状态，由 presentation 层调用方 toast
- **按钮/进度条操作** → 改为 `onProgress` 回调参数
- **从 UI 读取数据**（如 `getManualSelectionFromUI`）→ 改为参数传入，presentation 层在调用前收集好参数
- **剩余 UI 渲染调用** → 从 service 函数体中删除，改为返回结果，presentation 层在调用后执行

### P3-A：通用改造 — toast 清理（涉及 12 个 service 文件）

- [ ] P3-01 `service/ai/prompt-builder.ts`：删除 `showToastr` import，错误/警告通过 throw 或返回 `{ error }` 传递
- [ ] P3-02 `service/summary/merge-logic.ts`：同上
- [ ] P3-03 `service/optimization/content-optimization.ts`：同上 + `hideOptimizationOverlay` / `hideOptimizationProgressToast` 改为返回状态
- [ ] P3-04 `service/worldbook/injection-engine.ts`：同上 + `getImportBatchPrefix` 已在 P2 移走 + `updateCardUpdateStatusDisplay` 改为 onProgress 回调
- [ ] P3-05 `service/worldbook/pipeline.ts`：同上 + `renderTableSelector` / `isEntryBlocked`（已在 P2 移走）
- [ ] P3-06 `service/import/import-process.ts`：同上 + `getImportWorldbookTarget` / `updateImportStatusUI` / `getImportSelectionFromUI` → 全部改为参数传入
- [ ] P3-07 `service/data-admin/admin.ts`：同上 + `renderPromptSegments` / `syncMergeSettingsToUI` / `applyTemplateSnapshotToScope` → 改为返回值
- [ ] P3-08 `service/settings/settings-service.ts`：同上 + `syncAllSettingsToUI` → 改为返回 settings 对象，presentation 层在收到后自行 sync UI
- [ ] P3-09 `service/table/update-process.ts`：最重的一个。`resetManualUpdateButton` / `bindTableFillStopButton` / `collectManualExtraHint` / `getManualSelectionFromUI` → 全部改为参数 + 回调 + 返回值

### P3-B：service/runtime/ 三大文件改造

- [ ] P3-10 `service/runtime/helpers-remaining.ts`（7 条违规）：
  - `clickSendButton` / `setSendTextareaValue` → 改为返回"要发送的文本"，presentation 层执行发送
  - `updateLoopTimerDisplay` / `updateLoopUIStatus` → 改为 onProgress 回调
  - 其余纯逻辑引用已在 P2 解决
- [ ] P3-11 `service/runtime/init.ts`（8 条违规）：
  - `addAutoCardMenuItem` → 初始化编排移到 `presentation/bootstrap/startup.ts`，init.ts 只做非 UI 初始化
  - `attemptToLoadCoreApis` / `handleNewMessageDebounced` → 属于 presentation 的事件处理，从 init.ts 移到 presentation 层
  - `getSendTextareaValue` / `setSendTextareaValue` / `updateCardUpdateStatusDisplay` → 同 P3-10
- [ ] P3-12 `service/runtime/api-registry.ts`（11 条违规）：
  - 这个文件本质上是**外部 API 的路由层**，它把 `window.AutoCardUpdaterAPI.xxx()` 映射到内部实现
  - 策略：api-registry 只做"查找并委托"，具体实现由 presentation 和 service 各自注册
  - `openAutoCardPopup` / `openNewVisualizer` → 这些是 UI 行为，从 api-registry 中移到 presentation 层，api-registry 通过回调/注册表引用
  - 其余纯逻辑调用已在 P2 解决

### P3-C：验证

- [ ] P3-13 构建通过 + `check-arch.sh` 报告 service → presentation 0 条
- [ ] P3-14 全量功能等价验证（全部 16 项业务能力）

检查点：service 层完全干净：0 条 presentation 依赖

---

## 阶段 P4：presentation → data 跨层调用清理（约 36 条）

> 最后处理。难度低但数量多——大部分是 presentation 直接 import data 的常量/默认值/repo 函数。

策略：
- **常量/默认值**（`DEFAULT_PLOT_SETTINGS`、`TABLE_TEMPLATE` 等）→ 移到 `shared/defaults.ts` 或保留在 data 层但通过 service 层中转
- **repo 函数**（`loadOrCreateJsonTable`、`getDataIsolationHistory` 等）→ 在 service 层创建对应的中转函数

### P4-A：常量/默认值处理

- [ ] P4-01 评估 `data/models/defaults-json.js` 和 `data/models/defaults.ts` 中的常量归属：
  - 如果常量是"纯配置默认值"（与存储无关），移到 `shared/defaults.ts`
  - 如果常量与数据结构强绑定，保留在 data 层，presentation 通过 service 获取
- [ ] P4-02 `data/constants.ts` 中的存储键常量（`STORAGE_KEY_*`）→ 移到 `shared/constants.ts`（它们只是字符串，不含逻辑）

### P4-B：repo 调用中转

- [ ] P4-03 创建 service 层中转函数，包装 data 层 repo 调用：
  - `service/table/table-service.ts`：包装 `loadOrCreateJsonTable`、`saveIndependentTable`、`checkIfFirstTimeInit`
  - `service/isolation/isolation-service.ts`：包装 `getDataIsolationHistory`、`removeDataIsolationHistory`
  - `service/template/template-preset-service.ts`：补充包装 `getCurrentTemplatePresetName`、`persistCurrentTemplatePresetName`、`deriveTemplatePresetNameForImport` 等
  - `service/settings/character-settings-service.ts`：包装 `getCurrentWorldbookConfig`
  - `service/profile/profile-service.ts`：包装 `globalMeta`、`saveGlobalMeta`
- [ ] P4-04 更新所有 presentation 文件的 import：从 `data/repositories/xxx` → `service/xxx-service`
- [ ] P4-05 `presentation/components/import-status-ui.ts` 的 `data/storage/idb-import-temp` → 通过 service 层中转
- [ ] P4-06 `presentation/window/` 的 `data/storage/tavern-storage` → 通过 service 层中转或将窗口配置存取封装为 service

### P4-C：验证

- [ ] P4-07 构建通过 + `check-arch.sh` 报告 presentation → data 0 条
- [ ] P4-08 全量功能等价验证

检查点：presentation 层完全干净：0 条 data 直接依赖

---

## 阶段 P5：收尾 + 护栏

- [ ] P5-01 删除 `service/runtime/state-manager.ts`（已无用，全部拆分到三处）。如果仍有外部引用，改为 re-export 门面或逐个更新 import 路径。
- [ ] P5-02 将 `check-arch.sh` 集成到 `npm run build` 中（构建后自动检查，违规即失败）
- [ ] P5-03 更新 `docs/架构违规报告.md` 为 "0 条违规"
- [ ] P5-04 在 `CODEBUDDY.md` 中添加架构规则，确保后续开发遵守
- [ ] P5-05 最终全量手动测试（16 项业务能力）

检查点：`check-arch.sh` 输出 0 条违规，构建通过，全部功能正常

---

## 阶段依赖与执行顺序

```
P0 基础设施（state-manager 拆分 + lint 脚本）
 └→ P1 data 层纯化（14 条 → 0）
     └→ P2 错放函数归位（presentation 内纯逻辑 → service）
         └→ P3 service 层纯化（48 条 → 0）
             └→ P4 presentation → data 清理（36 条 → 0）
                 └→ P5 收尾 + 护栏
```

严格串行。每阶段完成必须：
1. `npm run build` 通过
2. `check-arch.sh` 违规数 ≤ 预期
3. 关键路径手动测试通过

---

## 预期工作量估算

| 阶段 | 涉及文件数 | 核心难点 | 预估 |
|------|-----------|----------|------|
| P0 | 4 个新文件 + state-manager 拆分 | re-export 兼容，不破坏现有 import | 中 |
| P1 | ~10 个 data 文件 + 调用方更新 | table-repo 拆分，参数化改造 | 中 |
| P2 | ~5 个文件迁移 | 函数依赖链理清 | 中 |
| P3 | 12 个 service 文件 | 最大最难：每个函数都要从"有副作用"改为"纯返回值" | 高 |
| P4 | ~12 个 presentation 文件 | 数量多但模式固定：加 service 中转层 | 中 |
| P5 | 清理 + 测试 | 护栏脚本 + 全量验证 | 低 |

---

## 风险与缓解

| 风险 | 缓解 |
|------|------|
| P0 state-manager 拆分后大面积 import 失败 | 先保留 state-manager 做 re-export 门面，逐步迁移引用 |
| P3 service 函数改为返回值后，presentation 调用方需要大量改写 | 逐个 service 文件改，每改一个验证一次 |
| api-registry 是 window API 的路由层，改造影响外部调用者 | 保持 `window.AutoCardUpdaterAPI` 的接口签名不变，只改内部实现 |
| 改造过程中功能回归 | 每阶段手动测试关键路径 |
