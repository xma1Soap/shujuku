<analysis>
  <context>
    <item>验收专家独立复核结论为“不通过”，阻断证据集中在 src/service/table/update-orchestrator.ts:397-419 只锁住 parse/apply，src/service/table/update-orchestrator.ts:449-503 在锁外执行 saving，导致 AI 并发返回后 parse/apply 与 save 之间的全局 currentJsonTableData_ACU 仍可能被同目标楼层其他 group 覆盖。</item>
    <item>src/service/table/update-orchestrator.ts:344-346 仅在 prepareAIInput_ACU 前根据 progressContext.batchBaseSnapshot 恢复一次全局表；AI 调用 src/service/table/update-orchestrator.ts:374 在锁外并发执行，返回后 src/service/table/update-orchestrator.ts:397 进入锁时没有再次恢复本组 batchBaseSnapshot。</item>
    <item>src/service/table/table-service.ts:67 已在 persistTablesToChatMessage_ACU 内部调用 runTableUpdateApplyWithScopeLock_ACU；如果 update-orchestrator 外层再用同一个 scopeKey 包裹 saveIndependentTableToChatHistory_ACU，会发生同 scope 自等待死锁，因为 table-update-queue.ts:22-28 对 active promise 会等待前序任务完成并递归进入。</item>
    <item>src/service/table/table-update-queue.ts:18-44 当前锁实现没有重入能力，也没有暴露锁内持有状态；最小安全改法不是粗暴外层包 save，而是在 table-service 增加可选 skipScopeLock 内部接口，公开 saveIndependentTableToChatHistory_ACU 默认保持旧行为，orchestrator 在已经持有同 scope 锁时调用内部无重入路径。</item>
    <item>tests/service/table/table-update-queue.test.ts 当前不存在，验收专家指出的锁契约缺少直接单测属实。</item>
    <item>tests/service/vector/summary-vector-index-archive-service.test.ts 已新增 t9 公开入口回归并通过局部测试，但验收专家认为向量项非当前阻断；本轮只需保持不回归。</item>
  </context>
  <needs>
    <item>把 executeCardUpdateCore_ACU 中 AI 调用之后的 parse/apply、first-time init 计算、template 补全、save、readable lorebook update 合并到同一个 applyScopeKey 临界区；AI 调用和重试等待继续在锁外，避免把远程调用串行化。</item>
    <item>在该临界区 parse 前再次 deep clone 恢复 progressContext.batchBaseSnapshot，保证每个 group 基于批次历史快照应用自己的 AI 结果，而不是基于其他 group 临时写入的全局对象。</item>
    <item>为 table-service 提供锁内复用的持久化入口，避免同 scope 嵌套锁死锁，同时保留 saveIndependentTableToChatHistory_ACU 与 persistTablesToChatMessage_ACU 的外部默认加锁语义。</item>
    <item>补 tests/service/table/table-update-queue.test.ts 覆盖同 scope 串行、前序失败不阻塞、不同 scope 并发、释放后可再次进入、多 waiter 不穿透。</item>
    <item>补或调整 update-orchestrator 并发测试，证明两个同目标 group AI 返回交错时，保存不会被全局 currentJsonTableData_ACU 污染；至少测试锁内恢复 batchBaseSnapshot 与 save 调用发生在同一连续临界区。</item>
  </needs>
  <key_challenges>
    <item>不能把 callCustomOpenAI_ACU 放入锁，否则会把多组 AI 调用串行化，违背并发性能目标。</item>
    <item>不能在已持有 runTableUpdateApplyWithScopeLock_ACU 的回调内直接调用现有 saveIndependentTableToChatHistory_ACU，否则 table-service 内部同 scope 锁会等待当前锁释放，形成死锁。</item>
    <item>测试若继续完全 mock table-service，只能证明调用顺序，无法证明真实保存链路合并目标楼层数据；需要至少新增 table-update-queue 直接测试，并尽量在 orchestrator 测试中验证临界区内状态恢复与保存输入。</item>
    <item>现有 SQL 重试测试依赖 setTimeout spy 立即执行，重构 try/catch 范围时必须保持 lastSqlError 注入与 maxRetries 行为。</item>
  </key_challenges>
  <confidence>MEDIUM：核心根因和死锁风险已由文件读取确认，最小改法路径清晰；置信度不是 HIGH，因为尚未读取 table-service.ts 271-310 的 saveIndependentTableToChatHistory_ACU 包装实现与 update-orchestrator.test.ts 的可插入 mock 细节，执行前需要窄范围补读。</confidence>
  <approach>
    <maintainability score="8/10">通过在 table-service 内部拆出 persistTablesToChatMessageCore_ACU 或增加 internal option，把“是否加锁”留在同一模块处理，避免在 orchestrator 复制持久化细节；缺点是会增加一个内部函数或选项，但这是为避免重入死锁所需的最低复杂度。</maintainability>
    <robustness score="8/10">parse/apply/save 连续锁可消除验收专家指出的裸奔窗口；锁内恢复 batchBaseSnapshot 可消除 AI 等待期间全局对象污染；保留 table-service 默认锁可保护其他调用方。</robustness>
    <extensibility score="7/10">内部无锁 persist 入口可供未来已持锁批处理复用，但不扩展公共 API；不会引入新依赖或配置项。</extensibility>
  </approach>
  <edge_cases>
    <item>同一 scope 第一个任务 throw 后，第二个任务仍按顺序执行并拿到锁。</item>
    <item>不同 scope 的任务不互相等待，慢任务未完成时快任务可完成。</item>
    <item>同一 scope 多个等待者同时排队时，执行顺序仍为提交顺序，不发生两个任务同时进入临界区。</item>
    <item>executeCardUpdateCore_ACU 在 AI 返回后，如果 batchBaseSnapshot 存在，parse 前必须恢复深拷贝；修改 currentJsonTableData_ACU 后 save 必须在同一临界区内完成。</item>
    <item>SQL 模式 parse 失败后仍能注入 SQL_ERROR_FEEDBACK 并重试，不因临界区重构改变错误返回。</item>
    <item>isImportMode 为 true 时仍跳过保存，只执行 parse/apply 并返回 chunk_done/complete 语义。</item>
  </edge_cases>
  <affected_scope>
    <file>src/service/table/table-service.ts</file>
    <file>src/service/table/update-orchestrator.ts</file>
    <file>tests/service/table/table-update-queue.test.ts</file>
    <file>tests/service/table/update-orchestrator.test.ts</file>
    <file>.analysis-cache.md</file>
  </affected_scope>
  <execution_plan>
    <step>窄范围读取 src/service/table/table-service.ts:260-310 与 tests/service/table/update-orchestrator.test.ts 的 mock 区和 batchBaseSnapshot/并发相关测试区，确认精确 diff 点。</step>
    <step>修改 src/service/table/table-service.ts：抽出 persistTablesToChatMessageUnlocked_ACU 或给 persistTablesToChatMessage_ACU 增加内部 skipScopeLock 路径，并新增 saveIndependentTableToChatHistoryWithinScopeLock_ACU 供已持锁调用；外部现有函数保持默认加锁。</step>
    <step>修改 src/service/table/update-orchestrator.ts：AI 调用后构造 applyScopeKey，使用一次 runTableUpdateApplyWithScopeLock_ACU 包住 parse/apply、success 标记、保存、updateReadableLorebookEntry_ACU；锁内 parse 前根据 batchBaseSnapshot 再次 deep clone 恢复；调用 table-service 的已持锁保存入口避免死锁。</step>


---
<output_quality_review>
  <task_summary>完成多组并发填表快照覆盖与串行落盘修复：表格填表在 AI 调用后把 batchBaseSnapshot 恢复、parse/apply、save、updateReadableLorebookEntry 放入同一 scope 临界区；table-service 增加已持锁保存入口避免重入死锁；补齐 table-update-queue、orchestrator、table-service 与向量 pending 回归测试。</task_summary>
  <deliverables>src/service/table/update-orchestrator.ts；src/service/table/table-service.ts；tests/service/table/table-update-queue.test.ts；tests/service/table/update-orchestrator.test.ts；tests/service/vector/summary-vector-index-archive-service.test.ts。</deliverables>
  <metrics>
    <total_files_modified>生产代码至少 3 个核心文件；测试新增/修改至少 4 个目标文件；计划与缓存已同步。</total_files_modified>
    <execution_plan_coverage>已覆盖连续临界区、避免重入死锁、同楼层合并、队列契约、batchBaseSnapshot 深拷贝/恢复、向量 pending 二次落盘。</execution_plan_coverage>
    <edge_cases_handled>同 scope 串行、前序失败释放、不同 scope 并发、多 waiter 顺序、AI 等待期间全局数据污染、保存失败、SQL 重试、import 跳过保存、向量 pending 第二次基于第一次结果叠加。</edge_cases_handled>
    <confidence_assessment>HIGH：目标回归 111 tests passed，typecheck 通过，build 架构检查通过，验收专家第二轮结论为通过。全量 npm test 仍有 plot/runtime 既有失败，未作为本任务完成依据。</confidence_assessment>
  </metrics>
  <substance_check>验收专家上一轮指出的 parse/apply 与 save 之间释放锁窗口已通过源码结构消除；新增测试能击穿 AI 返回后全局 currentJsonTableData_ACU 被其他组污染的旧问题。</substance_check>
  <completeness_check>affected_scope 内核心文件均已处理；目标测试、typecheck、build 与独立验收已执行。全量 npm test 失败集中在 plot/runtime 旧模块，需单独任务处理，不属于本次表格并发/向量 pending 链路。</completeness_check>
  <value_density_check>高价值内容集中在生产竞态修复和回归测试；未引入新依赖、未降低 maxConcurrentGroups、未用串行化 AI 调用逃避并发问题。</value_density_check>
  <alignment_check>满足用户本质需求：多组并发填表不再通过全局可变表对象互相污染，保存链路避免同楼层覆盖，并有目标测试和独立验收支撑。如果这是别人交给我的，我会接受，但会把全量 npm test 中 plot/runtime 失败另开缺陷处理。</alignment_check>
</output_quality_review>
