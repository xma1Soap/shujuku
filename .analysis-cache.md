# 剧情推进子任务 API 预设未实际生效分析

```text
<reconnaissance>
goal: 确认剧情推进子任务 taskApiPreset 从 UI 保存到运行时 API 调用的完整链路，定位“保存成功但实际调用未按预设走”的断点。
files_checked:
  - src/presentation/components/plot-editors.ts
  - src/service/plot/plot-logic.ts
  - src/service/runtime/plot-runtime/plot-entry.ts
  - src/service/runtime/plot-runtime/plot-task-engine.ts
  - src/service/ai/api-call.ts
findings:
  - UI 保存链路存在：loadCurrentPlotTaskToUI_ACU 会读取 selectedTask.taskApiPreset，saveCurrentPlotTaskFromUI_ACU 会把 plot-task-api-preset 写回 taskApiPreset。
  - normalizePlotTask_ACU 保留 taskApiPreset 字段，预设导入/应用 normalizePlotTasks_ACU 后不会丢字段。
  - runOptimizationLogic_ACU 使用 settings_ACU.plotSettings 合成 plotSettings 后进入 runPlotTasksRuntime_ACU。
  - executeSinglePlotTask_ACU 调用 callApiWithPlotPreset_ACU，理论上支持显式 presetName。
  - 断点在 runPlotTasksRuntime_ACU：它按 stage 取第一个显式 taskApiPreset 作为 stageEffectivePreset，并把同一个 stageEffectivePreset 传给该阶段所有任务。
  - executeSinglePlotTask_ACU 又优先使用 runtimeOptions.stageEffectivePreset，而不是 normalizedTask.taskApiPreset，导致同阶段后续任务自己的 taskApiPreset 被覆盖。
  - willPlotUseMainApiGenerateRaw_ACU 在 runPlotTasksRuntime_ACU 中无参调用，只按全局 plotApiPreset 判断是否应递增 planningGuard_ACU.ignoreNextGenerationEndedCount；如果某个子任务 taskApiPreset 使用 useMainApi，但全局不使用，则守卫计数可能不准确。
</reconnaissance>
```

```text
<analysis>
root_cause:
  - 主要根因：运行时把“子任务独立 API 预设”错误实现为“同阶段统一 API 预设”。stageEffectivePreset 的存在直接破坏了 taskApiPreset 的任务级优先级。
  - 次要风险：willPlotUseMainApiGenerateRaw_ACU 的判断只基于全局 plotApiPreset，没有按任务实际 effective preset 判断，可能导致主 API 路径产生额外 GENERATION_ENDED 事件时无法正确忽略。
impact:
  - 同一阶段内，只有第一个设置了 taskApiPreset 的任务会决定整个阶段的 API；其余任务即使保存了不同 taskApiPreset，也不会实际使用。
  - 如果阶段内第一个任务未设 preset，但后续任务设置了 preset，当前逻辑会扫描到后续第一个 preset 并反向覆盖前面的任务，行为更加反直觉。
  - 用户看到 UI 能保存，但运行结果不匹配，属于持久化与执行语义不一致。
expected_behavior:
  - 每个任务调用 API 时都应独立决议 effective preset：task.taskApiPreset > settings_ACU.plotApiPreset > 当前 API 配置。
  - 同阶段并行只应共享 relayTagMap 和阶段依赖，不应共享 API preset。
  - planningGuard_ACU.ignoreNextGenerationEndedCount 应按每个任务的实际 effective preset 判断是否走 generateRaw 主 API。
fix_plan:
  - 删除 stageEffectivePreset 的阶段级决议与传参。
  - 新增任务级 effective preset 解析函数，统一 trim taskApiPreset 与 fallback 到 settings_ACU.plotApiPreset。
  - executeSinglePlotTask_ACU 内使用任务级 effective preset 调 callApiWithPlotPreset_ACU。
  - 将 willUseMainApiGenerateRaw 从阶段统一布尔值改为任务级判断，避免不同任务 API 模式混用时守卫错误。
  - 补充调试日志，明确输出每个任务实际使用的 preset，便于用户验证。
</analysis>
```

```text
<output_quality_review>
changes:
  - src/service/runtime/plot-runtime/plot-task-engine.ts 新增 resolvePlotTaskApiPreset_ACU。
  - executeSinglePlotTask_ACU 改为每次任务调用前独立解析 taskApiPreset，并传给 callApiWithPlotPreset_ACU。
  - 删除 runPlotTasksRuntime_ACU 的 stageEffectivePreset 阶段统一覆盖逻辑。
  - planningGuard_ACU.ignoreNextGenerationEndedCount 改为按任务实际 effective preset 判断。
verification:
  - npm run typecheck: passed。
  - npm run build:nocheck: passed，生成 dist/index.bundle.js。
quality_notes:
  - 本次修复集中在运行时执行链路，没有改 UI 保存与预设导入逻辑，影响面较小。
  - 同阶段并行任务现在可以各自使用不同 taskApiPreset；这才符合“子任务独立 API 预设”的语义。
  - 调试日志会输出每个任务实际使用的 API 预设，便于实际验证。
remaining_risk:
  - 如果某些 API 预设本身配置了 useMainApi，底层 generateRaw 仍然只能使用酒馆当前主配置，这是 TavernHelper.generateRaw 的外部能力边界，不是本次运行时选择逻辑能彻底解决的问题。
  - 如果用户期望 tavern profile 级预设通过 generateRaw 切换生效，需要另行验证 TavernHelper/ConnectionManager 支持路径。
</output_quality_review>
```
