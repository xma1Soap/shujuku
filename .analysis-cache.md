<analysis>
context: 用户要求执行发布流程：重新生成 JS，将 `dist/index.bundle.js` 覆盖根目录 `index.js`，推送到 `main`，再推送到 `release`，覆盖更新远端 tag `spv3.1`，并新增 tag `spv3.1.1` 推送。侦察确认当前分支是 `main`，本地 HEAD 为 `e2aacc5 release: spv3.1 plot global toggle fixes`，当前工作区已有未提交改动：`.analysis-cache.md`、`dist/index.bundle.js`、`src/presentation/components/status-display.ts`、`src/presentation/pages/main-popup-table.ts`、`src/presentation/pages/popup-bindings-worldbook.ts`、`src/service/settings/settings-service.ts`，以及未跟踪归档 `.analysis-archive/2026-04-30_2054_交火模式关闭同步关闭向量.md`。这些改动包含上一轮“交火关闭同步关闭向量”的未提交修复、本轮“隐藏普通向量独立开关”的源码改动、构建产物和分析归档。当前本地已有 tag `spv3.1`，没有本地 tag `spv3.1.1`。发布资产树确认 `main` 跟踪的发布资产包括 `dist/extension/manifest.json`、`dist/index.bundle.js`、`manifest.json`、`manifest.plus-assistantembedded.json`，以及根目录 `index.js`；不存在被跟踪的 `dist/extension/index.js`。本轮必须注意 Windows `cmd.exe` 环境，使用 `copy /Y`、`fc /B`、`git` 命令执行发布，不使用 Unix `cp/rm/sed` 一类命令。
needs: 需要产出一个可追溯的发布提交链：`main` 应包含源码修复、构建产物、根目录 `index.js` 覆盖结果与分析归档；`release` 应同步发布资产和必要源码/归档，保持与当前 release 分支的发布形态一致；远端 tag `spv3.1` 需要被强制更新到新的 release HEAD，新增 tag `spv3.1.1` 也应指向同一 release HEAD。必须先构建并验证 `dist/index.bundle.js` 与 `index.js` 二进制一致，否则把旧入口发布出去就是事故，不是发布。
key_challenges: 第一，当前工作区包含两个连续任务的未提交改动，不能只提交本轮隐藏开关，否则会丢掉上一轮 `setSummaryVectorIndexMode_ACU` 同步关闭普通向量的关键修复。第二，`release` 分支历史与 `main` 曾经存在发布资产形态差异，不能盲目 merge；应沿用前序发布方式，从 `main` checkout 实际跟踪的发布资产、源码和归档到 `release` 后提交。第三，覆盖 tag `spv3.1` 是破坏性操作，必须在 release 提交完成并验证 HEAD 后再 force 更新。第四，`.analysis-cache.md` 之前已被移动归档，但 VSCode 仍显示旧文件；当前发布任务需要重新写本轮 analysis，并最终归档，避免发布过程没有决策链。
confidence: HIGH。侦察已经确认当前分支、工作区改动、已有 tag、发布资产集合和用户要求。唯一需要执行期验证的是远端 push/tag 是否成功；这将通过 `git ls-remote` 验证。
approach: 推荐方案是按发布链路顺序执行：重新运行 `npm run build`，用 `copy /Y dist\index.bundle.js index.js` 覆盖主入口，用 `fc /B` 验证一致；在 `main` 提交所有相关源码、构建产物和归档并推送；切换 `release` 后从 `main` checkout 发布资产及本轮/上一轮相关源码和归档，提交并推送 release；在 release HEAD 上删除/重建本地 `spv3.1` 和 `spv3.1.1`，force 推送 `spv3.1` 并普通/force 推送 `spv3.1.1`；最后用 `git ls-remote` 验证远端 `main`、`release`、`spv3.1`、`spv3.1.1` 指向。这个方案不会用 merge 强行混合 unrelated histories，也不会跳过入口文件一致性验证。
  三维评分（每个维度 1-5 分，5 为最优）：
  - 可维护性: 5/5 — 发布提交清晰区分 `main` 源码构建提交和 `release` 发布资产提交，tag 指向 release HEAD，符合前序发布约定。
  - 健壮性: 5/5 — 构建、二进制一致性、架构护栏、远端引用都会验证；tag 覆盖在 release 推送后执行，避免指向半成品提交。
  - 可扩展性: 4/5 — 沿用现有手工发布流程，改动安全但仍依赖手工 checkout 资产列表；未来可以脚本化发布流程降低人为错误。
edge_cases: 第一，`npm run build` 失败时必须停止发布并修复，不能继续覆盖 `index.js`。第二，`fc /B dist\index.bundle.js index.js` 不一致时必须停止，不能提交入口不一致的发布。第三，`release` 分支若缺少某些源码路径，仍应通过 `git checkout main -- <paths>` 同步需要发布的实际文件，而不是 merge。第四，远端 tag `spv3.1` 已存在，必须使用强制推送覆盖；`spv3.1.1` 如果远端意外已存在，也应先检查并按用户“新增”语义避免误覆盖，若发现存在需要 decision_point。第五，发布前后 `.analysis-cache.md` 必须归档，不能把临时 cache 留在根目录作为最终未归档状态。第六，当前工作区包含上一轮未提交修复，发布提交必须包含它，否则交火关闭同步关闭向量这个严重修复不会进入 release。
affected_scope: `dist/index.bundle.js`；`index.js`；`src/service/settings/settings-service.ts`；`src/presentation/pages/main-popup-table.ts`；`src/presentation/pages/popup-bindings-worldbook.ts`；`src/presentation/components/status-display.ts`；`.analysis-archive/2026-04-30_2054_交火模式关闭同步关闭向量.md`；`.analysis-archive/2026-04-30_2108_隐藏普通向量独立开关.md`；本轮将新增 `.analysis-archive/2026-04-30_21xx_spv3.1.1发布.md`；发布同步还会检查 `manifest.json`、`manifest.plus-assistantembedded.json`、`dist/extension/manifest.json` 是否需要纳入 release。
execution_plan:
  - step_1: 运行 `npm run build` 重新生成 `dist/index.bundle.js`，确认 Rollup 和架构护栏通过。
  - step_2: 使用 `copy /Y dist\index.bundle.js index.js` 覆盖根目录入口，再用 `fc /B dist\index.bundle.js index.js` 验证二进制一致。
  - step_3: 在 `main` 上 `git add` 所有相关源码、构建产物、入口文件和分析归档，提交并推送到 `origin/main`。
  - step_4: 切换到 `release`，从 `main` checkout 发布资产、相关源码与分析归档，提交并推送到 `origin/release`。
  - step_5: 在 release HEAD 上重建本地 tag `spv3.1` 和 `spv3.1.1`，强制推送 `spv3.1`，推送新增 `spv3.1.1`。
  - step_6: 使用 `git ls-remote` 验证 `origin/main`、`origin/release`、`refs/tags/spv3.1`、`refs/tags/spv3.1.1` 的最终提交指向，并读取 `.analysis-cache.md` 后追加验收报告归档。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。方案评分为可维护性 5/5、健壮性 5/5、可扩展性 4/5；它完整覆盖构建、入口覆盖、main/release 推送、tag 覆盖和远端验证。
  - 是否遗漏了已知边界条件？ → NO。已覆盖构建失败、入口不一致、release 分支形态差异、tag 已存在、分析归档、上一轮未提交修复必须一起发布等边界。
  - 是否因改动量大而想缩减方案？ → NO。没有跳过 release 或 tag 验证，也没有只推 main。
  - 是否打算跳过某些文件？ → NO。会提交当前相关源码、构建产物、入口和归档；release 同步按实际发布资产和相关源码执行。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。step_3 覆盖 main 文件提交，step_4 覆盖 release 同步，step_6 覆盖验证和归档。
  - context是否充分？是否有未读但可能相关的文件？ → YES，context 充分。已通过 git 状态和资产树确认发布所需信息；后续以命令输出验证。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO。当前发现的 release 资产差异、tag 覆盖风险、上一轮未提交修复都纳入计划。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO。命令只用于构建、复制发布资产、git 操作和验证；源码修改已完成且不使用 shell 文本替换。
</analysis>
