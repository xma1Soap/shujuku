<analysis>
context: 本轮任务是在上一轮功能修复之后继续做发布操作。当前 Git 状态显示位于 main 分支，main 相对 origin/main ahead 4，工作区已有大量未提交改动，包括 dist/index.bundle.js、多个 src 文件、新增 summary-vector-index 相关服务文件，以及 .analysis-cache.md 被删除但新归档文件未跟踪。UI 文案搜索确认“纪要向量索引模式（与0TK互斥）”出现在 src/presentation/pages/main-popup-status.ts 第 97 行和 src/presentation/pages/main-popup-worldbook.ts 第 29 行；说明注释出现在同两个文件，以及运行时状态刷新文案在 src/presentation/components/status-display.ts 第 201-205 行；toast 文案在 src/presentation/pages/popup-bindings-worldbook.ts 第 494-497 行。用户要求把 UI 的“纪要向量索引模式（与0TK互斥）”改名为“向量混合交火增强方案”，不再显示括号，并在下方注释提示需要配置好向量模型以及 rerank 模型后使用。构建产物当前为 dist/index.bundle.js，主目录 index.js 存在且需要用 bundle 覆盖。远端为 origin=https://github.com/AlbusKen/shujuku.git，main 与 release 分支都存在；spv3 tag 当前搜索未输出，说明本地暂未存在同名 tag。需要注意：推送 main、release、tag 是高风险发布动作，不能在未提交当前改动、未确认 release 分支同步策略、未确认远端认证成功时假装完成。
needs: 本质目标是完成一次发布级变更：UI 命名与说明文案更新、重新构建 bundle、用 dist/index.bundle.js 覆盖根目录 index.js、把全部相关改动形成可追溯提交并推送到 main 与 release，最后创建并推送 spv3 tag。还必须处理现有 .analysis-cache.md 删除状态和归档文件未跟踪状态，否则仓库会带着脏状态发布，后续追责时连变更边界都看不清。
key_challenges: 第一，用户要求的不只是代码修改，还包括 Git 发布动作；发布不能只构建不提交，也不能只推 main 忘 release。第二，当前工作区已经包含上一轮大量功能改动和归档文件，必须把本次 UI 文案改动与已有功能修复一起作为发布内容处理，否则 release/tag 不完整。第三，根目录 index.js 是大文件，覆盖时必须使用文件复制命令而不是手写，且之后要验证大小/状态。第四，release 分支推送策略需要决定是将 main 当前提交合并/快进到 release，还是在 release 上重复提交；三维评估下，将 main 提交后再把同一提交合并到 release 更可追溯。
confidence: MEDIUM
  - 侦察信息覆盖了 UI 文案位置、Git 分支、远端、tag 初始状态和构建产物路径。置信度不是 HIGH，因为远端推送可能受到认证/权限/网络影响，release 分支是否可快进也只有执行时才能确认。
approach: 三维评估综合最优的方案是：先修改所有用户可见 UI/状态/toast 文案，写入 analysis；运行 npm run build 生成 dist/index.bundle.js；用 Windows 兼容命令 copy /Y dist\index.bundle.js index.js 覆盖主入口；检查 git diff/status；提交当前所有发布相关改动；推送 main；切换 release 并合并 main 的发布提交后推送 release；创建 annotated 或 lightweight tag spv3 指向发布提交并推送 tag。
  三维评分（每个维度 1-5 分，5 为最优）：
  - 可维护性: 4/5 — 同一发布提交在 main/release/tag 上保持一致，后续追溯清晰；UI 文案集中替换，避免一个入口叫旧名另一个入口叫新名。
  - 健壮性: 4/5 — 构建、覆盖 index.js、git status 检查、分支推送和 tag 推送都有明确顺序；扣 1 分是远端推送存在外部认证风险。
  - 可扩展性: 4/5 — 后续发布可沿用同一流程；release 与 tag 指向同一提交，便于热修与回滚。
edge_cases: 如果 dist/index.bundle.js 构建失败，不允许覆盖 index.js 或推送；如果 copy 覆盖失败，不允许发布；如果 spv3 tag 已在远端存在，不能强推覆盖，必须停止并报告；如果 release 分支无法快进或合并冲突，必须停止并输出 decision_point，不得强行覆盖；如果 git push 因认证失败，不能声称已推送；如果 .analysis-cache.md 已被上一轮移动导致删除状态，需要重新创建本轮 analysis 文件并在最终归档，避免把删除状态作为普通发布变更带入；如果中文文件名在命令输出乱码，不影响 Git 跟踪内容，但不能用乱码路径做手工删除；如果 main ahead 4 表示已有未推提交，推送 main 会包含这些提交，这是符合用户“推送到 main”的要求，但需要在最终说明。
affected_scope: src/presentation/pages/main-popup-status.ts；src/presentation/pages/main-popup-worldbook.ts；src/presentation/components/status-display.ts；src/presentation/pages/popup-bindings-worldbook.ts；dist/index.bundle.js；index.js；.analysis-cache.md；.analysis-archive/；Git main 分支；Git release 分支；Git tag spv3。
execution_plan:
  - step_1: 修改 src/presentation/pages/main-popup-status.ts，把 label 文案改为“向量混合交火增强方案”，并把下方 small 注释改为提示“使用前请先配置好向量模型以及 rerank 模型；开启后会自动使用向量能力筛选概要索引，并跳过普通远记忆召回流程；旧对话需要点击立即执行远记忆归档”。
  - step_2: 修改 src/presentation/pages/main-popup-worldbook.ts，做同样 label 和 small 注释更新，保持两个入口一致。
  - step_3: 修改 src/presentation/components/status-display.ts 的动态 hint 文案，启用/未归档/关闭三种状态都使用“向量混合交火增强方案”，并保留模型与 rerank 配置提醒。
  - step_4: 修改 src/presentation/pages/popup-bindings-worldbook.ts 的 toast 文案，避免切换后仍提示旧名“纪要向量索引模式”。
  - step_5: 运行 npm run build，确认 TypeScript、Rollup 和架构护栏通过。
  - step_6: 使用 Windows 兼容 copy 命令将 dist/index.bundle.js 覆盖根目录 index.js，并检查 git status。
  - step_7: git add 所有相关变更并提交，提交信息使用 release: spv3 vector hybrid crossfire enhancement。
  - step_8: 推送 main 到 origin/main。
  - step_9: 切换 release，合并 main 的发布提交，推送 origin/release。
  - step_10: 创建 tag spv3 指向 release/main 同一发布提交，并推送 origin spv3。
  - step_11: 输出验收报告并归档 .analysis-cache.md。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。文案、构建、覆盖、提交、main/release/tag 发布都按发布链路完成，三维评分均为 4/5。
  - 是否遗漏了已知边界条件？ → NO。已覆盖 tag 已存在、release 冲突、推送失败、构建失败、index.js 覆盖失败等边界。
  - 是否因改动量大而想缩减方案？ → NO。没有只改 UI 或只构建，发布动作也列入计划。
  - 是否打算跳过某些文件？ → NO。两个 UI 入口、动态状态文案、toast 文案均纳入 affected_scope。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。每个文件/分支/tag 都有对应步骤。
  - context是否充分？是否有未读但可能相关的文件？ → NO。搜索已定位所有旧名文案；Git 状态已确认。
  - 是否有发现了但被我判断为“无关紧要”而跳过的问题？ → NO。.analysis-cache.md 删除状态和归档文件未跟踪状态已纳入发布风险。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO。源码用 apply_diff/edit_file 修改；命令仅用于 build、copy、git 操作。
</analysis>
---
<decision_point>
issue: 用户在执行阶段新增要求：数据库的 UI 和提示框所有版本号也要从 “SP·数据库 II” 改成 “SP·数据库 III”。这个要求不在原 execution_plan 中，属于发布范围变更。侦察搜索 “SP·数据库 II” 后确认，当前源码与清单中的命中位置包括 manifest.json 第 2 行、manifest.plus-assistantembedded.json 第 2 行、src/shared/runtime-env.ts 第 99 行、src/presentation/pages/main-popup.ts 第 121 行、src/presentation/bootstrap/startup.ts 第 43 行、src/index.ts 第 95 行；构建产物 dist/index.bundle.js、dist/extension/index.js、dist/extension/manifest.json 和根目录 index.js 也存在旧名，但构建产物会在源码与清单改完后重建/覆盖。搜索结果还包含历史归档 .analysis-archive/2026-04-24_2325_sp-database-ii-release.md，但归档是历史记录，不应篡改。
impact: YES。原 affected_scope 和 execution_plan 必须扩展，否则发布出去的 spv3 会出现标题/菜单/提示框仍显示 “SP·数据库 II” 的低级割裂。更糟的是 manifest display_name 如果不改，用户安装界面与运行时 UI 会显示不同版本名，漏洞明显得像是故意写给事故看的。
context_update: affected_scope 新增 manifest.json、manifest.plus-assistantembedded.json、src/shared/runtime-env.ts、src/presentation/pages/main-popup.ts、src/presentation/bootstrap/startup.ts、src/index.ts。execution_plan 需要在构建前增加版本显示名替换步骤，并在构建后验证 “SP·数据库 II” 在运行产物中不再出现。edge_cases 新增：历史归档中的 “SP·数据库 II” 不应修改；dist 与 index.js 不应手工改，应由构建和覆盖生成；如果还有 “SP·数据库 II” 残留在 src/manifest 以外，必须继续追踪。
options:
  - option_a:
      description: 只修改源码 UI 位置，不修改 manifest 与运行时提示。该方案改动少，但会导致插件安装显示名、菜单名、弹窗标题、重复实例告警之间版本不一致。
      approach_evaluation: 可维护性 1/5，因为版本名分裂；健壮性 1/5，因为用户可见提示不一致会误导排查；可扩展性 1/5，因为以后发布还要继续追残留。
      edge_cases: manifest 仍显示旧名；console.warn 仍显示旧名；构建产物会继续带旧名。
      affected_scope_delta: 仅覆盖部分 UI 文件，低于用户“所有版本号”的范围。
  - option_b:
      description: 修改源码、manifest、运行时提示中的 “SP·数据库 II” 为 “SP·数据库 III”，不修改历史归档；dist/index.bundle.js 和 index.js 通过后续构建与覆盖自然更新。
      approach_evaluation: 可维护性 5/5，因为单一源头统一并由构建产物同步；健壮性 5/5，因为避免手工改 dist 造成源码/产物不一致；可扩展性 5/5，因为后续版本升级只需沿同样源头和构建链路处理。
      edge_cases: 需要确认搜索结果中仅历史归档可保留旧名；需要构建后再次搜索 dist/index.bundle.js 与 index.js；manifest.plus-assistantembedded.json 也必须同步，否则并行清单会裂开。
      affected_scope_delta: 新增 manifest.json、manifest.plus-assistantembedded.json、src/shared/runtime-env.ts、src/presentation/pages/main-popup.ts、src/presentation/bootstrap/startup.ts、src/index.ts。
  - option_c:
      description: 直接全仓库替换 “SP·数据库 II” 为 “SP·数据库 III”，包括历史归档、dist、index.js。
      approach_evaluation: 可维护性 2/5，因为会污染历史决策记录；健壮性 2/5，因为手工改构建产物可能与源码不一致；可扩展性 1/5，因为未来无法追溯历史版本。
      edge_cases: .analysis-archive 历史记录被篡改；dist/extension 可能变成未按当前 rollup 输出的伪产物；Git diff 会混入无意义历史改动。
      affected_scope_delta: 覆盖全仓库，但包含不该修改的归档与构建产物。
recommendation: 选择 option_b。三维评估显示 option_b 在可维护性、健壮性、可扩展性上均为 5/5，是综合最优方案。它满足用户“UI 和提示框所有版本号”的本质需求，同时不篡改历史归档，不手工污染构建产物。
execution_plan_update: 在原 step_4 构建前新增版本显示名替换步骤：修改 manifest.json、manifest.plus-assistantembedded.json、src/shared/runtime-env.ts、src/presentation/pages/main-popup.ts、src/presentation/bootstrap/startup.ts、src/index.ts，把当前发布可见名称 “SP·数据库 II” 改为 “SP·数据库 III”。原 step_5 验证扩展为：构建和覆盖 index.js 后搜索 “SP·数据库 II”，只允许历史归档中存在；src、manifest、dist/index.bundle.js、index.js 中不得残留。
deviation_audit:
  original_plan_excerpt: step_1: 修改 src/presentation/pages/main-popup-status.ts，把 label 文案改为“向量混合交火增强方案”，并把下方 small 注释改为提示“使用前请先配置好向量模型以及 rerank 模型；开启后会自动使用向量能力筛选概要索引，并跳过普通远记忆召回流程；旧对话需要点击立即执行远记忆归档”。step_2: 修改 src/presentation/pages/main-popup-worldbook.ts，做同样 label 和 small 注释更新，保持两个入口一致。step_3: 修改 src/presentation/components/status-display.ts 的动态 hint 文案，启用/未归档/关闭三种状态都使用“向量混合交火增强方案”，并保留模型与 rerank 配置提醒。step_4: 修改 src/presentation/pages/popup-bindings-worldbook.ts 的 toast 文案，避免切换后仍提示旧名“纪要向量索引模式”。step_5: 运行 npm run build，确认 TypeScript、Rollup 和架构护栏通过。step_6: 使用 Windows 兼容 copy 命令将 dist/index.bundle.js 覆盖根目录 index.js，并检查 git status。step_7: git add 所有相关变更并提交，提交信息使用 release: spv3 vector hybrid crossfire enhancement。step_8: 推送 main 到 origin/main。step_9: 切换 release，合并 main 的发布提交，推送 origin/release。step_10: 创建 tag spv3 指向 release/main 同一发布提交，并推送 origin spv3。step_11: 输出验收报告并归档 .analysis-cache.md。
  current_proposal: 在原 step_4 与 step_5 之间插入版本显示名统一步骤；构建后增加旧版本名残留搜索；其余发布链路不缩减。
  diff_summary: 新增 manifest 和运行时版本名修改；新增构建后旧版本名残留验证；不删除原 UI 改名、构建、覆盖 index.js、推送 main/release/tag、归档步骤。
  deviation_motive_check:
    - 措辞替换规则逐类检查：未命中捷径导向、放弃正确方案、虚假完成、过度抽象、自我降权、逃避复杂度、内部省略、工具行为降级、伪理性化、执行顺序降级。
    - 偏离后的方案在三维评分上是否低于原方案？→ NO。新增范围提升一致性，不降低任一维度。
    - 偏离是否导致 affected_scope 缩小？→ NO。范围扩大以覆盖用户新增需求。
  self_dissection: 未触发。
degradation_check:
  - 推荐方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。option_b 三维均为 5/5，且避免 option_a 的版本分裂和 option_c 的历史污染。
  - 推荐方案是否遗漏了新发现的边界条件？ → NO。已覆盖 manifest、运行时提示、构建产物同步、历史归档不可篡改、残留搜索。
  - 是否因为想尽快完成而选择了改动量小的方案？ → NO。没有选择只改 UI 的 option_a，而是扩大范围覆盖所有发布可见源头。
  - 修改后的execution_plan是否仍覆盖所有affected_scope？ → YES。新增文件均进入 execution_plan_update，构建产物通过 build 与 copy 处理。
  - 是否有发现了但被判断为“无关紧要”而跳过的问题？ → NO。历史归档中的旧名是历史事实，不是发布残留，不应修改。
  - options 是否包含至少三个方案？ → YES。已列出 option_a、option_b、option_c。
  - 是否因为工具报错而准备换用 shell 命令修改源代码？ → NO。源码和 manifest 继续使用标准编辑工具；命令只用于构建、复制和 Git。
  - deviation_audit 是否触发了 self_dissection？ → NO。变更是用户新增需求引起的范围扩展，不是偷懒降级。
</decision_point>
---
<decision_point>
issue: 执行 extension 补建时命令 cmd /C "set BUILD_MODE=extension&& rollup -c" 失败，错误为“'rollup' 不是内部或外部命令”。这不是代码错误，而是命令绕过 npm scripts 后没有把 node_modules/.bin 加入 PATH。这个问题在原分析中没有预见，因为 npm run build 能正常找到 rollup，但直接在 cmd 中调用 rollup 不会自动注入 npm 的 bin 路径。
impact: YES。dist/extension/index.js 和 dist/extension/manifest.json 当前仍残留 “SP·数据库 II”，如果不补建就发布，extension 产物会显示旧版本名。能跑不等于能交付，发布产物残留旧名就是典型的表面完成。
context_update: 构建策略需要调整：extension 补建不能直接调用 rollup，应通过 npm scripts 或 npx 调用本地 rollup。package.json 已有 build:extension 脚本：BUILD_MODE=extension rollup -c；在 Windows cmd 下 npm 可能使用脚本解释兼容层执行，但该脚本含 Unix 风格环境变量写法，存在兼容不确定性。可使用 npx rollup -c 并在 cmd 中设置 BUILD_MODE，或使用 PowerShell 设置环境变量后执行 npm/rollup。
options:
  - option_a:
      description: 不补建 dist/extension，只发布 dist/index.bundle.js 和 index.js。
      approach_evaluation: 可维护性 1/5，因为仓库保留同一版本两套名称；健壮性 1/5，因为 release 分支或 extension 使用者会看到旧名；可扩展性 1/5，因为后续发布会继续继承脏产物。
      edge_cases: dist/extension/manifest.json 旧名残留；搜索验证无法通过；用户要求“所有版本号”无法满足。
      affected_scope_delta: 缩小构建产物覆盖范围，删除 dist/extension 验证。
  - option_b:
      description: 使用 npm exec 或 npx 调用本地 rollup，并在 cmd 中设置 BUILD_MODE=extension，补建 dist/extension 产物。
      approach_evaluation: 可维护性 5/5，因为仍使用现有 rollup.config.js 的 extensionConfig 和 copy-manifest 插件；健壮性 5/5，因为本地依赖解析由 npm/npx 处理，避免全局 rollup 依赖；可扩展性 5/5，因为后续 CI/本地都能沿用本地依赖构建。
      edge_cases: cmd 环境变量写法必须确保 BUILD_MODE 不含尾随空格；如果 npx 失败，需检查 node_modules 或 package-lock，而不是跳过产物。
      affected_scope_delta: 保持原 affected_scope，不新增源码改动，只补建 dist/extension/index.js 和 dist/extension/manifest.json。
  - option_c:
      description: 手工复制 dist/index.bundle.js 到 dist/extension/index.js，并复制 manifest.json 到 dist/extension/manifest.json。
      approach_evaluation: 可维护性 1/5，因为 userscript IIFE 与 extension ESM 产物格式不同，复制会破坏 extension 运行形态；健壮性 1/5，因为会把油猴头和 IIFE 注入 extension；可扩展性 1/5，因为这种伪产物无法作为后续发布基线。
      edge_cases: extension 加载失败；manifest 虽正确但 index.js 格式错误；问题会在用户安装时爆炸。
      affected_scope_delta: 直接污染 dist/extension/index.js。
recommendation: 选择 option_b。三维评估显示 option_b 在可维护性、健壮性、可扩展性均为 5/5，是综合最优方案。失败原因是 PATH，不是构建配置；正确处理是通过 npm/npx 调用本地 rollup，而不是跳过 extension 或手工复制错误格式产物。
execution_plan_update: 在验证阶段新增一步：执行 cmd /C "set BUILD_MODE=extension&& npx rollup -c" 或等价 npm 本地依赖调用，补建 dist/extension/index.js 和 dist/extension/manifest.json；随后重新搜索 “SP·数据库 II|纪要向量索引模式（与0TK互斥）”，确认除历史归档和 .analysis-cache.md 决策记录外，运行产物无残留。
deviation_audit:
  original_plan_excerpt: step_5: 运行 npm run build，确认 TypeScript、Rollup 和架构护栏通过。step_6: 使用 Windows 兼容 copy 命令将 dist/index.bundle.js 覆盖根目录 index.js，并检查 git status。
  current_proposal: npm run build 已完成 userscript 构建和架构检查；由于验证发现 dist/extension 旧名残留，需要补充 extension 构建，使用 npx rollup -c 解决 PATH 问题，再继续验证和发布。
  diff_summary: 新增 extension 补建命令；不修改源码；不跳过原构建、覆盖 index.js、Git 发布步骤。
  deviation_motive_check:
    - 措辞替换规则逐类检查：未命中捷径导向、放弃正确方案、虚假完成、过度抽象、自我降权、逃避复杂度、内部省略、工具行为降级、伪理性化、执行顺序降级。
    - 偏离后的方案在三维评分上是否低于原方案？→ NO。补建 extension 提高交付一致性。
    - 偏离是否导致 affected_scope 缩小？→ NO。范围扩大到实际残留产物。
  self_dissection: 未触发。
degradation_check:
  - 推荐方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。option_b 三维均为 5/5，并且直接解决 PATH 导致的命令失败。
  - 推荐方案是否遗漏了新发现的边界条件？ → NO。已覆盖 dist/extension 旧名残留、npx 失败、cmd 环境变量尾随空格、不能复制错误格式产物。
  - 是否因为想尽快完成而选择了改动量小的方案？ → NO。没有选择跳过 dist/extension 的 option_a，也没有选择手工复制的 option_c。
  - 修改后的execution_plan是否仍覆盖所有affected_scope？ → YES。dist/extension 已进入补建和验证范围。
  - 是否有发现了但被判断为“无关紧要”而跳过的问题？ → NO。dist/extension 旧名残留被视为发布阻断问题处理。
  - options 是否包含至少三个方案？ → YES。已列出 option_a、option_b、option_c。
  - 是否因为工具报错而准备换用 shell 命令修改源代码？ → NO。命令只用于构建产物，不用于修改源码；源码已通过标准编辑工具完成。
  - deviation_audit 是否触发了 self_dissection？ → NO。变更是构建命令 PATH 失败后的正确修正。
</decision_point>
