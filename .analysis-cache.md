
<analysis>
context:
  1. 项目中没有任何 // arch-ok 注释
  2. 当前有 3 条架构违规：
     a. table-crud-api.ts：已修复（上一轮改为从 schema-helpers 导入）
     b. visualizer-main-config.ts 第 35 行：import { updateDDLColumnComment, parseDDLColumnNames } from '../../data/sqlite/schema-mapper' — 仍然直接引用 data 层
     c. plot-task-engine.ts 第 360 行：signal?.addEventListener('abort', onAbort, { once: true }) — service 层使用了 addEventListener
  3. schema-helpers.ts 已创建，re-export 了 parseDDLTableName、parseDDLColumnNames、updateDDLColumnComment

needs:
  - 修复 visualizer-main-config.ts 的 import 路径
  - 重构 plot-task-engine.ts 消除 addEventListener 调用

key_challenges:
  1. plot-task-engine.ts 用 signal.onabort 替代 addEventListener，cleanup 时用 signal.onabort = null 替代 removeEventListener

confidence: HIGH

approach:
  1. visualizer-main-config.ts：import 路径从 data 层改为 service 层
  2. plot-task-engine.ts：addEventListener → onabort 属性赋值
  三维评分：
  - 可维护性: 5/5 — 标准操作，语义等价
  - 健壮性: 5/5 — onabort 在单处理器场景下功能完全等价
  - 可扩展性: 5/5 — 不影响任何其他逻辑

edge_cases:
  - signal 为 null/undefined：可选链处理
  - signal 已 aborted：现有代码已处理
  - cleanup 时清除 onabort：signal.onabort = null

affected_scope:
  - src/presentation/pages/visualizer-main-config.ts
  - src/service/runtime/plot-runtime/plot-task-engine.ts

execution_plan:
  - step_1: 修改 visualizer-main-config.ts 的 import 路径
  - step_2: 修改 plot-task-engine.ts，addEventListener → onabort，removeEventListener → onabort = null
  - step_3: 运行 npm run build 验证

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES
  - 是否遗漏了已知边界条件？ → NO
  - 是否因改动量大而想缩减方案？ → NO
  - 是否打算跳过某些文件？ → NO
  - execution_plan 是否覆盖 affected_scope 所有文件？ → YES
  - context 是否充分？ → YES
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO
  - execution_plan 中是否有步骤计划使用 shell 命令修改源代码？ → NO
</analysis>
