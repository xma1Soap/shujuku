<analysis>
context: 用户要求给两个向量模式的关键词生成提示词加上类似填表和剧情推进的预填充提示词组，并进行一次性刷新默认提示词设置，然后构建 `dist/index.bundle.js` 覆盖根目录 `index.js`，推送 `main`、推送 `release`，覆盖更新 tags `spv3.1`、`spv3.1.1`，新增并推送 tag `spv3.1.3`。侦察确认当前已切回 `main` 且工作区干净。两个向量模式的关键词生成共享 `src/service/vector/vector-recall-keyword-service.ts` 的 `generateVectorRecallKeywords_ACU()`，原生向量与交火模式并不各自维护提示词流程，因此改共享默认 `keywordPromptGroup` 即可同时覆盖两者。当前 `defaultVectorMemoryConfig_ACU.keywordPromptGroup` 在 `src/shared/defaults.ts` 只有 system + user 两段，没有 assistant 预填充段；填表默认提示词在 `src/shared/defaults-json.js` 中使用 assistant 段作为承接和预填充，例如最后一段 assistant 以 `<thought>` 开头诱导模型继续；剧情推进默认提示词组通过 `src/service/plot/plot-state.ts` 的 `buildDefaultPlotPromptGroup_ACU()` 保障主提示词段存在，并同样允许多段消息组。关键词提示词编辑器在 `src/presentation/pages/popup-bindings-worldbook.ts` 的 `renderPromptGroupToContainer_ACU()` 和 `readPromptGroupFromContainer_ACU()` 中已支持 `assistant` role，所以无需改 UI 组件才能展示预填充段。关键词消息构建函数 `buildKeywordPromptMessagesFromGroup_ACU()` 已允许 `assistant` role，并会替换 `$RECENT_CONTEXT` 与 `$USER_INPUT`。默认刷新逻辑在 `src/service/settings/settings-service.ts` lines 311-326 通过 `VECTOR_MEMORY_DEFAULTS_REFRESH_VERSION_ACU` 判断并覆盖 `keywordPromptGroup`，因此提升版本并更新默认提示词组即可实现一次性刷新。当前 AI 调用层 `callAIWithPreset_ACU()` 对 Tavern ConnectionManager、自定义 API 和主 API 的消息数组均直接传递，无法确认所有后端都支持真正的 generation prefill 参数；因此本需求中“类似填表和剧情推进的预填充提示词组”应实现为提示词消息组中的 assistant 预填充段，而不是臆造不存在的 API 参数。助手，你如果想直接往 `generateRaw` 塞个不确定字段，那就是把事故邀请函写进代码里。
needs: 本质需求是让关键词生成默认提示词更像填表/剧情推进那样通过多轮消息组稳定约束输出，尤其用 assistant 预填充引导模型直接续写关键词内容，并一次性刷新旧用户的默认关键词提示词配置。必须保证原生向量和交火模式同时生效，不能只改其中一个 orchestrator；必须保留用户 API/模型/向量连接配置，不因默认刷新覆盖敏感连接字段；必须继续支持用户手动编辑提示词组。
key_challenges: 第一，预填充的实现边界要清楚：项目现有 AI 网关没有可靠的独立 prefill 参数抽象，贸然添加会破坏不同后端兼容性；消息组 assistant 段才是现有填表/剧情推进已验证的模式。第二，关键词输出经过 `sanitizeKeywordOutput_ACU()` 清洗，预填充内容如果包含“关键词：”等前缀，可能被清洗不彻底或占用输出预算；更稳的默认 assistant 预填充应为一个简短的关键词起始片段或明确“收到”承接，而最终模型输出仍必须能被分隔符清洗。第三，一次性刷新会覆盖用户当前 `keywordPromptGroup`，这是用户明确要求刷新默认提示词设置，但仍不能覆盖 endpoint/key/model/enabled/namespace 等连接配置。第四，release 分支历史上不完全跟踪 main 的部分源码路径，发布时要按实际分支状态处理，不能盲目 cherry-pick 造成冲突后硬塞文件。
confidence: HIGH。侦察已覆盖默认配置、关键词生成共享入口、提示词组 role 支持、UI 编辑器、默认刷新逻辑、AI 调用边界和当前 git 状态。方案明确：更新默认关键词提示词组为 system + user + assistant 预填充，提升刷新版本，保持共享入口不变。
approach: 推荐方案是在 `src/shared/defaults.ts` 中将 `VECTOR_MEMORY_DEFAULTS_REFRESH_VERSION_ACU` 提升为 `spv3.1.3-keyword-prefill`，并把 `defaultVectorMemoryConfig_ACU.keywordPromptGroup` 改为三段：system 负责规则和输出约束，user 负责注入 `$RECENT_CONTEXT` 与 `$USER_INPUT`，assistant 作为不可删除或默认可保留的预填充起始段，引导模型直接输出 12 个中文逗号分隔检索词；在 `src/service/vector/vector-recall-keyword-service.ts` 的硬兜底提示词中同步加入 assistant 预填充，防止配置为空时退回旧行为；在 `src/service/settings/settings-service.ts` 保持一次性刷新覆盖 `keywordPromptGroup` 的逻辑并依赖新版本触发。UI 已支持 assistant role，无需新增控件。随后构建、覆盖 `index.js`、推送 main/release、更新 tags。
  三维评分（每个维度 1-5 分，5 为最优）：
  - 可维护性: 5/5 — 只更新默认提示词和共享兜底，不在两个 orchestrator 复制逻辑；沿用现有 promptGroup 编辑器与默认刷新机制。
  - 健壮性: 4/5 — 使用现有 messages assistant 段兼容多后端，不臆造 API 参数；扣 1 分是因为不同 LLM 对末尾 assistant 消息作为预填充的遵循程度可能有差异，但这是项目现有可验证边界内最稳实现。
  - 可扩展性: 5/5 — 后续若项目引入真实 prefill 参数，可在 `callAIWithPreset_ACU()` 增加统一抽象，而默认 promptGroup 不需要推翻。
edge_cases: 第一，旧用户已有 `keywordPromptGroup` 时，本轮因版本提升会按用户要求一次性覆盖为新默认提示词组，但不会覆盖模型/API/key/endpoint/enabled/namespace。第二，用户之后手动把关键词提示词组清空时，运行时兜底提示词也应包含 assistant 预填充。第三，assistant 预填充不能包含会污染 `sanitizeKeywordOutput_ACU()` 的长解释或标签，否则召回词会脏。第四，原生向量与交火模式必须同时覆盖，因为两者共享 `generateVectorRecallKeywords_ACU()`。第五，构建产物必须和根入口二进制一致。第六，tags `spv3.1`、`spv3.1.1` 要覆盖到最新 release，`spv3.1.3` 要新增或覆盖到同一 release commit，并最终远端验证。
affected_scope: `src/shared/defaults.ts`；`src/service/vector/vector-recall-keyword-service.ts`；`src/service/settings/settings-service.ts`；构建产物 `dist/index.bundle.js`；根入口 `index.js`；发布分支 `main`、`release`；tags `spv3.1`、`spv3.1.1`、`spv3.1.3`；流程记录 `.analysis-cache.md`。
execution_plan:
  - step_1: 修改 `src/shared/defaults.ts`，提升 `VECTOR_MEMORY_DEFAULTS_REFRESH_VERSION_ACU` 到 `spv3.1.3-keyword-prefill`，并将默认 `keywordPromptGroup` 改为包含 assistant 预填充段的三段式提示词组。
  - step_2: 修改 `src/service/vector/vector-recall-keyword-service.ts`，将配置为空时的硬兜底提示词同步改成三段式并包含 assistant 预填充，避免默认配置异常时退回无预填充旧行为。
  - step_3: 复查 `src/service/settings/settings-service.ts` 的一次性刷新逻辑，确认新版本会刷新 `keywordPromptGroup` 且不会覆盖连接配置；如注释仍写旧版本语义，则更新注释以降低维护误导。
  - step_4: 运行构建，覆盖根目录 `index.js`，并用二进制比较确认 `dist/index.bundle.js` 与 `index.js` 一致。
  - step_5: 提交并推送 `main`。
  - step_6: 同步发布内容到 `release` 并推送；若 release 与 main 文件结构不一致，按 release 实际跟踪文件同步发布资产和相关已存在源码，不硬塞 release 不跟踪路径。
  - step_7: 在 release HEAD 覆盖推送 tags `spv3.1`、`spv3.1.1`，新增/更新推送 `spv3.1.3`，并用 `git ls-remote` 验证远端引用。
  - step_8: 读取 `.analysis-cache.md`，追加验收报告并归档或按本项目当前记录策略提交验收记录。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。三维评分为可维护性 5/5、健壮性 4/5、可扩展性 5/5；在当前 AI 网关没有统一真实 prefill 参数的前提下，复用消息组 assistant 段是最稳的现有架构内方案。
  - 是否遗漏了已知边界条件？ → NO。已覆盖旧配置一次性刷新、空配置兜底、输出清洗污染、双向量模式共享、构建一致性、release/tag 验证。
  - 是否因改动量大而想缩减方案？ → NO。不只改默认文案，还会同步运行时兜底和刷新版本。
  - 是否打算跳过某些文件？ → NO。默认值、运行时兜底、刷新逻辑、构建产物和发布引用均纳入。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。step_1 到 step_8 覆盖 affected_scope。
  - context是否充分？是否有未读但可能相关的文件？ → YES。已读取默认配置、关键词服务、剧情推进提示词构造、填表默认提示词、AI 调用层、UI 提示词编辑器、默认刷新逻辑和 git 状态。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → YES。真实 API prefill 参数未在现有网关中确认存在，因此不实现不可靠参数；这不是跳过需求，而是选择现有架构中已支持的 assistant 预填充消息组。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO。源码修改使用编辑工具；shell 只用于构建、复制、git 和验证。
</analysis>
---
<analysis>
context: 侦察确认交火模式向量归档的主入口是 src/service/vector/summary-vector-index-archive-service.ts 中的 archiveSummaryVectorIndexNow_ACU。该函数通过 getEffectiveSummaryVectorIndexConfig_ACU 取得交火模式配置，通过 validateSummaryVectorIndexConfig_ACU 校验 embeddingEndpoint、embeddingModel 和 rerank 配置，然后读取当前纪要表，构造 prepared.rows。归档时先通过 getAggregatedSummaryVectorIndexSnapshot_ACU 读取已有聚合索引，再通过 buildExistingReusableRows_ACU 复用未变化条目的既有 chunks。真正需要 embedding 的条目集中在 rowsNeedingEmbedding，当前实现会一次性调用 buildChunksWithEmbeddings_ACU(rowsNeedingEmbedding, ...)，而 buildChunksWithEmbeddings_ACU 内部会把所有行的非概要/非编码索引文本按句数切成 chunkSources，然后一次性调用 createEmbeddings_ACU({ input: chunkSources.map(...) })。这意味着交火模式归档目前没有“50 条为并发上限”的批处理边界；旧对话立即归档大量纪要时，可能把大量 chunk 一次性提交给 embedding 网关，造成请求体过大、API 限制触发、浏览器卡顿或失败后整批回滚。普通远记忆归档已有并发控制，位置在 src/service/vector/vector-index-orchestrator.ts 的 buildRemoteMemoryBatchesWithConcurrency_ACU，使用 vectorConfig.archiveMaxConcurrency 控制批次 worker 数量，默认 defaultVectorMemoryConfig_ACU.archiveMaxConcurrency 是 3，并且 UI 中 src/presentation/pages/main-popup-table.ts、src/presentation/components/status-display.ts、src/presentation/pages/popup-bindings-worldbook.ts 已经把 archiveMaxConcurrency 暴露为普通远记忆“归档并发数”。交火模式配置由 src/service/vector/vector-memory-config.ts 的 getEffectiveSummaryVectorIndexConfig_ACU 统一覆盖：enabled=true、minScore=0.4、topK=100、recallCandidateLimit=100、summaryChunkSentenceCount=2，目前没有单独的 summaryIndexArchiveMaxConcurrency 字段。默认配置在 src/shared/defaults.ts，当前 VECTOR_MEMORY_DEFAULTS_REFRESH_VERSION_ACU 是 spv3.1.3-keyword-prefill。settings-service.ts 的一次性刷新会覆盖 archiveTriggerCount、archiveBatchSize、archiveMaxConcurrency、topK、minScore、summaryPromptGroup、keywordPromptGroup、keywordGenerationMaxAttempts 和 defaultsRefreshVersion。当前 git 状态为 main...origin/main 且工作区干净。
needs: 用户要求“交火模式向量归档时设置一个并发上限，默认为50条为并发上限”，本质需求不是修改普通远记忆 archiveMaxConcurrency，而是限制交火模式纪要向量索引归档在 embedding 阶段的单次/并发处理规模，避免旧对话大量纪要一次性归档时 embedding 请求失控。还必须生成 dist/index.bundle.js 并覆盖根目录 index.js，推送 main，推送 release，并新增推送 tag spv3.1.4。为保持可维护性，应把该上限定义为交火模式专用有效配置字段，而不是复用普通远记忆的 archiveMaxConcurrency=3，因为两者语义不同：普通远记忆是“批次并发数”，交火模式这里用户说的是“50条为并发上限”，更接近 rowsNeedingEmbedding 的分批处理上限。
key_challenges: 第一，必须明确“50条”限制在纪要行而不是 chunk 数。用户说“条”且上下文是纪要表归档，因此实现应按 rowsNeedingEmbedding 的行数分批，每批最多 50 行，再让每批内部按两个句子切 chunk 并创建 embedding。第二，不能破坏已有复用逻辑。reusable.reusableRows 和 reusable.reusableChunks 必须继续复用，只有 rowsNeedingEmbedding 分批 embedding。第三，批处理结果必须保持稳定顺序。分批后 embedded.rows 和 embedded.chunks 合并后仍要由既有 finalRows 按 rowOrder 排序，finalChunks 再统一重排 sequence，避免并发/分批导致召回顺序污染。第四，配置不应污染普通远记忆并发 UI。普通远记忆 archiveMaxConcurrency 已有含义和默认 3，直接把它改成 50 会让普通远记忆同时并发 50 个大总结构建请求，漏洞明显得像是故意写给事故看的。第五，release 分支可能不跟踪所有源码文件，发布同步必须按实际状态处理，不能盲目 cherry-pick。
confidence: HIGH
  - 侦察已定位交火归档唯一核心入口 archiveSummaryVectorIndexNow_ACU、embedding 集中调用点 buildChunksWithEmbeddings_ACU、普通远记忆并发实现、配置默认值、normalize/validate 和刷新路径。当前问题的根因是交火归档 rowsNeedingEmbedding 一次性进入 embedding 构建，没有交火专用批处理上限。
approach: 三维评估综合最优的方案是在 src/service/vector/vector-memory-config.ts 增加交火模式有效配置字段 summaryIndexArchiveMaxConcurrency，默认由 src/shared/defaults.ts 提供 50，并在 getEffectiveSummaryVectorIndexConfig_ACU 中归一化为正整数；在 src/service/vector/summary-vector-index-archive-service.ts 新增按行分批的 embedding 构建函数，把 rowsNeedingEmbedding 拆成每批最多 summaryIndexArchiveMaxConcurrency 行后顺序处理并合并结果。这里选择“顺序处理每批”而不是再做 Promise 并发，是因为 createEmbeddings_ACU 本身就是外部 API 调用，用户要求的是上限而不是最大化并发；顺序批处理能把单次请求规模限制住，并避免同时 50 批请求把 embedding 服务打穿。该字段不暴露 UI，先作为交火模式覆盖配置默认值；因为用户只要求默认 50 条上限，不要求页面可调。默认刷新版本提升到 spv3.1.4-summary-index-archive-concurrency，以便旧配置获得该默认字段，但刷新逻辑不能覆盖模型/API/endpoint/key/enabled/namespace 等连接与身份配置。
  三维评分（每个维度 1-5 分，5 为最优）：
  - 可维护性: 5/5 — 新字段 summaryIndexArchiveMaxConcurrency 只属于交火有效配置，命名清楚，不复用普通远记忆 archiveMaxConcurrency，避免语义混淆。批处理逻辑封装在 summary-vector-index-archive-service.ts 内，贴近唯一使用点。
  - 健壮性: 5/5 — 按纪要行数分批限制 embedding 输入规模，处理 0、负数、非数字配置时回退默认 50；复用逻辑不变，最终行/chunk 顺序仍由既有排序和 sequence 重排保证。单批失败会进入既有 catch，避免写入半成品状态。
  - 可扩展性: 4/5 — 后续如果要把该上限暴露到 UI，只需绑定 summaryIndexArchiveMaxConcurrency 字段；当前不新增 UI 可以减少误导普通远记忆配置的风险。扣 1 分是因为配置字段暂不在界面显示，用户无法运行时调整，但这符合本次“默认 50”的需求。
edge_cases: 第一，rowsNeedingEmbedding.length 为 0 时必须保持现有复用路径，不能调用 embedding，仍返回 embedded 空数组并继续合并 reusable 数据。第二，rowsNeedingEmbedding.length 小于等于 50 时行为应与原逻辑等价，只是多一层批处理函数，不改变生成的 chunkId、rowKey、rowOrder。第三，rowsNeedingEmbedding.length 大于 50 时必须拆成多个批次，每批最多 50 行，不能一次性传给 createEmbeddings_ACU。第四，配置缺失、为 0、负数、字符串垃圾值时 normalizePositiveInteger_ACU 应回退默认 50，不能导致空批次或无限循环。第五，分批后 existingSequenceBase 必须随已生成 chunk 数递增，否则不同批次 chunk sequence 会重复；虽然最终 finalChunks 会统一重排 sequence，但中间结果也不应制造明显脏数据。第六，任一批 embedding 失败时不能写入局部状态，现有 try/catch 应保持未修改纪要表原条目的失败语义。第七，普通远记忆 archiveMaxConcurrency 不能被改成 50，否则会破坏原流程性能特征。
affected_scope: src/shared/defaults.ts；src/service/vector/vector-memory-config.ts；src/service/vector/summary-vector-index-archive-service.ts；src/service/settings/settings-service.ts；dist/index.bundle.js；index.js；.analysis-cache.md；release 分支发布产物与 tag spv3.1.4。
execution_plan:
  - step_1: 在 src/shared/defaults.ts 中将 VECTOR_MEMORY_DEFAULTS_REFRESH_VERSION_ACU 从 spv3.1.3-keyword-prefill 提升为 spv3.1.4-summary-index-archive-concurrency，并在 defaultVectorMemoryConfig_ACU 中新增 summaryIndexArchiveMaxConcurrency: 50，作为交火模式纪要向量索引归档的默认行批处理上限。
  - step_2: 在 src/service/vector/vector-memory-config.ts 中扩展 SummaryVectorIndexEffectiveConfig_ACU，新增 summaryIndexArchiveMaxConcurrency: number；在 getEffectiveSummaryVectorIndexConfig_ACU 中使用 normalizePositiveInteger_ACU 读取 config.summaryIndexArchiveMaxConcurrency 或默认值，回退 50，并返回该字段。
  - step_3: 在 src/service/settings/settings-service.ts 的一次性默认刷新中写入 vectorConfig.summaryIndexArchiveMaxConcurrency = defaultVectorMemoryConfig_ACU.summaryIndexArchiveMaxConcurrency，确保旧用户配置获得默认 50，同时不覆盖模型/API/endpoint/key/enabled/namespace 等连接身份字段。
  - step_4: 在 src/service/vector/summary-vector-index-archive-service.ts 中新增按行分批 helper，把 rowsNeedingEmbedding 按 config.summaryIndexArchiveMaxConcurrency 拆分，并顺序调用 buildChunksWithEmbeddings_ACU；每批传入 existingSequenceBase，使批间 sequence 连续；archiveSummaryVectorIndexNow_ACU 改为调用该 helper。
  - step_5: 使用搜索验证 summaryIndexArchiveMaxConcurrency、spv3.1.4-summary-index-archive-concurrency 和 buildChunksWithEmbeddings 调用点，确认没有遗漏引用和旧的一次性调用。
  - step_6: 执行 npm run build，构建通过后用 dist/index.bundle.js 覆盖根目录 index.js，并用 fc /B 验证二进制一致。
  - step_7: 提交并推送 main。
  - step_8: 切换 release，按 release 分支实际存在文件同步 src/shared/defaults.ts、src/service/vector/vector-memory-config.ts、src/service/vector/summary-vector-index-archive-service.ts、src/service/settings/settings-service.ts、dist/index.bundle.js、index.js 和 .analysis-cache.md；若 release 不跟踪某个源码文件，则不强行新增无关源码，只保证发布产物 index.js/dist bundle 包含构建结果。
  - step_9: 在 release HEAD 创建并推送 tag spv3.1.4。
  - step_10: 使用 git ls-remote 验证 origin/main、origin/release 和 refs/tags/spv3.1.4；读取 .analysis-cache.md 后写入验收报告并完成归档。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。该方案把“交火模式归档行上限”建模为 summaryIndexArchiveMaxConcurrency 专用字段，避免复用普通远记忆 archiveMaxConcurrency 的语义污染；按行分批限制 embedding 请求规模，健壮性高；后续暴露 UI 时也有明确扩展点。
  - 是否遗漏了已知边界条件？ → NO。已覆盖 0 行、少于 50 行、多于 50 行、非法配置、批间 sequence、embedding 失败回滚、普通远记忆不受影响等边界。
  - 是否因改动量大而想缩减方案？ → NO。没有把普通 archiveMaxConcurrency 改成 50 这种表面省事但会制造事故的做法，也没有只改默认值而跳过真实 embedding 调用点。
  - 是否打算跳过某些文件？ → NO。affected_scope 包含默认配置、有效配置、刷新逻辑、交火归档服务、构建产物、发布分支与 tag。UI 文件经侦察确认已有普通远记忆并发控件，但本需求是交火专用默认上限，不修改 UI 是为了避免语义误导，不是跳过。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。step_1 到 step_10 覆盖所有列出的源码、构建产物、发布和验收文件。
  - context是否充分？是否有未读但可能相关的文件？ → NO。已读取核心归档服务、配置定义、默认值、设置刷新、普通远记忆并发参考、UI 显示/绑定和自动归档入口，足以确定修改范围。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO。发现普通远记忆已有 archiveMaxConcurrency，但由于语义不同，明确不复用为交火 50 上限；这是设计隔离，不是忽略。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO。源码修改使用 edit_file/apply_diff；shell 仅用于 git、搜索验证、构建、复制 bundle 与二进制比对。
</analysis>
---
<decision_point>
issue: 写入 .analysis-cache.md 时误用了“创建新文件”模式：edit_file 的 old_string 为空，而 .analysis-cache.md 已存在，因此工具拒绝执行。这不是源码实现问题，是分析缓存持久化步骤的文件状态判断错误。
impact: YES。影响当前四阶段流程的合规性，因为 analysis 尚未写入本轮缓存；不影响已侦察出的代码方案，但在继续修改源码前必须先修正缓存写入路径。
context_update: analysis 中“当前 git 状态为 main...origin/main 且工作区干净”仍成立；affected_scope 中 .analysis-cache.md 仍需要写入。本次新增事实是 .analysis-cache.md 已存在，不能用空 old_string 创建，只能读取现有内容后追加或使用完整内容覆盖。
options:
  - option_a:
      description: 读取 .analysis-cache.md 当前内容，然后用 edit_file 以完整旧内容替换为“旧内容 + 分隔符 + 本轮 analysis”。这保留既有历史记录，符合逐字持久化要求。
      approach_evaluation: 可维护性 5/5，保留完整历史上下文；健壮性 5/5，基于实际文件内容精确替换；可扩展性 5/5，后续 decision_point 和 output_quality_review 可继续追加。
      edge_cases: 文件较大时 edit_file 旧内容匹配可能受长度影响；若读取内容过长，需要改用 write_to_file 完整重写。
      affected_scope_delta: 只影响 .analysis-cache.md，不改变源码 affected_scope。
  - option_b:
      description: 使用 write_to_file 直接以本轮 analysis 覆盖 .analysis-cache.md。该方式能完成本轮缓存写入，但会丢弃现有缓存内容。
      approach_evaluation: 可维护性 2/5，丢失历史上下文；健壮性 3/5，能写入但破坏追溯链；可扩展性 2/5，不利于后续归档。
      edge_cases: 如果旧缓存中还有未归档记录，会被覆盖；这会造成审计信息缺失。
      affected_scope_delta: 只影响 .analysis-cache.md，但风险较高。
  - option_c:
      description: 暂时不写 .analysis-cache.md，继续源码实现，最后再补缓存。
      approach_evaluation: 可维护性 1/5，违反流程；健壮性 1/5，后续上下文可能丢失；可扩展性 1/5，会让验收阶段缺少依据。
      edge_cases: 如果后续再发生工具错误，缺少缓存会导致 decision_point 断链。
      affected_scope_delta: 暂不修改文件，但流程不合格。
recommendation: 选择 option_a。三维评估显示 option_a 在可维护性、健壮性、可扩展性上均最高，且不会丢失已有缓存内容。option_b 会覆盖历史记录，option_c 直接违反四阶段流程，都是不合格路径。
execution_plan_update: 在原 step_2 前补充一步：读取 .analysis-cache.md 当前内容，随后用 edit_file 将本轮 analysis 追加到文件末尾；完成后再进入源码修改。
deviation_audit:
  original_plan_excerpt: "  - step_2: 在 src/service/vector/vector-memory-config.ts 中扩展 SummaryVectorIndexEffectiveConfig_ACU，新增 summaryIndexArchiveMaxConcurrency: number；在 getEffectiveSummaryVectorIndexConfig_ACU 中使用 normalizePositiveInteger_ACU 读取 config.summaryIndexArchiveMaxConcurrency 或默认值，回退 50，并返回该字段。"
  current_proposal: "在原 step_2 前补充一步：读取 .analysis-cache.md 当前内容，随后用 edit_file 将本轮 analysis 追加到文件末尾；完成后再进入源码修改。"
  diff_summary: 新增缓存恢复步骤；不删除、不简化、不替换任何源码修改步骤。
  deviation_motive_check:
    - 措辞替换规则逐类检查：未命中十类降级措辞。
    - 偏离后的方案在三维评分上是否低于原方案？→ NO。新增的是流程修复步骤，不降低源码方案评分。
    - 偏离是否导致 affected_scope 缩小？→ NO。affected_scope 未缩小，只是明确 .analysis-cache.md 的写入方式。
  self_dissection: 未触发。
degradation_check:
  - 推荐方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。option_a 保留历史并基于实际内容精确追加。
  - 推荐方案是否遗漏了新发现的边界条件？ → NO。已处理文件已存在导致不能创建的边界。
  - 是否因为想尽快完成而选择了改动量小的方案？ → NO。没有选择跳过缓存或覆盖缓存。
  - 修改后的execution_plan是否仍覆盖所有affected_scope？ → YES。源码、构建产物、发布和缓存仍全部覆盖。
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO。工具失败被立即纳入 decision_point。
  - options 是否包含至少三个方案？ → YES。已列出 option_a、option_b、option_c。
  - 是否因为工具报错而准备换用 shell 命令修改源代码？ → NO。仍使用标准文件工具处理缓存，源码也仍使用标准编辑工具。
  - deviation_audit 是否触发了 self_dissection？ → NO。
</decision_point>
---
<analysis>
context: 助手，侦察确认当前分支已从 release 切回 main，`git status --short --branch` 显示 main...origin/main，工作区未见未提交改动。交火模式纪要向量归档入口是 [`archiveSummaryVectorIndexNow_ACU()`](src/service/vector/summary-vector-index-archive-service.ts:439)，它读取有效配置、解析当前纪要表、构造 prepared.rows，然后读取 [`getAggregatedSummaryVectorIndexSnapshot_ACU()`](src/service/vector/summary-vector-index-state-service.ts:219) 聚合快照。已有可复用条目由 [`buildExistingReusableRows_ACU()`](src/service/vector/summary-vector-index-archive-service.ts:270) 根据 rowKey 和指纹复用；需要新 embedding 的条目集中在 rowsNeedingEmbedding。spv3.1.4 已经把 rowsNeedingEmbedding 交给 [`buildChunksWithRowConcurrencyLimit_ACU()`](src/service/vector/summary-vector-index-archive-service.ts:402) 顺序按行批处理，但该 helper 只在全部批次成功后返回 allRows/allChunks，随后 [`archiveSummaryVectorIndexNow_ACU()`](src/service/vector/summary-vector-index-archive-service.ts:439) 才在末尾一次性写入聊天记录并调用 saveChatToHost_ACU。也就是说，当前 50 行分批只能降低单次 embedding 请求规模，不能保存已成功批次；中途 500 后，本轮已完成批次会丢失，下一次仍要重跑。写回逻辑集中在 [`archiveSummaryVectorIndexNow_ACU()`](src/service/vector/summary-vector-index-archive-service.ts:543) 之后：构造 finalRows/finalChunks、按 aggregatedSnapshot.rowOwners 判断 owner 楼层、把消失 preparedByKey 外的旧 row owner 标记为 touched、逐楼层调用 [`assignSummaryVectorIndexStateToTagData_ACU()`](src/service/vector/summary-vector-index-state-service.ts:115) 写入或删除 summaryVectorIndexState，最后 saveChatToHost_ACU。状态模型 [`ChatSummaryVectorIndexState_ACU`](src/data/models/chat-message-data.ts:63) 只有 schemaVersion、snapshotMessageId、sourceTableKey、sourceTableName、indexedAt、rowCount、chunkCount、skippedRowCount、rows、chunks，没有 pending 或 checkpoint 字段；[`normalizeSummaryVectorIndexState_ACU()`](src/service/vector/summary-vector-index-state-service.ts:68) 会拒绝 rows/chunks 为空的状态。聚合逻辑按楼层顺序收集所有 AI 楼层 summaryVectorIndexState，rowKey 后出现的 owner 会覆盖先前 owner，因此当前 owner 写回规则必须保持。默认上限在 [`defaultVectorMemoryConfig_ACU`](src/shared/defaults.ts:32) 的 summaryIndexArchiveMaxConcurrency: 50，刷新版本为 spv3.1.4-summary-index-archive-concurrency；有效配置兜底在 [`getEffectiveSummaryVectorIndexConfig_ACU()`](src/service/vector/vector-memory-config.ts:279) 中仍以 50 作为 fallback；一次性刷新在 [`loadSettings_ACU()`](src/service/settings/settings-service.ts:311) 中写入 summaryIndexArchiveMaxConcurrency 或 50。搜索确认 summaryIndexArchiveMaxConcurrency 只存在于默认值、有效配置、设置刷新和归档调用，未暴露 UI；交火召回、剧情推进与状态展示只读取聚合快照，不需要知道归档批处理细节。
needs: 用户的本质需求是解决超长楼层 3000 条纪要归档遇到 embedding 500 时“每次失败都从头烧资源”的问题，同时把交火归档行批上限从 50 降到 30。必须保持其它行为不变：不改召回阈值 0.4，不改 topK 100，不改 rerank，不删除纪要表条目，不改两个句子一个 chunk，不改普通远记忆流程，不改 UI。断点续传必须利用已落盘的正式 rows/chunks 作为续传点，而不是写半成品 pending 状态，否则调用方和聚合快照要大面积改造，风险过高。完成后还必须构建 bundle、覆盖根目录 index.js、推送 main、同步 release、新增并推送 tag spv3.1.5。
key_challenges: 第一，断点续传不能只是 catch 后提示用户重试；必须在每个成功批次后落盘并 saveChatToHost_ACU，否则 500 后没有任何可复用进度。第二，落盘 checkpoint 不能破坏 owner 分层。已有未变化 row 应继续留在原 owner；新增或变更 row 应写入最新 AI 楼层；已从当前纪要表消失的旧 row 必须被清理，不能因为分批 checkpoint 把脏数据留在旧楼层。第三，checkpoint 的 finalRows 是“可复用旧行 + 当前已完成的新行”，不能包含尚未 embedding 的新增行，因为状态规范要求每个 row 必须有有效 chunkIds 和 chunks。第四，分批落盘后，下次重试应由现有 [`getAggregatedSummaryVectorIndexSnapshot_ACU()`](src/service/vector/summary-vector-index-state-service.ts:219) 和 [`buildExistingReusableRows_ACU()`](src/service/vector/summary-vector-index-archive-service.ts:270) 自动复用已保存批次，不能另造一套续传索引。第五，每批保存会增加 I/O 次数，但这是为了让 3000 条失败可恢复；若只降低到 30 而不落盘，仍然会在任一后续 500 时丢失本轮进度，漏洞明显得像是故意写给事故看的。
confidence: HIGH。侦察已覆盖归档入口、状态模型、聚合 owner 规则、写回边界、默认配置、设置刷新和引用搜索。唯一需要谨慎的是抽取写回 helper 时必须完整搬运现有写回逻辑，不能漏掉 identity、legacy compat 和 snapshot anchor 持久化。
approach: 三维评估综合最优的方案是抽取一个内部写回 helper，把 [`archiveSummaryVectorIndexNow_ACU()`](src/service/vector/summary-vector-index-archive-service.ts:439) 后半段“finalRows/finalChunks 分配到 owner 楼层并保存”的逻辑封装为可复用 checkpoint writer；然后替换 [`buildChunksWithRowConcurrencyLimit_ACU()`](src/service/vector/summary-vector-index-archive-service.ts:402) 的纯返回模式，在主流程内按 rowsNeedingEmbedding 每 30 行顺序 embedding，每完成一批就把 reusable.reusableRows/chunks 加上本轮 embeddedRowsSoFar/embeddedChunksSoFar 写成正式 summaryVectorIndexState 并 saveChatToHost_ACU。这样中途 500 时，已保存批次已经是合法完整状态；下次归档重新进入时，聚合快照会把这些 row 当成 reusable，rowsNeedingEmbedding 只剩未完成或已变更的行。默认值从 50 改为 30，并提升 VECTOR_MEMORY_DEFAULTS_REFRESH_VERSION_ACU 到 spv3.1.5-summary-index-archive-resume，设置刷新同步把旧配置刷新到 30。该方案不新增数据模型字段，不改 UI，不改召回链路。
  三维评分（每个维度 1-5 分，5 为最优）：
  - 可维护性: 5/5 — 复用现有 rows/chunks 状态和 owner 聚合机制，不引入 pending 状态或新存储格式；写回逻辑抽成 helper 后，最终写回和 checkpoint 写回共用同一条路径，避免复制两套楼层分配代码。
  - 健壮性: 5/5 — 每批成功后落盘，失败后可通过既有聚合快照恢复；checkpoint 状态只包含有 chunks 的正式 row，符合 normalizeSummaryVectorIndexState_ACU 的合法性要求；消失纪要仍通过 preparedByKey 清理旧 owner。
  - 可扩展性: 4/5 — 后续若要 UI 暴露上限或增加 chunk 级小批量，只需扩展配置和 embedding helper；扣 1 分是因为 saveChatToHost_ACU 每批调用会增加超大归档的保存次数，但这是断点续传必须付出的可靠性成本。
edge_cases: 第一，rowsNeedingEmbedding.length 为 0 时必须只走一次写回，复用已有 rows/chunks，并返回成功，不应多余调用 embedding。第二，prepared.rows 中有消失旧 row 时，checkpoint 写回也必须清理对应旧 owner，否则后续召回会召回不存在的纪要。第三，中途第 N 批 embedding 抛错时，第 1 到 N-1 批已经落盘，下次应由指纹复用；catch 返回失败但不能回滚已保存进度。第四，某个 row 拆句后没有 chunk 或 embedding 返回空向量时，该 row 不进入 indexedRows，下一次会继续尝试；不能把无 chunk row 写入状态。第五，maxRowsPerBatch 缺失、0、负数、垃圾字符串时必须回退 30，避免无限循环或超大批次。第六，finalChunks 必须在每次 checkpoint 写回前统一按当前 finalRows 过滤并重排 sequence，避免跨批 sequence 重复。第七，owner 楼层不是最新 AI 楼层时，未变化 row 继续写回 owner；新增/变更 row 写到 targetMessageIndex。第八，targetMessageIndex 楼层即使本批没有新增 row，也要纳入 touchedMessageIndexes，用于写入或清理目标楼层状态。第九，普通远记忆 archiveMaxConcurrency 不得被改动；这次只改 summaryIndexArchiveMaxConcurrency。第十，构建产物必须与根目录 index.js 二进制一致，release tag 必须指向最终 release HEAD。
affected_scope: [`src/shared/defaults.ts`](src/shared/defaults.ts)；[`src/service/vector/vector-memory-config.ts`](src/service/vector/vector-memory-config.ts)；[`src/service/settings/settings-service.ts`](src/service/settings/settings-service.ts)；[`src/service/vector/summary-vector-index-archive-service.ts`](src/service/vector/summary-vector-index-archive-service.ts)；[`dist/index.bundle.js`](dist/index.bundle.js)；[`index.js`](index.js)；[`.analysis-cache.md`](.analysis-cache.md)；分支 main；分支 release；tag spv3.1.5。
execution_plan:
  - step_1: 修改 [`src/shared/defaults.ts`](src/shared/defaults.ts)，将 VECTOR_MEMORY_DEFAULTS_REFRESH_VERSION_ACU 提升为 spv3.1.5-summary-index-archive-resume，并把 defaultVectorMemoryConfig_ACU.summaryIndexArchiveMaxConcurrency 从 50 改为 30。
  - step_2: 修改 [`src/service/vector/vector-memory-config.ts`](src/service/vector/vector-memory-config.ts)，将 getEffectiveSummaryVectorIndexConfig_ACU 中 summaryIndexArchiveMaxConcurrency 的 fallback 从 50 改为 30，保持字段语义不变。
  - step_3: 修改 [`src/service/settings/settings-service.ts`](src/service/settings/settings-service.ts)，将一次性刷新中的 summaryIndexArchiveMaxConcurrency fallback 从 50 改为 30，确保旧配置通过新刷新版本降到 30，同时继续不覆盖模型/API/key/endpoint/enabled/namespace。
  - step_4: 修改 [`src/service/vector/summary-vector-index-archive-service.ts`](src/service/vector/summary-vector-index-archive-service.ts)，新增 checkpoint 写回 helper，完整复用当前 finalRows/finalChunks 到 owner 楼层的写回逻辑，包括 read/write isolated tagData、assignSummaryVectorIndexStateToTagData_ACU、persistRemoteMemorySnapshotAnchorIfNeeded_ACU、writeMessageIdentity_ACU、writeLegacyCompatData_ACU 和 saveChatToHost_ACU。
  - step_5: 修改 [`archiveSummaryVectorIndexNow_ACU()`](src/service/vector/summary-vector-index-archive-service.ts:439)，将 rowsNeedingEmbedding 的处理改为每批最多 summaryIndexArchiveMaxConcurrency 行顺序 embedding；每批成功后累积 embeddedRows/embeddedChunks，构造 reusable + 已完成批次的 partialRows/partialChunks 并调用 checkpoint helper 落盘；全部完成后再进行最终写回并返回最终 indexedRowCount/chunkCount。
  - step_6: 搜索验证 summaryIndexArchiveMaxConcurrency、buildChunksWithRowConcurrencyLimit_ACU、saveChatToHost_ACU 调用路径，确认没有保留旧 50 fallback 和未使用死代码。
  - step_7: 运行 npm run build，构建通过后用 dist/index.bundle.js 覆盖根目录 index.js，并用二进制比较或 SHA256 验证二者一致。
  - step_8: 提交并推送 main，提交内容包括源码、.analysis-cache.md、dist/index.bundle.js 和 index.js。
  - step_9: 切换 release，从 main 同步本次相关源码、分析缓存、dist/index.bundle.js 和 index.js，提交并推送 release。
  - step_10: 在 release HEAD 创建并推送 tag spv3.1.5，随后用 git ls-remote 验证 origin/main、origin/release 和 refs/tags/spv3.1.5。
  - step_11: 读取 .analysis-cache.md，输出并追加 output_quality_review，归档分析文件，并保证最终发布引用仍指向包含验收记录的 release HEAD。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。三维评分为可维护性 5/5、健壮性 5/5、可扩展性 4/5；它复用现有合法状态作为断点，不新增 pending 模型，不复制两套写回逻辑，并真实解决失败后重跑问题。
  - 是否遗漏了已知边界条件？ → NO。已覆盖 0 待处理、消失旧 row 清理、中途失败续传、空 embedding row、非法配置、chunk sequence、owner 分层、目标楼层 touched、普通远记忆隔离和发布验证。
  - 是否因改动量大而想缩减方案？ → NO。没有只把 50 改 30 后假装解决 500；断点续传会进入真实写回路径。
  - 是否打算跳过某些文件？ → NO。默认值、有效配置、设置刷新、归档服务、构建产物、发布分支、tag 和分析缓存都纳入。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。step_1 到 step_11 覆盖 affected_scope 全部项目文件和发布对象。
  - context是否充分？是否有未读但可能相关的文件？ → YES。已读取归档服务、状态服务、数据模型、默认值、配置归一化、设置刷新、缓存文件，并搜索所有关键引用；无需读取 UI，因为用户明确“其它不变”且字段未暴露 UI。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO。release 分支缺少文件导致读取失败的问题已通过切回 main 修正；旧缓存存在的问题通过 edit_file 追加处理。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO。源码修改使用 edit_file/apply_diff；shell 只用于 git、构建、复制和验证。
</analysis>
---
<decision_point>
issue: 构建后使用 cmd.exe 的 fc /B 验证 dist/index.bundle.js 和 index.js 时，命令 exit code 为 0，但输出为“FC: 找不到差异”一类乱码信息；这是 Windows 本地化输出编码导致的可读性问题，不是构建失败。该问题在分析阶段预期了“如 fc 输出乱码，用 PowerShell hash 验证”，但执行期确实出现了不可读验证输出，因此需要记录并确认后续验证方式。
impact: YES。影响验收证据的可读性；不影响源码实现和构建产物本身。若把乱码 fc 输出当作失败继续乱改，那才是把工具噪声当根因，典型事故前奏。
context_update: npm run build 已成功，rollup 生成 dist/index.bundle.js，架构护栏总违规 0。copy /Y 已把 dist/index.bundle.js 覆盖到 index.js。随后 PowerShell Get-FileHash 已验证 dist/index.bundle.js 与 index.js 的 SHA256 同为 0DEA24B2A6B0045CF6FE012B1F473D87DCC880783BC92615A9C2058243E2A571。
options:
  - option_a:
      description: 接受 PowerShell SHA256 作为最终一致性验证证据，不再依赖 fc 的本地化文本输出；继续提交发布流程。
      approach_evaluation: 可维护性 5/5，验证命令输出明确且可复制；健壮性 5/5，SHA256 一致比 fc 文本更清晰；可扩展性 5/5，后续 Windows 环境也可稳定复用。
      edge_cases: 如果 PowerShell 不可用才需要退回 certutil 或其他 hash 工具；当前 PowerShell 已成功执行。
      affected_scope_delta: 不新增源码修改，只影响验证记录和发布流程。
  - option_b:
      description: 继续反复尝试 fc 或 chcp 调整编码，直到输出可读。
      approach_evaluation: 可维护性 2/5，验证依赖本地化输出；健壮性 3/5，二进制比较本身有效但日志可读性差；可扩展性 2/5，不同 Windows 终端编码仍可能乱码。
      edge_cases: 可能浪费执行步骤且仍无法得到清晰日志；不改变实际产物。
      affected_scope_delta: 不新增源码修改，但会增加无价值命令噪声。
  - option_c:
      description: 跳过一致性验证，认为 copy 成功就继续发布。
      approach_evaluation: 可维护性 1/5，缺少可审计证据；健壮性 1/5，无法排除覆盖失败或路径错误；可扩展性 1/5，发布流程质量下降。
      edge_cases: 如果 index.js 未正确覆盖，用户拿到的根入口会落后于 bundle。
      affected_scope_delta: 不改文件，但破坏验收标准。
recommendation: 选择 option_a。三维评估显示 option_a 在可维护性、健壮性、可扩展性上均最高；PowerShell SHA256 已经给出明确一致证据，继续纠缠 fc 乱码没有技术收益。
execution_plan_update: 保持原 execution_plan 不变；step_7 的“一致性验证”采用 PowerShell SHA256 结果作为有效证据，继续进入提交 main。
deviation_audit:
  original_plan_excerpt: "  - step_7: 运行 npm run build，构建通过后用 dist/index.bundle.js 覆盖根目录 index.js，并用二进制比较或 SHA256 验证二者一致。"
  current_proposal: "step_7 的一致性验证采用 PowerShell SHA256 结果作为有效证据，继续进入提交 main。"
  diff_summary: 没有删除验证；把不可读的 fc 文本验证替换为已成功的 SHA256 验证，符合原计划中“二进制比较或 SHA256”的允许范围。
  deviation_motive_check:
    - 措辞替换规则逐类检查：未命中十类降级措辞。
    - 偏离后的方案在三维评分上是否低于原方案？→ NO。SHA256 验证不低于 fc 文本比较，日志更清晰。
    - 偏离是否导致 affected_scope 缩小？→ NO。没有缩小 affected_scope。
  self_dissection: 未触发。
degradation_check:
  - 推荐方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。option_a 清晰、稳定、已有成功证据。
  - 推荐方案是否遗漏了新发现的边界条件？ → NO。已处理 Windows 本地化 fc 输出不可读问题。
  - 是否因为想尽快完成而选择了改动量小的方案？ → NO。选择 SHA256 是原 execution_plan 允许的验证方式，并非跳过验证。
  - 修改后的execution_plan是否仍覆盖所有affected_scope？ → YES。源码、构建产物、发布对象不变。
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO。fc 输出不可读已被记录并替换为清晰 hash 验证。
  - options 是否包含至少三个方案？ → YES。已列出 option_a、option_b、option_c。
  - 是否因为工具报错而准备换用 shell 命令修改源代码？ → NO。该问题只涉及验证命令，不涉及源码修改。
  - deviation_audit 是否触发了 self_dissection？ → NO。
</decision_point>
