
<analysis>
context:
  1. prompt-prepare.ts 中原生模式的列索引生成逻辑：content[0].slice(1).map((h, i) => [${i}:${h}])
  2. content[0] 结构是 ['row_id', 'col1_name', 'col2_name', ...]，slice(1) 跳过了 row_id
  3. .map 的 i 从 0 开始，所以第一个实际属性列被标记为 [0:col1_name]
  4. 但实际上 col1_name 在 content 数组中是 index 1（index 0 是 row_id）
  5. 问题出现在两处：空表场景（第 87 行）和有数据场景（第 97 行）
  6. SQLite 模式不受影响
  7. 行索引 [originalRowIndex] 是数据行序号，不是列索引，不需要改

needs:
  - 列索引应该从 1 开始（i + 1），与 content 数组实际索引对齐

key_challenges:
  1. 需要确认下游 table-edit-parser.ts 是否依赖列索引从 0 开始

confidence: HIGH

approach:
  将 .map((h, i) => [${i}:${h}]) 改为 .map((h, i) => [${i + 1}:${h}])
  三维评分：
  - 可维护性: 5/5
  - 健壮性: 5/5
  - 可扩展性: 5/5

edge_cases:
  - 空表场景：列索引同样需要从 1 开始
  - 有数据场景：列索引从 1 开始，行索引不变
  - 下游解析器：需确认是否依赖列索引从 0 开始

affected_scope:
  - src/service/ai/prompt-builder/prompt-prepare.ts（修改列索引生成逻辑）
  - src/service/ai/prompt-builder/table-edit-parser.ts（修改 insertRow 和 updateRow 的列索引解析逻辑）

execution_plan:
  - step_1: [已完成] 搜索 table-edit-parser.ts 中列索引依赖。发现：updateRow 中 colIndex+1 映射到 content，insertRow 中 data[colIndex] 直接用 forEach 索引。两处都需要同步修改。
  - step_2: 修改 prompt-prepare.ts 两处列索引（i → i + 1），使 AI 看到的列索引从 1 开始
  - step_3: 修改 table-edit-parser.ts 的 insertRow：data[colIndex] → data[colIndex + 1]（因为 AI 现在返回的列索引从 1 开始）
  - step_4: 修改 table-edit-parser.ts 的 updateRow：移除 colIndex + 1 偏移，因为 AI 返回的列索引已经从 1 开始，直接用 colIndex 映射到 content 数组
  - step_5: 运行 TS 编译验证

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES
  - 是否遗漏了已知边界条件？ → NO
  - 是否因改动量大而想缩减方案？ → NO
  - 是否打算跳过某些文件？ → NO
  - execution_plan 是否覆盖 affected_scope 所有文件？ → YES
  - context 是否充分？ → NEED CHECK（step_1）
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO
  - execution_plan 中是否有步骤计划使用 shell 命令修改源代码？ → NO
</analysis>
