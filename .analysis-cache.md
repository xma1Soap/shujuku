<analysis>
context: 当前助手面板有三层关键滚动相关容器，全部在 [visualizer-template-assistant.ts](src/presentation/pages/visualizer-template-assistant.ts) 中生成。第一层是面板本体 [buildAssistantPanelStyle_ACU()](src/presentation/pages/visualizer-template-assistant.ts:199)，desktop 模式下仍保留 `overflow-y:auto; overflow-x:hidden;`，也就是“外部滚动”并没有在 panel 这一层被直接删掉。第二层是消息框外壳 [buildAssistantScrollFrameStyle_ACU()](src/presentation/pages/visualizer-template-assistant.ts:215)，我上一次把 desktop 场景改成了 `usesInternalChatScroll ? 'overflow:hidden;' : 'overflow:visible;'`，等价于在 desktop 下把 scroll frame 变成强裁剪容器。第三层是聊天内容容器 [buildAssistantChatContainerStyle_ACU()](src/presentation/pages/visualizer-template-assistant.ts:229)，我把 desktop 也改成了 `overflow-y:auto; min-height:0; height:100%`。这组改动的结果是：desktop 下聊天区拥有内部滚动，而 scroll frame 把它严格裁掉，于是 panel 外层虽然理论上仍可滚动，但由于聊天区高度被锁在 frame 内，外层失去了可滚动内容来源，所以用户感受到“外部滚动没了”。这不是浏览器抽风，而是当前布局职责被改成了“只有内部滚动”。另一个关键事实是，消息区域之后还有 footer，见 [renderVisualizerTemplateAssistantPanel_ACU()](src/presentation/pages/visualizer-template-assistant.ts:1020) 到 [footer 渲染](src/presentation/pages/visualizer-template-assistant.ts:1025)。因此如果完全去掉内部滚动，长消息又会重新把 footer 顶出可见区；如果只保留内部滚动，用户又失去面板整体滚动。用户现在明确要求“两种滚动都存在”，也就是 desktop 下既能在消息框内部滚动消息，又能让整个面板作为外层容器继续滚动。
needs: 需要恢复 desktop 模式下的双层滚动：聊天消息区保留内部滚动能力，避免长消息把聊天框撑穿；同时外层 panel 也必须能随内容增长继续滚动，满足用户已有的交互预期。并且不能影响 fullscreen-overlay 的既有逻辑，尤其不能破坏 mobile 下外层面板滚动。
key_challenges: 真正难点是双滚动会天然互相争抢滚轮事件。要同时保留两个滚动，必须让内层聊天框有自己的滚动高度约束，但又不能被父层完全裁死，否则外层没有任何额外可滚内容；同时也不能让内层完全脱离约束，否则它会重新撑开整个 panel，回到最初的缺陷。另一个难点是 desktop 还有 `max-height:min(56vh, 640px)` 这一高度上限，它决定了聊天区只占面板的一部分，因此外层 panel 的滚动价值主要体现在 footer 和整体面板内容，而不是替代聊天区滚动。
confidence: HIGH
  - HIGH: 用户的反馈直接验证了上一次修改的副作用，且当前代码已完整读取，双层滚动消失的原因能从 overflow/flex 组合直接推出，不依赖猜测。
approach: 三维评估综合最优的方案是：在 desktop 模式下保留 [`.acu-chat-container`](src/presentation/pages/visualizer-template-assistant.ts:1021) 的 `overflow-y:auto`，继续负责消息内部滚动；同时把 [buildAssistantScrollFrameStyle_ACU()](src/presentation/pages/visualizer-template-assistant.ts:215) 从 desktop 下的 `overflow:hidden` 调整为 `overflow:visible`，让消息框外壳不再把内部高度完全封死，从而恢复 panel 外层滚动的内容参与；fullscreen-overlay 的非 mobile 场景继续保持 `overflow:hidden + 内层滚动`，mobile fullscreen 继续保留外层面板滚动。这不是随便加两个 `overflow:auto` 的糊墙式处理，而是按 viewport 场景拆清桌面与全屏职责：desktop 允许双层滚动，fullscreen(non-mobile) 保持单一内部滚动，mobile 保持外层滚动。
  - 可维护性: 4/5 — 只需在现有两个样式函数中把 desktop 与 fullscreen(non-mobile) 分支拆得更明确，改动集中、可读性尚可。扣 1 分是因为样式字符串分支本身已经有点绕，不够优雅。
  - 健壮性: 4/5 — 能直接恢复用户要的双滚动，同时不回退 fullscreen/mobile 的现有保护逻辑。扣 1 分是因为双滚动本身就比单滚动更难完美，滚轮体验依赖浏览器的默认滚动链传递，但这正是用户明确要求保留的交互。
  - 可扩展性: 4/5 — 后续如果需要继续区分 desktop、tablet、mobile，只需围绕现有 viewport 分支再细化，不需要重做 DOM。
edge_cases:
  - desktop 模式下长聊天记录应继续在 [`.acu-chat-container`](src/presentation/pages/visualizer-template-assistant.ts:1021) 内部滚动，不能再次把消息区整体撑穿。
  - desktop 模式下外层 panel 必须能继续滚动到底部 footer，而不是被 scroll frame 完全截断。
  - fullscreen-overlay 的非 mobile 场景不能被这次改动带回双滚动，否则头尾固定会退化。
  - mobile fullscreen 仍应由 panel 外层滚动，避免输入法与小视口场景下聊天区锁死。
  - 现有 [captureScrollState_ACU()](src/presentation/pages/visualizer-template-assistant.ts:306) / [restoreScrollState_ACU()](src/presentation/pages/visualizer-template-assistant.ts:318) 继续只跟踪聊天容器，不应因为恢复外层滚动而丢失聊天区定位。
affected_scope:
  - src/presentation/pages/visualizer-template-assistant.ts
execution_plan:
  - step_1: 修改 [buildAssistantScrollFrameStyle_ACU()](src/presentation/pages/visualizer-template-assistant.ts:215)，把 desktop 与 fullscreen(non-mobile) 的 overflow 逻辑拆开：desktop 恢复 `overflow:visible`，fullscreen(non-mobile) 保持 `overflow:hidden`，mobile fullscreen 继续 `overflow:visible`。
  - step_2: 保持 [buildAssistantChatContainerStyle_ACU()](src/presentation/pages/visualizer-template-assistant.ts:229) 在 desktop 与 fullscreen(non-mobile) 下继续使用内部滚动，确认 mobile fullscreen 仍走外层滚动分支。
  - step_3: 做类型检查，确认本次修正未引入构建错误，并在验收中明确说明“双滚动”是有意恢复而不是副作用。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES + 它直接回应用户要求的“双滚动共存”，同时把这种行为限制在 desktop，避免 fullscreen/mobile 被一起拖下水。
  - 是否遗漏了已知边界条件？ → NO + 已覆盖 desktop 双滚动、fullscreen 单滚动、mobile 外层滚动、footer 可达、聊天滚动恢复五类边界。
  - 是否因改动量大而想缩减方案？ → NO + 这次只需要修正上轮引入的错误分支，不存在借口。
  - 是否打算跳过某些文件？ → NO + 根因仍然只在 [visualizer-template-assistant.ts](src/presentation/pages/visualizer-template-assistant.ts)。
  - execution_plan是否覆盖affected_scope所有文件？ → YES + affected_scope 只有一个文件，三个步骤都围绕它展开。
  - context是否充分？是否有未读但可能相关的文件？ → NO + 当前问题由本文件内联样式逻辑直接决定，不需要扩展到其他模块。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO + 用户指出的“外部滚动没了”就是必须处理的回归，已经纳入方案。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO + 将继续使用标准编辑工具做精确修改。
</analysis>