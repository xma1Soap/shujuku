<analysis>
context: 用户要求给“原生向量”和“交火模式”在发送前最开始的关键词生成新增一个重试配置，默认值为 3，并完成构建、覆盖根目录 `index.js`、推送 `main`、推送 `release`、覆盖更新 tag `spv3.1` 与 `spv3.1.1`，还要额外生成并推送 tag `spv3.1.2`。侦察确认当前已切回 `main`，工作区干净；远端查询曾因网络 reset 失败，这属于发布后验证阶段需要重试的外部网络问题。关键词生成的实际统一入口是 `src/service/vector/vector-recall-keyword-service.ts` 的 `generateVectorRecallKeywords_ACU()`，原生向量在 `src/service/plot/vector-recall-orchestrator.ts` 调用它，交火模式在 `src/service/plot/summary-vector-index-orchestrator.ts` 调用同一个函数，因此只要在该统一入口实现重试，两条流程都会覆盖。当前 `generateVectorRecallKeywords_ACU()` 只调用一次 `callAIWithPreset_ACU()`，如果抛错或输出清洗后为空，就直接 fallback 到用户输入；而两个 orchestrator 都会把 fallback 视为阻断错误。配置结构位于 `src/service/vector/vector-memory-config.ts` 的 `VectorMemoryConfig_ACU`，已有 `keywordApiPreset`、`keywordContextPairCount`、`keywordPromptGroup`，默认值在 `src/shared/defaults.ts` 的 `defaultVectorMemoryConfig_ACU`。UI 配置区在 `src/presentation/pages/main-popup-table.ts`，当前已有“关键词 API 预设”和“关键词上下文读取层数”；绑定在 `src/presentation/pages/popup-bindings-worldbook.ts`；全量同步在 `src/presentation/components/status-display.ts`。默认刷新机制在 `src/service/settings/settings-service.ts`，由 `VECTOR_MEMORY_DEFAULTS_REFRESH_VERSION_ACU` 控制一次性刷新部分向量默认项。
needs: 本质需求是让关键词生成这个最脆弱的 AI 调用环节具备可配置重试次数，默认 3，且原生向量和交火模式共享同一配置，不出现两套不同语义。必须把配置纳入默认值、normalize、UI 写入、UI 回显和实际调用逻辑；不能只在函数里硬编码 3，否则用户说的“配置”没有实现。重试应覆盖 AI 调用异常和输出为空两类失败，因为当前空输出同样会导致 fallback 并被 orchestrator 阻断。
key_challenges: 第一，关键词生成是两个流程共享入口，正确改法是在 `generateVectorRecallKeywords_ACU()` 内集中实现；如果在两个 orchestrator 分别套 retry，会重复逻辑并且未来容易行为漂移。第二，配置字段必须兼容旧配置，旧用户没有字段时 normalize 应回填默认 3，不应因 undefined 变成 1 或 NaN。第三，重试次数的语义要明确为“最大尝试次数”而不是“失败后额外重试次数”，因为用户说默认 3 更符合总尝试 3 次；UI 文案必须说明，避免误解成 1+3=4 次。第四，`settings-service.ts` 的默认刷新版本如果不提升，老用户配置可能通过 normalize 得到字段但不会把默认值持久化；这不影响运行，但 UI 保存后才会落盘。为了让默认配置可见且持久，应提升 `VECTOR_MEMORY_DEFAULTS_REFRESH_VERSION_ACU` 并只写入 `keywordGenerationMaxAttempts`，避免覆盖用户模型/API等敏感配置。
confidence: HIGH。侦察覆盖了关键词生成统一入口、原生/交火调用点、配置模型、默认值、UI、绑定、状态同步和默认刷新机制。方案边界明确：新增 `keywordGenerationMaxAttempts` 字段，默认 3，normalize 正整数，生成函数按该字段总尝试次数重试。
approach: 推荐方案是在 `VectorMemoryConfig_ACU` 中新增 `keywordGenerationMaxAttempts`，在 `defaultVectorMemoryConfig_ACU` 设置为 3，在 `normalizeVectorMemoryConfig_ACU()` 中通过 `normalizePositiveInteger_ACU` 标准化；在 UI“条目与调用”区域新增数字输入，绑定 change 保存该字段，并在 `syncAllSettingsToUI_ACU()` 回显；在 `generateVectorRecallKeywords_ACU()` 中读取配置后计算 `maxAttempts`，循环调用 `callAIWithPreset_ACU()`，每次失败或清洗后为空都记录错误并继续，成功则返回；全部失败后仍保持现有 fallback 阻断语义。默认刷新版本提升并在 `settings-service.ts` 一次性写入默认 3。发布按现有流程执行：构建、覆盖入口、main/release、tag `spv3.1`、`spv3.1.1`、`spv3.1.2`。
  三维评分（每个维度 1-5 分，5 为最优）：
  - 可维护性: 5/5 — 重试逻辑集中在共享关键词生成服务，配置字段进入现有配置体系和 UI，不复制到两个 orchestrator。
  - 健壮性: 5/5 — 同时处理异常和空输出，保留最终 fallback 阻断语义；旧配置缺字段自动默认 3；用户配置非法值会回落默认正整数。
  - 可扩展性: 4/5 — 后续若要加指数退避或区分异常/空输出重试次数，可在统一服务扩展；当前不加延迟避免改变发送前耗时特征。扣 1 分是因为没有新增单元测试，项目当前缺少该服务的测试结构，本轮以构建和搜索验证为主。
edge_cases: 第一，`keywordGenerationMaxAttempts` 缺失、为 0、负数、非数字时必须回落默认 3。第二，第一次 AI 调用抛错但第二次成功时，应返回成功关键词且 `usedFallback=false`，errors 可为空或仅用于失败时返回；不能因第一次失败污染成功结果。第三，AI 调用成功但输出为空时应计为一次失败并继续重试。第四，所有尝试都失败或空输出时，应返回 fallback 用户输入并带完整 errors，继续让 orchestrator 阻断，不能静默放行低质量召回。第五，原生向量和交火模式必须同时覆盖，因为它们共享 `generateVectorRecallKeywords_ACU()`。第六，发布 tag `spv3.1`、`spv3.1.1` 需要覆盖更新，`spv3.1.2` 需要新增或如果远端已存在则需要按覆盖策略处理；最终必须验证远端引用。
affected_scope: `src/shared/defaults.ts`；`src/service/vector/vector-memory-config.ts`；`src/service/vector/vector-recall-keyword-service.ts`；`src/presentation/pages/main-popup-table.ts`；`src/presentation/pages/popup-bindings-worldbook.ts`；`src/presentation/components/status-display.ts`；`src/service/settings/settings-service.ts`；构建产物 `dist/index.bundle.js` 与根目录 `index.js`；发布分支 `main`、`release` 和 tags `spv3.1`、`spv3.1.1`、`spv3.1.2`。
execution_plan:
  - step_1: 修改 `src/shared/defaults.ts`，将向量默认刷新版本提升到本轮版本，并在 `defaultVectorMemoryConfig_ACU` 增加 `keywordGenerationMaxAttempts: 3`。
  - step_2: 修改 `src/service/vector/vector-memory-config.ts`，在接口和 normalize 返回值中加入 `keywordGenerationMaxAttempts`，使用 `normalizePositiveInteger_ACU` 回落默认值。
  - step_3: 修改 `src/service/vector/vector-recall-keyword-service.ts`，在 `generateVectorRecallKeywords_ACU()` 中按 `keywordGenerationMaxAttempts` 执行最多 N 次 AI 关键词生成尝试，异常和空输出均可重试，成功立即返回，全部失败才 fallback。
  - step_4: 修改 `src/presentation/pages/main-popup-table.ts`、`src/presentation/pages/popup-bindings-worldbook.ts`、`src/presentation/components/status-display.ts`，新增“关键词生成最大尝试次数”输入、保存绑定和 UI 回显。
  - step_5: 修改 `src/service/settings/settings-service.ts` 的默认刷新逻辑，版本不一致时写入 `keywordGenerationMaxAttempts` 默认 3，但不覆盖模型、API、enabled、namespace 等连接配置。
  - step_6: 运行 `npm run build`，用 `copy /Y dist\index.bundle.js index.js` 覆盖入口，并用 `fc /B` 验证一致。
  - step_7: 提交并推送 `main`，同步 `release` 并推送，覆盖推送 tags `spv3.1`、`spv3.1.1`，新增/更新推送 `spv3.1.2`，最后 `git ls-remote` 验证远端分支和 tag。
  - step_8: 读取 `.analysis-cache.md`，追加验收报告并归档。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。三维评分为可维护性 5/5、健壮性 5/5、可扩展性 4/5；共享入口实现比两个 orchestrator 各写一份 retry 更稳。
  - 是否遗漏了已知边界条件？ → NO。已覆盖旧配置缺字段、非法次数、异常重试、空输出重试、全部失败 fallback、双流程共享、tag 发布验证。
  - 是否因改动量大而想缩减方案？ → NO。不会只硬编码 3，而是完整纳入配置、UI、normalize 和默认刷新。
  - 是否打算跳过某些文件？ → NO。配置、默认值、UI、绑定、状态同步、实际调用和发布资产都纳入计划。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。step_1 到 step_7 覆盖所有 affected_scope，step_8 覆盖归档。
  - context是否充分？是否有未读但可能相关的文件？ → YES，已读取统一关键词服务、两个 orchestrator 调用点、配置、默认值、UI、绑定、状态同步、默认刷新逻辑。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → YES。远端 `git ls-remote` 侦察时出现网络 reset，但这不影响本地实现；发布和最终验证阶段必须重试，不能跳过。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO。源码修改使用编辑工具；命令只用于 git、构建、复制和验证。
</analysis>
---
<decision_point>
issue: 执行 `git add .analysis-cache.md dist/index.bundle.js ...` 时失败，原因是 `dist` 被 `.gitignore` 忽略，Git 明确提示需要使用 `-f` 才能添加 `dist/index.bundle.js`。这是发布资产被忽略规则保护导致的操作失败，不是源码实现失败。侦察阶段知道 `dist/index.bundle.js` 是发布资产，但没有在本次提交命令中使用 `git add -f`，这属于执行命令参数遗漏。
impact: YES。该问题阻止当前提交和推送 `main`，但不影响已完成的源码实现、构建结果和 `index.js` 覆盖验证。影响范围仅为 Git 暂存/提交步骤；需要修正提交命令，不能绕过 `dist/index.bundle.js`，否则发布产物与源码不完整。
context_update: `execution_plan.step_7` 仍然有效，但提交命令必须区分普通跟踪文件和被忽略的发布产物；`affected_scope` 中的 `dist/index.bundle.js` 必须通过 `git add -f` 显式纳入。
options:
  - option_a:
      description: 对 `dist/index.bundle.js` 使用 `git add -f`，其他文件正常 `git add`，然后重新 commit/push。这遵循现有仓库发布资产被忽略但需强制纳入的约定。
      approach_evaluation: 可维护性 5/5，因为只修正 Git 暂存命令，不改变代码和发布结构；健壮性 5/5，因为确保构建产物进入提交；可扩展性 5/5，因为后续同类发布可复用同样模式。
      edge_cases: 如果部分文件已在上一次失败的 `git add` 前未暂存，则重新添加全量文件即可；如果 `dist/index.bundle.js` 被忽略但已跟踪，`-f` 仍安全。
      affected_scope_delta: 无新增源码文件，只调整 Git 操作方式。
  - option_b:
      description: 不提交 `dist/index.bundle.js`，只提交根目录 `index.js` 和源码。这会规避 `.gitignore` 报错，但与用户要求“生成 js 将 `index.bundle.js` 覆盖主目录 `index.js`”以及仓库既有发布资产流程不完全一致。
      approach_evaluation: 可维护性 2/5，因为源码、根入口和 bundle 资产可能在仓库中形成不完整发布状态；健壮性 2/5，因为 release 分支和 tag 可能缺少最新 bundle；可扩展性 2/5，因为后续发布校验会变得含混。
      edge_cases: 若远端 release 消费的是 `index.js` 而非 `dist/index.bundle.js`，短期可能能用，但发布资产不一致会给维护者埋雷。
      affected_scope_delta: 缩小 affected_scope，移除 `dist/index.bundle.js`，这是质量下降。
  - option_c:
      description: 修改 `.gitignore` 允许 `dist/index.bundle.js` 被普通 `git add` 捕获，再提交发布产物。这能消除未来 `-f`，但会改变仓库忽略规则，可能把其它 `dist` 文件误纳入候选范围。
      approach_evaluation: 可维护性 3/5，因为需要额外维护忽略规则例外；健壮性 3/5，因为可能引入无关构建产物误提交风险；可扩展性 4/5，因为未来发布命令稍方便，但代价是仓库规则变更。
      edge_cases: 如果 `.gitignore` 当前设计为整体忽略 `dist`，添加例外需要确认所有 dist 子路径，否则容易污染版本库。
      affected_scope_delta: 新增 `.gitignore`，扩大变更范围且不是用户需求。
recommendation: 选择 option_a。三维评估中 option_a 在可维护性、健壮性、可扩展性均为 5/5，且不改变源码方案与仓库规则，只修正发布资产暂存方式。option_b 会丢失发布资产完整性，option_c 会引入不必要的忽略规则变更。
execution_plan_update: 保持原 `execution_plan.step_7`，但执行命令改为先普通添加 `.analysis-cache.md`、源码文件和 `index.js`，再用 `git add -f dist/index.bundle.js` 强制添加被忽略的 bundle，然后 commit/push `main`。
deviation_audit:
  original_plan_excerpt: `- step_7: 提交并推送 `main`，同步 `release` 并推送，覆盖推送 tags `spv3.1`、`spv3.1.1`，新增/更新推送 `spv3.1.2`，最后 `git ls-remote` 验证远端分支和 tag。`
  current_proposal: `- step_7: 提交并推送 `main`，其中 `dist/index.bundle.js` 使用 `git add -f` 纳入暂存；随后同步 `release` 并推送，覆盖推送 tags `spv3.1`、`spv3.1.1`，新增/更新推送 `spv3.1.2`，最后 `git ls-remote` 验证远端分支和 tag。`
  diff_summary: 新增 `dist/index.bundle.js` 使用 `git add -f` 的 Git 暂存细节；没有删减任何发布步骤；没有改变源码实现方案。
  deviation_motive_check:
    - 措辞替换规则逐类检查：未命中任何降级措辞；当前变更是对 Git 忽略规则导致的命令失败做精确修正。
    - 偏离后的方案在三维评分上是否低于原方案？→ NO。原方案目标不变，修正后反而保证 affected_scope 中的发布产物被提交。
    - 偏离是否导致 affected_scope 缩小？→ NO。没有移除任何文件，仍包含 `dist/index.bundle.js`。
  self_dissection: 未触发。
degradation_check:
  - 推荐方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。option_a 为 5/5、5/5、5/5，且唯一同时满足不改仓库规则和不丢发布产物的方案。
  - 推荐方案是否遗漏了新发现的边界条件？ → NO。已处理 `dist` 被忽略、可能部分文件未暂存、发布产物必须进入提交的边界。
  - 是否因为想尽快完成而选择了改动量小的方案？ → NO。选择 option_a 不是缩减范围，而是保持完整 affected_scope 的必要修正。
  - 修改后的execution_plan是否仍覆盖所有affected_scope？ → YES。源码、配置、UI、构建产物、根入口和发布步骤全部保留。
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO。`.gitignore` 报错被正面处理，不跳过 `dist/index.bundle.js`。
  - options 是否包含至少三个方案？ → YES。已列出 option_a、option_b、option_c。
  - 是否因为工具报错而准备换用 shell 命令修改源代码？ → NO。该失败发生在 Git 暂存阶段，不涉及源码修改；不会使用 shell 修改源码。
  - deviation_audit 是否触发了 self_dissection？ → NO。没有质量降级或范围缩小。
</decision_point>
