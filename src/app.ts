/**
 * app.ts — 应用入口（渐进式模块化）
 *
 * 当前阶段：所有旧文件通过 virtual:legacy-concat 以拼接方式引入，
 * 保持原有的 IIFE 闭包作用域不变。
 *
 * 后续阶段：逐步将函数从旧文件迁移到 shared/、data/、service/、presentation/，
 * 通过标准 import/export 引入。迁移完成后，virtual:legacy-concat 将被移除。
 */

// ═══════════════════════════════════════════════════════════════
// 阶段 3+: shared/ 层模块（标准 ES module import）
// 随着函数从旧文件迁出，这里的 import 列表会逐步增长
// ═══════════════════════════════════════════════════════════════

// （后续 T016~T019 将在这里添加 shared/ 的 import）

// ═══════════════════════════════════════════════════════════════
// 旧文件拼接体（通过自定义 rollup 插件注入）
// 包含所有尚未模块化的源文件，保持原有执行顺序和闭包作用域
// ═══════════════════════════════════════════════════════════════
import 'virtual:legacy-concat';
