<analysis>
context: 已确认上一轮 wide-overlay 方案之所以在真实环境里仍只占页面一半，不是宽度常量没生效，而是定位上下文错了。`visualizer-template-assistant-addon.ts` 当前把 `#acu-vis-assistant-host` 追加到 `.acu-vis-content` 内部；而 `visualizer.ts` 明确显示 `.acu-vis-content` 下面有两个并列子项：`.acu-vis-sidebar` 和 `.acu-vis-main`。这意味着 assistant host 不是挂在整个可视化编辑器根上，而是作为 `.acu-vis-content` 的第三个 flex 子项存在。`visualizer-styles.ts` 又确认 `.acu-vis-content` 是 `display:flex`，`#acu-vis-assistant-host` 默认 `flex: 0 0 auto`。所以即便我们把 panel 设成 `position:absolute; width:82%`，它的 containing block 仍然极可能是一个受当前布局压缩的内部区域，而不是整个可视化界面。更糟的是，`.acu-vis-main` 才是真正的主编辑区，`.acu-vis-sidebar` 固定占 340px；用户看到的“仍然只有页面的一半”，更符合“assistant 实际只覆盖主编辑区宽度的一部分”而不是“覆盖整个 visualizer 内容区”。换句话说，上一轮把模式改成 wide-overlay 的方向没错，但挂载层级错了：我们把 overlay 仍留在了被 flex 布局支配的内容容器里，而不是提升到 `#acu-visualizer-content` 根级或窗口级容器。当前已知核心文件中，`#acu-visualizer-content` 是整个可视化编辑器根节点，包含 toolbar 和 `.acu-vis-content`；这才是应该作为 overlay 宿主的层级。用户现在的反馈恰恰证明：只改宽度，不改宿主挂载层级，最后还是会被现有布局吃掉。
needs: 用户这次的本质需求比上一轮更明确：AI 改表助手在非桌面宽屏下必须按“覆盖整个可视化编辑器工作区”的思路实现，而不是继续嵌在 `.acu-vis-content` 这个三列 flex 容器里做局部扩宽。要真正达到“不是页面一半，而是 80%+”，需要让 overlay 相对 `#acu-visualizer-content` 或等价的更高层根容器定位，并让宿主脱离内容区 flex 布局影响。
key_challenges: 第一，问题根因已经从“模式阈值/宽度值错误”升级为“宿主挂载层级错误”；如果继续只调宽度百分比，只会反复得到看似更大但仍受父容器限制的半残结果。第二，当前 addon 把 host 插入 `.acu-vis-content`，如果改挂到 `#acu-visualizer-content` 根级，需要确保不影响现有桌面侧栏模式的布局和 DOM 查询。第三，`visualizer-styles.ts` 里同时存在宿主基础样式和 wide/mobile overlay 状态样式，挂载层级改变后必须同步调整 base flex/position 规则，否则 host 可能继续参与 flex 排版或遮住 toolbar。第四，测试目前只校验 mode、position 和 width 字符串，但没有约束 host 是否挂在正确父节点；如果不补这一点，未来还会再退回错误层级。
confidence: HIGH
  - HIGH: 根因已经很清楚：assistant host 被挂在 `.acu-vis-content` 这个内部 flex 容器，而不是整个 `#acu-visualizer-content` 根节点。用户实际现象与当前 DOM 结构完全吻合，不存在大的不确定性。
approach: 三维评估综合最优的方案是修正宿主挂载层级，而不是继续在现有 `.acu-vis-content` 内部打补丁。具体做法：第一，修改 `visualizer-template-assistant-addon.ts`，把 `#acu-vis-assistant-host` 从 `.acu-vis-content` 改为挂载到 `#acu-visualizer-content` 根节点，这样 wide-overlay/mobile-overlay 都能相对整个编辑器区域定位，而不是相对内部内容列。第二，调整 `visualizer-styles.ts` 中 `#acu-vis-assistant-host` 的基础样式，使其在关闭状态下不参与 flex 布局、在 wide-overlay/mobile-overlay 状态下使用绝对/固定定位覆盖整个编辑器区域，同时避免遮挡 toolbar 的点击区域失控。第三，在 `visualizer-template-assistant.ts` 中保留上一轮的 wide-overlay 模式和 82% 宽度逻辑，但让它建立在正确宿主层级上；必要时把 top/bottom/inset 改成明确基于编辑器根区域的覆盖值。第四，更新测试：除了保留 1100px wide-overlay 断言，还要验证 `#acu-vis-assistant-host` 的父节点不再是 `.acu-vis-content`，而是 `#acu-visualizer-content`。这才是能把问题一次修透的方案。继续只改宽度比例，就是把错误设计换个数字再提交，质量低得像故意绕开根因。
  三维评分（每个维度 1-5 分，5 为最优）：
  - 可维护性: 5/5 — 把宿主挂在正确的根容器上，overlay 逻辑与内容区 flex 布局彻底解耦，后续维护不会继续被“为什么只占一半”这种假问题反复拖住。
  - 健壮性: 5/5 — 直接消除父级布局约束这个根因，比调百分比、调 max-width、调阈值都更稳。
  - 可扩展性: 4/5 — 以后如果要让 assistant 覆盖 toolbar 以下区域、全编辑器区域、甚至窗口级区域，都有清晰的宿主层级基础。
edge_cases:
  - wide-overlay 必须覆盖整个 `#acu-visualizer-content` 工作区，而不是只覆盖 `.acu-vis-main` 或 `.acu-vis-content` 的某一半。
  - 宿主改挂根节点后，桌面模式不能突然脱离原右侧栏布局；desktop 仍应表现为可视区右侧面板。
  - mobile-overlay 与 wide-overlay 的最小化/恢复按钮不能因为宿主层级变化而失效或遮住 toolbar 永远不可点击。
  - 测试必须验证宿主父节点层级，否则未来有人把 host 又塞回 `.acu-vis-content` 时现有测试不会报警。
  - 关闭 assistant 时，根节点下的 host 不应继续制造额外布局空白或拦截点击。
affected_scope:
  - E:\xiangmu\星河璀璨数据库\shujuku1\src\presentation\bootstrap\visualizer-template-assistant-addon.ts
  - E:\xiangmu\星河璀璨数据库\shujuku1\src\presentation\pages\visualizer-styles.ts
  - E:\xiangmu\星河璀璨数据库\shujuku1\src\presentation\pages\visualizer-template-assistant.ts
  - E:\xiangmu\星河璀璨数据库\shujuku1\tests\presentation\visualizer-template-assistant.test.ts
execution_plan:
  - step_1: 修改 `src/presentation/bootstrap/visualizer-template-assistant-addon.ts`，把 assistant host 的挂载位置从 `.acu-vis-content` 提升到 `#acu-visualizer-content` 根节点，确保 overlay 有正确的定位容器。
  - step_2: 修改 `src/presentation/pages/visualizer-styles.ts`，重写 `#acu-vis-assistant-host` 的基础布局规则，使其在根节点下不参与错误的 flex 排版，并让 wide-overlay/mobile-overlay 以根容器为基准覆盖。
  - step_3: 复查并微调 `src/presentation/pages/visualizer-template-assistant.ts` 的 wide-overlay 定位样式，确保 82% 宽度是在正确宿主层级上计算，必要时修正 inset/top/bottom 表达。
  - step_4: 更新 `tests/presentation/visualizer-template-assistant.test.ts`，新增宿主父节点断言，并验证 1100px 下仍为 wide-overlay，768px 下 mobile-overlay 不回归。
  - step_5: 运行 `tests/presentation/visualizer-template-assistant.test.ts` 与 `npm run typecheck`，确认宿主层级修正没有打坏现有状态机。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES + 可维护性 5/5、健壮性 5/5、可扩展性 4/5。修宿主层级是根治方案，明显优于继续微调宽度百分比或媒体查询。
  - 是否遗漏了已知边界条件？ → NO + 已覆盖宿主覆盖范围、桌面模式保留、最小化不失效、测试验证父节点、关闭态不拦截点击五项边界条件。
  - 是否因改动量大而想缩减方案？ → NO + 用户已经连续反馈同一问题，继续改数字就是在制造下一次返工。
  - 是否打算跳过某些文件？ → NO + 这次必须同时动 addon 挂载层级、styles、panel 定位和测试，少一个都不完整。
  - execution_plan是否覆盖affected_scope所有文件？ → YES + step_1 到 step_4 覆盖四个文件，step_5 做验证。
  - context是否充分？是否有未读但可能相关的文件？ → NO + 已读取决定根布局与宿主挂载位置的核心文件，当前上下文足够支撑修改。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO + 上一轮 wide-overlay 只修宽度不修层级的问题已明确纳入根因，没有再装作细节问题跳过。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO + 只使用标准补丁工具修改源码。
  → YES项必须就地写出修正内容，然后以修正后的方案进入执行
</analysis>
---
<decision_point>
issue: 运行测试后，业务逻辑和类型检查都通过，但新增的“宿主父节点是 `#acu-visualizer-content`”断言失败。根因不是实现没生效，而是测试基建本身仍然只在 `beforeEach` 里构造了 `<div id="acu-vis-assistant-host"></div>`，根本没有模拟真实的 `#acu-visualizer-content` 结构，因此这条断言在当前测试环境里必然拿到 `null`。这个问题在侦察/分析阶段没有预见到，因为当时关注的是生产代码挂载层级，没先回看测试夹具是否还停留在旧的极简 DOM。
impact: YES + 它阻断了测试通过，但影响范围仅限测试夹具；产品代码方案本身没有被推翻。若不修测试夹具，这条关键父节点断言永远无法成立，验收也不完整。
context_update: 新发现改变了 step_4 的实现前提：要验证宿主挂载到 `#acu-visualizer-content` 根节点，测试 `beforeEach` 必须先构造最小真实 visualizer DOM（至少包含 `#acu-visualizer-content`、`.acu-vis-content` 和原始 host 占位）。否则只能验证 mode/width，却测不到真正的挂载层级修复。
options:
  - option_a:
      description: 修改 `tests/presentation/visualizer-template-assistant.test.ts` 的测试夹具，在 `beforeEach` 和个别重置场景里构造最小真实 visualizer DOM：`#acu-visualizer-content` 下含 `.acu-vis-content`，同时保留初始 host 占位，让 addon/渲染逻辑有可验证的父节点上下文。
      approach_evaluation: 可维护性 5/5——测试环境更接近真实结构，后续宿主层级相关修改也能被正确约束；健壮性 5/5——直接解决当前 false negative；可扩展性 4/5——以后如果还测 overlay 根节点行为，可以继续复用这套夹具。
      edge_cases: 需要同步更新个别用例中手动重置 `document.body.innerHTML` 的地方，避免它们把真实 visualizer DOM 又重置回旧的单 host 结构。
      affected_scope_delta: 仅修改 `E:\xiangmu\星河璀璨数据库\shujuku1\tests\presentation\visualizer-template-assistant.test.ts`。
  - option_b:
      description: 删除宿主父节点断言，只保留 mode/width/position 断言，让测试重新通过。
      approach_evaluation: 可维护性 2/5——看似省事，实则把这次根因修复最关键的验证点丢掉；健壮性 1/5——未来 host 再被挂回错误层级时测试完全不会报警；可扩展性 2/5——等于回到半盲状态。
      edge_cases: 用户刚刚就是因为父容器错误导致反复返工，删掉这个断言和掩耳盗铃没有区别。
      affected_scope_delta: 缩减测试覆盖范围，丢失对根因的直接保护。
  - option_c:
      description: 不改现有测试文件，另起一个专门的 addon DOM 测试文件去验证 host 挂载层级，把当前失败用例改回旧夹具。
      approach_evaluation: 可维护性 3/5——理论可行，但会分散上下文；健壮性 4/5——能覆盖根因，但当前文件里的中屏 overlay 行为仍然脱离真实 DOM；可扩展性 3/5——需要额外维护第二套测试夹具。
      edge_cases: 两个测试文件若使用不同 DOM 夹具，后续容易出现“一个说通过，一个在真实结构里失败”的分裂。
      affected_scope_delta: 需要新增测试文件或显著扩大测试范围，复杂度更高。
recommendation: 选择 option_a。它在三维评估上综合最优：不删验证点，不拆散上下文，直接把现有测试夹具修到足够真实。助手，你要是现在退回 option_b，那就是明知道根因在哪，还主动把报警器拆了，未免太天真了。
execution_plan_update: 在原 step_4 基础上新增夹具修正：先更新 `tests/presentation/visualizer-template-assistant.test.ts` 的 `beforeEach` 和相关 reset 片段，使其构造 `#acu-visualizer-content > .acu-vis-content > #acu-vis-assistant-host` 的最小 DOM；保留宿主父节点断言后重新运行 `tests/presentation/visualizer-template-assistant.test.ts` 与 `npm run typecheck`。
deviation_audit:
  original_plan_excerpt: 
    - step_4: 更新 `tests/presentation/visualizer-template-assistant.test.ts`，新增宿主父节点断言，并验证 1100px 下仍为 wide-overlay，768px 下 mobile-overlay 不回归。
    - step_5: 运行 `tests/presentation/visualizer-template-assistant.test.ts` 与 `npm run typecheck`，确认宿主层级修正没有打坏现有状态机。
  current_proposal: 
    - step_4: 更新 `tests/presentation/visualizer-template-assistant.test.ts`，先修正测试夹具为最小真实 visualizer DOM，再保留宿主父节点断言，并验证 1100px 下仍为 wide-overlay，768px 下 mobile-overlay 不回归。
    - step_5: 重新运行 `tests/presentation/visualizer-template-assistant.test.ts` 与 `npm run typecheck`，确认夹具修正后测试全部通过。
  diff_summary: 
    - 在 step_4 中新增“修正测试夹具为真实 visualizer DOM”
    - step_5 仍是重跑测试与 typecheck，但前提从“旧夹具可用”改为“新夹具已修正”
  deviation_motive_check:
    - 措辞替换规则逐类检查（基于 decision_point 前置步骤中已读取的十类规则）：全部未命中。当前偏离是修正测试基建不足，不是缩范围或偷懒。
    - 偏离后的方案在三维评分上是否低于原方案？→ NO + 新方案让父节点断言真正有意义，测试质量高于原方案。
    - 偏离是否导致 affected_scope 缩小？→ NO + 仍是同一 affected_scope，只是补足测试夹具。
  self_dissection: [仅当上述检查触发偷懒判定时填写此字段]
degradation_check:
  - 推荐方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES + option_a 既保留关键验证点，又让测试环境贴近真实结构，综合最优。
  - 推荐方案是否遗漏了新发现的边界条件？ → NO + 已补充“个别测试手动重置 body 时也要同步修夹具”的边界条件。
  - 是否因为想尽快完成而选择了改动量小的方案？ → NO + 我没有删断言蒙混过关，而是修测试夹具。
  - 修改后的execution_plan是否仍覆盖所有affected_scope？ → YES + 仅增强测试文件的真实结构模拟，不影响其余文件覆盖。
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO + 测试夹具与真实结构脱节这个问题已纳入处理。
  - options 是否包含至少三个方案？ → YES + 已提供三个方案。
  - 是否因为工具报错而准备换用 shell 命令修改源代码？ → NO + 继续使用标准编辑工具。
  - deviation_audit 是否触发了 self_dissection？ → NO + 未触发偷懒判定。
  → YES项必须就地修正
</decision_point>
---
<decision_point>
issue: 即使把 `document.body.innerHTML` 改成真实 visualizer DOM，测试里 `document.querySelector('#acu-visualizer-content')` 仍然返回 `null`。继续读取测试夹具后发现根因更深：这个测试根本不是真 DOM，而是自定义的 `FakeDocument_ACU`。它的 `buildElements()` 只特判了 `#acu-vis-assistant-host`、`.acu-vis-assistant-panel`、若干按钮和聊天区 selector，根本不会识别 `#acu-visualizer-content` 或 `.acu-vis-content`，所以 body 里即便有对应 HTML，querySelector 也永远拿不到。前一个 decision_point 只修了 body HTML，没有同步修 fake selector 解析器，这就是为什么断言还在失败。
impact: YES + 这继续阻断宿主父节点断言通过，但影响范围依然局限在测试夹具。产品代码不需要变更，测试基础设施需要补足 selector 解析能力。
context_update: 现在可以明确：`tests/presentation/visualizer-template-assistant.test.ts` 的核心限制不是单纯 DOM 片段不真实，而是 `FakeDocument_ACU.buildElements()` 对新增选择器没有支持。要让“挂到 `#acu-visualizer-content`”可测，必须扩展 fake document 对 `#acu-visualizer-content` 和 `.acu-vis-content` 的识别，并让 `#acu-vis-assistant-host` 记录父节点关系。
options:
  - option_a:
      description: 扩展 `FakeDocument_ACU` 与 `FakeHTMLElement_ACU`：支持 `#acu-visualizer-content`、`.acu-vis-content` selector，且在 `#acu-vis-assistant-host` 的 `syncFromHtml` 里根据 body HTML 记录 `parentElement` 为相应 fake root element。这样现有测试框架继续工作，同时能验证宿主层级。
      approach_evaluation: 可维护性 5/5——顺着现有 fake DOM 机制补能力，不引入第二套测试范式；健壮性 5/5——父节点断言终于建立在可工作的夹具上；可扩展性 4/5——之后如果还测 visualizer 容器层级，可继续复用这些 selector 支持。
      edge_cases: 需要确保 `parentElement` 不污染其他 selector 缓存；还要保证 `#acu-vis-assistant-host` 在旧场景下若没有 `#acu-visualizer-content` 包裹时仍可工作。
      affected_scope_delta: 仅修改 `E:\xiangmu\星河璀璨数据库\shujuku1\tests\presentation\visualizer-template-assistant.test.ts`。
  - option_b:
      description: 放弃在这个 fake DOM 框架里测父节点关系，把断言改成检查 `document.body.innerHTML` 是否包含 `id="acu-visualizer-content"` 和 `id="acu-vis-assistant-host"` 的嵌套顺序。
      approach_evaluation: 可维护性 2/5——字符串匹配太脆，等于绕开 DOM 语义；健壮性 2/5——无法真正验证 `querySelector`/`parentElement` 行为；可扩展性 2/5——以后稍微改一点 HTML 排版就会误报或漏报。
      edge_cases: 字符串嵌套顺序可能正确，但 fake DOM 查询能力仍然缺失，后续其他基于 DOM 的测试继续会踩坑。
      affected_scope_delta: 仅测试文件，但验证质量明显下降。
  - option_c:
      description: 新建一个不使用 fake DOM 的 jsdom 测试文件，单独验证 addon host 的挂载父节点，把当前文件里的父节点断言删除。
      approach_evaluation: 可维护性 3/5——能测，但引入第二套测试基础设施；健壮性 4/5——真实 DOM 语义更准；可扩展性 3/5——测试体系被拆成两套，上下文更分裂。
      edge_cases: 需要重新 mock 大量依赖，成本高于当前问题本身；而且当前文件依旧保留半残 fake DOM 限制。
      affected_scope_delta: 需要新增测试文件，扩大范围和维护成本。
recommendation: 选择 option_a。它在三维评估上综合最优：直接把现有 fake DOM 框架补到能表达这次真实需求的程度，不走字符串断言那种偷懒路线，也不为了一个父节点验证再造一整套测试体系。助手，这次问题已经很直白了——不是产品代码在骗你，是测试夹具太简陋。
execution_plan_update: 在 step_4 基础上进一步修正测试基础设施：扩展 `FakeDocument_ACU.buildElements()` 以支持 `#acu-visualizer-content` 与 `.acu-vis-content`，并让 `#acu-vis-assistant-host` 的 fake element 带上正确 `parentElement`；然后保留父节点断言，再次运行 `tests/presentation/visualizer-template-assistant.test.ts` 与 `npm run typecheck`。
deviation_audit:
  original_plan_excerpt: 
    - step_4: 更新 `tests/presentation/visualizer-template-assistant.test.ts`，先修正测试夹具为最小真实 visualizer DOM，再保留宿主父节点断言，并验证 1100px 下仍为 wide-overlay，768px 下 mobile-overlay 不回归。
    - step_5: 重新运行 `tests/presentation/visualizer-template-assistant.test.ts` 与 `npm run typecheck`，确认夹具修正后测试全部通过。
  current_proposal: 
    - step_4: 更新 `tests/presentation/visualizer-template-assistant.test.ts`，除了构造最小真实 visualizer DOM，还扩展 fake DOM selector 支持和 `parentElement` 建模，再保留宿主父节点断言，并验证 1100px 下仍为 wide-overlay，768px 下 mobile-overlay 不回归。
    - step_5: 重新运行 `tests/presentation/visualizer-template-assistant.test.ts` 与 `npm run typecheck`，确认 fake DOM 修正后测试全部通过。
  diff_summary: 
    - 在 step_4 中新增“扩展 fake DOM selector 支持与 parentElement 建模”
    - step_5 不变，但前置条件从“DOM 片段正确”升级为“fake DOM 能表达该结构”
  deviation_motive_check:
    - 措辞替换规则逐类检查（基于 decision_point 前置步骤中已读取的十类规则）：全部未命中。当前偏离是修测试夹具能力，不是偷懒。
    - 偏离后的方案在三维评分上是否低于原方案？→ NO + 新方案把验证从假嵌套提升为真实可查询的父子关系，测试质量更高。
    - 偏离是否导致 affected_scope 缩小？→ NO + 仍然只在同一测试文件内补齐基础设施。
  self_dissection: [仅当上述检查触发偷懒判定时填写此字段]
degradation_check:
  - 推荐方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES + option_a 最能兼顾现有测试框架的一致性和这次根因验证的准确性。
  - 推荐方案是否遗漏了新发现的边界条件？ → NO + 已补充 selector 缓存与无根节点旧场景兼容这两个边界条件。
  - 是否因为想尽快完成而选择了改动量小的方案？ → NO + 我没有退回字符串断言，也没有删断言。
  - 修改后的execution_plan是否仍覆盖所有affected_scope？ → YES + 只是把测试文件修完整。
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO + fake DOM 不支持新 selector 这个根因已纳入处理。
  - options 是否包含至少三个方案？ → YES + 已提供三个方案。
  - 是否因为工具报错而准备换用 shell 命令修改源代码？ → NO + 继续使用标准编辑工具。
  - deviation_audit 是否触发了 self_dissection？ → NO + 未触发偷懒判定。
  → YES项必须就地修正
</decision_point>
---
<decision_point>
issue: 扩展 fake DOM selector 支持后，中屏测试仍失败，继续读测试夹具发现更根本的问题：`FakeDocument_ACU.body` 本身被实现成一个 `selector='#acu-vis-assistant-host'` 的 fake element，而不是通用 body 节点。也就是说，整个 fake DOM 的 body.innerHTML 都被当成“host 的 innerHTML”处理，任何包裹在外层的 `#acu-visualizer-content` 都不会像真实 DOM 那样成为 body 下的独立节点。这不是单一 selector 缺失，而是测试夹具的根建模从一开始就把 assistant host 当成文档根节点。继续往这个假结构上补丁，只会越补越歪。
impact: YES + 这使得“在当前测试文件里准确验证 host 挂载到 `#acu-visualizer-content`”的成本陡增；产品代码本身仍然可用，阻碍集中在测试模型失真。需要决定是继续重构 fake DOM，还是把验证降到不依赖这个失真的层面。
context_update: 新发现改变了测试策略假设：当前 fake DOM 不是“缺几个 selector”，而是根节点建模错误。要在此文件中测真实父子关系，必须大改 FakeDocument/FakeHTMLElement；否则只能退一步验证更稳定的行为信号，或单独拆出真实 jsdom 测试。
options:
  - option_a:
      description: 大改 `FakeDocument_ACU`，把 `body` 重构成真正通用的 body 节点，不再伪装成 `#acu-vis-assistant-host`；同步调整 `buildElements()`、`syncFromHtml()` 和所有依赖当前假设的测试查询逻辑，让测试文件整体更接近真实 DOM。
      approach_evaluation: 可维护性 3/5——从长期看更正确，但当前文件近 2000 行，牵连面大；健壮性 5/5——一旦完成，父子关系和更多真实 DOM 行为都能正确测试；可扩展性 5/5——后续容器层级测试都会受益。
      edge_cases: 高风险点在于现有 47 条已通过测试都依赖当前 fake DOM 语义，重构 body 根节点很可能引发连锁失败，当前这一轮会被拖进大面积测试基建重构。
      affected_scope_delta: 大幅修改 `E:\xiangmu\星河璀璨数据库\shujuku1\tests\presentation\visualizer-template-assistant.test.ts`，影响该文件大量既有用例。
  - option_b:
      description: 承认当前 fake DOM 不适合验证真实父节点关系，移除 `visualizerRoot`/`parentElement` 断言，改为保留能稳定证明宿主层级修复有效的行为断言：wide-overlay 使用根级 overlay 样式（例如 top=68px、absolute、82% 宽度），并新增对 addon 源码的独立文本级断言或轻量搜索验证，确认 host 已挂到 root 而非 `.acu-vis-content`。
      approach_evaluation: 可维护性 4/5——避免把当前任务拖成大规模测试基建翻修；健壮性 4/5——虽然少了真实 DOM 父子断言，但仍保留产品行为测试，并可用更轻量方式锁住挂载代码路径；可扩展性 3/5——不如完整 DOM 重构彻底，但足够约束这次修复。
      edge_cases: 需要明确说明这是对失真测试夹具的现实让步，而不是对产品代码正确性让步；同时应补一个不依赖 fake DOM 的断言来锁住 `root.appendChild(createAssistantHost_ACU(doc))` 这一变更。
      affected_scope_delta: 继续修改 `tests/presentation/visualizer-template-assistant.test.ts`，必要时新增一个更轻量的 addon 测试文件。
  - option_c:
      description: 新增一个独立、显式启用 jsdom 的 addon 测试文件，只测 `ensureVisualizerTemplateAssistantAddonDom_ACU()` 是否把 host 挂到 `#acu-visualizer-content` 根节点；当前大测试文件回退到行为断言，不再在 fake DOM 里硬测父节点。
      approach_evaluation: 可维护性 5/5——把“真实 DOM 挂载层级”验证放到适合它的环境里；健壮性 5/5——既保留现有大测试文件稳定性，又新增对根因的直接保护；可扩展性 4/5——以后 addon DOM 结构变化可以在独立测试里维护。
      edge_cases: 需要单独 mock addon 依赖，但范围比 option_a 小得多；同时要避免测试重复和上下文分裂。
      affected_scope_delta: 修改 `tests/presentation/visualizer-template-assistant.test.ts`，并新增 `tests/presentation/visualizer-template-assistant-addon.test.ts` 或同类文件。
recommendation: 选择 option_c。三维评估综合最优的方案是：别再拿一个先天失真的 fake DOM 去硬测真实父子关系，把该验证移到独立 jsdom 测试里，同时让现有大测试文件继续负责行为层验证。option_a 虽然理论最纯，但当前改动面太大，会把本轮修复拖进测试基建重构泥潭；option_b 则少了直接 DOM 保护。option_c 才是质量和范围的平衡点，而且这种平衡是有技术依据的，不是拿“务实”两个字糊弄过去。
execution_plan_update: 调整 step_4：在 `tests/presentation/visualizer-template-assistant.test.ts` 中移除对 fake DOM 无法稳定表达的 `visualizerRoot` 父节点断言，保留中宽度 wide-overlay 的行为断言；新增一个独立的 jsdom addon 测试文件，直接验证 `ensureVisualizerTemplateAssistantAddonDom_ACU()` 把 host 挂到 `#acu-visualizer-content` 根节点而非 `.acu-vis-content`。step_5 更新为同时运行这两个测试文件与 `npm run typecheck`。
deviation_audit:
  original_plan_excerpt: 
    - step_4: 更新 `tests/presentation/visualizer-template-assistant.test.ts`，除了构造最小真实 visualizer DOM，还扩展 fake DOM selector 支持和 `parentElement` 建模，再保留宿主父节点断言，并验证 1100px 下仍为 wide-overlay，768px 下 mobile-overlay 不回归。
    - step_5: 重新运行 `tests/presentation/visualizer-template-assistant.test.ts` 与 `npm run typecheck`，确认 fake DOM 修正后测试全部通过。
  current_proposal: 
    - step_4: 在 `tests/presentation/visualizer-template-assistant.test.ts` 中保留 wide-overlay/mobile-overlay 行为断言，移除 fake DOM 无法可靠表达的父节点断言；新增独立 jsdom addon 测试文件，直接验证 host 挂载到 `#acu-visualizer-content` 根节点。
    - step_5: 运行 `tests/presentation/visualizer-template-assistant.test.ts`、新增的 addon DOM 测试文件与 `npm run typecheck`，确认行为与挂载层级都被覆盖。
  diff_summary: 
    - 将“在同一个 fake DOM 大测试里验证父节点关系”替换为“行为测试 + 独立 jsdom DOM 挂载测试”
    - step_5 从单测一个文件扩展为同时跑两个互补测试文件
  deviation_motive_check:
    - 措辞替换规则逐类检查（基于 decision_point 前置步骤中已读取的十类规则）：全部未命中。当前偏离是因为测试模型客观不适合继续承载该验证，不是为省事缩质量。
    - 偏离后的方案在三维评分上是否低于原方案？→ NO + 相比继续在失真 fake DOM 上堆补丁，当前方案的健壮性和可维护性更高。
    - 偏离是否导致 affected_scope 缩小？→ NO + 测试范围不是缩小，而是从一个文件扩展到两个更合适的测试载体。
  self_dissection: [仅当上述检查触发偷懒判定时填写此字段]
degradation_check:
  - 推荐方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES + option_c 同时保住现有大测试文件的稳定性和对根因的直接 DOM 验证，综合最优。
  - 推荐方案是否遗漏了新发现的边界条件？ → NO + 已补充 fake DOM 根建模失真和双测试载体分工边界。
  - 是否因为想尽快完成而选择了改动量小的方案？ → NO + 我没有简单删掉验证，而是把它移动到更合适的测试环境。
  - 修改后的execution_plan是否仍覆盖所有affected_scope？ → YES + 仍覆盖代码文件和测试文件，只是测试方式升级。
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO + fake DOM 根建模错误已被正面处理，没有忽略。
  - options 是否包含至少三个方案？ → YES + 已提供三个方案。
  - 是否因为工具报错而准备换用 shell 命令修改源代码？ → NO + 继续使用标准编辑工具。
  - deviation_audit 是否触发了 self_dissection？ → NO + 未触发偷懒判定。
  → YES项必须就地修正
</decision_point>
---
<output_quality_review>
task_summary: 修正了 AI 改表助手 overlay 仍只占半边的问题根因：将 assistant host 的挂载位置从 `.acu-vis-content` 提升到 `#acu-visualizer-content` 根节点，并同步调整 wide-overlay/mobile-overlay 的宿主样式与测试结构，确保中宽度场景真正按整个编辑器区域进行 80%+ 覆盖。
deliverables:
  - E:\xiangmu\星河璀璨数据库\shujuku1\src\presentation\bootstrap\visualizer-template-assistant-addon.ts
  - E:\xiangmu\星河璀璨数据库\shujuku1\src\presentation\pages\visualizer-styles.ts
  - E:\xiangmu\星河璀璨数据库\shujuku1\src\presentation\pages\visualizer-template-assistant.ts
  - E:\xiangmu\星河璀璨数据库\shujuku1\tests\presentation\visualizer-template-assistant.test.ts
  - E:\xiangmu\星河璀璨数据库\shujuku1\tests\presentation\visualizer-template-assistant-addon.test.ts

# 量化指标总览
metrics:
  total_files_modified: 5 — 修改了 3 个实现文件和 2 个测试文件
  execution_plan_coverage: 5/5 = 100% — 原 execution_plan 五个步骤全部完成，执行期的测试策略调整也已纳入完成态
  edge_cases_handled: 5/5 = 100% — 已处理 wide-overlay 覆盖整个编辑器根区域、桌面模式保留、最小化/恢复不回归、关闭态不拦截点击、独立 DOM 挂载验证
  confidence_assessment: HIGH — 对本次交付质量的整体置信度为 HIGH
    - HIGH: 代码路径和挂载层级已经修正，`tests/presentation/visualizer-template-assistant.test.ts` 与 `tests/presentation/visualizer-template-assistant-addon.test.ts` 共 52 条测试通过，`npm run typecheck` 通过。仓库仍存在既有 `tsconfig.json` duplicate key warning，但不由本次修改引入

# 产物实质性检查
substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？
    → NO + `visualizer-template-assistant-addon.ts` 的改动把 host 从 `.acu-vis-content` 挂到 `#acu-visualizer-content` 根节点，这是这次真实根因；删除后 overlay 仍会受内部 flex 布局约束，问题继续复现。`visualizer-styles.ts` 的改动把 host 基础样式改为根级 absolute overlay 容器，并处理 open/close/pointer-events；删掉后即使 host 挂对了层级，也仍可能参与错误布局或拦截点击。`visualizer-template-assistant.ts` 的 wide-overlay `top:68px` 与 82% 宽度是为了在根节点下避开 toolbar 并覆盖主要工作区；删掉后视觉上会再次错位或覆盖不完整。两个测试文件分别从行为层和真实 DOM 挂载层级约束这次修复，不是摆设。
  - 产物是否能被其目标对象（被测代码/被重构模块/被修复的bug）的变化所"击穿"？
    → NO + 如果把 addon 中 `root.appendChild(createAssistantHost_ACU(doc))` 改回挂到 `.acu-vis-content`，`visualizer-template-assistant-addon.test.ts` 会失败；如果把 wide-overlay 改回内部半栏或去掉 `top:68px`/`width:82%`，`visualizer-template-assistant.test.ts` 的中屏行为断言会失败；如果把 mobile-overlay/最小化逻辑改坏，同文件既有测试也会失败。
  - 实质性比率: 5/5 = 100%

# 覆盖完整性检查
completeness_check:
  - 是否存在被跳过的模块/函数/路径？
    → NO + 本轮真正相关的实现路径就是 addon 挂载层级、overlay 样式、wide-overlay 定位，以及两类测试载体；都已经覆盖，没有把任何已知相关模块留空。
  - 产物覆盖的范围是否与 execution_plan 中 affected_scope 完全一致？
    → NO + 最终多了 `tests/presentation/visualizer-template-assistant-addon.test.ts`，这是执行期根据测试夹具失真问题新增的必要测试文件。它不是范围漂移，而是为保证根因验证可靠性而新增的补强范围。
  - 核心业务逻辑是否都有直接验证（不依赖间接覆盖）？
    → YES + 核心逻辑一“host 挂到 `#acu-visualizer-content` 根节点而非 `.acu-vis-content`”有直接验证，位置在 `tests/presentation/visualizer-template-assistant-addon.test.ts` 更新后的挂载测试。核心逻辑二“1100px 中屏 wide-overlay 以 82% 宽度覆盖工作区”有直接验证，位置在 `tests/presentation/visualizer-template-assistant.test.ts`。核心逻辑三“768px 手机全屏 overlay 与最小化/恢复不回归”也有直接验证，位置同文件既有窄屏测试。
  - affected_scope 覆盖率: 5/5 = 100%

# 价值密度检查
value_density_check:
  - 产物中高价值内容（验证核心逻辑/处理复杂场景）与低价值内容（验证trivial行为）的比例是多少？
    → 高价值:低价值 = 5:0，高价值占比 100%。本轮修改没有堆无意义样式微调，全部围绕根因：挂载层级、定位容器、overlay 行为和独立 DOM 验证。
  - 是否存在"用数量掩盖质量"的模式——大量 trivial 产物掩盖了核心逻辑缺少验证的事实？
    → NO + 测试新增数量不多，但分别负责行为验证与 DOM 层级验证，分工清楚，没有用 trivial 断言充数。

# 需求对齐检查
alignment_check:
  - 产物满足的是用户的字面需求还是本质需求？
    → 满足的是本质需求。用户持续反馈“还是只有页面一半”，说明问题早就不是单纯样式数字，而是 assistant 根本没有相对整个编辑器区域覆盖。当前实现把宿主挂载层级提到 `#acu-visualizer-content` 根节点，并让 wide-overlay 相对整个编辑器工作区覆盖，这才真正回应了“占据 80%+ 而不是半边”的要求。
  - "如果这是别人交给我的，我会接受吗？"
    → YES + 这次终于修到了根因，不再是继续调宽度常量自欺欺人。实现、样式、行为测试和 DOM 挂载测试都补齐了，已经达到可以交付测试的标准。
→ 任何检查项发现问题，必须就地修正，修正完成后重新检查该项。
   不允许"记录问题留待下次处理"。
</output_quality_review>
