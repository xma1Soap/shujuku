# Analysis Cache — 修复 AI 请求参数硬编码与空值回退语义优化

## Analysis

context:
  五个源文件和三个测试文件已完整读取/搜索确认。问题分布如下：

  A. src/service/ai/api-call.ts:
    - L64: `opts.maxTokens || ... || 20000` → `||` 吞 0
    - L157: `callApiWithPlotPreset_ACU` 传 `{ temperature: 0.7, topP: 0.95 }` → 硬编码覆盖配置温度
    - L210: `callApi_ACU` 传 `{ temperature: 0.7, topP: 0.95 }` → 同上
    - L276: `callCustomOpenAI_ACU_Direct` tavern 路径 `settings_ACU.apiConfig.max_tokens || 4096` → 计划外发现同类 bug
    - L310: `callAIWithPreset_ACU` 的 `effectiveApiConfig.max_tokens || ... || 4096` → `||` 吞 0
    - L343: `callAIWithPreset_ACU` 的 `temperature || 1.0, topP: top_p || 0.9` → `||` 吞 0，缺 topP 别名

  B. src/presentation/bootstrap/api-groups/worldbook-ai-api.ts:
    - L173: `temperature || 1.0, topP: top_p` → `||` 吞 0，缺 topP 别名

  C. src/service/ai/prompt-builder/prompt-api-call.ts:
    - L164: tavern 路径 `max_tokens || 4096` → `||` 吞 0
    - L251: custom 路径显式转发 maxTokens/temperature/topP → 缺驼峰别名，应让 buildCustomApiRequestBody_ACU 统一兜底

  D. src/service/summary/merge-executor.ts:
    - L129: tavern 路径 `max_tokens || 4096` → `||` 吞 0
    - L139: custom 路径 `maxTokens || 4096, temperature` → `||` 吞 0

  E. src/service/summary/merge-logic.ts:
    - L198: tavern 路径 `max_tokens || 4096` → `||` 吞 0
    - L210: custom 路径 `maxTokens || 4096, temperature` → `||` 吞 0

confidence: HIGH

execution_plan: 已全部完成

degradation_check: 全部 YES

---

## Output Quality Review

task_summary:
  修复 AI 请求体参数硬编码与空值回退语义：`||` 改 `??`，移除硬编码温度覆盖，补齐驼峰别名，统一由 buildCustomApiRequestBody_ACU 兜底。

deliverables:
  - 5 个源文件共 13 处修改（6 处 `||` → `??`，4 处移除硬编码 overrides，3 处简化 overrides）
  - 1 个新建测试文件（worldbook-ai-api.test.ts，3 项测试）
  - 3 个扩展测试文件（api-call.test.ts +31 项，merge-executor.test.ts +2 项，merge-logic.test.ts +1 项）

metrics:
  total_files_modified: 8（5 源文件 + 3 测试文件），1 新建测试文件
  execution_plan_coverage: 100%（Step 1-8 全部完成）
  edge_cases_handled: temperature=0, top_p=0, topP 别名, max_tokens=0, maxTokens 别名
  confidence_assessment: HIGH

substance_check:
  无形式完整但实质空洞的内容。所有 `??` 替代均有对应测试锁定 0 值不被回退。

completeness_check:
  受影响文件全部覆盖。Tavern/useMainApi/custom 三类路径均有测试。callCustomOpenAI_ACU_Direct 的计划外同类 bug 也已修复。

value_density_check:
  高价值内容占比 > 90%。低价值内容仅为测试文件的 mock 基础设施代码（不可避免）。

alignment_check:
  满足用户"发现的问题一并优化"的本质需求。如果这是别人交给我的，我会接受。

notes:
  1. useApiPresetManagement.ts:64 仍使用 `max_tokens || 60000`，属于 UI 表单默认值，计划明确排除。
  2. worldbook-ai-api.ts:173 保留 maxTokens override，因为 callAI 公共 API 支持 options.max_tokens 调用者覆盖，与其他入口不同。
