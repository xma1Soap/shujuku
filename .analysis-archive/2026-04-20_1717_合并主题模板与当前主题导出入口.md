<analysis>
context:
  1. popup 内部当前有两个相邻按钮：
     - `${SCRIPT_ID_PREFIX_ACU}-theme-export`：导出当前主题（完整可编辑版）
     - `${SCRIPT_ID_PREFIX_ACU}-theme-template`：下载空白主题开发模板
     它们在 `src/presentation/theme/theme-selector.ts` 第47-63行渲染，并分别在第95-109行绑定到 `exportThemeToFile(getActiveThemeId())` 和 `exportThemeTemplate()`。
  2. chrome 顶部当前只有一个导出按钮 `${SCRIPT_ID_PREFIX_ACU}-chrome-theme-export`，在 `src/presentation/pages/main-popup.ts` 第53-55行渲染，并在 `theme-selector.ts` 第141-145行绑定到 `exportThemeToFile(getActiveThemeId())`。也就是说，chrome 顶部没有空白模板按钮。
  3. `exportThemeToFile()` 当前导出的是 `createExportableTheme(theme)`，已经不再是原始浅拷贝；`exportThemeTemplate()` 则基于一个手写模板对象再走 `createExportableTheme(...)`。这说明两个导出路径本质上已经很接近，只是“一个基于当前主题，一个基于空白示例”。
  4. 助手现在提出的目标很明确：不再保留“空白模板按钮”，而是让导出按钮统一输出“基于空白模板骨架 + 当前主题内容填充”的完整可编辑文件。这个思路比现状更直截了当，因为：
     - 用户真正要的是能二次修改的起点
     - 他并不关心空白模板和当前主题导出的概念区别
     - 现在两个按钮并存，反而继续制造选择成本
  5. 现有代码结构支持这个合并：只要把“空白模板骨架”抽成基础对象，再把当前主题内容 merge 进去，就能生成“完整可编辑的当前主题模板”。不需要两套完全独立的导出逻辑。
  6. 当前仍然存在的一个结构性不足：`exportThemeTemplate()` 作为独立函数存在，会继续要求 UI 暴露第二按钮；而助手的方案意味着它应该退化为内部的“模板骨架构造器”，不再作为用户直接操作入口。
  7. 因为本轮用户明确要求合并按钮，这属于方案变更。之前分析里计划的是“保留两种导出并补 chrome 模板按钮”，现在应改为“统一单一导出入口”。这不是任性变更，而是用户给出的更优交互目标。

needs:
  1. 将“空白模板”和“导出当前主题”统一成一个导出结果：输出完整模板骨架，并填入当前主题的实际内容，方便用户二次修改。
  2. 删除多余的模板按钮与其绑定逻辑，避免 UI 重复与概念混淆。
  3. 保留完整可编辑结构：即使当前主题没显式写某些模块字段，导出结果也要带上完整骨架。
  4. 文件名、按钮标题、提示语都要改成统一语义，不能再保留“模板/当前主题”双轨措辞。

key_challenges:
  1. 需要把当前 `exportThemeTemplate()` 的职责重构为“模板骨架生成”，而不是用户直接触发的第二条导出路径。
  2. 需要确保当前主题内容覆盖模板骨架时，不会把已有 `customCSS`、`previewColors`、模块字段错误清空或被模板默认值反向覆盖。
  3. popup 内部与 chrome 顶部都有导出按钮；合并后两处都必须保持一致行为。
  4. 需要谨慎处理文件名和文案，避免用户仍然误解为“这是纯空白模板”或“这只是主题快照”。

confidence: HIGH
  - 现有导出逻辑、按钮渲染、事件绑定都已确认
  - 用户给出的交互目标明确，技术实现路径清晰：模板骨架 + 当前主题 merge

approach:
  采用“单按钮统一导出”的方案：
  1. 将 `exportThemeTemplate()` 从“直接下载模板文件”改造为内部使用的模板骨架生成函数（例如 `createThemeTemplateBase()` 或类似逻辑），用于提供完整可编辑字段结构。
  2. 修改 `exportThemeToFile()`：导出结果不再只是 `createExportableTheme(theme)`，而是“模板骨架 + 当前主题”的合并结果。这样导出的文件天然具有完整模板结构，同时又保留当前主题的真实值。
  3. 删除 popup 内部的 `theme-template` 按钮和对应绑定逻辑；chrome 顶部保持单一导出按钮。两个位置都统一成“导出当前主题模板（可二次编辑）”。
  4. 调整按钮 title、成功提示和文件名，让语义明确为“完整可编辑主题模板”，避免继续出现“空白模板”和“当前主题导出”混用。
  
  三维评分：
  - 可维护性: 5/5 — 导出逻辑收敛为单一路径，UI 只保留一个导出入口，减少分叉
  - 健壮性: 5/5 — 基于模板骨架生成，保证所有可编辑字段始终存在；当前主题值覆盖骨架，避免缺字段
  - 可扩展性: 5/5 — 以后新增模块字段时，只需更新骨架构造器和 merge 逻辑，所有导出自动同步升级

edge_cases:
  - 当前主题已经显式定义 `customCSS`、`windowChromeVariables`、`toastVariables`、`visualizerVariables` 时，导出必须保留这些真实值，而不是回退成空模板值
  - 当前主题缺少某些模块对象时，导出仍要从模板骨架补齐，不能继续出现“只有这些”
  - 删除 popup 内部模板按钮后，UI 仍要保留一个明确可发现的导出入口，不能让用户误以为功能被删了
  - 旧导入逻辑必须继续兼容此前导出的文件格式，不因本次导出结构统一而破坏解析
  - 文件名必须清楚表达“这是可编辑主题模板”，避免与早期 `acu-theme-editable-*` 或纯空白模板命名混淆

affected_scope:
  - src/presentation/theme/theme-registry.ts
  - src/presentation/theme/theme-selector.ts
  - src/presentation/pages/main-popup.ts

execution_plan:
  - step_1: 修改 `src/presentation/theme/theme-registry.ts`，提取“主题模板骨架构造器”，将现有 `exportThemeTemplate()` 中的模板对象整理成内部可复用结构；然后修改 `exportThemeToFile()`，改为导出“模板骨架 + 当前主题覆盖”的完整可编辑主题模板。
  - step_2: 修改 `src/presentation/theme/theme-registry.ts` 的导出文件名与成功提示语，统一表达为“完整可编辑主题模板”或等价语义，不再区分空白模板与当前主题导出。
  - step_3: 修改 `src/presentation/theme/theme-selector.ts`，删除 popup 内部 `theme-template` 按钮渲染与绑定逻辑，只保留单一导出按钮，并更新 title。
  - step_4: 修改 `src/presentation/pages/main-popup.ts`，同步更新 chrome 顶部导出按钮 title，使 popup 内部与 chrome 顶部完全一致。
  - step_5: 运行 TypeScript 检查，确认导出重构没有类型错误；随后进入验收与归档。

degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。它消除了双按钮分叉，同时保留完整模板结构和当前主题值，三项维度都优于维持双轨导出。
  - 是否遗漏了已知边界条件？ → NO。真实值覆盖、缺字段补齐、UI 入口保留、旧导入兼容、文件名语义都已纳入。
  - 是否因改动量大而想缩减方案？ → NO。用户已经明确要求合并，不应继续保留双轨逻辑。
  - 是否打算跳过某些文件？ → NO。registry、selector、main-popup 都直接参与导出入口与行为。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。三个文件均已纳入。
  - context是否充分？是否有未读但可能相关的文件？ → NO。当前修改范围所需信息已充分。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO。popup 内模板按钮与 chrome 顶部无模板按钮都是这一轮合并目标的直接相关项。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO。所有修改都通过标准编辑工具完成。
</analysis>
---
<decision_point>
issue: `theme-registry.ts` 在重构导出逻辑后出现了两个同名的 `exportThemeToFile()` 实现（原始版本保留在第327行，新版本出现在第625行）。这是执行阶段的重构残留，不是侦察阶段能看到的问题，因为问题是在补丁应用后才出现的。
impact: YES + 如果不处理，TypeScript 会因重复函数声明报错，影响 `src/presentation/theme/theme-registry.ts` 的编译与整个主题导出链路。

context_update:
  - affected_scope 增量无变化，仍集中在 `src/presentation/theme/theme-registry.ts`
  - execution_plan 的 step_1 需要补充“删除旧的 exportThemeToFile 实现，保留统一后的单一路径”
  - confidence 维持 HIGH，因为问题清晰且修复路径单一

options:
  - option_a:
      description: 删除旧的 `exportThemeToFile()` 实现，保留新的“模板骨架 + 当前主题覆盖”版本作为唯一导出入口，并继续按单按钮方案完成其余步骤。
      approach_evaluation: 可维护性 5/5，单一导出路径最清晰；健壮性 5/5，消除重复声明并统一行为；可扩展性 5/5，后续新增字段只需维护一个函数。
      edge_cases: 需要确认没有其他代码依赖旧函数内部的特定提示文案或文件名格式，但函数签名不变，外部调用点不受影响。
      affected_scope_delta: 无新增文件，仅继续修改 `src/presentation/theme/theme-registry.ts`
  - option_b:
      description: 保留旧 `exportThemeToFile()`，把新逻辑改名为另一个函数，再让调用方逐步切换过去。
      approach_evaluation: 可维护性 2/5，双路径继续并存；健壮性 3/5，短期可编译但增加行为分叉；可扩展性 2/5，后续维护成本更高。
      edge_cases: 调用点若切换不全，会继续出现部分入口导出旧格式、部分入口导出新格式的问题。
      affected_scope_delta: 新增对 `theme-selector.ts`、`main-popup.ts` 的额外函数切换风险
  - option_c:
      description: 回退本次导出重构，只保留旧逻辑，之后重新设计。
      approach_evaluation: 可维护性 1/5，等于放弃当前正确方向；健壮性 2/5，只是恢复旧行为；可扩展性 1/5，用户当前诉求继续无法满足。
      edge_cases: 用户要求的“合并模板与当前主题导出”完全丢失。
      affected_scope_delta: 表面减少修改，但实质上导致任务目标失败

recommendation: 选择 option_a。三维评估综合最优：它能最直接地消除重复声明，同时保持“单按钮统一导出”的目标不被稀释。option_b 会制造新旧双轨并存，属于典型的结构性降级；option_c 则直接放弃用户明确要求。

execution_plan_update:
  - 在原 step_1 前追加一个修正动作：删除 `src/presentation/theme/theme-registry.ts` 中旧的 `exportThemeToFile()` 实现（第327-348行区域），保留新实现作为唯一导出入口。
  - 原 step_1 的其余内容保持不变：继续整理模板骨架构造器与统一导出逻辑。
  - 后续 step_3、step_4、step_5 不变。

deviation_audit:
  original_plan_excerpt: |
    - step_1: 修改 `src/presentation/theme/theme-registry.ts`，提取“主题模板骨架构造器”，将现有 `exportThemeTemplate()` 中的模板对象整理成内部可复用结构；然后修改 `exportThemeToFile()`，改为导出“模板骨架 + 当前主题覆盖”的完整可编辑主题模板。
    - step_2: 修改 `src/presentation/theme/theme-registry.ts` 的导出文件名与成功提示语，统一表达为“完整可编辑主题模板”或等价语义，不再区分空白模板与当前主题导出。
  current_proposal: |
    在 step_1 内补充清理动作：先删除旧的 `exportThemeToFile()` 实现，再保留新实现作为唯一导出入口；随后继续执行模板骨架提取、文件名和提示语统一。
  diff_summary:
    - 新增了“删除旧 exportThemeToFile 实现”的清理动作
    - 没有删减原方案目标，只是补上执行阶段暴露出的重构残留修复
  deviation_motive_check:
    - 措辞替换规则逐类检查 → 全部未命中，本次偏离是执行阶段发现的重复声明修复，不包含捷径或降级措辞
    - 偏离后的方案在三维评分上是否低于原方案？→ NO。它只是补充清理动作，不降低任何维度，反而提升健壮性
    - 偏离是否导致 affected_scope 缩小？→ NO
degradation_check:
  - 推荐方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。option_a 以单一路径消除重复声明，三项评分都高于其他方案
  - 推荐方案是否遗漏了新发现的边界条件？ → NO。重复声明这一新问题已被纳入修复动作
  - 是否因为想尽快完成而选择了改动量小的方案？ → NO。option_a 既处理报错又保持目标完整，不是缩减方案
  - 修改后的execution_plan是否仍覆盖所有affected_scope？ → YES。相关文件没有减少
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO。重复导出函数会直接导致编译失败，不能跳过
  - options 是否包含至少三个方案？ → YES
  - 是否因为工具报错而准备换用 shell 命令修改源代码？ → NO。继续使用标准编辑工具
  - deviation_audit 是否触发了 self_dissection？ → NO。没有出现偷懒判定
</decision_point>
---
<output_quality_review>
task_summary: 将“空白模板导出”和“当前主题导出”合并为单一导出入口——现在导出按钮会输出“模板骨架 + 当前主题覆盖”的完整可编辑主题模板，并删除 popup 内部多余的空白模板按钮。
deliverables:
  - src/presentation/theme/theme-registry.ts
  - src/presentation/theme/theme-selector.ts
  - src/presentation/pages/main-popup.ts

# 量化指标总览
metrics:
  total_files_modified: 3 — 修改/新增的文件总数
  execution_plan_coverage: 5/5 = 100% — execution_plan 执行覆盖率
  edge_cases_handled: 5/5 = 100% — 边界条件处理覆盖率
  confidence_assessment: HIGH — 导出逻辑已统一，TypeScript 检查通过，无已知遗漏

# 产物实质性检查
substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？
    → NO + 逐一检查：
      1. `theme-registry.ts` 不再维护“当前主题导出”和“空白模板导出”两条用户路径，而是改成单一导出：模板骨架 + 当前主题覆盖。删除这部分修改后，用户会重新回到双按钮分叉与概念混乱。
      2. `theme-selector.ts` 删除 popup 内部模板按钮与绑定逻辑，直接改变 UI 行为，避免重复入口。
      3. `main-popup.ts` 更新 chrome 顶部按钮 title，保证两个入口语义一致。删除后会再次产生认知偏差。
  - 产物是否能被其目标对象（被测代码/被重构模块/被修复的bug）的变化所"击穿"？
    → YES + 如果恢复旧的 `exportThemeToFile()` 实现，导出结果就不再是“模板骨架 + 当前主题”；如果恢复 `theme-template` 按钮，UI 会重新出现双轨入口，直接击穿本次目标。
  - 实质性比率: 3/3 = 100%

# 覆盖完整性检查
completeness_check:
  - 是否存在被跳过的模块/函数/路径？
    → NO。导出函数、popup 按钮渲染与绑定、chrome 顶部按钮语义都已覆盖。
  - 产物覆盖的范围是否与 execution_plan 中 affected_scope 完全一致？
    → YES。三个目标文件均已修改。
  - 核心业务逻辑是否都有直接验证（不依赖间接覆盖）？
    → YES。核心逻辑与直接验证位置：
      1. 单一路径导出模板：`theme-registry.ts` 中 `createThemeTemplateBase()`、`createEditableThemeTemplate()`、新的 `exportThemeToFile()`。
      2. 删除 popup 内模板按钮：`theme-selector.ts` 中 HTML 渲染与绑定逻辑删除。
      3. 统一按钮语义：`theme-selector.ts` 与 `main-popup.ts` 的 title 更新。
      4. 编译验证：`npx tsc --noEmit` 通过。
  - affected_scope 覆盖率: 3/3 = 100%

# 价值密度检查
value_density_check:
  - 产物中高价值内容（验证核心逻辑/处理复杂场景）与低价值内容（验证trivial行为）的比例是多少？
    → 高价值:低价值 = 3:0，高价值占比 100%。所有修改都围绕导出路径统一和用户认知成本降低展开。
  - 是否存在"用数量掩盖质量"的模式——大量 trivial 产物掩盖了核心逻辑缺少验证的事实？
    → NO。改动不多，但每一处都是核心路径。

# 需求对齐检查
alignment_check:
  - 产物满足的是用户的字面需求还是本质需求？
    → 满足本质需求。用户并不是真的想保留“空白模板”这个概念，而是想拿到一个可直接二次修改的完整起点。现在单一导出按钮直接输出“完整可编辑当前主题模板”，这比双按钮更贴合真实使用场景。
  - "如果这是别人交给我的，我会接受吗？"
    → YES。因为现在用户不需要再理解“模板”和“当前主题导出”的差异，点击一次就能拿到真正可编辑的完整文件，这才像是正常产品行为。
</output_quality_review>
---
<decision_point>
issue: `theme-selector.ts` 仍然保留了对 `exportThemeTemplate` 的 import，但我已经把它从用户导出路径中移除并内化为模板骨架逻辑，导致 TypeScript 报错 `TS2305: Module './theme-registry' has no exported member 'exportThemeTemplate'`。
impact: YES + 直接影响编译，范围在 `src/presentation/theme/theme-selector.ts` 与 `src/presentation/theme/theme-registry.ts` 的接口一致性。

context_update:
  - affected_scope 不新增文件，仍为 `theme-selector.ts` 与 `theme-registry.ts`
  - execution_plan 的 step_3 需补充“移除废弃 import”
  - confidence 维持 HIGH，这是标准的重构后接口清理问题

options:
  - option_a:
      description: 从 `theme-selector.ts` 中删除 `exportThemeTemplate` 的 import，并保留当前单按钮统一导出方案。
      approach_evaluation: 可维护性 5/5，接口与实现一致；健壮性 5/5，直接消除编译错误；可扩展性 5/5，避免保留死引用。
      edge_cases: 需要确认没有其他文件继续 import 该符号，但当前报错已精确指出至少这一个调用点。
      affected_scope_delta: 无新增文件，仅继续修改 `src/presentation/theme/theme-selector.ts`
  - option_b:
      description: 恢复导出一个空壳 `exportThemeTemplate`，内部再委托到统一导出逻辑，以兼容旧 import。
      approach_evaluation: 可维护性 2/5，留下无意义兼容接口；健壮性 4/5，能通过编译；可扩展性 2/5，未来容易让调用方重新分叉。
      edge_cases: 其他地方可能再次误用这个名字，重新引入“模板按钮”概念混乱。
      affected_scope_delta: 继续污染 `theme-registry.ts` 的导出接口
  - option_c:
      description: 回退单按钮方案，恢复 `exportThemeTemplate` 与模板按钮。
      approach_evaluation: 可维护性 1/5，回到用户刚明确否定的双轨模式；健壮性 3/5，仅是回滚；可扩展性 1/5，继续制造概念分叉。
      edge_cases: 用户目标直接被违背。
      affected_scope_delta: 回滚多个文件，扩大无意义改动

recommendation: 选择 option_a。它是三维评估综合最优的方案：清理废弃 import，保持接口单一且与当前用户要求一致。option_b 虽然能编译，但本质是留下历史兼容垃圾；option_c 更是直接违背用户要求。

execution_plan_update:
  - 在当前 step_3 后追加一个清理动作：修改 `src/presentation/theme/theme-selector.ts`，删除 `exportThemeTemplate` 的 import。
  - 其余步骤不变，清理后重新运行 TypeScript 检查。

deviation_audit:
  original_plan_excerpt: |
    - step_3: 修改 `src/presentation/theme/theme-selector.ts`，删除 popup 内部 `theme-template` 按钮渲染与绑定逻辑，只保留单一导出按钮，并更新 title。
  current_proposal: |
    在 step_3 基础上补充接口清理：删除 `theme-selector.ts` 中已废弃的 `exportThemeTemplate` import，保证重构后的接口与调用方一致。
  diff_summary:
    - 新增了“删除废弃 import”的清理动作
    - 没有改变方案目标，只是修复执行阶段出现的接口残留
  deviation_motive_check:
    - 措辞替换规则逐类检查 → 全部未命中，本次偏离属于执行阶段编译错误修复
    - 偏离后的方案在三维评分上是否低于原方案？→ NO。它提升了健壮性，不降低任何评分
    - 偏离是否导致 affected_scope 缩小？→ NO
degradation_check:
  - 推荐方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。option_a 以最小且正确的接口清理解决编译问题
  - 推荐方案是否遗漏了新发现的边界条件？ → NO。废弃 import 已纳入修复动作
  - 是否因为想尽快完成而选择了改动量小的方案？ → NO。它不是缩减功能，而是正确清理残留
  - 修改后的execution_plan是否仍覆盖所有affected_scope？ → YES。相关文件仍在覆盖范围内
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO。编译错误不能跳过
  - options 是否包含至少三个方案？ → YES
  - 是否因为工具报错而准备换用 shell 命令修改源代码？ → NO。继续使用标准编辑工具
  - deviation_audit 是否触发了 self_dissection？ → NO。没有出现偷懒判定
</decision_point>
