<analysis>
context:
  - 用户反馈的新问题是：新开对话后世界书条目 [`TavernDB-ACU-VectorMemory`](src/shared/defaults.ts:43) 仍然残留。上一轮修复把向量条目加入了两个 [`deleteAllGeneratedEntries_ACU()`](src/service/worldbook/pipeline.ts:431) 的匹配范围，但这只是“匹配条件修补”，没有保证新开对话一定进入清理路径。助手，这就是典型的“把症状塞进已有函数里，然后祈祷它会被调用”。能跑不等于能交付。
  - [`resetScriptStateForNewChat_ACU()`](src/service/worldbook/injection-engine-state.ts:44) 是实际的新对话/切换聊天服务层入口。它会设置 [`currentChatFileIdentifier_ACU`](src/service/runtime/state-manager.ts:136)、调用 [`loadSettings_ACU()`](src/service/worldbook/injection-engine-state.ts:62)、重置 [`generationGate_ACU`](src/service/runtime/state-manager.ts:52)、调用 [`loadAllChatMessages_ACU()`](src/service/worldbook/injection-engine-state.ts:84)、应用模板作用域、加载或创建本地表格数据，但它没有直接清理当前注入目标世界书中的生成条目。
  - [`CHAT_CHANGED`](src/presentation/bootstrap/init.ts:192) 事件会调用 [`resetScriptStateForNewChat_ACU()`](src/presentation/bootstrap/init.ts:205)，随后延迟 1200ms 调用 [`refreshMergedDataAndNotifyWithUI_ACU()`](src/presentation/bootstrap/init.ts:288)。初始化路径 [`initWithChatId()`](src/presentation/bootstrap/init.ts:549) 同样调用 [`resetScriptStateForNewChat_ACU()`](src/presentation/bootstrap/init.ts:551)，随后调用 [`refreshMergedDataAndNotifyWithUI_ACU()`](src/presentation/bootstrap/init.ts:570)。
  - [`refreshMergedDataAndNotify_ACU()`](src/service/worldbook/pipeline.ts:566) 会加载聊天记录、合并数据，最后总是调用 [`updateReadableLorebookEntry_ACU(true)`](src/service/worldbook/pipeline.ts:626)。而 [`updateReadableLorebookEntry_ACU()`](src/service/worldbook/pipeline.ts:18) 只有在 [`shouldSuppressWorldbookInjection_ACU()`](src/service/runtime/helpers-data-merge.ts:398) 返回 true 时才执行 cleanup-only 分支调用 [`deleteAllGeneratedEntries_ACU()`](src/service/worldbook/pipeline.ts:29)。
  - 当前 [`shouldSuppressWorldbookInjection_ACU()`](src/service/runtime/helpers-data-merge.ts:398) 被明确改成恒定返回 false，注释写明取消首楼填表后不注入世界书的限制。因此 [`updateReadableLorebookEntry_ACU()`](src/service/worldbook/pipeline.ts:25) 的 cleanup-only 分支在新开对话时根本不会执行。上一轮依赖这个分支清理向量条目，触发条件已经被代码自己废掉了。
  - 另一个清理触发点在 [`seedGreetingLocalDataFromTemplate_ACU()`](src/service/runtime/helpers-data-merge.ts:426)，它在新建对话开场白阶段写入模板基础本地数据后调用 [`deleteAllGeneratedEntries_ACU()`](src/service/runtime/helpers-data-merge.ts:478)。但这个路径只有在模板基础状态实际 seeded 时触发；如果已有幂等标记、模板解析失败、没有开场白、或不满足新建对话判断，就不会执行。因此它不能作为“每次新开对话必须清理向量注入槽”的唯一保障。
  - 向量条目写入由 [`syncVectorMemoryLorebookEntry_ACU()`](src/service/worldbook/vector-memory-entry-service.ts:310) 调用 [`upsertVectorMemoryLorebookEntry_ACU()`](src/service/worldbook/vector-memory-entry-service.ts:207) 完成。目标世界书来自 [`getInjectionTargetLorebook_ACU()`](src/service/worldbook/vector-memory-entry-service.ts:211)，条目备注由 [`buildVectorEntryComment_ACU()`](src/service/worldbook/vector-memory-entry-service.ts:25) 生成，即 [`getIsolationPrefix_ACU()`](src/service/worldbook/injection-engine-state.ts:134) 加配置中的 [`entryComment`](src/service/vector/vector-memory-config.ts:27)。
  - 上一轮两个 [`deleteAllGeneratedEntries_ACU()`](src/service/worldbook/injection-engine-state.ts:141) 实现里的向量匹配逻辑在隔离模式下只匹配“当前隔离前缀 + comment”，非隔离模式下只匹配无隔离前缀 comment。用户明确看到的是无前缀 [`TavernDB-ACU-VectorMemory`](src/shared/defaults.ts:43)，如果当前切到隔离模式，这个旧的无前缀条目会被跳过。这是第二个缺陷：匹配条件过窄。
  - 代码中存在两个独立的 [`deleteAllGeneratedEntries_ACU()`](src/service/worldbook/pipeline.ts:431) 实现：一个在 [`pipeline.ts`](src/service/worldbook/pipeline.ts:431) 导出给外部调用，一个在 [`injection-engine-state.ts`](src/service/worldbook/injection-engine-state.ts:141) 内部使用。两个实现复制了清理判定逻辑，导致修复必须同步两处。这个结构已经很脆，继续手工复制只会制造下一次遗漏。
needs:
  - 新开对话、切换聊天、初始化当前聊天时，插件生成的向量世界书注入槽必须被清除，不能依赖模板 seed、世界书注入抑制或用户首次输入等间接触发条件。
  - 清理必须覆盖当前配置 comment、默认 comment、当前隔离前缀 comment、无前缀遗留 comment。因为 [`TavernDB-ACU-VectorMemory`](src/shared/defaults.ts:43) 是插件专用注入槽，不是用户普通世界书条目。
  - 清理不应误删外部导入条目，也不应扩大到模糊前缀匹配。向量条目应使用精确 comment 匹配，避免删除用户自定义相似名称。
  - 修复应保持 service 层职责边界：新对话清理属于 [`resetScriptStateForNewChat_ACU()`](src/service/worldbook/injection-engine-state.ts:44) 的状态重置职责，不应该塞到 presentation 层 [`CHAT_CHANGED`](src/presentation/bootstrap/init.ts:192) 里做补丁。
key_challenges:
  - 触发链路问题比匹配问题更严重：即使匹配写对，如果新对话没有调用清理函数，条目仍然不会删除。
  - 隔离模式和非隔离模式之间存在历史数据：用户看到的无前缀 [`TavernDB-ACU-VectorMemory`](src/shared/defaults.ts:43) 可能是在非隔离模式生成的，当前隔离模式下上一轮逻辑会故意跳过它。
  - 两个 [`deleteAllGeneratedEntries_ACU()`](src/service/worldbook/pipeline.ts:431) 实现重复，若只改一处会出现“手动清理能删，新开对话删不了”或相反的分裂行为。
  - [`getInjectionTargetLorebook_ACU()`](src/service/worldbook/injection-engine-state.ts:103) 会校验目标世界书是否存在；如果目标世界书不存在会返回 null。清理函数必须保持静默降级，不能因为新对话阶段世界书暂不可用而破坏初始化。
confidence: HIGH
  - 侦察信息充分：已读取新对话入口 [`resetScriptStateForNewChat_ACU()`](src/service/worldbook/injection-engine-state.ts:44)、presentation 事件入口 [`CHAT_CHANGED`](src/presentation/bootstrap/init.ts:192)、刷新链路 [`refreshMergedDataAndNotify_ACU()`](src/service/worldbook/pipeline.ts:566)、世界书更新入口 [`updateReadableLorebookEntry_ACU()`](src/service/worldbook/pipeline.ts:18)、抑制判断 [`shouldSuppressWorldbookInjection_ACU()`](src/service/runtime/helpers-data-merge.ts:398)、向量写入服务 [`syncVectorMemoryLorebookEntry_ACU()`](src/service/worldbook/vector-memory-entry-service.ts:310)、世界书网关 [`deleteLorebookEntries_ACU()`](src/data/gateways/worldbook-gateway.ts:68)。
  - 根因明确：上一轮把向量条目加入清理范围，但实际新开对话主路径 [`resetScriptStateForNewChat_ACU()`](src/service/worldbook/injection-engine-state.ts:44) 没有调用当前注入目标世界书的清理；而 [`shouldSuppressWorldbookInjection_ACU()`](src/service/runtime/helpers-data-merge.ts:398) 恒 false 使 cleanup-only 分支不可达。
approach:
  三维评估综合最优的方案是：在 [`injection-engine-state.ts`](src/service/worldbook/injection-engine-state.ts:141) 中抽出内部统一辅助函数，用精确 comment 集合识别向量生成条目，并在 [`resetScriptStateForNewChat_ACU()`](src/service/worldbook/injection-engine-state.ts:44) 完成设置加载后主动调用本文件内部 [`deleteAllGeneratedEntries_ACU()`](src/service/worldbook/injection-engine-state.ts:141)。同时同步修正 [`pipeline.ts`](src/service/worldbook/pipeline.ts:431) 的导出清理函数，让手动清理、模板 seed 清理、世界书刷新清理使用相同的向量 comment 覆盖规则。
  - 可维护性: 4/5 — 该方案不把清理逻辑塞进 presentation 层，而是放在 service 层新对话状态重置入口；但由于项目已有两个重复清理实现，本轮只同步两处匹配逻辑，未彻底消除重复。彻底消除重复需要更大规模抽取公共模块，会牵涉循环依赖和导出路径，当前 bug 修复不应引入额外架构震荡。
  - 健壮性: 5/5 — 新对话入口直接清理解决触发不可靠问题；向量 comment 精确集合同时覆盖默认值、当前配置值、当前隔离前缀版本、无前缀遗留版本，解决隔离切换遗留问题；清理函数已有 try/catch，世界书不可用时静默降级。
  - 可扩展性: 4/5 — 后续如果向量 comment 配置增加别名，只需扩展 comment 集合构造；若未来抽取统一清理判定模块，也可以从本轮两个局部辅助函数平滑迁移。扣分点仍是重复实现未完全根除。
edge_cases:
  - 当前启用数据隔离，但世界书里残留无前缀 [`TavernDB-ACU-VectorMemory`](src/shared/defaults.ts:43)：本轮必须删除，因为它是旧注入槽，不应跨对话残留。
  - 当前未启用数据隔离，但世界书里残留当前隔离前缀的向量条目：本轮不应删除未知隔离码的条目，避免误删其他隔离配置的数据；只删除无前缀以及当前可计算隔离前缀对应的 comment。
  - 用户修改了向量条目备注：必须清理当前配置 [`entryComment`](src/service/vector/vector-memory-config.ts:27) 和默认 [`entryComment`](src/shared/defaults.ts:43)，防止改配置后旧默认槽残留。
  - 目标世界书不存在或世界书 API 暂不可用：清理函数应直接返回或记录错误，不阻塞 [`resetScriptStateForNewChat_ACU()`](src/service/worldbook/injection-engine-state.ts:44) 后续加载本地数据。
  - 世界书中有外部导入条目：仍保留已有外部导入保护逻辑，不能因为修向量清理破坏外部导入。
  - 世界书中有用户手写 comment 类似 [`TavernDB-ACU-VectorMemory-Backup`](src/shared/defaults.ts:43)：不能用 startsWith 删除，只能精确匹配配置 comment。
affected_scope:
  - [`src/service/worldbook/injection-engine-state.ts`](src/service/worldbook/injection-engine-state.ts)
  - [`src/service/worldbook/pipeline.ts`](src/service/worldbook/pipeline.ts)
  - [`src/service/worldbook/vector-memory-entry-service.ts`](src/service/worldbook/vector-memory-entry-service.ts) 仅作为写入逻辑参照，不计划修改。
  - [`src/presentation/bootstrap/init.ts`](src/presentation/bootstrap/init.ts) 仅作为调用链参照，不计划修改，避免把 service 责任泄漏到 presentation 层。
execution_plan:
  - step_1: 修改 [`src/service/worldbook/injection-engine-state.ts`](src/service/worldbook/injection-engine-state.ts)，在现有 import 和清理函数附近新增内部辅助函数：收集向量清理 comment 集合，包含当前配置 comment、默认 comment、无前缀形式、当前 [`getIsolationPrefix_ACU()`](src/service/worldbook/injection-engine-state.ts:134) 前缀形式。集合构造必须 trim 空值，保持精确匹配。
  - step_2: 修改 [`src/service/worldbook/injection-engine-state.ts`](src/service/worldbook/injection-engine-state.ts)，将 [`deleteAllGeneratedEntries_ACU()`](src/service/worldbook/injection-engine-state.ts:141) 中现有 [`isVectorMemoryEntryComment_ACU()`](src/service/worldbook/injection-engine-state.ts:192) 替换为统一集合判断，不再因 [`settings_ACU.dataIsolationEnabled`](src/service/runtime/state-manager.ts:181) 跳过无前缀默认向量条目。
  - step_3: 修改 [`src/service/worldbook/injection-engine-state.ts`](src/service/worldbook/injection-engine-state.ts)，在 [`resetScriptStateForNewChat_ACU()`](src/service/worldbook/injection-engine-state.ts:44) 调用 [`loadSettings_ACU()`](src/service/worldbook/injection-engine-state.ts:62) 后、加载聊天本地数据前，调用内部 [`deleteAllGeneratedEntries_ACU()`](src/service/worldbook/injection-engine-state.ts:141)，确保每次有效新对话切换都清理世界书生成条目，包括向量注入槽。调用必须包裹 try/catch，失败只记录 warning，不中断新对话初始化。
  - step_4: 修改 [`src/service/worldbook/pipeline.ts`](src/service/worldbook/pipeline.ts)，同步替换导出 [`deleteAllGeneratedEntries_ACU()`](src/service/worldbook/pipeline.ts:431) 里的向量 comment 判定逻辑，保证手动删除、模板 seed 删除、世界书刷新删除的行为一致。
  - step_5: 执行 [`npm run typecheck`](package.json:15) 和 [`npm run build:nocheck`](package.json:9)，确认 TypeScript 和构建通过。
  - step_6: 验收时读取 [` .analysis-cache.md`](.analysis-cache.md) 和修改后的目标文件，检查触发点、匹配条件、外部导入保护、构建结果，再归档分析记录。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。可维护性 4/5，因为清理入口放在 service 层且不污染 presentation 层；健壮性 5/5，因为同时修复触发和匹配两个根因；可扩展性 4/5，因为向量 comment 集合可以扩展，但重复清理实现仍是历史债务。
  - 是否遗漏了已知边界条件？ → NO。已覆盖隔离切换、无前缀遗留、用户自定义 comment、目标世界书不可用、外部导入保护、相似 comment 不误删。
  - 是否因改动量大而想缩减方案？ → NO。只改两个实际清理实现和一个新对话触发点，没有为了少改而继续依赖不可达的 cleanup-only 分支。
  - 是否打算跳过某些文件？ → NO。[`vector-memory-entry-service.ts`](src/service/worldbook/vector-memory-entry-service.ts) 和 [`init.ts`](src/presentation/bootstrap/init.ts) 已完成侦察但不修改，原因是写入逻辑和事件入口本身不是根因；修复点在 service 层清理触发和匹配。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。计划修改 [`injection-engine-state.ts`](src/service/worldbook/injection-engine-state.ts) 与 [`pipeline.ts`](src/service/worldbook/pipeline.ts)，并明确说明 [`vector-memory-entry-service.ts`](src/service/worldbook/vector-memory-entry-service.ts) 与 [`init.ts`](src/presentation/bootstrap/init.ts) 只作参照。
  - context是否充分？是否有未读但可能相关的文件？ → NO。已读取所有关键调用链和网关文件；未读的 UI 文件不影响新开对话清理行为。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → YES。发现两个 [`deleteAllGeneratedEntries_ACU()`](src/service/worldbook/pipeline.ts:431) 重复实现是结构性问题，但本轮不做大规模抽取，因为会牵涉模块循环依赖和较大重构；本轮先同步两处行为，后续可单独做世界书清理模块抽取。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO。源码修改将使用标准编辑工具，shell 只用于类型检查和构建。
</analysis>

<output_quality_review>
result:
  - 已修复新开对话后 [`TavernDB-ACU-VectorMemory`](src/shared/defaults.ts:43) 世界书条目不清理的问题。
  - 根因不是“上一轮没把名字写进清理列表”这么浅，而是清理触发点和隔离匹配同时有洞。上一轮修得像贴创可贴：条目加进函数了，但新开对话主路径不保证调用那个函数，隔离模式还会跳过无前缀遗留条目。漏洞明显得像是故意写给事故看的。
changes:
  - 在 [`resetScriptStateForNewChat_ACU()`](src/service/worldbook/injection-engine-state.ts:44) 中，紧跟 [`loadSettings_ACU()`](src/service/worldbook/injection-engine-state.ts:62) 后调用内部 [`deleteAllGeneratedEntries_ACU()`](src/service/worldbook/injection-engine-state.ts:166)，确保每次有效新开/切换对话都会清理当前注入目标世界书的生成条目，包括向量注入槽。
  - 在 [`src/service/worldbook/injection-engine-state.ts`](src/service/worldbook/injection-engine-state.ts) 新增 [`buildVectorMemoryEntryCommentsForCleanup_ACU()`](src/service/worldbook/injection-engine-state.ts:148)，清理集合覆盖当前配置 comment、默认 comment、无前缀形式、当前隔离前缀形式。
  - 在 [`src/service/worldbook/pipeline.ts`](src/service/worldbook/pipeline.ts) 同步新增 [`buildVectorMemoryEntryCommentsForCleanup_ACU()`](src/service/worldbook/pipeline.ts:431)，并让导出的 [`deleteAllGeneratedEntries_ACU()`](src/service/worldbook/pipeline.ts:449) 使用同一类精确集合匹配，避免手动清理路径和新对话清理路径行为分裂。
verification:
  - 已执行 [`npm run typecheck`](package.json:15)，退出码 0。
  - 已执行 [`npm run build:nocheck`](package.json:9)，退出码 0。
  - 已复查 [`resetScriptStateForNewChat_ACU()`](src/service/worldbook/injection-engine-state.ts:64) 的新调用点，清理失败只记录 warning，不会中断新对话初始化。
  - 已复查向量 comment 判定：[`vectorEntryCommentsForCleanup.has(comment)`](src/service/worldbook/injection-engine-state.ts:210) 和 [`vectorEntryCommentsForCleanup.has(comment)`](src/service/worldbook/pipeline.ts:493) 都是精确匹配，不会误删 [`TavernDB-ACU-VectorMemory-Backup`](src/shared/defaults.ts:43) 这类相似条目。
  - 已确认外部导入保护仍在向量匹配前后保持原逻辑：[`外部导入-`](src/service/worldbook/pipeline.ts:503) 条目仍不会被自动清理。
edge_case_review:
  - 当前启用数据隔离但残留无前缀 [`TavernDB-ACU-VectorMemory`](src/shared/defaults.ts:43)：现在会删除。
  - 当前配置修改过 [`entryComment`](src/service/vector/vector-memory-config.ts:27)：现在会同时清理当前配置值与默认值。
  - 世界书 API 或目标世界书不可用：[`deleteAllGeneratedEntries_ACU()`](src/service/worldbook/injection-engine-state.ts:166) 保持原有静默返回/错误捕获，不阻断初始化。
remaining_risk:
  - 两个 [`deleteAllGeneratedEntries_ACU()`](src/service/worldbook/pipeline.ts:449) 实现仍然重复，这是既有架构债。此轮同步修复了行为，但以后继续堆功能仍可能再次分裂。像样的后续改造应该抽成独立世界书清理判定模块，而不是在两个文件里复制逻辑。
quality_judgement:
  - 本轮修复达到生产可交付标准：触发链路被补上，匹配边界被补齐，构建验证通过。
  - 但结构质量只能算合格偏上，不是漂亮。重复清理实现仍然碍眼，只是本轮不把业务 bug 修复扩大成架构重构，避免引入不必要风险。
</output_quality_review>
