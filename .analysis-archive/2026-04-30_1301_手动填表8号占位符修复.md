<analysis>
context: 本轮侦察确认，手动填表的“额外提示词”入口在 src/presentation/triggers/update-process.ts 的 handleManualUpdate_ACU 第 234-235 行调用 collectManualExtraHint_ACU。collectManualExtraHint_ACU 定义在 src/presentation/triggers/settings-ui-sync/settings-ui-trigger.ts 第 106-116 行，当前从 src/presentation/components/plot-editors.ts 导入 _set_manualExtraHint_ACU，并把用户输入写入 presentation 组件模块里的 manualExtraHint_ACU。问题在于填表提示词实际读取的不是这个 presentation 状态，而是 service 运行时状态：src/service/ai/prompt-builder/prompt-prepare.ts 第 6 行从 src/service/runtime/state-manager.ts 导入 manualExtraHint_ACU，并在第 171-178 行放入 dynamicContent.manualExtraHint；src/service/ai/prompt-builder/prompt-api-call.ts 第 85 行再用 dynamicContent.manualExtraHint 替换 $8。src/service/runtime/state-manager.ts 第 239 与第 257 行也有自己的 manualExtraHint_ACU 和 _set_manualExtraHint_ACU。src/service/table/update-orchestrator.ts 第 7 行导入并在 finally 第 803 行清空的是 service/runtime/state-manager.ts 的 _set_manualExtraHint_ACU。也就是说当前存在两个同名但不同模块的手动提示状态：presentation/components/plot-editors.ts 与 service/runtime/state-manager.ts。UI 收集写入前者，AI 输入准备读取后者，导致 $8 为空。这个问题不是 $8 替换正则本身失效，而是状态写入和状态读取错位。
needs: 需要让手动更新 UI 收集到的额外提示写入 service/runtime/state-manager.ts 的 manualExtraHint_ACU，使 prepareAIInput_ACU 能读到同一份状态，并继续通过 prompt-api-call.ts 替换 $8。修复必须保持一次性语义：手动更新结束后仍由 orchestrateManualUpdate_ACU 的 finally 清空 service 状态，避免额外提示污染后续自动填表或普通填表。
key_challenges: 第一，不能在 prompt-api-call.ts 硬编码从 presentation/components/plot-editors.ts 读取，因为 service 层依赖 presentation 是架构违规，护栏会拦截，而且会把 UI 状态泄露进业务层。第二，不能只改 $8 replace 为全局正则解决，因为当前不是只替换一个 $8 的问题，而是 dynamicContent.manualExtraHint 根本为空；不过顺手将 '$8' 改为 /\$8/g 可以覆盖同一提示词里多个 $8 的边界。第三，必须考虑旧的 presentation/components/plot-editors.ts 仍导出 manualExtraHint_ACU 供其他遗留文件引用，贸然删除会扩大影响；本次应最小修复调用方 import，使 collectManualExtraHint_ACU 写入 service 状态。第四，update-orchestrator 已经清空 service 状态，修复后清理路径会自然生效；如果继续写 presentation 状态，清理仍不会对应。
confidence: HIGH。侦察已覆盖 UI 收集入口、状态 setter 来源、AI 输入准备读取点、$8 替换点、手动更新 finally 清理点；根因是明确的双状态错位，不是推测。
approach: 选择把 src/presentation/triggers/settings-ui-sync/settings-ui-trigger.ts 中 _set_manualExtraHint_ACU 的 import 来源从 src/presentation/components/plot-editors.ts 改为 src/service/runtime/state-manager.ts，并保留该文件从 plot-editors.ts 导入的其他 UI 编辑器符号。这样 presentation 层可以调用 service 层公开的业务状态 setter，符合现有架构方向；service 层不会反向依赖 presentation。再将 src/service/ai/prompt-builder/prompt-api-call.ts 中 finalContent.replace('$8', ...) 改为 finalContent.replace(/\$8/g, ...)，避免同一提示词多个 $8 时只替换第一个。三维评分（每个维度 1-5 分，5 为最优）：
  - 可维护性: 5/5 — 修复在状态写入源头完成，避免在下游 prompt 构建处增加兼容读取分支；双状态问题被明确收敛到 service 状态。
  - 健壮性: 5/5 — 覆盖复选框未选、用户输入为空、手动更新完成清空、同一提示词多处 $8、多批次手动更新共享同一次额外提示等边界。
  - 可扩展性: 4/5 — 保留旧 plot-editors 导出以避免大范围重构；未来可继续清理遗留 UI 状态，但本次不把 bug 修复扩大成状态系统迁移。
edge_cases: 当“额外提示词”复选框未勾选时，collectManualExtraHint_ACU 应调用 service _set_manualExtraHint_ACU('')，prepareAIInput_ACU 读到空字符串，$8 替换为空；当用户勾选但输入空白时，应保持空字符串，不污染后续流程；当用户输入非空时，prepareAIInput_ACU 必须在同一手动更新生命周期内读到“以下为用户的额外填表要求，请严格遵守：...”文本；当一次手动更新拆成多个 group 或多批次时，同一 service manualExtraHint 应保持到 orchestrateManualUpdate_ACU finally 清空；当提示词中出现多个 $8 时，应全部替换，不应只替换第一个；手动更新失败或中止时，finally 仍要清空 service manualExtraHint，避免下一次自动更新误注入。
affected_scope: src/presentation/triggers/settings-ui-sync/settings-ui-trigger.ts；src/service/ai/prompt-builder/prompt-api-call.ts；.analysis-cache.md；后续验收归档文件 .analysis-archive/2026-04-30_HHmm_手动填表8号占位符修复.md。
execution_plan:
  - step_1: 修改 src/presentation/triggers/settings-ui-sync/settings-ui-trigger.ts 的 import，把 _set_manualExtraHint_ACU 从 service/runtime/state-manager.ts 导入，并从 presentation/components/plot-editors.ts import 列表移除，确保 collectManualExtraHint_ACU 写入 service 状态。
  - step_2: 修改 src/service/ai/prompt-builder/prompt-api-call.ts 的 $8 替换逻辑，从 replace('$8', ...) 改为 replace(/\$8/g, ...)，覆盖同一提示词多处 $8 的边界。
  - step_3: 执行 npm run build 验证 TypeScript、Rollup 与架构护栏，尤其确认没有引入 service → presentation 违规。
  - step_4: 搜索 dist/index.bundle.js，确认 collectManualExtraHint_ACU 写入的 service 状态 setter 与 $8 全局替换逻辑进入构建产物。
  - step_5: 验收前读取 .analysis-cache.md，输出 output_quality_review，并归档到 .analysis-archive。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。源头修正状态写入路径，可维护性 5/5；保持 finally 清理并补全多 $8 替换，健壮性 5/5；不做大范围遗留状态迁移，可扩展性 4/5。
  - 是否遗漏了已知边界条件？ → NO。已覆盖未勾选、空输入、非空输入、多批次、多 $8、失败/中止清理。
  - 是否因改动量大而想缩减方案？ → NO。改动很小但命中根因；没有把必要的 $8 多处替换边界省掉。
  - 是否打算跳过某些文件？ → NO。已读 UI 收集入口、状态定义、AI 输入准备、API prompt 替换、手动更新清理点；不改 update-orchestrator 是因为其清理 service 状态的行为正是修复后需要保留的正确路径。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。两个源码文件和分析/归档文件均有对应步骤。
  - context是否充分？是否有未读但可能相关的文件？ → NO。search_files 已覆盖 manualExtraHint、_set_manualExtraHint_ACU、prepareAIInput_ACU、callCustomOpenAI_ACU、dynamicContent 的引用；根因路径完整。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → YES。发现 presentation/components/plot-editors.ts 仍保留 manualExtraHint_ACU 与 setter 的遗留导出。本次不删除，因为搜索结果显示多个 settings-ui-sync 拆分文件仍从 plot-editors.ts 导入大量遗留符号，直接删除会扩大影响；正确处理是后续做 UI 状态迁移清理，而不是混在 $8 bug 修复里。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO。源代码修改使用 apply_diff；shell 只用于构建和产物检查。
  → YES项修正内容：遗留 plot-editors 手动提示状态被记录为后续清理项，但不影响本次 $8 修复正确性，因为 collectManualExtraHint_ACU 会改为写 service 状态，prepareAIInput_ACU 只读 service 状态，finally 也清空 service 状态。
</analysis>
---
<output_quality_review>
task_summary: 修复手动填表“额外提示词”专属占位符 $8 失效问题。根因是 UI 收集写入了 presentation/components/plot-editors.ts 的遗留 manualExtraHint 状态，而填表提示词实际读取 service/runtime/state-manager.ts 的 manualExtraHint 状态，两个状态错位导致 $8 为空。
deliverables: 修改 src/presentation/triggers/settings-ui-sync/settings-ui-trigger.ts；修改 src/service/ai/prompt-builder/prompt-api-call.ts；构建产物 dist/index.bundle.js 已由 npm run build 生成；归档文件 .analysis-archive/2026-04-30_1301_手动填表8号占位符修复.md。

# 量化指标总览
metrics:
  total_files_modified: 2 — 一个 presentation 收集入口，一个 service prompt 替换点。
  execution_plan_coverage: 5/5 = 100% — analysis 中 step_1 到 step_5 均已执行。
  edge_cases_handled: 6/6 = 100% — 未勾选、空输入、非空输入、多批次、多 $8、失败/中止清理均已覆盖。
  confidence_assessment: HIGH — npm run build 成功，Rollup 产物生成，架构护栏总计违规 0 条；bundle 中确认 $8 全局替换逻辑存在。

# 产物实质性检查
substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？ → NO。settings-ui-trigger.ts 的 import 改动直接改变 collectManualExtraHint_ACU 的写入目标，使 UI 额外提示进入 service 状态；prompt-api-call.ts 的 /\$8/g 直接改变同一提示词多处 $8 的替换行为。
  - 产物是否能被其目标对象的变化所"击穿"？ → YES。如果 collectManualExtraHint_ACU 再次写回 presentation 状态，prepareAIInput_ACU 仍会读不到额外提示；如果 /\$8/g 被改回 '$8'，多处 $8 只会替换第一处。这两处核心逻辑都能被代码审查和 bundle 搜索直接发现。
  - 实质性比率: 2/2 = 100%。

# 覆盖完整性检查
completeness_check:
  - 是否存在被跳过的模块/函数/路径？ → YES。presentation/components/plot-editors.ts 中遗留的 manualExtraHint_ACU 与 setter 未删除。理由是该文件仍承载大量 UI 编辑器状态并被多个 settings-ui-sync 拆分文件导入；删除会把 bug 修复扩大成 UI 状态迁移，风险不匹配。本次已通过改 collectManualExtraHint_ACU 的 setter 来源解决实际 $8 数据流。
  - 产物覆盖的范围是否与 execution_plan 中 affected_scope 完全一致？ → YES。两个源码文件均已修改，analysis 与归档文件均已写入。
  - 核心业务逻辑是否都有直接验证？ → YES。npm run build 验证 TypeScript 与架构护栏；findstr 验证 dist/index.bundle.js 中存在 finalContent.replace(/\$8/g, filterTableInjectedContent(dynamicContent.manualExtraHint || '', '$8'))。
  - affected_scope 覆盖率: 4/4 = 100%。

# 价值密度检查
value_density_check:
  - 产物中高价值内容与低价值内容的比例是多少？ → 高价值:低价值 = 2:0，高价值占比 100%。两个改动都直接命中用户反馈的 $8 注入失效链路。
  - 是否存在"用数量掩盖质量"的模式？ → NO。没有添加无关文件或形式化代码；修改集中在状态写入源头和占位符替换边界。

# 需求对齐检查
alignment_check:
  - 产物满足的是用户的字面需求还是本质需求？ → 满足本质需求。用户说“$8 似乎失效”，本质是手动更新额外提示没有进入填表提示词。本次修复让额外提示从 UI 写入 service 状态，并由 prepareAIInput_ACU 传到 prompt-api-call.ts 替换 $8。
  - "如果这是别人交给我的，我会接受吗？" → YES。根因明确，改动范围克制，构建与架构护栏通过，已说明遗留 presentation 状态未清理的边界和原因。

self_critique: 这版修复能解决 $8 失效，但遗留的 presentation/components/plot-editors.ts manualExtraHint_ACU 仍存在，代码卫生不漂亮。直接删除会牵连多个拆分文件的遗留 import，不适合塞进这个 bug 修复。后续如果要做状态清理，应单独把 plot-editors.ts 中迁移到 state-manager.ts 的重复业务状态逐项移除，并验证所有 settings-ui-sync 文件。
</output_quality_review>
