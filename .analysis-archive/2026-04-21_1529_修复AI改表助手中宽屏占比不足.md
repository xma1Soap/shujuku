<analysis>
context: 已确认 AI 改表助手当前有三档模式：`desktop | compact | mobile-overlay`，定义在 `src/presentation/pages/visualizer-template-assistant.ts`。窄屏判定阈值仍是 `MOBILE_VIEWPORT_MAX_ACU = 899`、`COMPACT_VIEWPORT_MAX_ACU = 1279`。当前 compact 模式宽度被硬编码为 `340px`，而非按父容器比例伸缩；mobile-overlay 才会走 `width:100vw` 的全屏覆盖。用户现在反馈“窄屏模式下只占整个界面一半而不是 80%+”，这与源码完全对上：如果实际测试设备/浏览器宽度落在 900~1279 区间，就只会进入 compact 模式，面板固定 340px，而且 `visualizer-styles.ts` 在 `@media (max-width: 1279px)` 下还额外把 `#acu-vis-assistant-host .acu-vis-assistant-panel` 的 `max-width` 限制为 `min(360px, 100%)`，相当于双重把宽度卡死。宿主 DOM 位置也已确认：`visualizer-template-assistant-addon.ts` 把 `#acu-vis-assistant-host` 追加到 `.acu-vis-content` 内部，而 `.acu-vis-content` 本身是一个 flex 容器（`visualizer.ts` 内联 `flex:1; display:flex; overflow:hidden;`，`visualizer-styles.ts` 也有 `.acu-vis-content { flex: 1; display: flex; overflow: hidden; min-width: 0; }`）。这意味着当前 assistant 在非 mobile-overlay 下仍是作为右侧 flex 子项参与布局，而不是单独悬浮窗口。用户说“只占整个界面的一半”，更可能不是 mobile-overlay 失效，而是实际命中了 compact 模式，且 340px/360px 上限在可视化容器里看起来像半边侧栏。现有实现没有“占据数据可视化界面 80%+”这一中间态模式，也没有在 compact 模式下按父容器比例扩展宽度。要满足用户这次更精确的要求，不能只盯着 mobile-overlay，必须把 compact 模式改成 large-overlay / wide-overlay 一类覆盖式布局，或者直接把 mobile-overlay 触发阈值上调到覆盖用户实际窄屏场景。同时，`tests/presentation/visualizer-template-assistant.test.ts` 目前只验证 1100px 时是 compact 宽度 340px、768px 时是 mobile-overlay，全都在维护旧行为；如果改阈值或宽度策略，测试必须同步更新。
needs: 用户的本质需求已经从“手机窄屏能显示”收敛为更具体的交付标准：当可视化区域变窄到 assistant 侧栏无法正常使用时，assistant 不应继续停留在固定 340px 的侧栏模式，而应自动切换为覆盖式宽窗口，至少占据数据可视化界面的 80% 以上，从而保证可读性和可操作性。与此同时，最小化/恢复能力不能被破坏，桌面宽屏也不能被误伤。
key_challenges: 第一，当前“窄屏”概念被拆成 compact 与 mobile-overlay 两档，而用户显然把 compact 也视为“窄屏”。如果只继续沿用 899px 的 mobile 阈值，用户会反复踩到 900~1279px 区间的固定 340px 侧栏。第二，宽度限制存在于 TS 与 CSS 两层：`getAssistantPanelWidth_ACU(mode)` 返回 340px，`visualizer-styles.ts` 还用媒体查询把 panel 的 `max-width` 压到 360px。只改一层会被另一层打回原形。第三，assistant 宿主挂在 `.acu-vis-content` 内部，非 overlay 模式下仍受 flex 布局影响；如果要实现“占据可视化界面 80%+”，更稳妥的是把 compact 改成覆盖式宽窗口，而不是继续侧栏扩宽到某个魔法比例，否则底层编辑区和 assistant 会互相挤压。第四，现有测试明确断言了 1100px -> 340px compact 行为，这些测试现在已经变成阻止正确修复的旧护栏，必须同步调整。
confidence: HIGH
  - HIGH: 已经直接定位到导致“只占一半/太窄”的真实实现点：TS 中 compact=340px，CSS 中 max-width=360px，且阈值 899px 过低。问题根因明确，影响范围明确，修改路径也明确。
approach: 三维评估综合最优的方案是把当前 `compact` 从“窄侧栏”升级为“宽覆盖窗口”模式，而不是单纯把 340px 改成更大的固定值。具体做法：第一，在 `visualizer-template-assistant.ts` 中将 viewport 模式从 `desktop | compact | mobile-overlay` 调整为 `desktop | wide-overlay | mobile-overlay`，或至少保留类型名但把 compact 行为改成覆盖式宽窗口；使 900~1279px 区间不再走右侧侧栏，而是使用相对父界面更大的 overlay 样式，宽度至少为可视区的 80% 以上。第二，在 `visualizer-styles.ts` 中移除/改写 `@media (max-width: 1279px)` 下把 panel 压到 `360px` 的规则，改成与新的 wide-overlay 宿主 data attribute 联动，确保不会再被 CSS 二次限宽。第三，保持 899px 以下的 mobile-overlay 全屏行为与最小化恢复逻辑，避免把已修好的手机场景再弄坏。第四，同步修改 `tests/presentation/visualizer-template-assistant.test.ts`，把 1100px 场景从“340px compact 侧栏”更新为“80%+ overlay 宽窗口”断言，并验证 host data mode 与 display/width 行为。这个方案比“只把 340px 改成 600px”更像样，因为后者仍然把 assistant 留在 flex 侧栏里，本质上还是挤压布局，不是解决可用性问题。
  三维评分（每个维度 1-5 分，5 为最优）：
  - 可维护性: 5/5 — 明确把“侧栏桌面模式”和“覆盖窄屏模式”分开，模式职责清晰；比继续在 compact 上叠一堆 if/魔法宽度更可读。
  - 健壮性: 5/5 — 同时处理 TS 阈值、CSS 限宽、宿主模式联动和测试回归，避免一处修好另一处继续把宽度锁死。
  - 可扩展性: 4/5 — 未来还能继续细分为 tablet overlay / phone fullscreen 等模式，当前状态机可以自然扩展。
edge_cases:
  - 1100px 左右的中等宽度设备必须进入宽覆盖窗口而不是 340px 侧栏，否则用户当前问题继续复现。
  - 899px 以下的手机场景必须继续保持全屏覆盖与最小化/恢复能力，不能被这次阈值/模式调整误伤。
  - 桌面宽屏模式下 assistant 仍应保留右侧栏形态，不能因为扩大覆盖窗口而抢占整个编辑界面。
  - CSS 中旧的 `max-width: min(360px, 100%)` 限制必须移除或重写，否则新的 TS 宽度策略会被样式层覆盖掉。
  - 测试中原本验证 compact=340px 的断言必须同步更新，否则新行为会被旧测试当成回归错误。
affected_scope:
  - E:\xiangmu\星河璀璨数据库\shujuku1\src\presentation\pages\visualizer-template-assistant.ts
  - E:\xiangmu\星河璀璨数据库\shujuku1\src\presentation\pages\visualizer-styles.ts
  - E:\xiangmu\星河璀璨数据库\shujuku1\tests\presentation\visualizer-template-assistant.test.ts
execution_plan:
  - step_1: 修改 `src/presentation/pages/visualizer-template-assistant.ts` 的 viewport 模式与宽度策略，让 900~1279px 区间不再使用固定 340px 侧栏，而改为占据可视化区域 80% 以上的覆盖式宽窗口，同时保持 desktop 与 mobile-overlay 的语义清晰。
  - step_2: 修改 `src/presentation/pages/visualizer-styles.ts`，移除/改写把 panel 限制到 360px 的媒体查询规则，并为新的中等窄屏覆盖模式补齐宿主/面板样式，确保不会继续被父级 flex 侧栏挤压成半边。
  - step_3: 更新 `tests/presentation/visualizer-template-assistant.test.ts`，把 1100px 场景的断言从“compact=340px”改为“80%+ 宽覆盖窗口”，并补充新的 mode/data attribute 断言，继续验证 768px 手机全屏与最小化逻辑不回归。
  - step_4: 运行 `tests/presentation/visualizer-template-assistant.test.ts` 与 `npm run typecheck`，确认模式切换、宽度策略与现有状态机兼容。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES + 可维护性 5/5、健壮性 5/5、可扩展性 4/5。该方案直接消除 compact 340px 侧栏这个错误模型，并同步处理样式与测试，优于“只调一个宽度常量”的局部补丁。
  - 是否遗漏了已知边界条件？ → NO + 已覆盖中等宽度设备、手机全屏保留、桌面不回归、CSS 限宽冲突、旧测试更新五类边界条件。
  - 是否因改动量大而想缩减方案？ → NO + 用户当前问题的根因正是 TS + CSS 双重限宽；缩成单点修改只会留下半修状态。
  - 是否打算跳过某些文件？ → NO + 这次真正相关的只有 TS 模式文件、样式文件、测试文件三处，范围明确，不会凭感觉漏改。
  - execution_plan是否覆盖affected_scope所有文件？ → YES + step_1 覆盖 `visualizer-template-assistant.ts`，step_2 覆盖 `visualizer-styles.ts`，step_3 覆盖测试文件，step_4 做验证。
  - context是否充分？是否有未读但可能相关的文件？ → NO + 当前已读取决定宽度与宿主布局的核心文件，问题根因已明确，没有必要再盲目扩读其他模块。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO + `build:extension` 的 Windows 脚本问题与本轮 UI 宽度问题无关，但它已在上一轮明确说明，不属于本次功能范围。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO + 只使用标准补丁工具修改源码。
  → YES项必须就地写出修正内容，然后以修正后的方案进入执行
</analysis>
---
<output_quality_review>
task_summary: 将 AI 改表助手在 900~1279px 的中等窄屏区间从固定 340px 的 compact 侧栏改为 wide-overlay 宽覆盖窗口，使其占据可视化区域 80% 以上，同时移除样式层的 360px 限宽，并更新对应回归测试。
deliverables:
  - E:\xiangmu\星河璀璨数据库\shujuku1\src\presentation\pages\visualizer-template-assistant.ts
  - E:\xiangmu\星河璀璨数据库\shujuku1\src\presentation\pages\visualizer-styles.ts
  - E:\xiangmu\星河璀璨数据库\shujuku1\tests\presentation\visualizer-template-assistant.test.ts

# 量化指标总览
metrics:
  total_files_modified: 3 — 修改了 2 个实现文件和 1 个测试文件
  execution_plan_coverage: 4/4 = 100% — execution_plan 四个步骤全部完成
  edge_cases_handled: 5/5 = 100% — 已处理 1100px 中宽屏覆盖、899px 以下手机全屏保留、桌面模式不回归、CSS 360px 限宽冲突、旧 compact 测试更新
  confidence_assessment: HIGH — 对本次交付质量的整体置信度为 HIGH
    - HIGH: 宽度根因已经在 TS 与 CSS 两层同时修正，`tests/presentation/visualizer-template-assistant.test.ts` 通过，`npm run typecheck` 通过。仓库仍有既有的 `tsconfig.json` duplicate key warning，但不影响本次逻辑正确性

# 产物实质性检查
substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？
    → NO + `visualizer-template-assistant.ts` 的改动直接把 900~1279px 的模式从 compact 侧栏切成 wide-overlay，并把面板宽度从固定 340px 改为 82% 覆盖式窗口；删掉这些代码，用户当前问题会原样复发。`visualizer-styles.ts` 的改动直接移除了中宽度区间的 360px 限宽，并补齐 wide-overlay 宿主 pointer-events/z-index/最小化悬浮按钮规则；删掉后 TS 中的新宽度策略仍会被 CSS 限死。测试文件更新把 1100px 场景从旧的 340px 侧栏断言改成 wide-overlay 宽覆盖窗口断言；删掉后旧错误行为会重新被当成“正确”。
  - 产物是否能被其目标对象（被测代码/被重构模块/被修复的bug）的变化所"击穿"？
    → NO + 如果把 `getAssistantViewportMode_ACU()` 改回 compact，或把 `getAssistantPanelWidth_ACU()` 改回 340px，1100px 的测试会失败；如果把 CSS 中 `max-width: calc(100% - 24px)` 或 `width: max(80%, calc(100% - 24px))` 删掉，wide-overlay 宽度相关断言也会失败。手机 768px 的 mobile-overlay 测试继续通过，说明旧修复没有被这次改坏。
  - 实质性比率: 3/3 = 100%

# 覆盖完整性检查
completeness_check:
  - 是否存在被跳过的模块/函数/路径？
    → NO + 本轮真正相关的文件只有 viewport 模式逻辑、样式限宽规则和对应测试三处，均已覆盖，没有把任何已知相关路径丢下不管。
  - 产物覆盖的范围是否与 execution_plan 中 affected_scope 完全一致？
    → YES + 三个 deliverables 与 affected_scope 完全一致。
  - 核心业务逻辑是否都有直接验证（不依赖间接覆盖）？
    → YES + 核心逻辑一“1100px 中宽度进入 wide-overlay 并占据 80%+ 宽度”有直接验证，位置在 `tests/presentation/visualizer-template-assistant.test.ts` 更新后的中屏测试。核心逻辑二“768px 手机仍保持 mobile-overlay 全屏”有直接验证，位置在同文件既有窄屏测试。核心逻辑三“模式切换后最小化/恢复逻辑不回归”继续由同文件既有最小化/恢复测试直接覆盖。
  - affected_scope 覆盖率: 3/3 = 100%

# 价值密度检查
value_density_check:
  - 产物中高价值内容（验证核心逻辑/处理复杂场景）与低价值内容（验证trivial行为）的比例是多少？
    → 高价值:低价值 = 3:0，高价值占比 100%。没有用 trivial 样式碎片凑数，全部围绕当前真实问题：模式判断、覆盖宽度、旧 CSS 限宽冲突。
  - 是否存在"用数量掩盖质量"的模式——大量 trivial 产物掩盖了核心逻辑缺少验证的事实？
    → NO + 修改量不大，但每一处都正中根因，没有堆无效改动。

# 需求对齐检查
alignment_check:
  - 产物满足的是用户的字面需求还是本质需求？
    → 满足的是本质需求。用户字面上说“不要只占一半，要像 flex 一样自动调到占据数据可视化界面 80%+”；本质上是要 assistant 在中等窄屏下真正可用，而不是继续当一个固定宽度的挤压侧栏。当前实现让该区间切到 wide-overlay 覆盖窗口，宽度提升到 82%，同时保留手机全屏与桌面侧栏语义，解决的是可用性问题而不是表面数字。
  - "如果这是别人交给我的，我会接受吗？"
    → YES + 这次修的是根因，不是继续在 340px 上抹粉。TS 模式和 CSS 限宽都处理了，测试也跟上了，达到了可交付标准。
→ 任何检查项发现问题，必须就地修正，修正完成后重新检查该项。
   不允许"记录问题留待下次处理"。
</output_quality_review>
