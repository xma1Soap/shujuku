<analysis>
context: 侦察确认当前实现仍是“最近快照”模型，不符合用户要求的“跨楼层唯一逻辑数据集”模型。`src/service/vector/summary-vector-index-state-service.ts` 中 `getActiveSummaryVectorIndexSnapshot_ACU()` 从聊天末尾向上找到第一个含 `summaryVectorIndexState` 的 AI 楼层就返回，导致更早楼层的纪要向量索引完全不会参与召回。`src/service/vector/summary-vector-index-archive-service.ts` 中 `archiveSummaryVectorIndexNow_ACU()` 读取这个最近快照作为 existingState，然后把复用行和新增行合并成一个完整 state 写回最新 AI 楼层；这会把旧楼层已有 row/chunk 复制到新楼层，制造重复持久化内容。`src/service/plot/summary-vector-index-orchestrator.ts` 发送前召回也调用 `getActiveSummaryVectorIndexSnapshot_ACU()`，所以新模式使用时只读最近一份 state。普通远记忆同样有问题：`src/service/vector/remote-memory-active-snapshot-service.ts` 中 `getActiveRemoteMemorySnapshot_ACU()` 只返回最近一个含 `vectorMemoryState.remoteMemoryBatches` 的 AI 楼层；`src/service/vector/vector-index-orchestrator.ts` 的 `buildSummaryVectorIndexIfNeeded_ACU()` 读取 activeSnapshot 后把 existing batches 与 nextBatches merge，再用 `replaceVectorRemoteMemoryBatches_ACU()` 将全量 batches 写入 targetMessage；这同样会复制旧楼层批次到新楼层。`src/service/plot/vector-recall-orchestrator.ts` 召回只使用 activeSnapshot.vectorState。`src/service/vector/remote-memory-management-service.ts` 的 `getLatestRemoteMemorySnapshotView_ACU()`、`saveEditedRemoteMemoryBatch_ACU()`、`deleteRemoteMemoryBatch_ACU()`、`deleteAllArchivedVectorData_ACU()` 都围绕最近快照操作，无法定位 batch 原始 owner 楼层，也无法删除所有楼层的归档状态。UI 侧 `src/presentation/pages/popup-bindings-data.ts` 渲染数据管理列表时调用 `getLatestRemoteMemorySnapshotView_ACU()`，`src/presentation/components/status-display.ts` 和 `src/presentation/pages/popup-bindings-worldbook.ts` 也用 `getActiveSummaryVectorIndexSnapshot_ACU()` 判断新模式归档存在与统计数量。底层写入工具 `readIsolatedTagData_ACU()` / `writeIsolatedTagData_ACU()` 可直接读写指定消息隔离 tagData，保留 `independentData`、`modifiedKeys`、`updateGroupKeys`、`_acu_base_state` 是现有约定。`ChatVectorRemoteMemoryBatch_ACU` 以 `batchId` 可稳定去重；`ChatSummaryVectorIndexRow_ACU` 以 `rowKey` 稳定去重，chunk 以 `chunkId` 去重。
needs: 本次需求本质是把原生远记忆和新模式纪要向量索引都改造成跨楼层分片持久化模型：每次新归档只写当前最新 AI 楼层负责的新数据；使用时从当前聊天记录向上追溯所有 AI 楼层并聚合；聚合时以稳定 id 去重，保证逻辑上只有一份向量数据；删除或编辑时必须定位数据原 owner 楼层并写回该楼层，而不是写到最新楼层；全部删除必须遍历所有相关楼层清空对应向量状态；召回、世界书同步、状态提示、数据管理列表必须全部基于聚合视图，不能继续读最近快照。还必须保留现有表格持久化、数据隔离、旧纪要删除逻辑和新模式不删纪要表条目的行为。
key_challenges: 第一，普通远记忆归档当前会把 existing batches merge 后全量写入最新楼层，这是重复数据的主要来源，必须改为目标楼层只保存自身已有 batches + 新 batches。第二，新模式归档当前用最近 state 做完整同步覆盖，既漏掉更早楼层，也复制旧 row；必须改为聚合 rows 后判断新增、变更、消失，并按 owner 楼层写回。第三，编辑/删除普通远记忆 batch 需要 owner 索引；仅返回聚合 batches 不够，service 层必须维护 `batchId -> layer` 映射。第四，新模式“消失纪要自动删除”不能靠写一个最新全量 state 覆盖完成，必须从该 row 的 owner 楼层删除 row/chunks。第五，重复历史数据已经可能存在：聚合时必须按稳定 id 去重，优先较新楼层作为有效数据，并可在归档同步时清理较旧重复 owner，避免重复继续污染。第六，全部删除跨楼层写入多条消息，必须每层保留非向量字段，否则会破坏表格数据。第七，世界书同步普通远记忆归档完成后应使用聚合后的 batches，否则世界书只含本次新增或单层数据。第八，presentation 层已有调用必须改成聚合视图，否则 UI 会继续误报“无归档”或只显示最近批次。
confidence: MEDIUM
  - 侦察覆盖了新模式状态服务、归档服务、召回编排、普通远记忆 active snapshot、普通远记忆归档、管理服务、数据管理 UI、状态显示和相关 recall/worldbook 服务，核心调用链已经明确。
  - 仍存在 1 个未验证假设：历史楼层中重复数据的“较新优先”清理策略是否完全符合用户对“对应楼层”的直觉。选择较新优先是因为当前系统已经可能把旧数据复制到新楼层；如果继续保留旧 owner，会让最新复制层无法被清理，重复会持续存在。该策略会在分析中以聚合去重和同步清理实现，并不会改变新增数据写最新楼层、编辑删除写 owner 楼层的目标模型。
approach: 采用“聚合视图 + owner 映射 + 分层写回”的方案。新增或改造状态服务，让普通远记忆和新模式都能扫描所有 AI 楼层，读取当前隔离 key 下的状态，按稳定 id 聚合并记录 owner。召回、UI、管理列表统一读聚合视图。归档时只把新增普通 batches 写入最新 AI 楼层；新模式则将新增 row 写最新楼层、已存在但变更 row 写回 owner 楼层、消失 row 从 owner 楼层删除。删除/编辑通过 owner 映射定位原楼层。全部删除遍历所有含对应 state 的楼层逐层删除状态。历史重复数据在聚合时以较新楼层为有效 owner，并在新模式同步或普通全部删除中清理；普通单个编辑/删除也按有效 owner 操作。
  三维评分（每个维度 1-5 分，5 为最优）：
  - 可维护性: 4/5 — 聚合逻辑收敛在 state/snapshot service，调用方只替换为聚合接口；写回逻辑集中在 management/archive/orchestrator，不把 owner 定位散落到 UI。扣 1 分是因为普通远记忆和新模式两套结构不同，短期内会有相似的分层写回代码。
  - 健壮性: 4/5 — 按 stable id 去重、owner 定位写回、逐层保留非向量字段、空 state 自动删除，可以覆盖回退楼层、删除楼层、重复历史快照、缺失 tagData 等边界。扣 1 分是因为历史重复清理无法在不执行归档/删除的情况下主动迁移所有旧数据，只能在读取时去重并在相关写操作中纠正。
  - 可扩展性: 4/5 — 聚合视图可以支持未来更多向量类型；owner 映射也适合后续增加 tombstone 或压缩迁移。扣 1 分是因为当前数据模型没有显式 owner 字段，owner 由扫描结果推导，未来若要跨会话迁移需要再补元数据。
edge_cases: 当聊天记录为空或没有 AI 楼层时，归档、编辑、删除应返回明确错误，不应创建悬空状态。当某 AI 楼层 `TavernDB_ACU_IsolatedData` 不存在或当前 isolationKey 下没有 tagData 时，聚合扫描应跳过而不是抛错。当某层 state normalize 后 rows/chunks/batches 为空时，应视为无有效向量数据；写回空 state 时应删除对应字段而不是保留空壳污染 UI。普通远记忆 batchId 在多个楼层重复时，聚合视图只保留较新楼层的 batch 作为有效 owner，防止同一记忆被召回两次。新模式 rowKey 在多个楼层重复时，聚合视图只保留较新楼层的 row/chunks，且 chunks 必须与有效 row.chunkIds 对齐。新模式当前纪要表删除某行时，应从该 rowKey 的 owner 楼层删除 row 和对应 chunks；如果该 owner 楼层删除后 state 为空，应移除 `summaryVectorIndexState` 字段。新模式当前纪要表修改某行但 rowKey 不变时，应重新 embedding 并写回原 owner 楼层，不应在最新楼层创建重复 row。新模式当前纪要表新增 rowKey 时，应写到最新 AI 楼层；若最新楼层已有 summaryVectorIndexState，只追加/替换本层自身 rows，不复制其它楼层 rows。普通远记忆手动/自动归档新增 batch 时，应只把新 batch 与目标楼层自身已有 batches 合并写入 targetMessage，不应 merge 聚合全量 batches。普通远记忆编辑 batch summary 时，应 rebuild embedding 后写回该 batch owner 楼层，并刷新世界书为聚合后的全部 batches。普通远记忆删除 batch 时，应从该 batch owner 楼层删除；删除后世界书同步应使用聚合后剩余 batches。全部删除普通远记忆时，应遍历所有含 `vectorMemoryState.remoteMemoryBatches` 的楼层清空 state；全部删除新模式时，应遍历所有含 `summaryVectorIndexState` 的楼层删除字段。状态显示和模式切换提示必须使用聚合统计，否则旧楼层有数据时会误报无归档。数据隔离开启/关闭时扫描只能读取当前 isolationKey，不能跨隔离标识混合向量。保存聊天失败时不应更新世界书为未持久化的新状态。
affected_scope: src/service/vector/summary-vector-index-state-service.ts; src/service/vector/summary-vector-index-archive-service.ts; src/service/plot/summary-vector-index-orchestrator.ts; src/service/vector/remote-memory-active-snapshot-service.ts; src/service/vector/vector-index-orchestrator.ts; src/service/plot/vector-recall-orchestrator.ts; src/service/vector/remote-memory-management-service.ts; src/presentation/components/status-display.ts; src/presentation/pages/popup-bindings-worldbook.ts; src/presentation/pages/popup-bindings-data.ts; .analysis-cache.md; .analysis-archive/待生成_跨楼层向量聚合存储模型修复.md
execution_plan:
  - step_1: 修改 `src/service/vector/summary-vector-index-state-service.ts`，新增 summary 向量索引 layer/aggregated snapshot 类型，扫描所有 AI 楼层读取 `summaryVectorIndexState`，按 rowKey/chunkId 去重聚合 rows/chunks，记录 rowOwners/chunkOwners/layers，并保留旧 `getActiveSummaryVectorIndexSnapshot_ACU()` 兼容但让新调用方使用聚合接口。
  - step_2: 修改 `src/service/vector/remote-memory-active-snapshot-service.ts`，新增普通远记忆 layer/aggregated snapshot 类型，扫描所有 AI 楼层读取 `vectorMemoryState.remoteMemoryBatches`，按 batchId 去重聚合，记录 batchOwners/layers，并保留旧 active snapshot 兼容。
  - step_3: 修改 `src/service/vector/summary-vector-index-archive-service.ts`，把归档同步从“最近 state 全量覆盖最新楼层”改为“聚合判断 + owner 写回”：复用未变 row；新增 row 写最新楼层；变更 row 重新 embedding 后写回原 owner 楼层；消失 row 从 owner 楼层删除；每个被写回楼层只保存该楼层负责的 rows/chunks；最后保存聊天。
  - step_4: 修改 `src/service/vector/vector-index-orchestrator.ts`，普通远记忆新增归档不再读取 activeSnapshot 并合并聚合全量，而是只读取 targetMessage 当前层自身 vectorState，将 nextBatches 与目标层自身 batches 合并写入；删除纪要表、保存表格和世界书同步仍保持，但世界书同步改用聚合后的全部 batches。
  - step_5: 修改 `src/service/plot/summary-vector-index-orchestrator.ts` 和 `src/service/plot/vector-recall-orchestrator.ts`，发送前召回分别使用 summary 聚合 state 与 remote memory 聚合 vectorState，避免漏读早期楼层。
  - step_6: 修改 `src/service/vector/remote-memory-management-service.ts`，让列表视图返回聚合 batches；编辑/删除普通 batch 根据 batchOwners 定位对应楼层写回；全部删除遍历所有相关楼层清空普通或新模式向量状态；每次保存后普通模式世界书同步使用聚合剩余 batches。
  - step_7: 修改 `src/presentation/components/status-display.ts` 和 `src/presentation/pages/popup-bindings-worldbook.ts`，把新模式归档存在判断和 row/chunk 统计改为聚合视图。
  - step_8: 检查 `src/presentation/pages/popup-bindings-data.ts` 的数据管理列表无需直接知道 owner，但提示文案中“保存到最新楼层”应改为“保存回对应楼层”，避免 UI 误导用户。
  - step_9: 执行 `npm run build`，若构建通过，再搜索 bundle 中聚合函数、删除函数和召回引用，确认产物包含关键路径。
  - step_10: 读取 `.analysis-cache.md`，输出 `<output_quality_review>` 并归档到 `.analysis-archive/2026-04-30_HHmm_跨楼层向量聚合存储模型修复.md`。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。聚合视图 + owner 映射在可维护性 4/5、健壮性 4/5、可扩展性 4/5 上优于继续修补最近快照模型；最近快照模型无法满足跨楼层拼接和无重复。
  - 是否遗漏了已知边界条件？ → NO。已覆盖空聊天、无 AI 楼层、空 tagData、重复 batchId/rowKey、消失行、修改行、新增行、全部删除、隔离 key、世界书同步和保存失败等核心边界。
  - 是否因改动量大而想缩减方案？ → NO。普通远记忆和新模式都必须改，用户明确说“不管是原生向量化还是新模式”；只改新模式是明显偷工减料。
  - 是否打算跳过某些文件？ → NO。affected_scope 中的状态服务、归档、召回、管理、UI 提示都在 execution_plan 覆盖；未把 UI owner 细节暴露给 presentation，是因为 owner 写回应属于 service 层职责。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。每个列出的源文件都有对应 step；归档文件将在验收阶段生成。
  - context是否充分？是否有未读但可能相关的文件？ → YES。仍可能存在状态显示之外的统计调用，但 `search_files` 已确认核心 active snapshot 引用只有列出的路径；后续如构建暴露遗漏，会进入 decision_point 而不是盲改。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO。已把普通远记忆世界书同步只用单层 state、UI 提示误导、历史重复优先级都纳入方案。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO。源码修改使用标准编辑工具；shell 只用于构建和必要搜索验证。
</analysis>
---
<output_quality_review>
task_summary: 修复原生远记忆与新模式纪要向量索引的持久化模型，使其从最近快照改为跨 AI 楼层聚合读取、按稳定 id 去重、按 owner 楼层写回编辑/删除，并保证新归档只写目标楼层自身数据。
deliverables: 修改 `src/service/vector/summary-vector-index-state-service.ts`，新增纪要向量索引跨楼层聚合视图与 owner 映射；修改 `src/service/vector/summary-vector-index-archive-service.ts`，新模式归档改为聚合判断、owner 写回、新增写最新楼层、消失行从 owner 楼层删除；修改 `src/service/vector/remote-memory-active-snapshot-service.ts`，新增普通远记忆跨楼层聚合视图与 batch owner 映射；修改 `src/service/vector/vector-index-orchestrator.ts`，普通远记忆新增归档只写目标楼层自身 state，并用聚合 batches 同步世界书；修改 `src/service/plot/summary-vector-index-orchestrator.ts` 和 `src/service/plot/vector-recall-orchestrator.ts`，发送前召回改用聚合 state；修改 `src/service/vector/remote-memory-management-service.ts`，数据管理列表、编辑、删除、全部删除改为聚合视图和 owner 楼层写回；修改 `src/presentation/components/status-display.ts` 与 `src/presentation/pages/popup-bindings-worldbook.ts`，新模式归档提示改用聚合统计；修改 `src/presentation/pages/popup-bindings-data.ts`，保存提示改为保存回对应归档楼层。

# 量化指标总览
metrics:
  total_files_modified: 10 — 修改了 10 个源文件，覆盖状态聚合、归档、召回、管理 UI 和状态提示路径。
  execution_plan_coverage: 10/10 = 100% — analysis 中 step_1 到 step_10 均已执行；构建与 bundle 关键符号检查已完成。
  edge_cases_handled: 14/14 = 100% — 已处理空聊天/无归档、跨隔离读取、重复 batchId/rowKey 去重、消失纪要删除、修改纪要 owner 写回、新增纪要写最新楼层、普通归档不复制全量、编辑/删除普通 batch owner 写回、全部删除遍历所有楼层、世界书聚合同步、UI 聚合统计等边界。
  confidence_assessment: HIGH — `npm run build` 通过，Rollup 成功生成 `dist/index.bundle.js`，架构护栏 0 违规，并用 `findstr` 在 bundle 中确认聚合函数和 owner 映射关键路径已进入产物。

quality_checks:
  - 是否仍存在“只读最近快照”的召回路径？ → NO。`src/service/plot/summary-vector-index-orchestrator.ts` 使用 `getAggregatedSummaryVectorIndexSnapshot_ACU()`，`src/service/plot/vector-recall-orchestrator.ts` 使用 `getAggregatedRemoteMemorySnapshot_ACU()`。
  - 普通远记忆新增归档是否还会把聚合全量复制到最新楼层？ → NO。`src/service/vector/vector-index-orchestrator.ts` 读取 targetMessage 当前层自身 `vectorMemoryState`，只把 `nextBatches` 与该层自身 batches 合并写入；世界书同步才使用归档后的聚合 batches。
  - 新模式纪要向量索引是否还会把所有历史 rows/chunks 全量覆盖到最新楼层？ → NO。`src/service/vector/summary-vector-index-archive-service.ts` 基于聚合视图判断 row owner：未变 row 保留 owner，变更 row 写回 owner，新 row 写 target 最新楼层，消失 row 不再写回 owner。
  - 编辑/删除普通远记忆是否能保存回对应楼层？ → YES。`src/service/vector/remote-memory-management-service.ts` 通过 `batchOwners.get(batchId)` 定位 owner messageIndex，再只修改该楼层自身 batches，保存后用聚合剩余 batches 同步世界书。
  - 全部删除是否覆盖所有楼层？ → YES。新模式遍历 `snapshot.layers` 删除每层 `summaryVectorIndexState`；普通远记忆遍历 `getRemoteMemoryLayers_ACU()` 清空每层 `vectorMemoryState.remoteMemoryBatches`。
  - 是否保留非向量数据？ → YES。所有写回路径都通过 `buildBaseTagDataForArchiveWrite_ACU()` 或等价构造保留 `independentData`、`modifiedKeys`、`updateGroupKeys`、`_acu_base_state`，新模式写回还保留同层 `vectorMemoryState`。
  - UI 是否仍误导“保存到最新楼层”？ → NO。`src/presentation/pages/popup-bindings-data.ts` 成功提示已改为“远记忆总结已保存回对应归档楼层。”
  - 是否通过构建和架构检查？ → YES。`npm run build` 成功，Rollup 生成 `dist/index.bundle.js`，三层架构护栏全部 PASS，总计违规 0 条。

known_limitations:
  - 历史已经重复持久化的数据不会在纯读取时物理迁移；读取时按稳定 id 去重，后续归档/编辑/删除/全部删除会按 owner 映射逐步纠正。这个限制不影响召回唯一性，但如果要求立即物理清扫所有历史重复，需要另做一次迁移命令。
</output_quality_review>
