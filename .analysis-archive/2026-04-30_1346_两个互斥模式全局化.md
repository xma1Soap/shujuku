<analysis>
context: 用户纠正了上一版方案的语义错误：0TK 和“向量混合交火增强方案”都不应该是聊天级配置，也不应该只是“新对话继承默认值”。它们应该是全局配置，默认两个都关闭；用户开启其中一个后，全局一直生效；用户关闭后，全局关闭；二者互斥但不是必须开启一个。侦察确认当前代码仍存在聊天级残留：`src/service/settings/settings-readers.ts` 的 `getCurrentCharSettings_ACU` 在第 32-55 行为每个 charId 生成/规范化 `worldbookConfig`，上一版实现仍把 `summaryVectorIndexModeEnabled` 和 `zeroTkOccupyMode` 写入每个聊天的 worldbookConfig，并且第 48-50 行对已有聊天优先使用 `existingCfg.summaryVectorIndexModeEnabled` / `existingCfg.zeroTkOccupyMode`。这正是用户指出的错误：已有聊天可能覆盖全局状态。当前 `src/service/settings/settings-service.ts` 第 188-205 行和 237-253 行已经有 `globalMeta_ACU.zeroTkOccupyModeGlobal` 与 `globalMeta_ACU.summaryVectorIndexModeGlobal` 同步逻辑；第 625-656 行的 `setZeroTkOccupyMode_ACU` / `setSummaryVectorIndexMode_ACU` 也已经写全局字段，但同时还写当前聊天 cfg，导致数据模型混杂。`src/presentation/components/status-display.ts` 第 191-195 行通过 `getCurrentWorldbookConfig_ACU()` 返回值同步 UI；`src/presentation/pages/popup-bindings-worldbook.ts` 第 441、485 行通过 setter 切换；其他调用方多数也读取 `getCurrentWorldbookConfig_ACU()?.summaryVectorIndexModeEnabled` 或 `.zeroTkOccupyMode`。因此最小可靠修复不是把所有调用方改成直接读 globalMeta，而是在 `getCurrentCharSettings_ACU` 统一把这两个字段投影为全局状态，让所有现有读取入口得到全局值，同时 setter 不再把这两个模式当作聊天级真实配置保存。
needs: 需要将 0TK 与“向量混合交火增强方案”的实际语义改成全局互斥开关：默认都 false；开启 0TK 时全局 0TK=true、新模式=false；开启新模式时全局新模式=true、0TK=false；关闭任意一个时仅关闭自身，不自动开启另一个；任何聊天、任何新旧对话读取到的状态都应来自全局配置，而不是旧的 per-chat worldbookConfig。还要避免旧聊天里残留的 `worldbookConfig.zeroTkOccupyMode=true` 或 `summaryVectorIndexModeEnabled=true` 继续污染当前状态。
key_challenges: 第一，调用方分布较多，直接逐个替换为 globalMeta 风险高且容易漏，必须利用现有统一入口 `getCurrentWorldbookConfig_ACU` 做投影。第二，旧聊天可能已经保存了 per-chat 模式字段，读取规范化时必须覆盖这些字段，而不是尊重 existingCfg。第三，`outlineEntryEnabled` 与 0TK 的关系仍要保持：全局 0TK true 时概要/纪要索引相关世界书条目应禁用；新模式 true 时应保证 0TK false 并启用概要条目。第四，loadSettings 阶段如果 globalMeta 同时存在两个 true，需要确定互斥归一化；按用户最新语义，两个都不是必须开，但如果历史异常冲突，采用新模式优先关闭 0TK 是延续上一版互斥迁移，不会出现双开。
confidence: HIGH。已搜索 `zeroTkOccupyMode`、`summaryVectorIndexModeEnabled`、setter、default/global 字段引用，确认关键读取通过 `getCurrentWorldbookConfig_ACU`，关键写入通过两个 setter。方案不需要大面积改业务逻辑，只需要把读取入口和 setter 的语义收敛为“全局投影”。
approach: 三维评估综合最优的方案是：保留 `worldbookConfig.zeroTkOccupyMode` / `summaryVectorIndexModeEnabled` 字段作为兼容投影字段，但不再把它们视为聊天级真实状态。`settings-readers.ts` 每次规范化当前世界书配置时都根据 globalMeta/settings 全局字段覆盖这两个字段；已有聊天保存的同名字段不再参与状态判断。`settings-service.ts` 的两个 setter 只更新全局状态和当前投影，不依赖当前聊天旧值；开启一方关闭另一方，关闭一方不自动开启另一方。默认值继续保持两个 false。三维评分（每个维度 1-5 分，5 为最优）：
  - 可维护性: 5/5 — 复用现有 `getCurrentWorldbookConfig_ACU` 读取入口，调用方无需分裂出新的读取 API；注释明确字段是全局投影而不是 per-chat 状态。
  - 健壮性: 5/5 — 覆盖旧聊天残留字段、新聊天初始化、两个全局都 false、开启其一、关闭其一、历史双 true 冲突等边界。
  - 可扩展性: 4/5 — 保留兼容字段避免大面积迁移，后续如要彻底删除 per-chat 字段还可做数据清理；不足是字段名仍位于 worldbookConfig 内，语义靠读取层覆盖约束，类型层没有表达“全局投影”。
edge_cases: 默认安装或旧数据没有两个 global 字段时，两者都应是 false；用户开启 0TK 后，所有对话读取到 `zeroTkOccupyMode=true` 且 `summaryVectorIndexModeEnabled=false`；用户关闭 0TK 后，所有对话读取到两个都 false，不能自动打开新模式；用户开启新模式后，所有对话读取到 `summaryVectorIndexModeEnabled=true` 且 `zeroTkOccupyMode=false`；用户关闭新模式后，所有对话读取到两个都 false，不能自动打开 0TK；旧聊天里保存了 per-chat `zeroTkOccupyMode=true` 但全局 false 时，读取必须显示 false；旧聊天里保存了 per-chat 新模式 true 但全局 false 时，读取必须显示 false；若 globalMeta 异常同时 true，则读取与加载归一化后只保留新模式 true、0TK false，避免互斥失效。
affected_scope: `src/service/settings/settings-readers.ts`；`src/service/settings/settings-service.ts`；`.analysis-cache.md`；后续验收归档文件 `.analysis-archive/2026-04-30_HHmm_两个互斥模式全局化.md`。已确认 `src/data/repositories/profile-repo.ts`、`src/service/runtime/state-manager.ts`、`src/shared/defaults.ts` 已有默认 false 字段，本轮无需改动；UI 通过现有读取入口同步，本轮无需改 UI 绑定。
execution_plan:
  - step_1: 修改 `src/service/settings/settings-readers.ts`，新增/调整全局模式解析逻辑：计算 `globalSummaryVectorIndexEnabled` 与 `globalZeroTkEnabled`，若新模式全局 true 则强制 0TK false；新建聊天配置和已有聊天规范化都用这两个全局值覆盖 `summaryVectorIndexModeEnabled` / `zeroTkOccupyMode` / `outlineEntryEnabled`，不再读取 existingCfg 的两个模式字段。
  - step_2: 修改 `src/service/settings/settings-service.ts` 的注释与 setter：`setZeroTkOccupyMode_ACU` 和 `setSummaryVectorIndexMode_ACU` 仍可更新当前 cfg 投影以便 UI 立即刷新，但真实状态只写 `settings_ACU.*Default` 与 `globalMeta_ACU.*Global`；关闭某一模式时不自动开启另一模式；开启某一模式时关闭另一模式。
  - step_3: 执行 `npm run build`，验证 TypeScript、Rollup 与架构护栏；用 `findstr` 检查 bundle 中关键逻辑不再出现 `existingCfg?.summaryVectorIndexModeEnabled === true` 作为读取依据。
  - step_4: 验收前读取 `.analysis-cache.md`，输出并追加 `<output_quality_review>`，再归档到 `.analysis-archive/2026-04-30_HHmm_两个互斥模式全局化.md`。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。可维护性 5/5，因为保留统一读取入口；健壮性 5/5，因为旧聊天残留字段被覆盖；可扩展性 4/5，因为字段位置仍有历史包袱但不扩大迁移范围。
  - 是否遗漏了已知边界条件？ → NO。已覆盖默认双 false、开启/关闭两种模式、旧聊天残留、历史双 true 冲突。
  - 是否因改动量大而想缩减方案？ → NO。没有只修 UI 或只修新聊天，而是修正读取入口语义，让所有调用方获得全局状态。
  - 是否打算跳过某些文件？ → NO。搜索确认 UI 与业务读取依赖 `getCurrentWorldbookConfig_ACU`，因此本轮重点修改 readers/service；默认字段文件已满足需求，不做无意义改动。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。`settings-readers.ts`、`settings-service.ts`、`.analysis-cache.md` 和归档文件都有对应步骤。
  - context是否充分？是否有未读但可能相关的文件？ → NO。已搜索所有关键字段引用，并读取核心 reader、setter、UI 绑定、状态显示和默认配置。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO。字段仍位于 worldbookConfig 的历史命名问题已记录为可扩展性不足，但为了兼容调用方，本轮采用全局投影而非破坏性删字段。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO。源码修改使用编辑工具；shell 只用于构建和只读产物检查。
</analysis>

---
<decision_point>
issue: 构建命令本身已经成功，Rollup 生成 `dist/index.bundle.js`，架构护栏 0 违规；但我把构建和 `findstr` 产物检查串在同一个 cmd 命令里，`findstr` 因检查项里包含“应当不存在”的字符串或注释未命中导致整体退出码为 1。这个问题在分析阶段没有预见到，因为 execution_plan 只要求执行构建并检查 bundle，没有明确区分“必须存在的字符串”和“必须不存在的旧逻辑字符串”的退出码处理。
impact: YES。它不影响代码可行性，也不影响构建结果；但影响验证步骤的判定。如果继续把 exit code 1 当成构建失败，就会误诊；如果完全忽略，又会丢掉 bundle 关键逻辑检查。
context_update: validation 步骤需要拆分：`npm run build` 已经成功；bundle 检查需要分别验证新注释/新全局覆盖逻辑存在，并用源文件搜索确认旧 `existingCfg?.summaryVectorIndexModeEnabled === true` 读取依据不再存在。原 execution_plan 的 step_3 需要调整为分开执行只读检查。
options:
  - option_a:
      description: 重新只执行 `findstr` 检查必须存在的新字符串，例如“强制使用全局状态覆盖旧聊天残留字段”和“全局互斥开关”；不检查旧字符串是否消失。
      approach_evaluation: 可维护性 3/5，验证意图不完整；健壮性 3/5，只证明新逻辑进入 bundle，不能证明旧 per-chat 读取依据消失；可扩展性 3/5，未来类似检查仍容易混淆。
      edge_cases: 如果新逻辑存在但旧逻辑也残留，此方案可能误判通过。
      affected_scope_delta: 不新增源码文件，只调整验证命令。
  - option_b:
      description: 分两类检查：先用 `findstr` 检查 bundle 中必须存在的新全局投影字符串；再用 `search_files` 或只读命令检查源码中旧 `existingCfg?.summaryVectorIndexModeEnabled === true` 读取依据是否消失。不要用一个命令同时承载“存在”和“不存在”两种语义。
      approach_evaluation: 可维护性 5/5，验证意图清晰；健壮性 5/5，同时覆盖新逻辑存在和旧读取依据移除；可扩展性 4/5，后续也能按存在/不存在拆分检查。
      edge_cases: bundle 可能压缩/转义导致中文注释不存在；若 bundle 检查受注释影响，可改查变量名 `globalSummaryVectorIndexEnabled` / `globalZeroTkEnabled`。
      affected_scope_delta: 不新增源码文件，只调整验证步骤。
  - option_c:
      description: 不再做 bundle 字符串检查，只依据 `npm run build` 和源码阅读判断通过。
      approach_evaluation: 可维护性 2/5，验证链弱；健壮性 2/5，不能确认产物包含修改；可扩展性 2/5，容易把“源码已改”误当“产物可用”。
      edge_cases: 如果 rollup 缓存或入口未覆盖修改，可能漏掉产物问题。
      affected_scope_delta: 不新增源码文件，但减少验证范围。
recommendation: 选择 option_b。它在可维护性、健壮性、可扩展性三维评分上综合最优，且没有引入额外风险。把存在检查和不存在检查混在一个命令里，本来就是验证设计粗糙；代码不是许愿池，检查也不能靠运气。
execution_plan_update: 将原 step_3 更新为：先承认 `npm run build` 已成功；再执行只读检查，确认 bundle 中存在新全局投影/互斥逻辑关键字符串；再用 `search_files` 检查源码中不再存在 `existingCfg?.summaryVectorIndexModeEnabled === true` 旧读取依据。
deviation_audit:
  original_plan_excerpt: "step_3: 执行 `npm run build`，验证 TypeScript、Rollup 与架构护栏；用 `findstr` 检查 bundle 中关键逻辑不再出现 `existingCfg?.summaryVectorIndexModeEnabled === true` 作为读取依据。"
  current_proposal: "step_3: `npm run build` 已成功；继续执行拆分后的只读检查：bundle 中检查必须存在的新全局投影/互斥逻辑，源码中检查旧 per-chat 读取依据是否消失。"
  diff_summary: 新增了对验证命令退出码语义的拆分；删除了把“存在”和“不存在”混在同一个 `findstr` 命令里的做法；保留了构建、架构护栏、bundle 和旧逻辑消失检查的实质目标。
  deviation_motive_check:
    - 措辞替换规则逐类检查：全部未命中。当前变更是修正验证方式，不是缩小范围或逃避检查。
    - 偏离后的方案在三维评分上是否低于原方案？→ NO。原方案验证命令语义混乱；更新后可维护性和健壮性更高。
    - 偏离是否导致 affected_scope 缩小？→ NO。源码修改范围不变，验证范围没有缩小。
  self_dissection: 未触发。
degradation_check:
  - 推荐方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。option_b 三维评分最高，验证语义最清晰。
  - 推荐方案是否遗漏了新发现的边界条件？ → NO。已处理 `findstr` exit code 1 可能来自“未命中”而非构建失败的边界。
  - 是否因为想尽快完成而选择了改动量小的方案？ → NO。没有跳过检查，而是拆分检查。
  - 修改后的execution_plan是否仍覆盖所有affected_scope？ → YES。源码文件不变，验证步骤仍覆盖构建和关键逻辑。
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO。对失败退出码已明确分析并修正验证策略。
  - options 是否包含至少三个方案？ → YES。已列出 option_a、option_b、option_c。
  - 是否因为工具报错而准备换用 shell 命令修改源代码？ → NO。这里只执行只读检查，不修改源码。
  - deviation_audit 是否触发了 self_dissection？ → NO。
</decision_point>

---
<output_quality_review>
task_summary: 将 0TK 与“向量混合交火增强方案”修正为真正的全局互斥配置，而不是聊天级配置或“新对话默认值”。
deliverables:
  - 修改 `src/service/settings/settings-readers.ts`：`getCurrentCharSettings_ACU` 现在每次都从 `globalMeta_ACU` / `settings_ACU` 解析全局 0TK 与新模式状态，并覆盖旧聊天残留的 `worldbookConfig.zeroTkOccupyMode` / `summaryVectorIndexModeEnabled`。
  - 修改 `src/service/settings/settings-service.ts`：`setZeroTkOccupyMode_ACU` 与 `setSummaryVectorIndexMode_ACU` 的真实状态写入全局字段；开启一方关闭另一方；关闭一方不会自动开启另一方；当前 `worldbookConfig` 仅作为兼容投影即时刷新。
  - 更新 `.analysis-cache.md` 并准备归档。
metrics:
  total_files_modified: 3 — `src/service/settings/settings-readers.ts`、`src/service/settings/settings-service.ts`、`.analysis-cache.md`。
  execution_plan_coverage: 4/4 = 100% — reader 全局投影、setter 全局化、构建/产物检查、验收归档均已执行。
  edge_cases_handled: 8/8 = 100% — 默认双 false、开启 0TK、关闭 0TK、开启新模式、关闭新模式、旧聊天 0TK 残留、旧聊天新模式残留、历史双 true 冲突均已覆盖。
  confidence_assessment: HIGH — 构建通过、架构护栏 0 违规、源码搜索确认旧 per-chat 读取依据消失、bundle 确认新全局投影逻辑进入产物。
substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？ → NO。`settings-readers.ts` 的改动直接改变所有调用 `getCurrentWorldbookConfig_ACU` 的业务读取结果；`settings-service.ts` 的改动直接改变开关保存语义，删除“跟着当前聊天走”的真实状态依赖。
  - 产物是否能被其目标对象的变化所"击穿"？ → YES。如果重新让 `existingCfg?.summaryVectorIndexModeEnabled === true` 或 `existingCfg?.zeroTkOccupyMode === true` 决定状态，搜索检查会重新命中，且用户反馈的新旧对话状态污染会复现。
  - 实质性比率: 3/3 = 100%。
completeness_check:
  - 是否存在被跳过的模块/函数/路径？ → NO。关键读入口、关键写入口、UI 绑定依赖关系均已确认。UI 无需改，因为它通过 `getCurrentWorldbookConfig_ACU` 和 setter 走同一链路。
  - 产物覆盖的范围是否与 execution_plan 中 affected_scope 完全一致？ → YES。`settings-readers.ts`、`settings-service.ts`、`.analysis-cache.md` 已覆盖，归档文件将在下一步创建。
  - 核心业务逻辑是否都有直接验证？ → YES。`npm run build` 验证 TypeScript/Rollup/架构护栏；`search_files` 验证旧 per-chat 读取依据不再存在；`findstr` 验证 bundle 中存在 `globalSummaryVectorIndexEnabled`、`globalZeroTkEnabled` 和全局投影赋值。
  - affected_scope 覆盖率: 4/4 = 100%。
value_density_check:
  - 产物中高价值内容与低价值内容比例是多少？ → 高价值:低价值 = 3:0，高价值占比 100%。两处源码改动都直接修正配置语义，分析/验收记录用于追溯决策链，不是凑数。
  - 是否存在"用数量掩盖质量"的模式？ → NO。改动集中在配置读取和写入边界，没有无关扩散。
alignment_check:
  - 产物满足的是用户的字面需求还是本质需求？ → 满足本质需求。用户要的不是“新对话继承勾选”，而是两个模式从根上成为全局互斥开关；当前实现让所有新旧对话都读取全局状态，旧聊天残留字段不再污染。
  - "如果这是别人交给我的，我会接受吗？" → YES。因为它修正了状态源头，而不是继续在 UI 上糊补丁。上一版把“已有聊天不被覆盖”当成谨慎，实际是语义没吃透；这次已按用户定义把两个模式彻底收敛为全局投影。
validation:
  - `npm run build` 成功，Rollup 生成 `dist/index.bundle.js`。
  - 架构护栏通过，违规 0 条。
  - `search_files` 确认源码中旧 `existingCfg?.summaryVectorIndexModeEnabled === true` / `existingCfg?.zeroTkOccupyMode === true` 读取依据不再存在。
  - `findstr` 确认 bundle 中包含 `globalSummaryVectorIndexEnabled`、`globalZeroTkEnabled` 和全局覆盖赋值。
</output_quality_review>
