<analysis>
context:
  1. 用户要求把数据库的版本号/产品名从“星数据库 III”改为“SP·数据库 I”。当前代码里用户可见的核心命中位置主要有：
     - `src/presentation/pages/main-popup.ts` 第115行：主弹窗标题 `title: '星·数据库 III'`
     - `src/presentation/bootstrap/startup.ts` 第43行：扩展菜单入口文案 `<span>星·数据库 III</span>`
     - `src/shared/runtime-env.ts` 第99行：实例冲突警告日志前缀 `[星·数据库 III]`
     - `src/index.ts` 第92行：油猴脚本实例冲突警告日志前缀 `[星·数据库 III]`
     - `manifest.json` 第2行：`display_name: "星·数据库 III"`
     - `manifest.plus-assistantembedded.json` 第2行：`display_name: "星·数据库 III + AI 改表助手"`
     - `package.json` 第6行：description 中仍写 `星·数据库 III (AutoCardUpdater)`
  2. `index.js` 中也有同样字符串，但那是构建产物，不应直接手改；修改源码后重新打包即可同步。
  3. 还有很多 specs / plans / analysis-archive / backups 中也有旧名字，但这些多数是历史文档、归档记录或备份，不属于运行时产品显示。用户说的是“数据库的版本号”，本质上是当前产品对外展示名，不是要篡改历史归档。
  4. `manifest.plus-assistantembedded.json` 的 `display_name` 目前是“星·数据库 III + AI 改表助手”。如果主产品名改为“SP·数据库 I”，这个文件应同步变成“SP·数据库 I + AI 改表助手”，否则两个安装形态名字会分裂。
  5. 当前项目 package 名 `acu-star-database-iii` 也带旧名，但它属于 npm package 标识，不一定需要立即改。直接改 package name 可能影响构建缓存、依赖引用或发布流程。用户当前请求聚焦“版本号/产品显示名”，不宜顺手扩大到包名重命名。
  6. 当前运行时 UI 与日志前缀是用户最容易感知的区域，因此需要优先同步：菜单名、弹窗标题、manifest 显示名、日志前缀、package 描述。

needs:
  1. 统一所有运行时与安装元数据中的产品显示名为“SP·数据库 I”。
  2. 保持双形态（普通插件 / plus-assistantembedded）显示名一致，不出现一半旧名一半新名。
  3. 不修改历史归档、spec 文档、backup 等只读历史记录，避免污染审计信息。
  4. 修改后重新构建，确保 `index.js` / `dist/index.bundle.js` 同步到新名称。

key_challenges:
  1. 需要区分“当前运行时显示名”和“历史文档/归档中的历史名称”，不能无差别全局替换。
  2. 需要确认 plus manifest 的附加文案也同步更新，而不是只改主 manifest。
  3. 不能直接改构建产物，应改源码与 manifest 后重打包。
  4. 需要避免顺手改 package name 这种超出用户当前目标的高影响操作。

confidence: HIGH
  - 命中位置明确，运行时与元数据路径清晰
  - 影响范围小，改名逻辑直接，可通过构建验证

approach:
  采用“只修改当前运行时与安装元数据中的显示名，不动历史记录和包标识”的方案。
  1. 修改源码中的用户可见名称与日志前缀：
     - `main-popup.ts`
     - `startup.ts`
     - `runtime-env.ts`
     - `index.ts`
  2. 修改安装元数据中的显示名与描述：
     - `manifest.json`
     - `manifest.plus-assistantembedded.json`
     - `package.json` description
  3. 不修改：
     - `index.js`（构建产物，交由构建生成）
     - `backups/*`
     - `.analysis-archive/*`
     - `plans/*`
     - `specs/*`
     因为这些不是当前运行时真源，且许多属于历史记录。
  
  三维评分：
  - 可维护性: 5/5 — 修改集中在真实源头，避免污染备份和历史文档
  - 健壮性: 5/5 — 构建产物由源码重新生成，不存在手改产物导致不同步
  - 可扩展性: 4/5 — 若未来还要做更彻底品牌重命名，可在此基础上继续扩展到 specs/docs/package name

edge_cases:
  - plus manifest 追加后缀“+ AI 改表助手”必须保留，只替换主产品名部分
  - 日志前缀若不改，运行中仍会输出旧名，用户会误以为改名没生效
  - `index.js` 构建产物当前仍含旧名，必须通过重新打包同步；否则源码改了但插件运行文件没改
  - package `name` 不在本次修改范围；若误改，可能引入额外发布/工具链影响

affected_scope:
  - src/presentation/pages/main-popup.ts
  - src/presentation/bootstrap/startup.ts
  - src/shared/runtime-env.ts
  - src/index.ts
  - manifest.json
  - manifest.plus-assistantembedded.json
  - package.json

execution_plan:
  - step_1: 修改 `src/presentation/pages/main-popup.ts` 与 `src/presentation/bootstrap/startup.ts`，将主弹窗标题和扩展菜单入口文本从“星·数据库 III”改为“SP·数据库 I”。
  - step_2: 修改 `src/shared/runtime-env.ts` 与 `src/index.ts`，将实例冲突警告日志前缀从“[星·数据库 III]”改为“[SP·数据库 I]”。
  - step_3: 修改 `manifest.json`、`manifest.plus-assistantembedded.json`、`package.json`，同步更新安装显示名与 description 文案。
  - step_4: 运行 TypeScript 检查或构建，确认改名未引入错误，并让构建产物同步新名称。
  - step_5: 进入验收与归档。

degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。它覆盖了所有当前真实源头，同时避免污染历史文档和备份。
  - 是否遗漏了已知边界条件？ → NO。plus manifest 后缀保留、日志前缀同步、构建产物重生成都已纳入。
  - 是否因改动量大而想缩减方案？ → NO。命中点不多，全部都属于真实显示源头。
  - 是否打算跳过某些文件？ → YES。跳过 `plans/specs/backups/.analysis-archive/index.js` 等历史或产物文件，因为它们不是当前源码真源；产物将通过构建同步，历史文件不应篡改。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。列出的真实源文件均已覆盖。
  - context是否充分？是否有未读但可能相关的文件？ → NO。用于当前运行时和安装显示的关键文件已全部定位。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO。被跳过的是历史记录/备份，不是无关紧要问题，而是刻意不篡改历史。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO。所有源码修改都通过标准编辑工具完成。
</analysis>
---
<output_quality_review>
task_summary: 将当前运行时与安装元数据中的产品显示名从“星·数据库 III”统一改为“SP·数据库 I”，并通过重新构建让产物同步新名称。
deliverables:
  - src/presentation/pages/main-popup.ts
  - src/presentation/bootstrap/startup.ts
  - src/shared/runtime-env.ts
  - src/index.ts
  - manifest.json
  - manifest.plus-assistantembedded.json
  - package.json

# 量化指标总览
metrics:
  total_files_modified: 7 — 修改/新增的文件总数
  execution_plan_coverage: 5/5 = 100% — execution_plan 执行覆盖率
  edge_cases_handled: 4/4 = 100% — 边界条件处理覆盖率
  confidence_assessment: HIGH — 改名源头已全部覆盖，构建成功生成 bundle；尾部仅有既有 CRLF/bash 脚本问题

# 产物实质性检查
substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？
    → NO + 逐一检查：
      1. `main-popup.ts` 直接决定主弹窗标题，删除改动会立即恢复旧名。
      2. `startup.ts` 直接决定扩展菜单入口文案，删除改动会立即恢复旧名。
      3. `runtime-env.ts` 与 `index.ts` 直接决定实例冲突日志前缀，删除改动会继续输出旧名。
      4. `manifest.json` 与 `manifest.plus-assistantembedded.json` 直接决定插件安装显示名，删除改动会继续显示旧名。
      5. `package.json` description 更新了项目元数据描述，避免构建/项目说明继续保留旧名。
  - 产物是否能被其目标对象（被测代码/被重构模块/被修复的bug）的变化所"击穿"？
    → YES + 如果把任一用户可见源头改回旧名字，运行时 UI、manifest 或日志中都会立即重新出现“星·数据库 III”，直接暴露改名不完整。
  - 实质性比率: 7/7 = 100%

# 覆盖完整性检查
completeness_check:
  - 是否存在被跳过的模块/函数/路径？
    → YES + 被跳过项：
      1. `index.js` / `dist/index.bundle.js` 源码层未直接编辑——理由：它们是构建产物，应由重构生成而非手改。这个理由是技术上不可绕过的，不是回避困难。
      2. `plans/*`、`specs/*`、`backups/*`、`.analysis-archive/*`——理由：这些是历史文档、备份或归档记录，不属于当前运行时真实源头。修改它们会污染历史，不是正确交付路径。
  - 产物覆盖的范围是否与 execution_plan 中 affected_scope 完全一致？
    → YES。execution_plan 中列出的 7 个真实源文件全部完成修改。
  - 核心业务逻辑是否都有直接验证（不依赖间接覆盖）？
    → YES。核心逻辑与直接验证位置：
      1. 主弹窗标题：`src/presentation/pages/main-popup.ts`
      2. 菜单入口显示名：`src/presentation/bootstrap/startup.ts`
      3. 实例冲突日志前缀：`src/shared/runtime-env.ts`、`src/index.ts`
      4. 插件显示名：`manifest.json`、`manifest.plus-assistantembedded.json`
      5. 元数据描述：`package.json`
      6. 构建验证：`npm run build` 成功生成 `dist/index.bundle.js`
  - affected_scope 覆盖率: 7/7 = 100%

# 价值密度检查
value_density_check:
  - 产物中高价值内容（验证核心逻辑/处理复杂场景）与低价值内容（验证trivial行为）的比例是多少？
    → 高价值:低价值 = 7:0，高价值占比 100%。所有改动都直接命中真实显示源头，没有无意义铺垫。
  - 是否存在"用数量掩盖质量"的模式——大量 trivial 产物掩盖了核心逻辑缺少验证的事实？
    → NO。文件虽多，但每个文件都是改名链路中的真实源头。

# 需求对齐检查
alignment_check:
  - 产物满足的是用户的字面需求还是本质需求？
    → 满足本质需求。用户要的不是某个局部标题改名，而是产品当前对外展示名统一切换为“SP·数据库 I”。本次修改覆盖了 UI、安装 manifest、日志前缀与项目元数据，并通过构建同步到产物。
  - "如果这是别人交给我的，我会接受吗？"
    → YES。因为这次改的是所有真实运行时源头，而不是只改一个弹窗标题装作完成。
</output_quality_review>
