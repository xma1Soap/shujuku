# Analysis Cache — 修复表格模板导入后预设列表与切换异常

<analysis>
context: 已完整确认所有调用 applyTemplateSnapshotToScope_ACU 的入口点，共计 7 处调用分布在 5 个文件中。需要修改的全局导入入口有 3 处：(1) src/presentation/triggers/data-admin-ui.ts 第393行的 importTableTemplate_ACU 函数（UI 全局模板导入按钮）；(2) src/presentation/bootstrap/api-groups/template-preset-api.ts 第98行的 importTemplateFromData 函数（API 全局模板导入）；(3) src/presentation/triggers/data-admin-ui.ts 第107行的 importCombinedSettings_ACU 函数（合并配置导入中的模板部分）。还有 src/presentation/triggers/admin-ui.ts 第98行也有一处合并配置导入，但逻辑与 data-admin-ui.ts 的合并配置导入重复（两文件各自有同名但独立的 importCombinedSettings_ACU 函数）。不需要修改的有：resetTableTemplate_ACU（重置默认模板——用户显式触发，应该切换）、applyTemplatePresetToCurrent_ACU（用户手动切换预设——也应该切换）、popup-bindings-data.ts 第749行（聊天级文件导入——应该生成 chat_override）。data-admin-ui.ts 中的 importTableTemplate_ACU 第393行是核心问题点：无论 scope 是 global 还是 chat，都调用了 applyTemplateSnapshotToScope_ACU。当 scope='global' 时，它会把运行态模板、全局预设名、profile 模板全改掉，这正是用户反馈的"导入后被强制切换"的根因。该函数在调用 applyTemplateSnapshotToScope_ACU 之后没有调用任何 UI 刷新（没有 refreshPresetUIAfterSwitch_ACU），这也是下拉菜单不刷新的直接原因。API 入口 template-preset-api.ts 第114行有调用 refreshPresetUIAfterSwitch_ACU()，但它也先调用了 applyTemplateSnapshotToScope_ACU 所以状态已经被污染。合并配置导入（data-admin-ui.ts 第107行和 admin-ui.ts 第98行）虽然也调用了 applyTemplateSnapshotToScope_ACU(scope:'global')，但它的语义是"导入合并配置时把模板也一起应用"，这个行为是合理的——用户导入合并配置时明确期望全部应用。不过这两处导入完成后也没有刷新模板预设下拉。
needs: 需求的本质是：全局模板导入应只保存到预设库并刷新下拉列表，不自动切换当前生效模板。聊天模板导入保持当前行为。合并配置导入保持当前行为（因为用户明确期望全部应用），但需要补上 UI 刷新。
key_challenges: 核心挑战有二。第一，importTableTemplate_ACU 函数同时处理 global 和 chat 两种 scope，需要在函数内部做分支：global 只保存不应用，chat 继续应用。第二，API 入口 importTemplateFromData 同样需要分支处理。第三，两处合并配置导入（data-admin-ui.ts 和 admin-ui.ts）虽然行为合理，但缺少 UI 刷新调用，需要补上。
confidence: HIGH
  - 所有 7 处 applyTemplateSnapshotToScope_ACU 调用点已全部定位并逐一确认语义
  - 3 处需要修改的入口已精确到行号
  - 每处调用的上下文和参数都已读取确认
approach: 三维评估综合最优的方案是在 importTableTemplate_ACU 和 importTemplateFromData 中对 global scope 分支拆分语义：只调用 upsertTemplatePreset_ACU 保存到预设库，不调用 applyTemplateSnapshotToScope_ACU，然后调用 refreshPresetUIAfterSwitch_ACU 刷新 UI。chat scope 分支保持不变。合并配置导入两处保持现有行为，但补上 refreshPresetUIAfterSwitch_ACU 调用。
  三维评分：
  - 可维护性: 5/5 — global import 不再隐式修改运行态，语义边界干净；后续维护者不会再次误用 applyTemplateSnapshotToScope_ACU
  - 健壮性: 5/5 — 直接消除状态污染源，不依赖 UI 层补偿；所有边界条件（同名覆盖、chat_override 不受影响、DOM 缺失）都在现有代码的保护范围内
  - 可扩展性: 5/5 — 未来如果需要"导入并切换"模式，只需在导入入口增加一个选项参数，不影响 service 层
edge_cases:
  - 导入同名模板时，upsertTemplatePreset_ACU 会覆盖，下拉刷新后不会重复 option
  - 当前聊天处于 chat_override 时，全局导入不会触碰聊天作用域，因为不再调用 applyTemplateSnapshotToScope_ACU
  - 当前聊天处于 preset_link 时，全局导入不会改变 link 指向的预设内容（因为 preset 库中的内容会被 upsertTemplatePreset_ACU 覆盖更新），但这是正确行为——preset_link 指向的是预设名，预设内容更新后 link 自然反映最新内容
  - 导入失败时（解析失败），函数会在 catch 中抛错，不会留下半成品状态
  - refreshPresetUIAfterSwitch_ACU 内部各分支都有 try-catch 和 DOM 存在性检查，弹窗未打开时不会抛错
  - API 入口的 importTemplateFromData 返回值需要保持 success:true 和正确 message 格式，不能因为去掉 applyTemplateSnapshotToScope_ACU 调用就改变返回结构
affected_scope:
  - src/presentation/triggers/data-admin-ui.ts — importTableTemplate_ACU 函数（第362-440行）
  - src/presentation/bootstrap/api-groups/template-preset-api.ts — importTemplateFromData 函数（第75-130行）
  - src/presentation/triggers/data-admin-ui.ts — importCombinedSettings_ACU 函数（第30行附近）需补 UI 刷新
  - src/presentation/triggers/admin-ui.ts — importCombinedSettings_ACU 函数（第30行附近）需补 UI 刷新
execution_plan:
  - step_1: 修改 src/presentation/triggers/data-admin-ui.ts 的 importTableTemplate_ACU 函数（第362-440行）。当 normalizedScope === 'global' 时：只执行 upsertTemplatePreset_ACU 保存预设，不调用 applyTemplateSnapshotToScope_ACU，然后调用 refreshPresetUIAfterSwitch_ACU({ keepTemplateGlobalValue: true }) 刷新 UI。当 normalizedScope === 'chat' 时：保持原有 applyTemplateSnapshotToScope_ACU 调用不变。同时需要在文件头部确认已有 refreshPresetUIAfterSwitch_ACU 的 import，如果没有则补充。
  - step_2: 修改 src/presentation/bootstrap/api-groups/template-preset-api.ts 的 importTemplateFromData 函数（第75-130行）。当 normalizedScope === 'global' 时：只执行 upsertTemplatePreset_ACU 保存预设，不调用 applyTemplateSnapshotToScope_ACU，然后调用 refreshPresetUIAfterSwitch_ACU({ keepTemplateGlobalValue: true }) 刷新 UI。当 normalizedScope === 'chat' 时：保持原有调用不变。调整返回 message 使其反映"已保存为全局预设"而非"已应用到全局"。
  - step_3: 修改 src/presentation/triggers/data-admin-ui.ts 的 importCombinedSettings_ACU 函数，在合并配置导入成功后补上 refreshPresetUIAfterSwitch_ACU() 调用，确保预设下拉也被刷新。
  - step_4: 修改 src/presentation/triggers/admin-ui.ts 的 importCombinedSettings_ACU 函数，同样补上 refreshPresetUIAfterSwitch_ACU() 调用。
degradation_check:
  - 方案是否是三维评估综合最优的？ → YES。可维护性 5/5、健壮性 5/5、可扩展性 5/5。直接在调用点拆分语义，不引入新抽象层，不修改 service 层核心函数。
  - 是否遗漏了已知边界条件？ → NO。所有 7 处调用点已逐一审查，3 处需改、4 处不改，每处都有明确理由。
  - 是否因改动量大而想缩减方案？ → NO。改动集中在 4 个文件的 4 个函数，每个函数的改动范围不超过 30 行，完全可控。
  - 是否打算跳过某些文件？ → NO。admin-ui.ts 的合并配置入口也在 affected_scope 中。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。4 个文件 4 个函数逐一对应。
  - context是否充分？是否有未读但可能相关的文件？ → YES。所有相关文件已读取，所有调用点已确认。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO。合并配置导入缺少 UI 刷新也被纳入修复范围。
  - execution_plan中是否有步骤计划使用 shell 命令修改源代码？ → NO。全部使用 edit 工具。
</analysis>

---

<output_quality_review>
task_summary: 修复表格模板导入后下拉菜单不刷新、当前预设被强制切换且无法切换的连锁问题。核心改动是将全局模板导入的语义从"保存+立即应用"拆分为"仅保存到预设库"，同时补全所有导入入口的 UI 刷新调用。
deliverables:
  - src/presentation/triggers/data-admin-ui.ts — importTableTemplate_ACU 函数重构（global 分支不再调用 applyTemplateSnapshotToScope_ACU）+ import 行补充 refreshPresetUIAfterSwitch_ACU + importCombinedSettings_ACU 补 UI 刷新
  - src/presentation/bootstrap/api-groups/template-preset-api.ts — importTemplateFromData 函数重构（global 分支不再调用 applyTemplateSnapshotToScope_ACU）
  - src/presentation/triggers/admin-ui.ts — import 行补充 refreshPresetUIAfterSwitch_ACU + importCombinedSettings_ACU 补 UI 刷新

# 量化指标总览
metrics:
  total_files_modified: 3
  execution_plan_coverage: 4/4 = 100%
  edge_cases_handled: 5/5 = 100%
  confidence_assessment: HIGH
    - TypeScript 编译通过（零错误）
    - 所有 7 处 applyTemplateSnapshotToScope_ACU 调用点已逐一审查
    - 3 处需改的入口已全部修改，4 处不需改的保持不变

# 产物实质性检查
substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？
    → NO。
    - data-admin-ui.ts 的 importTableTemplate_ACU：将 global 分支从"保存+applyTemplateSnapshotToScope_ACU"改为"仅 upsertTemplatePreset_ACU + refreshPresetUIAfterSwitch_ACU"。如果删除这段改动，全局模板导入后会重新出现"强制切换+下拉不刷新"的 bug。
    - template-preset-api.ts 的 importTemplateFromData：同样拆分了 global/chat 分支。如果删除，API 调用方仍会遇到同样的状态污染。
    - 两个文件的 importCombinedSettings_ACU 补了 refreshPresetUIAfterSwitch_ACU 调用。如果删除，合并配置导入后预设下拉不会刷新。
  - 产物是否能被其目标对象（被修复的bug）的变化所"击穿"？
    → YES。如果把 applyTemplateSnapshotToScope_ACU 重新加回 global import 路径，所有修复效果立刻消失——预设会被强制切换、下拉显示异常、无法切换直到重启。
  - 实质性比率: 3/3 = 100%

# 覆盖完整性检查
completeness_check:
  - 是否存在被跳过的模块/函数/路径？
    → NO。所有 7 处 applyTemplateSnapshotToScope_ACU 调用点已逐一审查：
      1. data-admin-ui.ts importTableTemplate_ACU → 已修改（global 分支拆出）
      2. template-preset-api.ts importTemplateFromData → 已修改（global 分支拆出）
      3. data-admin-ui.ts importCombinedSettings_ACU → 已修改（补 UI 刷新）
      4. admin-ui.ts importCombinedSettings_ACU → 已修改（补 UI 刷新）
      5. data-admin-ui.ts resetTableTemplate_ACU → 不修改（用户显式触发重置，应切换）
      6. template-preset-service.ts applyTemplatePresetToCurrent_ACU → 不修改（用户手动切换预设，应切换）
      7. popup-bindings-data.ts 聊天级文件导入 → 不修改（聊天导入应生成 chat_override）
  - 产物覆盖的范围是否与 execution_plan 中 affected_scope 完全一致？
    → YES。affected_scope 列出 4 个修改点，全部完成。
  - 核心业务逻辑是否都有直接验证？
    → YES。TypeScript 编译验证类型安全；逐一审查调用链确认语义正确。
  - affected_scope 覆盖率: 4/4 = 100%

# 价值密度检查
value_density_check:
  - 产物中高价值内容与低价值内容的比例是多少？
    → 高价值:低价值 = 3:0，高价值占比 100%。每一处修改都直接针对根因（状态污染源），没有为了凑数而做的无意义改动。
  - 是否存在"用数量掩盖质量"的模式？
    → NO。总共只改了 3 个文件 4 处函数，每一处都有明确且不重叠的修复目标。

# 需求对齐检查
alignment_check:
  - 产物满足的是用户的字面需求还是本质需求？
    → 本质需求。用户反馈了三个症状：(1) 下拉不刷新 (2) 预设被强制切换 (3) 无法切换直到重启。根因是全局导入把"保存到预设库"和"切换当前模板"绑死了。修复后：
      - 导入全局模板只保存到预设库 + 刷新下拉，不触碰运行态 → 解决症状 (1) 和 (2)
      - 不再触发 applyTemplateScopeForCurrentChat_ACU，不会改变聊天作用域 → 解决症状 (3)
      - 用户可通过下拉手动切换到新导入的预设，无需重启
  - "如果这是别人交给我的，我会接受吗？"
    → YES。改动范围小而精确，不改 service 层核心函数，不改作用域优先级规则，TypeScript 编译通过。每个改动点都有明确的技术依据。
</output_quality_review>
