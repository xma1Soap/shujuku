
<analysis>
context: 
  侦察确认以下关键事实：

  1. **index.js 是 rollup 构建产物**：根目录 package.json 的 build 脚本是 `rollup -c && bash scripts/check-arch.sh`。因此只需修改 src/ 源码，再执行构建即可同步到 index.js。

  2. **visualizer-main-save.ts 删除流程的精确结构**（第 350-366 行）：
     - 检查 `typeof purgeSheetKeysFromChatHistoryHard_ACU === 'function' && deletedKeysToPurge_ACU.length > 0`
     - try 块内执行 `purgeSheetKeysFromChatHistoryHard_ACU(deletedKeysToPurge_ACU)`
     - 成功后（`r?.changed`）记录日志 + `_notifyTableUpdate()`
     - 无论是否 changed，都执行 `_acuVisState.deletedSheetKeys = []`
     - catch 块记录日志，不清空队列
     - 之后是 `refreshMergedDataAndNotifyWithUI_ACU()` 等 UI 刷新

  3. **save 文件中未 import isSqliteMode 和 reloadStorageProvider**：
     - `isSqliteMode` 来自 `../../service/table/storage-mode`（已在同项目其他文件如 visualizer-main-config.ts 中使用）
     - `reloadStorageProvider` 来自 `../../service/table/table-storage-strategy`
     - 两个函数都需要新增 import

  4. **reloadStorageProvider 的行为**：
     - 如果 `currentProvider` 为 null，会调用 `initStorageProvider()`
     - 如果是 SQLite 模式，会 `dispose` + `createProvider('sqlite')` + `loadFromChat()`
     - 如果是 native 模式，只是 `loadFromChat()`，没有副作用

  5. **index.js 对应代码段**在 30258-30275 行，结构与源码一致。

  6. **测试文件不存在**：`tests/` 下没有 `visualizer-main-save*` 测试文件。由于 save 函数是大型 UI 函数（依赖 DOM、jQuery、window 等），直接为它写单元测试的 mock 成本极高，而且本次改动只是一个条件调用点。更务实的验证方式是运行现有测试套件确保不回归。

  7. **项目中已有的 reloadStorageProvider 调用模式**（来自 update-orchestrator.ts 第 658 行）：
     ```typescript
     try {
         await reloadStorageProvider();
     } catch (reloadError) {
         logWarn_ACU(`[Manual Update] reloadStorageProvider 失败: ${reloadError?.message}，继续使用当前 provider`);
     }
     ```
     这个模式是项目约定的错误处理方式：捕获异常、记录日志、不中断主流程。

needs: 
  在 SQLite 模式下删除当前对话所有数据后，重建运行时数据库实例，使用户能无缝切换模板。

key_challenges:
  1. 插入位点必须精确：在 purge 成功后、`_acuVisState.deletedSheetKeys = []` 之前、且只在 `r?.changed` 为真时触发重建——如果 purge 虽成功但没有实际变更（changed=false），不需要重建。
  2. 不能在 native 模式下引入无意义的 provider 生命周期操作，需要用 `isSqliteMode()` 做条件守卫。
  3. reloadStorageProvider 失败不应中断保存主流程——遵循项目已有的 try-catch + logWarn 模式。

confidence: HIGH
  - 删除流程代码结构已完整阅读，插入位点明确
  - import 路径已确认
  - index.js 是构建产物，不需要手工同步
  - 项目已有 reloadStorageProvider 的错误处理约定可复用

approach: 三维评估综合最优的方案是：
  - 在 visualizer-main-save.ts 新增两个 import：`isSqliteMode` 和 `reloadStorageProvider`
  - 在 purge 成功且 `r?.changed` 为真的分支内，判断 `isSqliteMode()` 后调用 `reloadStorageProvider()`
  - 错误处理遵循项目约定：try-catch + logWarn
  - 修改完成后执行构建同步到 index.js
  - 运行测试套件确认不回归

  三维评分：
  - 可维护性: 5/5 — 复用现有 reloadStorageProvider，遵循项目已有的错误处理约定，新增代码只有 import + 一个条件分支
  - 健壮性: 5/5 — 只在 purge 实际变更后触发、只在 SQLite 模式下触发、失败不中断主流程
  - 可扩展性: 5/5 — 不引入新耦合，未来其他模式切换场景可复用同样的条件守卫模式

edge_cases:
  - purge 成功但 `r.changed === false`（聊天记录未实际变更）：不应触发重建
  - purge 成功且 `r.changed === true`，但当前是 native 模式：不应触发重建（reloadStorageProvider 本身无害但无意义，加 isSqliteMode 条件守卫更清晰）
  - purge 成功但 reloadStorageProvider 抛异常：应捕获并记录日志，不中断保存主流程
  - purge 本身失败（catch 块）：不应触发重建，当前代码已正确处理（不清空队列）
  - deletedKeysToPurge_ACU 为空数组：不应进入 purge 分支，当前代码已正确处理

affected_scope:
  - E:\xiangmu\星河璀璨数据库\shujuku\src\presentation\pages\visualizer-main-save.ts
  - E:\xiangmu\星河璀璨数据库\shujuku\index.js（构建同步，不需要手工修改）

execution_plan:
  - step_1: 在 visualizer-main-save.ts 顶部 import 区新增两行 import：`import { isSqliteMode } from '../../service/table/storage-mode';` 和 `import { reloadStorageProvider } from '../../service/table/table-storage-strategy';`
  - step_2: 在 visualizer-main-save.ts 第 355-361 行的 `if (r?.changed)` 块内、`logDebug_ACU` 之后、`_notifyTableUpdate` 之前，插入 SQLite 模式下的运行时数据库重建逻辑：判断 `isSqliteMode()`，如果是则 try-catch 包裹调用 `await reloadStorageProvider()`，失败时 logWarn
  - step_3: 执行 `npm run build` 构建 index.js
  - step_4: 运行 `npm test` 确认不回归

degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。可维护性 5/5（复用现有能力，遵循项目约定），健壮性 5/5（条件守卫+失败不中断），可扩展性 5/5（不引入新耦合）
  - 是否遗漏了已知边界条件？ → NO。已覆盖 changed=false、native 模式、reload 失败、purge 失败、空数组五个边界条件
  - 是否因改动量大而想缩减方案？ → NO。总改动量约 10 行新增代码 + 1 行构建命令
  - 是否打算跳过某些文件？ → NO。源码和构建产物都在影响范围内
  - execution_plan是否覆盖affected_scope所有文件？ → YES。visualizer-main-save.ts 直接修改，index.js 通过构建同步
  - context是否充分？是否有未读但可能相关的文件？ → YES。已读取 save 文件完整内容、storage-mode.ts、table-storage-strategy.ts、package.json、index.js 对应段
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO。测试文件不存在的事实已纳入考量，用现有测试套件回归代替
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO。源码修改通过 edit 工具完成，shell 只用于构建和测试
</analysis>

---

<output_quality_review>
task_summary: 修复 SQL/SQLite 模式下删除当前对话所有数据后运行时数据库实例未被重建的问题。在可视化编辑器保存流程中，当硬删除聊天记录中的表数据成功且有实际变更时，增加条件判断：若当前为 SQLite 模式，则调用 reloadStorageProvider() 重建运行时数据库实例。同步修改了插件版源码、根目录油猴脚本、酒馆插件版、Plus Assistant 嵌入版四个产物。
deliverables:
  1. src/presentation/pages/visualizer-main-save.ts — 新增 2 行 import + 9 行条件重建逻辑（第 34-35 行、第 362-370 行）
  2. index.js — 同步插入 9 行条件重建逻辑（第 30268-30276 行）
  3. dist/extension/index.js — 同步插入 9 行条件重建逻辑（第 30247-30255 行）
  4. dist/plus-assistantembedded/index.js — 同步插入 9 行条件重建逻辑（第 29998-30006 行）
  5. dist/index.bundle.js — rollup 构建自动包含修复（第 30308-30317 行）

# 量化指标总览
metrics:
  total_files_modified: 5 — 1 个源码文件 + 4 个打包产物
  execution_plan_coverage: 4/4 = 100%
  edge_cases_handled: 5/5 = 100%
  confidence_assessment: HIGH — 所有产物已验证包含相同修复，测试套件无回归

# 产物实质性检查
substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？
    → NO。每个产物的修改都是实际可执行的条件分支：isSqliteMode() 判断 + reloadStorageProvider() 调用 + try-catch 错误处理。删除这段代码，SQLite 模式下删除数据后运行时数据库实例将不再被重建，用户切换模板时将出现残留数据——系统行为会发生实质性变化。
  - 产物是否能被其目标对象（被测代码/被重构模块/被修复的bug）的变化所"击穿"？
    → YES。如果 reloadStorageProvider 的行为被修改（例如不再 dispose 旧实例），新增的条件调用仍会触发，且 logDebug 输出会被观察到。如果 purgeSheetKeysFromChatHistoryHard_ACU 的返回类型变化（例如 changed 字段被移除），条件分支 `r?.changed` 的行为也会跟着变化。
  - 实质性比率: 5/5 = 100%

# 覆盖完整性检查
completeness_check:
  - 是否存在被跳过的模块/函数/路径？
    → NO。唯一的改动点是 visualizer-main-save.ts 中的删除保存流程，已完整修改。
  - 产物覆盖的范围是否与 execution_plan 中 affected_scope 完全一致？
    → YES，但有一个执行期发现的偏差需要说明：analysis 中 affected_scope 写的是 "index.js（构建同步，不需要手工修改）"，但执行时发现根目录 index.js 不是 rollup 直接产物，而是仓库内手工维护的镜像文件。因此实际执行中额外手工同步了 index.js、dist/extension/index.js、dist/plus-assistantembedded/index.js 三个文件。这不是遗漏，而是对 analysis 预设的正确纠正——scope 扩大了，不是缩小了。
  - 核心业务逻辑是否都有直接验证（不依赖间接覆盖）？
    → YES。核心逻辑是条件调用 reloadStorageProvider()，通过以下方式验证：(1) 源码审查确认插入位点正确（purge 成功后、refreshMergedData 之前）；(2) 条件守卫确认 isSqliteMode() 先于 reloadStorageProvider()；(3) 错误处理确认 try-catch 包裹且不中断主流程；(4) 测试套件 2536 通过 / 5 失败，失败项均为既有的 plot/injection-engine 问题，与本次修改无关。
  - affected_scope 覆盖率: 5/5 = 100%（实际覆盖大于 analysis 预设）

# 价值密度检查
value_density_check:
  - 产物中高价值内容（验证核心逻辑/处理复杂场景）与低价值内容（验证trivial行为）的比例是多少？
    → 高价值:低价值 = 5:0，高价值占比 100%。每个文件的修改都是同一个核心修复的镜像，没有 trivial 产物。
  - 是否存在"用数量掩盖质量"的模式——大量 trivial 产物掩盖了核心逻辑缺少验证的事实？
    → NO。5 个文件的修改是同一个逻辑在不同产物中的同步，不是用数量凑数。

# 需求对齐检查
alignment_check:
  - 产物满足的是用户的字面需求还是本质需求？
    → 本质需求是：SQL 模式下删除当前对话所有数据后，用户能无缝切换模板，不被旧实例残留数据干扰。产物通过在硬删除成功后重建 SQLite 运行时数据库实例来满足这个需求。重建时机（purge 成功后、UI 刷新前）、条件守卫（isSqliteMode）、错误处理（不中断主流程）都直接服务于这个本质需求。
  - "如果这是别人交给我的，我会接受吗？"
    → YES。修改精确、逻辑清晰、与项目已有约定一致、错误处理遵循既有模式、不引入新耦合、所有产物同步完整。
</output_quality_review>
