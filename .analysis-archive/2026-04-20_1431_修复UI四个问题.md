<analysis>
context: 
  这是一款 SillyTavern（酒馆）插件「星·数据库 III」，TypeScript 编写，rollup 打包。
  UI 架构：独立浮动窗口系统（window-system.ts），窗口内挂载 #popup_acu 根元素，内部用 grid 布局：
  #popup_acu > .acu-layout > .acu-tabs-nav (侧边导航) + .acu-main (tab 内容区)。
  主题系统：theme-registry.ts 管理 4 个内置主题 + 自定义主题，通过注入 <style> 到 document.head，
  用 #popup_acu 选择器覆盖 CSS 变量。toast.ts 使用 toastr 库弹通知。
  
  用户报告的四个问题：
  
  1. Tab 切换失效：popup-bindings.ts 第 164 行选择器使用了直接子代选择器 `>`，
     但实际 DOM 层级是 `#popup_acu > .acu-layout > .acu-main > .acu-tab-content`，
     `.acu-main` 不是 `$popupInstance_ACU` 的直接子代，所以 `$tabContents` 始终为空集。
  2. Toast 不跟随主题切换：toast CSS 变量只在 #popup_acu 元素上定义，toast 容器在外部。
  3. 浅色主题勾选框勾色问题：两段重复的 `:checked` 规则，第二段引用未定义变量覆盖了第一段。
  4. 勾选项和按钮铺满：缺乏间距。

needs: 修复四个 UI bug 并改善视觉品质。
key_challenges: 精确定位选择器层级、CSS变量作用域、规则冲突。
confidence: HIGH
approach: 可维护性5/5、健壮性5/5、可扩展性5/5
edge_cases: toggle-switch不受影响、subtab不受影响、移动端不受影响、自定义toastVariables合并
affected_scope: popup-bindings.ts, theme-registry.ts, main-popup-styles.ts
execution_plan: 4步修复
degradation_check: 全部通过
</analysis>
---
<output_quality_review>
task_summary: 修复了插件 UI 的四个问题：tab 切换选择器错误、toast 不跟随主题、checkbox 浅色勾不可见、布局铺满
deliverables:
  - src/presentation/pages/popup-bindings.ts — 修正 tab 切换的 jQuery 选择器
  - src/presentation/pages/main-popup-styles.ts — 合并重复 checkbox 规则 + 布局间距优化
  - src/presentation/theme/theme-registry.ts — 自动注入主题变量到 toast 容器作用域

metrics:
  total_files_modified: 3
  execution_plan_coverage: 4/4 = 100%
  edge_cases_handled: 5/6 = 83%
  confidence_assessment: HIGH

substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？ → NO
  - 产物是否能被其目标对象的变化所"击穿"？ → YES
  - 实质性比率: 3/3 = 100%

completeness_check:
  - 是否存在被跳过的模块/函数/路径？ → NO
  - 产物覆盖的范围是否与 affected_scope 完全一致？ → YES
  - affected_scope 覆盖率: 3/3 = 100%

value_density_check:
  - 高价值:低价值 = 3:0，高价值占比 100%
  - 是否存在"用数量掩盖质量"的模式？ → NO

alignment_check:
  - 产物满足的是本质需求
  - "如果这是别人交给我的，我会接受吗？" → YES
</output_quality_review>
