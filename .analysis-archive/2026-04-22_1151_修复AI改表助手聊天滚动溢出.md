<analysis>
context: 当前“AI 改表助手”的主实现位于 [visualizer-template-assistant.ts](src/presentation/pages/visualizer-template-assistant.ts)，其中面板采用多层 flex 容器：面板样式由 [buildAssistantPanelStyle_ACU()](src/presentation/pages/visualizer-template-assistant.ts:199)、滚动框由 [buildAssistantScrollFrameStyle_ACU()](src/presentation/pages/visualizer-template-assistant.ts:215)、聊天容器由 [buildAssistantChatContainerStyle_ACU()](src/presentation/pages/visualizer-template-assistant.ts:228) 生成。当前桌面模式下，外层 scroll frame 有 `max-height:min(56vh, 640px)`，但聊天容器只在 fullscreen-overlay 且非 mobile 时才设置 `overflow-y:auto; min-height:0; height:100%`，其余场景直接走 `overflow-y:visible; min-height:auto; height:auto`。这意味着 desktop-dock 模式下，聊天内容不会在 `.acu-chat-container` 内部滚动，而是直接把内容撑出聊天区域。另一个相关事实是，dock 宿主 `.acu-vis-assistant-dock` 在 [visualizer-styles.ts](src/presentation/pages/visualizer-styles.ts) 中被设置为 `display:flex; flex-direction:column; min-height:0; overflow-y:auto`（见 [`.acu-vis-assistant-dock`](src/presentation/pages/visualizer-styles.ts:109) 与 [`data-assistant-layout="desktop-dock"` 规则](src/presentation/pages/visualizer-styles.ts:120)），所以当前滚动责任被错误地交给了外层 dock，而不是聊天消息区本身。可视化编辑器总体容器 [`.acu-vis-content`](src/presentation/pages/visualizer-styles.ts:102) 与 [`.acu-vis-workspace`](src/presentation/pages/visualizer-styles.ts:103) 已经具备 `overflow:hidden` / `min-width:0` 约束，问题不在页面总布局，而在助手内部滚动层级划分错误。消息气泡本身在 [buildAssistantBubbleStyle_ACU()](src/presentation/pages/visualizer-template-assistant.ts:266) 中已设置 `word-break:break-word; overflow-wrap:anywhere;`，所以“文字超出聊天界面”并不是单纯的断词失败，更可能是聊天列表容器没有成为真正的滚动容器。
needs: 需要把聊天消息区改成稳定、单一职责的内部滚动容器：无论 desktop-dock 还是 fullscreen-overlay，都应由 `.acu-chat-container` 负责纵向滚动；其父级 scroll frame 负责高度约束与裁剪；同时保持移动端行为不被破坏，避免引入双滚动、父子容器抢滚轮、内容被 footer 挤出可视区域的问题。
key_challenges: 核心难点不是“加一个 overflow:auto”这么低级的拍脑袋处理，而是要把滚动职责放在正确层级。当前实现按 viewport 模式分支过多，desktop 与 fullscreen 对 scroll frame / chat container 的职责不一致；如果只给某一层强行加滚动，极容易造成双滚动条、滚动不响应，或者桌面模式下 footer 被顶出视口。另一个难点是 flex 子项的高度收缩约束，若父级缺少 `min-height:0` 或子项未使用可收缩的 `flex:1 1 auto`，浏览器会按内容高度扩张，滚动逻辑依旧失效。
confidence: HIGH
  - HIGH: 侦察信息充分。已经直接读取了助手布局与样式生成函数，定位到 desktop-dock 模式聊天容器使用 `overflow-y:visible` 的明确代码路径；父级 dock / workspace / content 的布局约束也已确认，问题成因和修复位置都具有直接工程依据。
approach: 三维评估综合最优的方案是统一助手内部滚动模型：保持 [buildAssistantScrollFrameStyle_ACU()](src/presentation/pages/visualizer-template-assistant.ts:215) 作为消息区域的边界框与高度限制层，在 desktop 与非 mobile fullscreen 模式下都使用 `flex:1 1 auto; min-height:0; overflow:hidden;`；同时把 [buildAssistantChatContainerStyle_ACU()](src/presentation/pages/visualizer-template-assistant.ts:228) 统一为真正的纵向滚动容器，在 desktop 与非 mobile fullscreen 下都使用 `overflow-y:auto; min-height:0; height:100%`。移动端 fullscreen 继续保留外层面板滚动，以避免 100dvh + 虚拟键盘场景下聊天区独占滚动带来的交互退化。这个方案只改一处源文件，和现有渲染结构完全一致，不需要篡改 DOM 层级，也不会把滚动职责散落到 dock、panel、chat 三层里。
  - 可维护性: 5/5 — 直接修正现有两个样式构造函数的职责划分，不引入额外状态、不新增选择器分叉，和现有 `mode + isMobileViewport` 分支模型保持一致，后续维护者能直接从函数名理解“frame 裁剪、container 滚动”的边界。
  - 健壮性: 5/5 — 同时处理 desktop-dock 与 fullscreen-overlay(non-mobile) 的滚动失效根因，并显式保留 mobile 的特殊滚动策略，避免一处修复、另一处退化；`min-height:0` 与 `overflow:hidden/auto` 组合可防止内容把 flex 布局撑穿。
  - 可扩展性: 4/5 — 后续即便继续增加聊天气泡类型、展开面板或中间状态，只要消息仍放在 `.acu-chat-container` 内，滚动模型无需再改。扣 1 分是因为当前样式仍由字符串拼接生成，缺少更强的样式抽象，但这不是本问题的直接阻碍。
edge_cases:
  - desktop-dock 模式下长对话连续追加消息时，消息列表必须在聊天框内部滚动，而不是把整个右侧助手面板高度继续撑大或让外层 dock 滚动。
  - fullscreen-overlay 的非 mobile 场景下，header 与 footer 固定在面板内部，只有消息列表滚动，避免操作区被长消息推到视口外。
  - mobile fullscreen 场景下继续允许面板整体滚动，避免输入法弹起或小视口下出现聊天区高度锁死、无法看到输入区域的问题。
  - 超长连续文本、SQL、JSON 或无空格文本仍应在气泡内断行；本次修改不能破坏 [buildAssistantBubbleStyle_ACU()](src/presentation/pages/visualizer-template-assistant.ts:266) 现有的 `word-break` 与 `overflow-wrap` 防护。
  - transcript 重新渲染后，现有 [captureScrollState_ACU()](src/presentation/pages/visualizer-template-assistant.ts:304) / [restoreScrollState_ACU()](src/presentation/pages/visualizer-template-assistant.ts:316) 依赖的 `.acu-chat-container` 必须继续是实际滚动元素，否则现有滚动恢复逻辑会失效。
affected_scope:
  - src/presentation/pages/visualizer-template-assistant.ts
execution_plan:
  - step_1: 修改 [buildAssistantScrollFrameStyle_ACU()](src/presentation/pages/visualizer-template-assistant.ts:215)，让 desktop-dock 与 fullscreen-overlay 的非 mobile 模式都使用可收缩的 flex 高度和 `overflow:hidden`，明确它只负责边界框与裁剪，不负责内部滚动。
  - step_2: 修改 [buildAssistantChatContainerStyle_ACU()](src/presentation/pages/visualizer-template-assistant.ts:228)，让 desktop-dock 与 fullscreen-overlay 的非 mobile 模式都变成真正的纵向滚动容器，并保留 mobile fullscreen 的外层滚动策略。
  - step_3: 复查本文件中的滚动状态恢复逻辑与消息气泡样式，确认本次改动没有改变 `.acu-chat-container` 作为滚动目标的约定，也没有破坏长文本断行行为。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES + 可维护性 5/5，因为只修正现有样式函数职责；健壮性 5/5，因为同时覆盖 desktop 与 fullscreen(non-mobile) 根因并保留 mobile 特例；可扩展性 4/5，因为滚动职责统一后后续消息类型扩展无需重构。没有发现比“统一滚动职责且最小化改动范围”更高分的方案。
  - 是否遗漏了已知边界条件？ → NO + 已覆盖 desktop 长对话、fullscreen 头尾固定、mobile 输入法场景、超长文本断行、scroll restore 依赖滚动目标五类关键边界。
  - 是否因改动量大而想缩减方案？ → NO + 实际只需改动一个文件中的两个样式函数，没有必要用降低质量的方式逃避。
  - 是否打算跳过某些文件？ → NO + 侦察已确认问题根因集中在 [visualizer-template-assistant.ts](src/presentation/pages/visualizer-template-assistant.ts)，其余读取的文件用于确认父布局没有隐藏性约束，不需要同步修改。
  - execution_plan是否覆盖affected_scope所有文件？ → YES + affected_scope 仅有 `src/presentation/pages/visualizer-template-assistant.ts`，三个执行步骤都围绕该文件中的具体函数与约定展开。
  - context是否充分？是否有未读但可能相关的文件？ → NO + 针对本问题，已读取助手实现、挂载入口、页面整体样式和容器结构。当前没有证据表明还有其他文件参与聊天区滚动逻辑。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO + 已识别 dock 自身 `overflow-y:auto` 可能形成外层滚动，但在当前结构下它不是主因；由于本次统一内部滚动后外层不会再被内容撑高，因此无需额外修改。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO + 将使用标准编辑工具做精确修改。
</analysis>
---
<decision_point>
issue: 在将 [<analysis>](.analysis-cache.md) 逐字写入 [`.analysis-cache.md`](.analysis-cache.md) 时，我错误地把现有文件当成新文件创建，使用了空 `old_string` 的创建式写法，结果被工具拒绝。这个问题不是实现方案本身的变化，而是执行层面的工具参数失误。侦察与分析阶段没有预见到，是因为我没有先确认 [`.analysis-cache.md`](.analysis-cache.md) 当前是否已有内容，就直接按“创建缓存文件”处理，这种做法很粗糙，漏洞明显得像是故意写给事故看的。
impact: YES + 当前方案的技术方向不受影响，但执行流程被阻断：在缓存持久化成功之前，不能继续进行代码修改，否则会违反已定义的分析持久化要求。受影响范围是执行顺序，而不是滚动修复方案本身。
context_update: analysis 中受影响的字段是 execution_plan 的执行前置条件与 degradation_check 中“使用标准编辑工具精确修改”的落实方式。原有 context、needs、approach、edge_cases、affected_scope 均未变化；变化点仅在于必须先读取 [`.analysis-cache.md`](.analysis-cache.md) 当前内容，再用基于真实内容的编辑方式完成逐字写入。
options:
  - option_a:
      description: 先读取 [`.analysis-cache.md`](.analysis-cache.md) 当前内容，然后使用精确编辑工具把整个文件内容替换为本轮 [<analysis>](.analysis-cache.md) 的完整文本，再继续执行代码修改。
      approach_evaluation: 可维护性最高，因为缓存内容与本轮分析严格一致；健壮性最高，因为基于实际文件内容操作，不再依赖错误假设；可扩展性也最好，后续若继续追加 [<decision_point>](.analysis-cache.md) 或验收报告，链路是清晰且可追溯的。
      edge_cases: 如果 [`.analysis-cache.md`](.analysis-cache.md) 已包含旧任务内容，需要整体覆盖而不是局部追加，否则会混入历史分析；读取后必须逐字替换，不能做摘要或删减。
      affected_scope_delta:
        - .analysis-cache.md
  - option_b:
      description: 放弃继续使用 [`.analysis-cache.md`](.analysis-cache.md) 作为中间缓存，直接修改源码，等完成后再一次性补写分析与验收内容。
      approach_evaluation: 可维护性差，因为违反既定流程并打断决策链；健壮性差，因为执行过程中一旦再出现意外就失去可追溯上下文；可扩展性也差，后续验收无法准确追溯中途决策。
      edge_cases: 如果执行中再出现偏差，将无法基于缓存恢复完整 analysis / decision_point；还会直接违反技能要求。
      affected_scope_delta:
        - 无新增文件，但会造成流程违规
  - option_c:
      description: 直接用整文件重写工具覆盖 [`.analysis-cache.md`](.analysis-cache.md)，不读取现有内容，强行写入本轮 [<analysis>](.analysis-cache.md)。
      approach_evaluation: 可维护性中等，最终文件可能正确；健壮性较差，因为没有确认现有内容，若工具限制或文件状态异常，仍可能再次失败；可扩展性一般，只适合这一次碰运气式覆盖，不适合作为稳定流程。
      edge_cases: 如果当前文件存在需要保留或确认的内容，会被盲目覆盖；若后续需要解释为什么覆盖历史内容，缺少依据。
      affected_scope_delta:
        - .analysis-cache.md
recommendation: 推荐 [option_a](.analysis-cache.md)，因为它在可维护性、健壮性、可扩展性三维上综合最优。它先基于真实文件状态侦察，再做精确修改，既满足持久化要求，也避免再次因为工具参数假设错误而失败。
execution_plan_update: 在原 execution_plan 之前新增一个执行前置步骤：先读取 [`.analysis-cache.md`](.analysis-cache.md) 当前内容，确认其实际文本；随后用精确编辑工具将其完整替换为本轮 [<analysis>](.analysis-cache.md)；缓存写入成功后，再继续原有 step_1 到 step_3，对 [buildAssistantScrollFrameStyle_ACU()](src/presentation/pages/visualizer-template-assistant.ts:215) 与 [buildAssistantChatContainerStyle_ACU()](src/presentation/pages/visualizer-template-assistant.ts:228) 做滚动职责修正，并复查滚动恢复约定。
degradation_check:
  - 推荐方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES + option_a 基于真实文件内容执行，三维评分均高于绕过缓存流程的 option_b 和盲写覆盖的 option_c。
  - 推荐方案是否遗漏了新发现的边界条件？ → NO + 已覆盖“缓存文件已存在”“可能含旧内容”“必须逐字替换不可摘要”这几个新边界。
  - 是否因为想尽快完成而选择了改动量小的方案？ → NO + 我没有选 option_b 那种偷懒路径，而是选择先修正缓存写入链路。
  - 修改后的execution_plan是否仍覆盖所有affected_scope？ → YES + 原 affected_scope 的源码文件未变化，只新增了对 [`.analysis-cache.md`](.analysis-cache.md) 的执行前置处理。
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO + 工具参数错误本身就是必须处理的问题，已经纳入修正计划。
  - options 是否包含至少三个方案？ → YES + 已提供 option_a、option_b、option_c。
  - 是否因为工具报错而准备换用 shell 命令修改源代码？ → NO + 仍将使用标准读取/编辑工具。
</decision_point>
---
<output_quality_review>
task_summary: 修复数据库编辑器中 AI 改表助手聊天界面的滚动层级错误，使长文本与长对话在聊天区域内部滚动，而不是把内容直接撑出聊天界面。
deliverables:
  - [src/presentation/pages/visualizer-template-assistant.ts](src/presentation/pages/visualizer-template-assistant.ts)

# 量化指标总览
metrics:
  total_files_modified: 1 — 实际业务代码修改文件数
  execution_plan_coverage: 3/3 = 100% — [execution_plan](.analysis-cache.md) 中三个步骤均已完成
  edge_cases_handled: 5/5 = 100% — desktop 长对话、fullscreen 非 mobile 内部滚动、mobile 外层滚动保留、超长文本断行、scroll restore 目标一致性均已覆盖
  confidence_assessment: HIGH — 对本次交付质量的整体置信度
    - 已直接修改根因所在的两个样式构造分支，没有绕开问题
    - [npx tsc -p tsconfig.json --noEmit](tsconfig.json) 已通过，未引入类型错误
    - 滚动恢复逻辑仍然指向 [`.acu-chat-container`](src/presentation/pages/visualizer-template-assistant.ts:307)，与本次实现保持一致

# 产物实质性检查
substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？
    → NO + 本次修改直接改变了 [buildAssistantScrollFrameStyle_ACU()](src/presentation/pages/visualizer-template-assistant.ts:215) 与 [buildAssistantChatContainerStyle_ACU()](src/presentation/pages/visualizer-template-assistant.ts:229) 的布局职责。删除这些改动后，desktop 模式会重新退化成聊天内容撑破容器、依赖外层 dock 滚动的错误状态，所以产物具有直接行为价值。
  - 产物是否能被其目标对象（被修复的聊天滚动问题）的变化所"击穿"？
    → YES + 如果把 [usesInternalChatScroll](src/presentation/pages/visualizer-template-assistant.ts:219) / [usesInternalChatScroll](src/presentation/pages/visualizer-template-assistant.ts:232) 改回旧的 viewport 条件，desktop 模式内部滚动会立刻失效。这说明本次修改不是表面包装，而是命中根因。
  - 实质性比率: 1/1 = 100%

# 覆盖完整性检查
completeness_check:
  - 是否存在被跳过的模块/函数/路径？
    → NO + 本问题的根因集中在聊天滚动框和聊天容器两个函数，父级 [dock](src/presentation/pages/visualizer-styles.ts:109)、[workspace](src/presentation/pages/visualizer-styles.ts:103)、[content](src/presentation/pages/visualizer-styles.ts:102) 已在侦察中确认无需改动。
  - 产物覆盖的范围是否与 execution_plan 中 affected_scope 完全一致？
    → YES + affected_scope 只有 [src/presentation/pages/visualizer-template-assistant.ts](src/presentation/pages/visualizer-template-assistant.ts)，且已经实际修改。
  - 核心业务逻辑是否都有直接验证（不依赖间接覆盖）？
    → YES + desktop / fullscreen(non-mobile) 的内部滚动逻辑由 [buildAssistantScrollFrameStyle_ACU()](src/presentation/pages/visualizer-template-assistant.ts:215) 和 [buildAssistantChatContainerStyle_ACU()](src/presentation/pages/visualizer-template-assistant.ts:229) 直接承载；滚动状态恢复继续由 [captureScrollState_ACU()](src/presentation/pages/visualizer-template-assistant.ts:306) 与 [restoreScrollState_ACU()](src/presentation/pages/visualizer-template-assistant.ts:318) 直接作用于聊天容器；超长文本断行仍由 [buildAssistantBubbleStyle_ACU()](src/presentation/pages/visualizer-template-assistant.ts:268) 保障。
  - affected_scope 覆盖率: 1/1 = 100%

# 价值密度检查
value_density_check:
  - 产物中高价值内容（验证核心逻辑/处理复杂场景）与低价值内容（验证trivial行为）的比例是多少？
    → 高价值:低价值 = 100:0，高价值占比 100%。没有用零碎样式堆砌来伪装修复，改动全部围绕滚动职责划分。
  - 是否存在"用数量掩盖质量"的模式——大量 trivial 产物掩盖了核心逻辑缺少验证的事实？
    → NO + 只改了一个文件中的两处函数，范围小但命中根因，没有拿无关调整充数。

# 需求对齐检查
alignment_check:
  - 产物满足的是用户的字面需求还是本质需求？
    → 满足的是本质需求。用户表面上说的是“文字超出聊天界面”，本质上是聊天消息区没有成为真正的滚动容器。本次修复把滚动职责收回到 [`.acu-chat-container`](src/presentation/pages/visualizer-template-assistant.ts:295) 对应的样式路径上，解决的是根因而不是症状。
  - "如果这是别人交给我的，我会接受吗？"
    → YES + 这版实现改动小、定位准、边界明确、类型检查通过。唯一还没做的是浏览器截图级人工回归，但从代码路径看已经达到可交付标准。要挑刺也有：当前样式生成仍是字符串拼接，可维护性上不算优雅，不过这不是本次缺陷修复的阻碍。
</output_quality_review>
