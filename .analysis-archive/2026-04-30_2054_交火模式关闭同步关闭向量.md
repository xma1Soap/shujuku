<analysis>
context: 用户指出的问题是向量混合交火增强方案开启时会自动打开普通向量模式，但关闭交火模式时不会关闭普通向量模式，导致普通向量流程继续处于 enabled 状态。侦察确认当前应在 main 分支修复；起初工作区停在 release，且 release 分支没有 src/service/vector 目录，这是发布分支资产形态，不适合源码修复；已切回 main，main 与 origin/main 同步。交火模式 setter 位于 src/service/settings/settings-service.ts:671-687 的 setSummaryVectorIndexMode_ACU。当前逻辑只写 settings_ACU.summaryVectorIndexModeDefault、globalMeta_ACU.summaryVectorIndexModeGlobal，并在 enabled=true 时关闭 0TK；它没有操作 settings_ACU.vectorMemoryConfig.enabled。普通向量配置读取和规范化位于 src/service/vector/vector-memory-config.ts:153-173 的 getCurrentVectorMemoryConfig_ACU，返回 settings_ACU.vectorMemoryConfig 的引用；注释明确直接修改返回值会反映到 settings_ACU.vectorMemoryConfig，但需要保存。普通向量 UI 开关在 src/presentation/pages/popup-bindings-worldbook.ts:174-178 直接 updateVectorMemoryField_ACU('enabled', checked)，并会 toggle 配置块和 syncManualUpdateButtonAvailability_ACU。世界书 UI 状态同步在 src/presentation/components/status-display.ts:171-195，会按 getCurrentVectorMemoryConfig_ACU().enabled 勾选普通向量开关并显示配置块；交火模式开关 UI 在 src/presentation/pages/popup-bindings-worldbook.ts:481-500 调用 setSummaryVectorIndexMode_ACU(modeEnabled)，但没有刷新普通向量 checkbox 和配置块。交火模式运行入口在 src/presentation/bootstrap/init.ts:121-123，如果 summaryVectorIndexModeEnabled 为 true 会走 orchestrateSummaryVectorIndexBeforeSend_ACU，否则走普通 vector preprocess；因此关闭交火后如果普通 vector enabled 仍为 true，就会回到普通向量流程，这正是用户说的严重问题。
needs: 本质需求不是单纯把某个 checkbox 取消勾选，而是让交火模式与普通向量模式之间的自动联动具备对称性：开启交火模式时为了复用向量模型/API/rerank 配置，可以自动打开向量能力；关闭交火模式时必须同步关闭普通向量 enabled，避免普通远记忆流程在用户以为已退出交火模式后继续工作。还要保证不清空模型、API、rerank、namespace 等配置，只改 enabled；保存必须持久化；UI 必须立即反映普通向量已关闭，否则用户会看到状态污染。
key_challenges: 关键难点有三个。第一，普通向量配置是 settings_ACU.vectorMemoryConfig 的全局配置，不在 worldbookConfig 内，不能只改 cfg.summaryVectorIndexModeEnabled。第二，setSummaryVectorIndexMode_ACU 位于 service 层，不能在 service 里直接操作 DOM 或调用 presentation 的 syncManualUpdateButtonAvailability_ACU；UI 刷新必须在 presentation 事件处理器里做。第三，开启交火模式“自动打开向量模式”的逻辑必须确认位置，目前 setter 里没有看到显式开启 vectorMemoryConfig.enabled，说明可能依赖 UI 或其他代码；修复应在 service setter 中建立明确单点联动，避免再靠隐式副作用。
confidence: HIGH。已确认交火模式 setter、普通向量配置 getter、普通向量 UI 绑定、交火模式 UI 绑定、发送前向量/交火分流入口。修改范围明确，风险集中在 service setter 与 UI 事件处理器两处。
approach: 三维评估综合最优的方案是在 src/service/settings/settings-service.ts 的 setSummaryVectorIndexMode_ACU 中显式读取 getCurrentVectorMemoryConfig_ACU() 并设置 vectorMemoryConfig.enabled = enabled：开启交火时 enabled=true，关闭交火时 enabled=false；只改 enabled，不触碰 endpoint/model/api/rerank/namespace 等配置。然后在 src/presentation/pages/popup-bindings-worldbook.ts 的交火模式 change handler 调用 setSummaryVectorIndexMode_ACU 后，同步普通向量 checkbox 状态、配置块显示状态和手动填表按钮软禁用状态。这样业务状态联动在 service 层，UI 状态刷新在 presentation 层，分层清晰。
  三维评分（每个维度 1-5 分，5 为最优）：
  - 可维护性: 5/5 — 交火模式与普通向量 enabled 的业务联动集中在 setSummaryVectorIndexMode_ACU；UI 仅负责刷新显示，不承担业务状态推导。
  - 健壮性: 5/5 — 开启和关闭交火模式都对称设置 vectorMemoryConfig.enabled，避免关闭后残留普通向量流程；只改 enabled，不破坏模型/API/rerank 配置。
  - 可扩展性: 4/5 — 后续若交火模式还需要联动其他向量能力，可继续扩展 service setter；扣 1 分是因为普通向量 enabled 仍是全局开关，交火模式关闭会关闭用户原先手动开启的普通向量，这是用户当前明确要求的行为，但未来若要区分“用户手动开”和“交火自动开”，需要额外来源标记。
edge_cases: 交火模式开启时 vectorMemoryConfig 不存在，应通过 getCurrentVectorMemoryConfig_ACU 自动初始化后设置 enabled=true；交火模式关闭时 vectorMemoryConfig 不存在，也应初始化后设置 enabled=false，不能抛错；关闭交火模式不能清空 embeddingEndpoint、embeddingModel、embeddingApiKey、rerankEndpoint、rerankModel、rerankApiKey；关闭交火后普通向量配置块应隐藏，普通向量 checkbox 应取消勾选；开启交火后 0TK checkbox 仍应取消勾选；关闭交火后不应自动恢复 0TK；保存仍由 setSummaryVectorIndexMode_ACU 内部 saveSettings_ACU 完成；presentation 层不能直接 import data 层；service 层不能调用 DOM/UI 函数。
affected_scope: src/service/settings/settings-service.ts；src/presentation/pages/popup-bindings-worldbook.ts；.analysis-cache.md；构建产物 dist/index.bundle.js 会在 build 后变化，但本次先不覆盖根目录 index.js，除非用户要求发布。
execution_plan:
  - step_1: 修改 src/service/settings/settings-service.ts，引入 getCurrentVectorMemoryConfig_ACU，并在 setSummaryVectorIndexMode_ACU 中设置 vectorMemoryConfig.enabled = enabled，注释说明交火模式自动启停普通向量能力但不覆盖模型/API/rerank 配置。
  - step_2: 修改 src/presentation/pages/popup-bindings-worldbook.ts，在交火模式 toggle handler 调用 setSummaryVectorIndexMode_ACU 后，同步 #worldbook-vector-memory-enabled 的 checked 状态为 modeEnabled，显示/隐藏 #worldbook-vector-memory-config-block，并调用 syncManualUpdateButtonAvailability_ACU。
  - step_3: 执行 npm run build，验证 Rollup 与架构护栏通过。
  - step_4: 搜索/读取确认 setSummaryVectorIndexMode_ACU 中存在 vectorMemoryConfig.enabled = enabled，popup-bindings-worldbook 中交火 toggle 同步普通向量 checkbox/config block/manual update 状态。
  - step_5: 验收前读取 .analysis-cache.md，追加 output_quality_review 并归档。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。该方案把业务联动放 service，把 UI 刷新放 presentation，三维评分 5/5、5/5、4/5。
  - 是否遗漏了已知边界条件？ → NO。已覆盖配置不存在、只改 enabled、不清空模型/API、UI 即时刷新、0TK 互斥保持、保存持久化、分层边界。
  - 是否因改动量大而想缩减方案？ → NO。没有只改 UI checkbox 这种表面修复；会改真实配置源。
  - 是否打算跳过某些文件？ → NO。setter 与 UI handler 都纳入 affected_scope；构建验证纳入计划。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。step_1 覆盖 settings-service，step_2 覆盖 popup-bindings-worldbook，step_3 到 step_5 覆盖验证与归档。
  - context是否充分？是否有未读但可能相关的文件？ → YES。已读交火 setter、向量配置 getter、普通向量 UI 绑定、交火 UI 绑定、状态显示、发送前分流。无需读取所有向量召回实现，因为问题发生在开关状态源头。
  - 是否有发现了但被我判断为“无关紧要”而跳过的问题？ → NO。release 分支形态不适合源码修复已处理，切回 main；交火关闭后普通向量流程会恢复的风险已纳入核心修复。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO。源码修改使用 edit_file；shell 仅用于构建与只读验证。
</analysis>

<output_quality_review>
task_summary: 修复向量混合交火增强方案关闭时普通向量模式未同步关闭的问题。现在交火模式启用会同步设置普通向量 enabled=true，交火模式关闭会同步设置普通向量 enabled=false；只改 enabled，不清空模型、API、rerank、namespace 等配置。
deliverables:
  - src/service/settings/settings-service.ts: 引入 getCurrentVectorMemoryConfig_ACU，并在 setSummaryVectorIndexMode_ACU 中对称设置 vectorMemoryConfig.enabled = enabled。
  - src/presentation/pages/popup-bindings-worldbook.ts: 交火模式 toggle 后同步普通向量 checkbox、普通向量配置块显示状态，并刷新手动填表按钮状态。
  - dist/index.bundle.js: npm run build 生成的构建产物。
metrics:
  total_files_modified: 3 — settings-service.ts、popup-bindings-worldbook.ts、dist/index.bundle.js。
  execution_plan_coverage: 5/5 = 100%。
  edge_cases_handled: 8/8 = 100%。
  confidence_assessment: HIGH — 构建通过，架构护栏总违规 0 条，关键源码位置已用 findstr 验证。
substance_check:
  - 产物中是否存在形式完整但实质空洞的内容？ → NO。settings-service.ts 的 vectorMemoryConfig.enabled = enabled 是实际状态源修改，删除它就会复现用户报告的关闭交火后普通向量继续启用；popup-bindings-worldbook.ts 的 UI 同步会立即修正 checkbox 和配置块显示，删除它会造成 UI 状态与真实配置不一致。
  - 产物是否能被目标问题击穿？ → YES。目标问题是关闭交火后普通向量 enabled 残留 true；现在同一个 setter 在 modeEnabled=false 时必然写入 false，且保存设置。除非绕过 setSummaryVectorIndexMode_ACU 直接改 globalMeta，否则正常 UI 与业务入口无法击穿。
  - 实质性比率: 100%。两个源码改动都直接作用于问题根因。
completeness_check:
  - 是否存在被跳过的模块/函数/路径？ → NO。交火模式 service setter、交火模式 UI handler、普通向量 UI 状态均已处理。
  - 产物覆盖范围是否与 execution_plan 中 affected_scope 完全一致？ → YES。src/service/settings/settings-service.ts、src/presentation/pages/popup-bindings-worldbook.ts、.analysis-cache.md 已覆盖；dist/index.bundle.js 由构建更新。
  - 核心业务逻辑是否都有直接验证？ → YES。npm run build 验证编译与架构；findstr 验证 settings-service.ts 存在 getCurrentVectorMemoryConfig_ACU import 和 vectorMemoryConfig.enabled = enabled；findstr 验证 popup-bindings-worldbook.ts 存在 worldbook-vector-memory-enabled 与 worldbook-vector-memory-config-block 同步逻辑。
  - affected_scope 覆盖率: 100%。
value_density_check:
  - 高价值内容与低价值内容比例: 高价值 4，低价值 0。高价值内容包括 service 状态源联动、UI checkbox 同步、UI config block 同步、手动填表按钮状态刷新。
  - 是否存在用数量掩盖质量的模式？ → NO。改动很少，但每一处都命中故障链路；没有增加无关包装。
alignment_check:
  - 产物满足的是用户的字面需求还是本质需求？ → 两者都满足。字面需求是关闭交火模式时关闭向量模式；本质需求是防止普通向量流程在退出交火后继续运行。service 层真实关闭 enabled，presentation 层同步显示状态。
  - 如果这是别人交给我的，我会接受吗？ → YES。修复在状态源头，不是只把 checkbox 取消勾选糊弄过去；构建和架构护栏通过。
self_critique:
  - 这次修复仍有一个设计层面的取舍：交火模式关闭会无条件关闭普通向量，即便用户在开启交火前本来手动启用了普通向量。按助手这次的紧急描述，这是正确的事故止血策略；如果未来要区分“用户本来开启”和“交火自动开启”，需要新增来源标记，而不是让 enabled 残留继续污染流程。能跑不等于安全，这次问题就是之前联动不对称留下的雷。
</output_quality_review>
