# 向量大总结提示词 500TK 一次性覆盖与 spv2.1.2 发布分析

```text
<reconnaissance>
goal: 确认向量大总结提示词中的 1000TK 引用、一次性刷新版本机制、构建产物状态与当前 tag 发布风险。
files_checked:
  - src/shared/defaults.ts
  - src/service/settings/settings-service.ts
  - package.json
  - index.js
  - dist/index.bundle.js
  - .analysis-cache.md
commands_checked:
  - git diff -- src\shared\defaults.ts index.js dist\index.bundle.js .analysis-cache.md
findings:
  - [`src/shared/defaults.ts`](src/shared/defaults.ts) 中向量默认配置 defaultVectorMemoryConfig_ACU.summaryPromptGroup 当前相对 HEAD 的 diff 显示两处从 500TK 改回 1000TK：system 提示词的“最终输出最高 1000TK”和 user 提示词的“请在 1000TK 以内输出”。
  - [`index.js`](index.js) 当前相对 HEAD 同样显示两处从 500TK 改回 1000TK，说明根目录发布文件已经不是 500TK 版本。
  - [`dist/index.bundle.js`](dist/index.bundle.js) 当前相对 HEAD 显示仍包含 500TK、spv2.1.2-vector-defaults 和 spv2.1.2-table-template-defaults，并包含默认表格模板一次性刷新逻辑；这是被 .gitignore 忽略的构建中间产物，不应作为最终提交依据。
  - [`src/service/settings/settings-service.ts`](src/service/settings/settings-service.ts) 已有向量配置一次性刷新逻辑：当 vectorConfig.defaultsRefreshVersion !== VECTOR_MEMORY_DEFAULTS_REFRESH_VERSION_ACU 时，会覆盖 archiveTriggerCount、archiveBatchSize、archiveMaxConcurrency、topK、minScore、summaryPromptGroup，并写入 defaultsRefreshVersion。
  - 当前只要更新 [`src/shared/defaults.ts`](src/shared/defaults.ts) 的 VECTOR_MEMORY_DEFAULTS_REFRESH_VERSION_ACU 为新值，并把 summaryPromptGroup 两处 1000TK 改为 500TK，就能触发已有用户的向量配置一次性覆盖。
  - 构建命令来自 [`package.json`](package.json)：npm run typecheck 和 npm run build:nocheck。
  - 侦察阶段尝试 git ls-remote --tags origin refs/tags/spv2.1.2 时 GitHub 连接被重置，远端 tag 当前状态暂未确认；同一命令链此前显示本地 HEAD 与本地 tag spv2.1.2 均指向 677764f9f57015f2d855069e3e4eb097327b7ca2。
  - 当前工作区已有脏文件：[` .analysis-cache.md`](.analysis-cache.md)、[`dist/index.bundle.js`](dist/index.bundle.js)、[`index.js`](index.js)、[`src/shared/defaults.ts`](src/shared/defaults.ts)。其中 [` .analysis-cache.md`](.analysis-cache.md) 是流程缓存，[`dist/index.bundle.js`](dist/index.bundle.js) 是忽略产物，最终提交应只包含源码与根目录 [`index.js`](index.js)。
</reconnaissance>
```

```text
<decision_point>
issue: 侦察阶段执行 `git ls-remote --tags origin refs/tags/spv2.1.2` 时 GitHub 连接被重置，导致远端 tag 状态未能确认。此前同一命令链中的本地状态已输出：工作区有 [` .analysis-cache.md`](.analysis-cache.md)、[`dist/index.bundle.js`](dist/index.bundle.js)、[`index.js`](index.js)、[`src/shared/defaults.ts`](src/shared/defaults.ts) 修改；本地 HEAD 与本地 tag [`spv2.1.2`](refs/tags/spv2.1.2) 均指向 `677764f9f57015f2d855069e3e4eb097327b7ca2`。
impact: YES。该问题不影响源码修改方案，但影响发布阶段的远端确认。不能在无法访问远端时宣称“已覆盖推送当前版本”；必须在发布阶段重试 push 和 ls-remote，并以远端返回为准。
context_update: analysis 中的 tag 侦察信息应记录为“本地 HEAD 与本地 tag 当前一致，远端查询暂因网络失败未确认”。execution_plan 需要保留远端 push 与 ls-remote 验证步骤，若再次失败则不能 attempt_completion 为已发布。
options:
  - option_a:
      description: 忽略远端查询失败，继续修改、提交并在最终汇报中声称 tag 已覆盖。
      approach_evaluation: 可维护性 1/5，因为发布状态不可追溯；健壮性 1/5，因为远端可能根本没更新；可扩展性 1/5，会污染发布流程可信度。
      edge_cases: 用户拉取 tag 时仍是旧版本；本地 tag 与远端 tag 分叉无人发现。
      affected_scope_delta: 无源码新增，但发布风险扩大。
  - option_b:
      description: 继续完成源码修改、构建、index 覆盖和本地提交；发布阶段执行 `git tag -f spv2.1.2 HEAD`、`git push origin spv2.1.2 --force`，随后重试 `git ls-remote --tags origin refs/tags/spv2.1.2` 验证远端。若远端仍失败，则停止并明确报告发布未确认。
      approach_evaluation: 可维护性 5/5，发布链路可审计；健壮性 5/5，不把网络失败包装成成功；可扩展性 5/5，保留标准 tag 发布流程。
      edge_cases: push 成功但 ls-remote 失败时，应报告“推送命令返回成功但远端验证失败”；push 失败时不得声称发布完成。
      affected_scope_delta: 无源码新增；发布步骤增加失败分支。
  - option_c:
      description: 放弃 tag 推送，只提交本地变更，让用户之后手动推送。
      approach_evaluation: 可维护性 2/5，偏离用户明确要求；健壮性 3/5，本地代码可靠但发布目标未完成；可扩展性 2/5，增加人工发布断点。
      edge_cases: 用户误以为 tag 已更新；当前版本分发仍旧。
      affected_scope_delta: 缩小发布范围。
recommendation: 选择 option_b。三维评估显示 option_b 在可维护性、健壮性、可扩展性上均为 5/5，是唯一既能继续任务又不伪造发布成功的方案。
execution_plan_update: 保持源码修改、构建、覆盖 [`index.js`](index.js)、提交变更、强制更新 tag 的步骤；在发布阶段必须重试远端 push 与 ls-remote 验证。若远端网络仍失败，最终不得报告“已覆盖推送”，只能报告源码与本地 tag 状态以及远端失败原因。
deviation_audit:
  original_plan_excerpt: - check: 检查当前 git status、当前 tag spv2.1.2 指向，确认发布目标仍是当前版本 tag 而不是 release 分支。
  current_proposal: 本地 status 与本地 tag 已确认；远端 tag 查询因网络失败暂未确认，发布阶段重试 push 和 ls-remote，并以远端验证结果决定最终结论。
  diff_summary: 新增远端网络失败分支；不减少源码修改范围；不跳过发布验证。
  deviation_motive_check:
    - 措辞替换规则逐类检查：全部未命中降级措辞。没有使用“当前约束下”“务实”等伪理性化措辞。
    - 偏离后的方案在三维评分上是否低于原方案？→ NO。增加远端失败分支提高健壮性。
    - 偏离是否导致 affected_scope 缩小？→ NO。仍覆盖源码、index、tag 与归档。
degradation_check:
  - 推荐方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。option_b 三项均为 5/5。
  - 推荐方案是否遗漏了新发现的边界条件？ → NO。覆盖 push 失败、ls-remote 失败、本地/远端 tag 分叉。
  - 是否因为想尽快完成而选择了改动量小的方案？ → NO。反而增加远端验证失败分支。
  - 修改后的execution_plan是否仍覆盖所有affected_scope？ → YES。源码、产物、tag、归档仍覆盖。
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO。网络失败已进入决策。
  - options 是否包含至少三个方案？ → YES。包含 option_a、option_b、option_c。
  - 是否因为工具报错而准备换用 shell 命令修改源代码？ → NO。该问题是 Git 远端查询，不涉及源码修改方式。
  - deviation_audit 是否触发了 self_dissection？ → NO。偏离是补充验证分支，不是降级。
</decision_point>
```

```text
<analysis>
context: 用户要求将向量大总结归档提示词中的 1000TK 限制改为 500TK，并进行一次性设置覆盖；同时用新生成的 [`dist/index.bundle.js`](dist/index.bundle.js) 覆盖根目录 [`index.js`](index.js)，最后覆盖推送当前版本 tag [`spv2.1.2`](refs/tags/spv2.1.2)。项目的 userscript 发布路径是执行 npm run build:nocheck 生成 [`dist/index.bundle.js`](dist/index.bundle.js)，再复制覆盖 [`index.js`](index.js)。向量默认配置定义在 [`src/shared/defaults.ts`](src/shared/defaults.ts) 的 defaultVectorMemoryConfig_ACU；已有用户配置的一次性覆盖由 [`src/service/settings/settings-service.ts`](src/service/settings/settings-service.ts) 中 loadSettings_ACU 内的 defaultsRefreshVersion 比较触发。当前源码和根目录 index 相对 HEAD 已被改回 1000TK，而 dist 中间产物残留 500TK；这是明显的源码/产物不一致状态，必须以源码为准重新修改、重新构建、重新覆盖 index。
needs: 本轮需要完成四件事。第一，在 [`src/shared/defaults.ts`](src/shared/defaults.ts) 中把 summaryPromptGroup 两处 1000TK 改为 500TK。第二，把 VECTOR_MEMORY_DEFAULTS_REFRESH_VERSION_ACU 从当前 spv2.1.2-vector-defaults 升级为新版本字符串，例如 spv2.1.2-vector-defaults-500tk，使已有用户 settings_ACU.vectorMemoryConfig.defaultsRefreshVersion 与新常量不相等，从而触发一次性覆盖 summaryPromptGroup。第三，执行类型检查和构建，并用新构建的 [`dist/index.bundle.js`](dist/index.bundle.js) 覆盖 [`index.js`](index.js)，确保源码与发布文件一致。第四，提交源码和 [`index.js`](index.js)，强制移动并推送 tag [`spv2.1.2`](refs/tags/spv2.1.2)，然后用远端查询验证。
key_challenges: 难点一，不能只改提示词文本而不改 VECTOR_MEMORY_DEFAULTS_REFRESH_VERSION_ACU；否则已有用户本地 vectorMemoryConfig.defaultsRefreshVersion 已等于旧版本，loadSettings_ACU 不会覆盖旧 summaryPromptGroup，用户仍会使用 1000TK。难点二，当前 [`dist/index.bundle.js`](dist/index.bundle.js) 与 [`src/shared/defaults.ts`](src/shared/defaults.ts) 状态相互矛盾，不能直接提交任何现有产物；必须重新构建后覆盖 [`index.js`](index.js)。难点三，一次性覆盖逻辑当前不仅覆盖 summaryPromptGroup，也会再次覆盖 archiveTriggerCount、archiveBatchSize、archiveMaxConcurrency、topK、minScore；这些值必须保持上次用户要求的 9、3、3、10、0.6，不能被本轮误改。难点四，远端 tag 查询曾因网络 reset 失败，发布阶段必须重试 push 和 ls-remote，不能把本地 tag 移动伪装成远端发布成功。难点五，500TK 仍然只是提示词级约束，不是 tokenizer 级硬截断；如果用户要求绝对 token 截断，需要另行改生成后裁剪或 tokenizer 逻辑。
confidence: HIGH
  - 已确认默认提示词唯一源码位置是 [`src/shared/defaults.ts`](src/shared/defaults.ts) 的 summaryPromptGroup 两处。
  - 已确认一次性刷新机制已经存在于 [`src/service/settings/settings-service.ts`](src/service/settings/settings-service.ts)，本轮只需变更版本常量即可触发。
  - 已确认构建命令和 userscript 发布路径。
  - 不确定项仅为远端 GitHub 网络可用性，已纳入发布失败分支。
approach: 三维评估综合最优的方案是：只修改 [`src/shared/defaults.ts`](src/shared/defaults.ts) 中向量默认刷新版本常量和 summaryPromptGroup 两处 TK 文本，不改 [`src/service/settings/settings-service.ts`](src/service/settings/settings-service.ts) 的迁移逻辑，因为现有逻辑已经覆盖本轮业务目标。推荐版本字符串为 `spv2.1.2-vector-defaults-500tk`，语义明确且能与旧版本区分。修改后执行 npm run typecheck 与 npm run build:nocheck，再用 Windows copy 命令把 [`dist/index.bundle.js`](dist/index.bundle.js) 覆盖 [`index.js`](index.js)。提交时只加入 [`src/shared/defaults.ts`](src/shared/defaults.ts) 和 [`index.js`](index.js)，不提交 [` .analysis-cache.md`](.analysis-cache.md) 和被忽略的 [`dist/index.bundle.js`](dist/index.bundle.js)。最后强制更新 tag [`spv2.1.2`](refs/tags/spv2.1.2) 并查询远端验证。
  三维评分（每个维度 1-5 分，5 为最优）：
  - 可维护性: 5/5 — 变更集中在默认配置源头，复用现有一次性刷新机制，不引入重复迁移函数。
  - 健壮性: 5/5 — 通过版本常量触发已有用户覆盖；通过 typecheck/build/findstr 验证源码和产物一致；通过远端 ls-remote 验证 tag。
  - 可扩展性: 4/5 — 后续默认提示词再变更时可继续递增版本常量；扣分点是当前刷新逻辑会把归档/召回参数与 summaryPromptGroup 绑定覆盖，未来如果要只刷新提示词，应拆分 defaultsRefreshVersion 的粒度。
edge_cases: 已有用户 vectorMemoryConfig.defaultsRefreshVersion 等于 spv2.1.2-vector-defaults 时，新版本 spv2.1.2-vector-defaults-500tk 会触发覆盖；新用户直接使用 500TK 默认提示词，无需迁移；用户自定义过 summaryPromptGroup 也会被本次一次性覆盖，因为用户明确要求“一次性设置覆盖”，这不是遗漏，但应在结果中说明；endpoint、apiKey、model、enabled、namespace、世界书 entryComment/entryKey 不会被覆盖，因为现有逻辑没有写这些字段；archiveTriggerCount、archiveBatchSize、archiveMaxConcurrency、topK、minScore 会再次被覆盖为默认值 9、3、3、10、0.6，必须确保这些默认值保持不变；如果 build 失败，不能覆盖 [`index.js`](index.js) 或推 tag；如果 copy 覆盖后 [`index.js`](index.js) 仍含 1000TK，不能提交；如果 push 或 ls-remote 失败，不能报告远端发布成功；[`dist/index.bundle.js`](dist/index.bundle.js) 即使变更也不提交，因为它被 .gitignore 忽略，用户要求提交的是覆盖后的根目录 [`index.js`](index.js)。
affected_scope: [`src/shared/defaults.ts`](src/shared/defaults.ts)；[`index.js`](index.js)；[`dist/index.bundle.js`](dist/index.bundle.js) 作为构建中间产物；[` .analysis-cache.md`](.analysis-cache.md)；[` .analysis-archive/`](.analysis-archive/)；Git tag [`spv2.1.2`](refs/tags/spv2.1.2)。[`src/service/settings/settings-service.ts`](src/service/settings/settings-service.ts) 是受依赖的迁移逻辑文件，本轮不修改，但它决定一次性覆盖是否生效。
execution_plan:
  - step_1: 使用编辑工具修改 [`src/shared/defaults.ts`](src/shared/defaults.ts)：将 VECTOR_MEMORY_DEFAULTS_REFRESH_VERSION_ACU 从 `spv2.1.2-vector-defaults` 改为 `spv2.1.2-vector-defaults-500tk`。
  - step_2: 使用编辑工具修改 [`src/shared/defaults.ts`](src/shared/defaults.ts)：将 summaryPromptGroup 两处 `1000TK` 改为 `500TK`，不改 archiveTriggerCount、archiveBatchSize、archiveMaxConcurrency、topK、minScore。
  - step_3: 执行 `npm run typecheck`，确认类型层面没有破坏默认配置导出和引用。
  - step_4: 执行 `npm run build:nocheck`，生成新的 [`dist/index.bundle.js`](dist/index.bundle.js)。
  - step_5: 用 `copy /Y dist\index.bundle.js index.js` 覆盖根目录 [`index.js`](index.js)。
  - step_6: 验证 [`index.js`](index.js) 包含 `spv2.1.2-vector-defaults-500tk` 和 `500TK`，且不再包含 `1000TK`。
  - step_7: 检查 git diff，确认将提交的只有 [`src/shared/defaults.ts`](src/shared/defaults.ts) 和 [`index.js`](index.js) 的目标变更；不提交 [` .analysis-cache.md`](.analysis-cache.md) 和 [`dist/index.bundle.js`](dist/index.bundle.js)。
  - step_8: 提交变更，提交信息使用 `chore: reduce vector summary prompt limit to 500tk`。
  - step_9: 执行 `git tag -f spv2.1.2 HEAD` 和 `git push origin spv2.1.2 --force`，随后执行 `git ls-remote --tags origin refs/tags/spv2.1.2` 验证远端 tag 指向新提交。
  - step_10: 读取 [` .analysis-cache.md`](.analysis-cache.md) 完成 output_quality_review，归档到 [` .analysis-archive/`](.analysis-archive/) 下的新文件。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。复用现有迁移机制，改动集中，验证链完整。
  - 是否遗漏了已知边界条件？ → NO。覆盖已有用户、新用户、自定义 summaryPromptGroup、连接配置保留、参数覆盖、构建失败、copy 后验证失败、远端网络失败和 dist 不提交。
  - 是否因为想尽快完成而选择了改动量小的方案？ → NO。不新增迁移函数是因为现有 [`settings-service.ts`](src/service/settings/settings-service.ts) 已完整覆盖需求，而不是偷工减料。
  - 是否打算跳过构建或产物覆盖？ → NO。build、copy、findstr 验证都在 execution_plan 中。
  - execution_plan 是否覆盖 affected_scope 所有文件？ → YES。源码、根 index、构建产物、分析缓存、归档、tag 均有对应步骤。
  - 是否有发现了但被判断为“无关紧要”而跳过的问题？ → NO。远端网络失败已写入 decision_point；源码/产物不一致已纳入执行计划。
  - 是否会用 shell 命令修改源码？ → NO。源码修改使用编辑工具；shell 只用于 typecheck、build、copy、git 与只读验证。
  - 是否存在 500TK 被误解为硬 token 限制的风险？ → YES。已明确这只是提示词级约束，最终汇报必须说明，不把它包装成 tokenizer 级硬限制。
</analysis>
```

```text
<decision_point>
issue: 覆盖 [`index.js`](index.js) 与产物验证命令返回 exit code 1，但输出显示 `copy /Y dist\index.bundle.js index.js` 已成功，`spv2.1.2-vector-defaults-500tk` 和两处 `500TK` 已找到；失败原因是最后一个 `findstr /C:"1000TK" index.js` 未找到匹配时按 Windows 约定返回 1。这里不是功能失败，而是验证命令把“未找到 1000TK 的期望结果”表现为非零退出码。
impact: YES。不能把这个 exit code 1 误判为覆盖失败，也不能跳过后续确认。需要用能区分“期望未找到”和“命令失败”的验证命令重新确认 [`index.js`](index.js) 不含 1000TK。
context_update: [`index.js`](index.js) 已被覆盖；当前需要补一次只读验证，明确 1000TK 不存在，并检查 git diff 待提交范围。
options:
  - option_a:
      description: 把 exit code 1 当成失败，重复 copy 和构建。
      approach_evaluation: 可维护性 2/5，重复无价值操作；健壮性 3/5，不会破坏但浪费流程；可扩展性 2/5，验证语义不清。
      edge_cases: 重复构建可能掩盖真正需要检查的 diff 范围。
      affected_scope_delta: 无新增范围。
  - option_b:
      description: 把 exit code 1 当成预期的“未找到 1000TK”，但仍执行明确的条件验证命令：找到 1000TK 则报错，否则输出确认；同时检查目标字符串。
      approach_evaluation: 可维护性 5/5，验证语义明确；健壮性 5/5，避免误判；可扩展性 5/5，后续同类负向搜索可复用。
      edge_cases: 如果文件损坏导致 findstr 读取异常，需要命令输出体现异常；如果 1000TK 出现在无关上下文，也会被拦截，需要人工判断。
      affected_scope_delta: 无新增范围。
  - option_c:
      description: 不再验证，直接进入提交。
      approach_evaluation: 可维护性 1/5，缺少发布前证据；健壮性 1/5，可能提交残留 1000TK；可扩展性 1/5，发布流程不可审计。
      edge_cases: 源码与 index 再次不一致。
      affected_scope_delta: 无新增范围但风险扩大。
recommendation: 选择 option_b。三维评估显示 option_b 在可维护性、健壮性、可扩展性上均为 5/5，且不改变源码。
execution_plan_update: step_5 可标记为完成，但 step_6 需改成条件式验证：确认 [`index.js`](index.js) 包含 `spv2.1.2-vector-defaults-500tk` 与 `500TK`，并显式输出“不含 1000TK”。
deviation_audit:
  original_plan_excerpt: - step_6: 验证 [`index.js`](index.js) 包含 `spv2.1.2-vector-defaults-500tk` 和 `500TK`，且不再包含 `1000TK`。
  current_proposal: 使用条件式 findstr 命令表达负向匹配预期，避免把“未找到 1000TK”误当作命令失败。
  diff_summary: 验证方式改变；验证目标不变；源码和产物范围不变。
  deviation_motive_check:
    - 措辞替换规则逐类检查：全部未命中降级措辞。
    - 偏离后的方案在三维评分上是否低于原方案？→ NO。验证语义更准确。
    - 偏离是否导致 affected_scope 缩小？→ NO。
degradation_check:
  - 推荐方案是否是三维评估综合最优的？ → YES。option_b 三项均 5/5。
  - 是否遗漏新发现的边界条件？ → NO。覆盖负向搜索返回 1 的 Windows 行为。
  - 是否因为想尽快完成而选择跳过验证？ → NO。明确拒绝 option_c。
  - 修改后的 execution_plan 是否仍覆盖所有 affected_scope？ → YES。
  - 是否有发现但被判断为“无关紧要”而跳过的问题？ → NO。exit code 1 已解释并重新验证。
  - options 是否包含至少三个方案？ → YES。
  - 是否因为工具返回非零而准备换用 shell 修改源码？ → NO。仅做只读验证。
  - deviation_audit 是否触发 self_dissection？ → NO。属于验证命令语义修正。
</decision_point>
```

```text
<decision_point>
issue: 本地提交与本地 tag 更新已成功，但 `git push origin spv2.1.2 --force` 因无法连接 GitHub 443 端口失败：`Could not connect to server`。因此远端 tag [`spv2.1.2`](refs/tags/spv2.1.2) 尚未确认更新。
impact: YES。当前状态只能证明本地提交 [`0ec2aa4`](./:1) 和本地 tag 已更新，不能证明“覆盖推送当前版本”完成。若此时结束并声称已发布，就是典型的虚假完成，漏洞明显得像是故意写给事故看的。
context_update: 发布步骤需要重试远端 push 与 ls-remote。源码、构建、[`index.js`](index.js) 覆盖已完成；剩余阻塞点是网络连接到 GitHub。
options:
  - option_a:
      description: 忽略 push 失败，最终汇报为已推送。
      approach_evaluation: 可维护性 1/5，发布状态不可审计；健壮性 1/5，用户下载仍可能是旧 tag；可扩展性 1/5，破坏发布流程可信度。
      edge_cases: 远端 tag 保持旧提交，用户以为已更新但实际没有。
      affected_scope_delta: 无新增源码范围，但发布风险失控。
  - option_b:
      description: 立即重试 `git push origin spv2.1.2 --force`，成功后执行 `git ls-remote --tags origin refs/tags/spv2.1.2` 验证远端指向。若仍失败，保留本地完成状态并报告远端发布阻塞。
      approach_evaluation: 可维护性 5/5，保留标准发布链；健壮性 5/5，以远端返回为准；可扩展性 5/5，失败分支清晰。
      edge_cases: 网络短暂恢复后成功；push 成功但 ls-remote 失败时需报告验证失败；push 多次失败时不能继续假装完成。
      affected_scope_delta: 无新增源码范围。
  - option_c:
      description: 改用其他远端或协议发布。
      approach_evaluation: 可维护性 2/5，未经用户授权改变发布目标；健壮性 2/5，可能推到错误位置；可扩展性 2/5，后续追踪混乱。
      edge_cases: SSH remote 未配置；误推 fork 或错误仓库。
      affected_scope_delta: 可能扩大到远端配置风险。
recommendation: 选择 option_b。三维评估显示 option_b 在可维护性、健壮性、可扩展性上均为 5/5，是唯一符合用户发布目标且不伪造结果的方案。
execution_plan_update: 保持提交 [`0ec2aa4`](./:1) 和本地 tag [`spv2.1.2`](refs/tags/spv2.1.2) 不变；重试远端 push 与 ls-remote。若失败，暂停最终完成汇报，说明网络阻塞。
deviation_audit:
  original_plan_excerpt: - step_9: 执行 `git tag -f spv2.1.2 HEAD` 和 `git push origin spv2.1.2 --force`，随后执行 `git ls-remote --tags origin refs/tags/spv2.1.2` 验证远端 tag 指向新提交。
  current_proposal: 已完成 tag -f；push 网络失败后重试 push 与 ls-remote，并以远端验证为最终发布依据。
  diff_summary: 新增网络失败重试分支；不改变发布目标；不缩小验证范围。
  deviation_motive_check:
    - 措辞替换规则逐类检查：全部未命中降级措辞。
    - 偏离后的方案在三维评分上是否低于原方案？→ NO。保持或提升发布健壮性。
    - 偏离是否导致 affected_scope 缩小？→ NO。
degradation_check:
  - 推荐方案是否是三维评估综合最优的？ → YES。option_b 三项均 5/5。
  - 是否遗漏新发现边界？ → NO。覆盖 push 失败、push 成功但 ls-remote 失败、多次网络失败。
  - 是否因为想尽快完成而跳过远端验证？ → NO。明确拒绝 option_a。
  - 修改后的 execution_plan 是否仍覆盖所有 affected_scope？ → YES。
  - 是否有发现但被判断为无关紧要而跳过的问题？ → NO。远端失败被列为阻塞点。
  - options 是否包含至少三个方案？ → YES。
  - 是否准备改远端配置绕过问题？ → NO。拒绝 option_c，除非用户授权。
  - deviation_audit 是否触发 self_dissection？ → NO。属于发布失败处理，不是降级。
</decision_point>
```

```text
<output_quality_review>
task_summary: 将向量大总结归档提示词的 1000TK 限制改为 500TK，通过刷新版本常量触发已有用户向量配置一次性覆盖，重新构建 userscript 并覆盖根目录 [`index.js`](index.js)，最后强制更新远端 tag [`spv2.1.2`](refs/tags/spv2.1.2)。
deliverables: 修改 [`src/shared/defaults.ts`](src/shared/defaults.ts)，将 VECTOR_MEMORY_DEFAULTS_REFRESH_VERSION_ACU 更新为 `spv2.1.2-vector-defaults-500tk`，并保证 defaultVectorMemoryConfig_ACU.summaryPromptGroup 两处提示词为 500TK；执行 `npm run typecheck` 与 `npm run build:nocheck`；用 [`dist/index.bundle.js`](dist/index.bundle.js) 覆盖 [`index.js`](index.js)；提交 [`0ec2aa451076438c7fcdab08ef8b538ae9a21ecb`](./:1)；强制更新远端 tag [`spv2.1.2`](refs/tags/spv2.1.2) 到该提交。
verification:
  - `npm run typecheck`: passed。
  - `npm run build:nocheck`: passed，生成 [`dist/index.bundle.js`](dist/index.bundle.js)。
  - `copy /Y dist\index.bundle.js index.js`: succeeded。
  - [`index.js`](index.js) 验证包含 `spv2.1.2-vector-defaults-500tk`。
  - [`index.js`](index.js) 验证包含两处 `500TK`。
  - [`index.js`](index.js) 条件验证不包含 `1000TK`。
  - `search_files` 对 `*.ts` 搜索 `1000TK` 返回 0 结果。
  - `git push origin spv2.1.2 --force`: retry succeeded。
  - `git ls-remote --tags origin refs/tags/spv2.1.2`: returned `0ec2aa451076438c7fcdab08ef8b538ae9a21ecb refs/tags/spv2.1.2`。
quality_notes:
  - 这次修复复用 [`src/service/settings/settings-service.ts`](src/service/settings/settings-service.ts) 已有的一次性覆盖机制，没有再塞一套重复迁移逻辑。重复造轮子不是严谨，是给后续维护者挖坑。
  - 一次性覆盖会覆盖 summaryPromptGroup，并按既有逻辑再次覆盖 archiveTriggerCount、archiveBatchSize、archiveMaxConcurrency、topK、minScore 为默认值 9、3、3、10、0.6；不会覆盖 endpoint、apiKey、model、enabled、namespace、世界书条目标识等连接与身份配置。
  - 500TK 是提示词级约束，不是 tokenizer 级硬截断。若模型不遵守提示词，仍可能超出；要硬限制需要另做生成后裁剪或 token 计数逻辑。
remaining_risk:
  - 当前刷新版本粒度仍是整体向量默认值刷新，summaryPromptGroup 与归档/召回默认参数绑定。以后若只想刷新提示词而不碰 topK/minScore 等参数，应该拆分刷新版本字段，例如 `summaryPromptDefaultsRefreshVersion`。
  - [`dist/index.bundle.js`](dist/index.bundle.js) 是被 .gitignore 忽略的中间产物，未提交；最终分发提交的是根目录 [`index.js`](index.js)。
</output_quality_review>
```
