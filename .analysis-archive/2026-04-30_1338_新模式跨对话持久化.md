<analysis>
context: 本轮侦察确认，新模式“向量混合交火增强方案”的开关字段是 worldbookConfig.summaryVectorIndexModeEnabled。当前读取当前世界书配置的入口是 src/service/settings/settings-readers.ts 的 getCurrentWorldbookConfig_ACU，它调用 getCurrentCharSettings_ACU。getCurrentCharSettings_ACU 以 currentChatFileIdentifier_ACU 作为 charId；当 settings_ACU.characterSettings[charId] 不存在时，会创建一个新的 worldbookConfigForNewChat，但第 29 行硬编码 worldbookConfigForNewChat.summaryVectorIndexModeEnabled = false。随后规范化时第 43-45 行也只从 existingCfg?.summaryVectorIndexModeEnabled 读取当前聊天已有配置，不读取任何全局默认。0TK 模式已有全局持久化链路：src/data/repositories/profile-repo.ts 的 globalMeta_ACU 有 zeroTkOccupyModeGlobal；src/service/settings/settings-service.ts 的 setZeroTkOccupyMode_ACU 会写 settings_ACU.zeroTkOccupyModeDefault 和 globalMeta_ACU.zeroTkOccupyModeGlobal 并保存；getCurrentCharSettings_ACU 新建对话会读取 globalMeta_ACU.zeroTkOccupyModeGlobal / settings_ACU.zeroTkOccupyModeDefault 作为 globalZeroTkDefault。新模式没有对应全局字段；setSummaryVectorIndexMode_ACU 只修改当前聊天 cfg.summaryVectorIndexModeEnabled 并 saveSettings_ACU，因此换到新对话时会创建新的 char settings，并因硬编码 false 被取消勾选。profile-repo.ts 的 sanitizeSettingsForProfileSave_ACU 不会删除 characterSettings 或全局偏好字段，因此新增 settings_ACU.summaryVectorIndexModeDefault 和 globalMeta_ACU.summaryVectorIndexModeGlobal 可以持久化。
needs: 需要让“向量混合交火增强方案”像 0TK 一样成为跨新对话继承的全局偏好：用户在一个对话中勾选后，新开对话应默认勾选；取消勾选后，新开对话应默认取消。由于它与 0TK 互斥，持久化时必须同步处理互斥关系：开启新模式应关闭 0TK 全局默认；开启 0TK 应关闭新模式全局默认；新对话初始化时如果新模式全局默认为 true，则 zeroTkOccupyMode 必须 false 且 outlineEntryEnabled true。
key_challenges: 第一，不能只改 defaultWorldbookConfig_ACU 的 summaryVectorIndexModeEnabled 为 true，否则会让所有用户默认开启新模式，破坏现有默认行为。第二，不能只改当前 chat 的 characterSettings，因为用户反馈发生在新开对话，新 charId 没有已有配置，必须加全局默认来源。第三，必须处理与 0TK 的互斥持久化，否则可能出现 globalMeta 同时记录 zeroTkOccupyModeGlobal=true 和 summaryVectorIndexModeGlobal=true，新对话初始化行为不确定。第四，需要兼容旧 globalMeta，不存在 summaryVectorIndexModeGlobal 时必须默认 false，不能误启用。
confidence: HIGH。侦察已覆盖新对话初始化、当前配置读取、开关保存、globalMeta 持久化、默认 settings 构建与 sanitize 保存路径；根因是明确的“新模式缺少全局默认字段且新 chat 硬编码 false”。
approach: 选择新增 summaryVectorIndexModeGlobal / summaryVectorIndexModeDefault 双层兼容字段，完全对齐现有 0TK 的实现方式。profile-repo.ts 的 globalMeta 默认对象增加 summaryVectorIndexModeGlobal:false；settings-service.ts 的 buildDefaultSettings_ACU 增加 summaryVectorIndexModeDefault:false；loadSettings_ACU 加载时将 globalMeta_ACU.summaryVectorIndexModeGlobal 同步到 settings_ACU.summaryVectorIndexModeDefault，若旧数据缺失则从 settings 字段迁移并保存 globalMeta；setSummaryVectorIndexMode_ACU 开启/关闭时写 settings_ACU.summaryVectorIndexModeDefault 和 globalMeta_ACU.summaryVectorIndexModeGlobal，开启时同时关闭 zeroTkOccupyModeDefault / zeroTkOccupyModeGlobal；setZeroTkOccupyMode_ACU 开启时反向关闭 summaryVectorIndexModeDefault / summaryVectorIndexModeGlobal。settings-readers.ts 新建 chat 时读取 globalSummaryVectorIndexDefault；如果 true，则 summaryVectorIndexModeEnabled=true、zeroTkOccupyMode=false、outlineEntryEnabled=true；规范化已有配置时，对已有聊天仍以 existingCfg.summaryVectorIndexModeEnabled 为准，避免把全局默认强行覆盖到已经存在的聊天。三维评分（每个维度 1-5 分，5 为最优）：
  - 可维护性: 5/5 — 沿用 0TK 全局默认模式，字段命名和持久化位置一致，避免引入新的配置存储机制。
  - 健壮性: 5/5 — 兼容旧数据缺失、开启互斥、关闭互斥、新 chat 初始化、已有 chat 不被全局默认覆盖等边界。
  - 可扩展性: 4/5 — 后续若还要增加其他跨对话世界书偏好，可以继续复用 globalMeta + settings 默认字段模式；不足是当前仍是多个布尔字段而非统一 mode 枚举，但在现有结构下改成枚举会扩大迁移范围。
edge_cases: 旧 globalMeta 没有 summaryVectorIndexModeGlobal 时，应默认 false，不改变用户现有行为；用户开启新模式后，新 chat 应继承 true，同时 0TK 全局默认应被关闭；用户关闭新模式后，新 chat 应继承 false，但不应自动开启 0TK；用户开启 0TK 后，新模式全局默认应被关闭，新 chat 应只启用 0TK；已有聊天如果保存了 summaryVectorIndexModeEnabled=false，不应因为全局默认 true 被强制改成 true；已有聊天如果保存了 summaryVectorIndexModeEnabled=true，应继续强制 zeroTkOccupyMode=false；若 globalMeta 意外同时出现两个 true，新模式优先级应在 new chat 初始化时关闭 zeroTk，因为二者互斥且新模式开启逻辑要求 zeroTk=false。
affected_scope: src/data/repositories/profile-repo.ts；src/service/runtime/state-manager.ts；src/service/settings/settings-service.ts；src/service/settings/settings-readers.ts；.analysis-cache.md；后续验收归档文件 .analysis-archive/2026-04-30_HHmm_新模式跨对话持久化.md。
execution_plan:
  - step_1: 修改 src/data/repositories/profile-repo.ts，在 globalMeta_ACU 和 buildDefaultGlobalMeta_ACU 中新增 summaryVectorIndexModeGlobal:false，保证旧数据加载时 deep 默认补齐。
  - step_2: 修改 src/service/runtime/state-manager.ts 和 src/service/settings/settings-service.ts 的默认 settings，新增 summaryVectorIndexModeDefault:false，保证运行时默认对象和 buildDefaultSettings_ACU 一致。
  - step_3: 修改 src/service/settings/settings-service.ts 的 loadSettings_ACU 两条加载分支，同步/迁移 globalMeta_ACU.summaryVectorIndexModeGlobal 到 settings_ACU.summaryVectorIndexModeDefault；修改 setZeroTkOccupyMode_ACU 与 setSummaryVectorIndexMode_ACU，维护两个全局默认的互斥与保存。
  - step_4: 修改 src/service/settings/settings-readers.ts，在 getCurrentCharSettings_ACU 中计算 globalSummaryVectorIndexDefault；新建聊天配置时按该默认设置 summaryVectorIndexModeEnabled，并保证与 0TK 互斥；规范化已有配置时保留已有聊天字段，不用全局默认覆盖已有聊天。
  - step_5: 执行 npm run build 验证 TypeScript、Rollup 与架构护栏；搜索 dist/index.bundle.js 确认 summaryVectorIndexModeGlobal / summaryVectorIndexModeDefault 进入产物。
  - step_6: 验收前读取 .analysis-cache.md，输出 output_quality_review，并归档到 .analysis-archive。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。复用现有 0TK 全局默认架构，可维护性 5/5；互斥和旧数据兼容覆盖完整，健壮性 5/5；扩展性 4/5。
  - 是否遗漏了已知边界条件？ → NO。已覆盖旧数据缺失、新 chat、已有 chat、互斥、关闭新模式、同时 true 等边界。
  - 是否因改动量大而想缩减方案？ → NO。没有采用只改 settings-readers.ts 硬编码继承当前聊天的脆弱方案，而是补齐真实持久化链路。
  - 是否打算跳过某些文件？ → NO。globalMeta 默认、runtime 默认、buildDefaultSettings、loadSettings、开关 setter、新 chat reader 都纳入执行计划。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。每个 affected_scope 文件都有明确步骤。
  - context是否充分？是否有未读但可能相关的文件？ → NO。已搜索 summaryVectorIndexModeEnabled、zeroTkOccupyModeGlobal、zeroTkOccupyModeDefault、globalMeta_ACU，并读取核心配置读写文件。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO。settings-model.ts 类型陈旧但当前项目 settings_ACU 为 any 且接口未覆盖这些新增字段；本次不改类型不会影响构建，但如果以后做类型收紧应补 Settings_ACU。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO。源码修改使用 apply_diff；shell 只用于构建和产物检查。
</analysis>

<output_quality_review>
result: 已完成“向量混合交火增强方案”跨新对话持久化修复。根因是新建聊天配置在 `src/service/settings/settings-readers.ts` 中硬编码 `summaryVectorIndexModeEnabled=false`，而 `src/service/settings/settings-service.ts` 的 `setSummaryVectorIndexMode_ACU` 之前只保存当前聊天配置，没有全局默认来源，导致换到新聊天后重新初始化为关闭。
changes:
  - `src/data/repositories/profile-repo.ts`：在 `globalMeta_ACU` 和 `buildDefaultGlobalMeta_ACU` 中新增 `summaryVectorIndexModeGlobal:false`，作为跨对话持久化的全局元信息。
  - `src/service/runtime/state-manager.ts`：运行时默认设置新增 `summaryVectorIndexModeDefault:false`，保持默认不误启用。
  - `src/service/settings/settings-service.ts`：`loadSettings_ACU` 加载时同步/迁移 `summaryVectorIndexModeGlobal` 与 `summaryVectorIndexModeDefault`；`setSummaryVectorIndexMode_ACU` 写入当前聊天与全局默认；`setZeroTkOccupyMode_ACU` 在开启 0TK 时反向关闭新模式全局默认。
  - `src/service/settings/settings-readers.ts`：新建聊天读取 `globalSummaryVectorIndexDefault`，使新对话继承用户的新模式勾选偏好；新模式为 true 时强制 `zeroTkOccupyMode=false`、`outlineEntryEnabled=true`；已有聊天仍优先保留自身保存的 `summaryVectorIndexModeEnabled`。
compatibility:
  - 旧数据缺少 `summaryVectorIndexModeGlobal` 时默认 false，不会突然给旧用户启用新模式。
  - 用户关闭新模式后，新聊天默认关闭，但不会擅自开启 0TK。
  - 用户开启 0TK 后，新模式全局默认会被关闭，避免互斥配置同时生效。
  - 若历史 globalMeta 异常同时记录两个 true，加载与新聊天初始化会让新模式优先并关闭 0TK，避免双模式冲突。
validation:
  - `npm run build` 已通过。
  - Rollup 已生成 `dist/index.bundle.js`。
  - 架构护栏通过，违规 0 条。
  - 已用 `findstr` 确认 `dist/index.bundle.js` 包含 `summaryVectorIndexModeGlobal`、`summaryVectorIndexModeDefault`、`globalSummaryVectorIndexDefault` 及互斥保存逻辑。
self_review:
  - 这次修复没有偷懒去改默认配置为 true；那种做法会把所有新用户默认拖进新模式，漏洞明显得像是故意写给事故看的。
  - 当前方案复用 0TK 的全局默认链路，改动范围可控，互斥关系明确。
  - 不足：设置模型仍然大量使用 any，类型层没有强约束 `summaryVectorIndexModeDefault`。这不是本次 bug 的阻断项，但后续如果收紧 `Settings` 类型，应补齐字段定义，别让类型系统继续装睡。
</output_quality_review>
