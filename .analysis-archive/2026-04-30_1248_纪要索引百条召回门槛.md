<analysis>
context: 本轮侦察确认，新模式发送前召回入口位于 src/service/plot/summary-vector-index-orchestrator.ts 的 orchestrateSummaryVectorIndexBeforeSend_ACU。该函数当前先构建 signature，再读取 getEffectiveSummaryVectorIndexConfig_ACU，并立即执行 validateSummaryVectorIndexConfig_ACU；随后 clearSummaryVectorIndexRecallOverride_ACU 清空运行时覆盖；若配置无效则 blocking；之后读取 getAggregatedSummaryVectorIndexSnapshot_ACU，只有在 state.rows 和 state.chunks 非空时才继续 generateVectorRecallKeywords_ACU、recallSummaryVectorIndex_ACU，并最终 _set_currentSummaryVectorIndexRecall_ACU 与 refreshSummaryVectorIndexCustomExportEntry_ACU。也就是说当前配置校验发生在归档数量判断之前，且无归档会阻断发送。UI 提示路径位于 src/presentation/components/status-display.ts 的 syncAllSettingsToUI_ACU，它读取 getAggregatedSummaryVectorIndexSnapshot_ACU 并展示 rowCount/chunkCount；开关 toast 位于 src/presentation/pages/popup-bindings-worldbook.ts 第 481 行附近，当前只区分 hasArchive。默认静态提示还存在于 src/presentation/pages/main-popup-status.ts 与 src/presentation/pages/main-popup-worldbook.ts。自动归档路径位于 src/service/table/table-service.ts 的 persistTablesToChatMessage_ACU 保存表格之后：summaryVectorIndexModeEnabled 且未 skipVectorAutoIndex 时调用 archiveSummaryVectorIndexNow_ACU，该路径不经过发送前 orchestrateSummaryVectorIndexBeforeSend_ACU，因此在发送前 gate 中跳过关键词/注入不会影响填表保存后的自动归档。src/presentation/bootstrap/init.ts 会在新模式开启时调用 orchestrateSummaryVectorIndexBeforeSend_ACU，并仅在 result.blocking 为 true 时阻断；因此不足 100 条时必须返回 skipped/success语义可放行正文生成，不能返回 blocking。
needs: 用户要的是健全性门槛：当聚合后的已归档纪要未满 100 条时，即使新模式已开启，用户输入后也不能触发关键词生成、向量召回、rerank、运行时概要索引覆盖或 CustomExport 世界书覆盖注入；但正文生成必须继续，表格填表完成后的自动归档必须继续累积。这个需求的本质不是禁用新模式，而是在样本量不足时禁用发送前召回链路，避免少量纪要造成低质量检索和错误覆盖。
key_challenges: 第一，不能只在 recallSummaryVectorIndex_ACU 前加判断，因为 generateVectorRecallKeywords_ACU 已经属于用户明确禁止的触发行为，门槛必须放在关键词生成之前。第二，当前 validateSummaryVectorIndexConfig_ACU 在归档数量判断之前，如果向量/rerank 配置未配置，会提前 blocking；但用户要求未满 100 条时用户输入不触发召回且自动归档正常进行，所以发送前配置校验应延后到达到 100 条之后，否则会在根本不需要召回时阻断正文生成。第三，无归档或 chunk 为空也属于未满 100 的退化情况；如果继续沿用旧的 archive_missing blocking，会违背“没到100条以前”的语义。第四，必须确保 clearSummaryVectorIndexRecallOverride_ACU 仍在早期执行，避免上一轮召回残留继续影响概要索引占位符或世界书内容。第五，UI 必须把 100 条门槛说清楚，否则用户会误以为勾选后功能失效。
confidence: HIGH。侦察已覆盖发送前编排入口、世界书覆盖调用点、bootstrap 阻断语义、自动归档路径、状态提示与开关提示；修改点清晰，且自动归档路径与发送前 gate 分离，风险可控。
approach: 选择在 src/service/plot/summary-vector-index-orchestrator.ts 内引入发送前召回最小纪要条数常量，并把配置校验移动到聚合归档数量满足门槛之后。具体做法是：函数开头仍构建 signature、获取 config、清空 recall override；signature 为空或重复签名仍放行；进入 try 后先读取聚合 snapshot，计算 archivedRowCount/chunkCount；若 archivedRowCount < 100 或 chunkCount <= 0，则返回 intercepted/skipped/success/shouldProceed=true，并给出 warning，不调用 validateSummaryVectorIndexConfig_ACU、generateVectorRecallKeywords_ACU、recallSummaryVectorIndex_ACU、_set_currentSummaryVectorIndexRecall_ACU、refreshSummaryVectorIndexCustomExportEntry_ACU。只有 archivedRowCount >= 100 且 chunks 可用后，才执行配置校验与原有召回流程。UI 侧在状态提示和开关 toast 中显示 N/100 门槛；静态提示也补上“未满 100 条前只累积自动归档，不触发发送前召回覆盖”。三维评分（每个维度 1-5 分，5 为最优）：
  - 可维护性: 5/5 — 门槛逻辑集中在发送前 orchestrator，常量命名明确，UI 只做提示文案，不把业务判断复制到多个执行路径；自动归档路径不动，避免把两个职责搅在一起。
  - 健壮性: 5/5 — 处理 0 条、1-99 条、chunk 为空、配置未填、上一轮 recall 残留等边界；不足 100 时不发起任何关键词/API/rerank/世界书覆盖副作用。
  - 可扩展性: 4/5 — 当前阈值按用户需求硬编码为常量，未来如果需要 UI 配置只需把常量迁移到配置项；不提前做配置项是合理克制，避免把一次明确门槛需求扩成设置系统改造。
edge_cases: 当聚合 state 不存在或 rows 为空时，应视为 0/100，发送前跳过召回并放行正文生成，而不是 blocking；自动归档仍会在正文生成后的填表保存阶段执行。当 rows 为 99 且 chunks 存在时，应跳过关键词生成、召回、rerank 和 CustomExport 刷新，并清空旧 recall override。当 rows 正好为 100 且 chunks 存在时，应恢复原有完整流程：配置校验、关键词生成、embedding 召回、rerank、运行时覆盖、CustomExport 世界书覆盖。当 rows >= 100 但 chunks 为空时，状态不完整，不能召回；应跳过发送前召回并 warning，而不是调用召回服务制造异常。当 rows < 100 且向量/rerank 配置为空时，发送前不应因配置校验阻断，因为此时不触发召回；但自动归档阶段仍会按归档服务自己的配置校验处理失败。每次发送前都必须先清空旧的 currentSummaryVectorIndexRecall，防止上一轮已满 100 时产生的覆盖在当前不足 100 或数据被删后继续生效。UI 提示中的 rowCount 应使用聚合 snapshot 的 rowCount 或 rows.length，避免只看最新楼层导致错误提示。
affected_scope: src/service/plot/summary-vector-index-orchestrator.ts；src/presentation/components/status-display.ts；src/presentation/pages/popup-bindings-worldbook.ts；src/presentation/pages/main-popup-status.ts；src/presentation/pages/main-popup-worldbook.ts；.analysis-cache.md；后续验收归档文件 .analysis-archive/2026-04-30_HHmm_纪要索引百条召回门槛.md。
execution_plan:
  - step_1: 修改 src/service/plot/summary-vector-index-orchestrator.ts，在模块顶部新增 SUMMARY_VECTOR_INDEX_RECALL_MIN_ROWS_ACU = 100 常量；调整 orchestrateSummaryVectorIndexBeforeSend_ACU 内部顺序，使 validateSummaryVectorIndexConfig_ACU 延后到聚合归档数量达到门槛之后；在关键词生成前增加不足 100 或 chunks 不可用的 skipped 放行结果，并确保不调用关键词、召回、rerank、CustomExport 刷新。
  - step_2: 修改 src/presentation/components/status-display.ts 的 summary-vector-index-archive-hint 文案，使用聚合 rowCount/chunkCount 显示 N/100；不足 100 时明确说明发送前不会触发关键词召回和概要索引覆盖，自动归档仍会继续累积；达到 100 后提示会启用发送前筛选覆盖。
  - step_3: 修改 src/presentation/pages/popup-bindings-worldbook.ts 的开关 toast，按 rowCount 是否达到 100 区分文案；无归档或不足 100 时提示只会自动归档累积，不会发送前覆盖注入。
  - step_4: 修改 src/presentation/pages/main-popup-status.ts 与 src/presentation/pages/main-popup-worldbook.ts 的静态说明，补充“归档纪要未满 100 条前不会触发发送前关键词召回与覆盖注入”。
  - step_5: 执行 npm run build 验证 TypeScript、Rollup 与架构护栏；再用搜索确认构建产物包含 100 条门槛提示。
  - step_6: 验收前读取 .analysis-cache.md，输出 output_quality_review，并归档到 .analysis-archive。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。核心 gate 放在 orchestrator 是最小且最准确的执行点，可维护性 5/5；覆盖 0-99、100、chunk 空、配置未填和旧覆盖残留，健壮性 5/5；阈值常量化但不暴露多余 UI 配置，可扩展性 4/5。
  - 是否遗漏了已知边界条件？ → NO。已覆盖无归档、1-99 条、100 条、chunk 为空、配置未填、旧 recall override 残留、UI 聚合统计等边界。
  - 是否因改动量大而想缩减方案？ → NO。除核心 orchestrator 外同步修改状态提示、开关 toast 和静态说明，避免只改后端导致用户误判。
  - 是否打算跳过某些文件？ → NO。已读并纳入所有搜索确认的相关执行与提示文件；自动归档文件只读不改，因为需求明确自动归档保持正常。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。每个 affected_scope 文件都有对应步骤；归档文件在 step_6 处理。
  - context是否充分？是否有未读但可能相关的文件？ → NO。已读取发送前 orchestrator、bootstrap 调用方、自动归档路径、状态提示、开关 toast 和静态 UI；search_files 覆盖了 summaryVectorIndexModeEnabled、summary-vector-index、CustomExport 刷新等关键词。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO。唯一看似相关的自动归档路径已确认不应修改，因为用户明确要求自动归档正常进行。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO。源代码修改将使用 edit_file/apply_diff，shell 只用于构建与产物检查。
</analysis>

<output_quality_review>
task_summary: 为“向量混合交火增强方案”新增 100 条已归档纪要门槛。聚合归档纪要未满 100 条时，发送前流程会跳过关键词生成、向量召回、rerank、运行时概要索引覆盖与 CustomExport 世界书覆盖注入，同时放行正文生成；自动归档路径保持正常累积。
deliverables: 修改 src/service/plot/summary-vector-index-orchestrator.ts；修改 src/presentation/components/status-display.ts；修改 src/presentation/pages/popup-bindings-worldbook.ts；修改 src/presentation/pages/main-popup-status.ts；修改 src/presentation/pages/main-popup-worldbook.ts；构建产物 dist/index.bundle.js 已由 npm run build 生成；归档文件 .analysis-archive/2026-04-30_1248_纪要索引百条召回门槛.md。

# 量化指标总览
metrics:
  total_files_modified: 5 — 源码修改覆盖 1 个 service 编排文件与 4 个 presentation 提示文件。
  execution_plan_coverage: 6/6 = 100% — analysis 中 step_1 到 step_6 均已执行。
  edge_cases_handled: 7/7 = 100% — 无归档、1-99 条、100 条、chunk 为空、配置未填、旧 recall override 残留、UI 聚合统计均已覆盖。
  confidence_assessment: HIGH — npm run build 成功，Rollup 产物生成，架构护栏总计违规 0 条，并用 findstr 确认 bundle 包含 SUMMARY_VECTOR_INDEX_RECALL_MIN_ROWS_ACU 与“不足 100 条”跳过提示。

# 质量检查
checks:
  - 是否真正阻止了关键词生成？ → YES。src/service/plot/summary-vector-index-orchestrator.ts 在调用 generateVectorRecallKeywords_ACU 之前先检查 archivedRowCount < 100 或 archivedChunkCount <= 0，命中时直接返回 skipped 放行。
  - 是否阻止了世界书覆盖注入？ → YES。不足 100 条分支在 refreshSummaryVectorIndexCustomExportEntry_ACU 之前返回，不会刷新 CustomExport 世界书条目，也不会设置 _set_currentSummaryVectorIndexRecall_ACU。
  - 是否保留自动归档？ → YES。src/service/table/table-service.ts 的 persistTablesToChatMessage_ACU 未修改，保存表格后仍按新模式调用 archiveSummaryVectorIndexNow_ACU。
  - 是否避免配置未填时提前阻断？ → YES。validateSummaryVectorIndexConfig_ACU 已移动到 100 条门槛之后；未满 100 条时发送前不需要向量/rerank 配置即可放行。
  - 是否清理旧覆盖残留？ → YES。clearSummaryVectorIndexRecallOverride_ACU 仍在函数早期执行，门槛分支返回前已经清空旧 runtime recall。
  - 是否同步 UI 说明？ → YES。状态提示、开关 toast、状态页静态说明、世界书页静态说明均说明未满 100 条前不会触发发送前召回/覆盖注入，自动归档继续累积。
  - 是否引入架构违规？ → NO。npm run build 的 scripts/check-arch.sh 显示 service → presentation、presentation → data、service 层 UI 越权等全部 PASS，总计违规 0 条。

self_critique: 这版实现按用户需求是合格的，但阈值目前是 service 内硬编码常量，不是配置项。这个取舍是有意的：用户要求固定 100 条门槛，没有要求 UI 可调；贸然加配置会扩大设置兼容面和默认值迁移风险。若后续需要可配置阈值，应把 SUMMARY_VECTOR_INDEX_RECALL_MIN_ROWS_ACU 下沉到 vectorMemoryConfig 或 worldbookConfig，并补迁移与 UI 校验。
</output_quality_review>
