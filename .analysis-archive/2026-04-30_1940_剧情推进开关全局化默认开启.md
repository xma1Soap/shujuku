<analysis>
context: 侦察确认上一轮把 src/shared/defaults-json.js:142 的 DEFAULT_PLOT_SETTINGS_ACU.enabled 改成了 false，这确实违背了助手这次指出的产品语义：剧情推进预设默认应该打开。当前剧情推进总开关运行时仍是 settings_ACU.plotSettings.enabled，发送前硬门禁在 src/service/runtime/state-manager.ts:113-116，plot-orchestrator.ts 也会检查 settings_ACU.plotSettings.enabled。UI 开关在 src/presentation/pages/popup-bindings-plot.ts:31-33 直接写 settings_ACU.plotSettings.enabled 并 saveSettingsAndNotify_ACU()。预设/聊天快照链路已经把 enabled 从预设和聊天快照中删除：src/service/template/chat-scope/chat-scope-plot.ts:20-22 删除 snapshot.enabled，src/presentation/pages/popup-helpers.ts:491-493 保存预设时删除 currentSettings.enabled。上一轮在 src/service/plot/plot-logic.ts:423-498 对 applyPlotPresetToSettings_ACU、resetPlotSettingsToDefault_ACU、replaceCurrentPlotSettingsWithSnapshot_ACU 做了 preservedEnabled 保护，避免预设/快照恢复覆盖总开关。问题剩余点是：enabled 仍只存在 settings_ACU.plotSettings 里，没有进入 globalMeta_ACU 这种明确跨 profile/跨对话的全局元状态；刷新/新对话时如果 settings/profile/default 合并链路里 enabled 缺失或被默认值回填，就可能不符合“全局设置为关就始终关，全局设置为开就始终开”。globalMeta 当前在 src/data/repositories/profile-repo.ts:13-20 只有 zeroTkOccupyModeGlobal 和 summaryVectorIndexModeGlobal，没有 plotEnabledGlobal。设置加载在 src/service/settings/settings-service.ts:163-205 的有已保存配置分支，以及 227-253 的无保存配置分支中处理 0TK/summaryVectorIndex 的全局偏好，但没有处理 plot enabled。默认 settings 在 src/service/settings/settings-service.ts:468-469 和 src/service/runtime/state-manager.ts:160 都来自 DEFAULT_PLOT_SETTINGS_ACU，因此只要默认改回 true，新安装/缺省会默认开启。deepMerge_ACU 会用 source 覆盖 target；如果旧设置里 plotSettings.enabled=false，深合并会保留 false，但这不是全局元语义，而是 profile/settings 内字段语义。
needs: 用户的本质需求是把剧情推进“总开关”从预设/对话状态中剥离为全局配置，同时默认值为开启。具体要满足：第一，新安装或没有任何全局记录时，剧情推进默认开启；第二，用户手动关掉后，该关闭状态写入全局配置，刷新页面、新开对话、切换预设、恢复聊天快照都不能把它打开；第三，用户手动打开后，该开启状态写入全局配置，同样跨刷新/新对话保持开启；第四，预设和聊天快照仍不携带 enabled，避免“某个预设把总开关覆盖掉”。
key_challenges: 核心难点不是把 DEFAULT_PLOT_SETTINGS_ACU.enabled 改回 true，那只是表面修补。真正要处理的是状态源头：当前 UI 写 settings_ACU.plotSettings.enabled，而加载链路没有把它投影到 globalMeta_ACU，也没有在初始化后强制用 globalMeta 覆盖 settings。若只改默认值，旧对话或缺字段场景仍可能被预设/默认合并影响；若只写 globalMeta 而不保留旧 profile false 的迁移，又会把已经关闭过剧情推进的用户在更新后重新打开，直接复现用户抱怨。另一个边界是 resetPlotSettingsToDefault_ACU 当前 preservedEnabled = plotSettings.enabled === true；如果默认改回 true，但当前全局关，这个函数仍会保留 false，符合要求；不过最稳的是加载时统一由 globalMeta 投影到 plotSettings.enabled。
confidence: HIGH。已确认默认值位置、UI 写入位置、运行时门禁、预设保存删除 enabled、聊天快照删除 enabled、预设/快照恢复保护 enabled、globalMeta 缺少 plot 全局字段、settings 加载两个分支的位置。方案依据充分，修改范围明确。
approach: 三维评估综合最优的方案是新增 globalMeta_ACU.plotEnabledGlobal，默认 true；加载 globalMeta 时缺失该字段则从已有 settings_ACU.plotSettings.enabled 迁移，若旧设置明确 false 就保留 false，否则默认 true；之后每次设置加载都强制 settings_ACU.plotSettings.enabled = globalMeta_ACU.plotEnabledGlobal === true。UI 开关变更时同时写 settings_ACU.plotSettings.enabled 和 globalMeta_ACU.plotEnabledGlobal，并 saveGlobalMeta_ACU()。同时把 DEFAULT_PLOT_SETTINGS_ACU.enabled 改回 true。这样默认开启、用户全局选择优先、预设/聊天快照不覆盖三者同时成立。
  三维评分（每个维度 1-5 分，5 为最优）：
  - 可维护性: 5/5 — plotEnabledGlobal 与已有 zeroTkOccupyModeGlobal/summaryVectorIndexModeGlobal 模式一致，加载时投影、UI 写入时持久化，状态源头清晰。
  - 健壮性: 5/5 — 覆盖新安装、旧设置迁移、已保存配置、无保存配置、刷新页面、新开对话、预设应用、快照恢复等路径；不会依赖“默认值应该碰巧正确”这种脆弱假设。
  - 可扩展性: 4/5 — 后续如果还有全局总开关，可沿用同一 globalMeta 投影模式。扣 1 分是因为 settings_ACU.plotSettings.enabled 仍作为运行时投影存在，未完全重命名为只读派生字段，但这是为了兼容现有运行时门禁。
edge_cases: 新安装且 globalMeta 没有 plotEnabledGlobal 时应默认 true；旧用户如果 settings_ACU.plotSettings.enabled 明确为 false，应迁移为 globalMeta_ACU.plotEnabledGlobal=false，不能升级后强行打开；旧用户如果 settings_ACU.plotSettings.enabled 缺失或非 boolean，应迁移为 true；用户在 UI 关闭后必须写入 globalMeta，刷新后 settings_ACU.plotSettings.enabled 被 globalMeta 覆盖为 false；用户在 UI 打开后必须写入 globalMeta，刷新后为 true；应用剧情预设时不能改变 enabled；重置剧情设置时不能改变 enabled；聊天快照恢复时不能改变 enabled；保存预设和聊天快照仍应删除 enabled，避免它重新变成预设/对话字段。
affected_scope: src/shared/defaults-json.js；src/data/repositories/profile-repo.ts；src/service/settings/settings-service.ts；src/presentation/pages/popup-bindings-plot.ts；.analysis-cache.md；后续构建会更新 dist/index.bundle.js。
execution_plan:
  - step_1: 修改 src/shared/defaults-json.js，将 DEFAULT_PLOT_SETTINGS_ACU.enabled 从 false 改回 true，恢复“默认打开”的产品语义。
  - step_2: 修改 src/data/repositories/profile-repo.ts，在 globalMeta_ACU 默认对象和 buildDefaultGlobalMeta_ACU() 中新增 plotEnabledGlobal: true，让全局元信息拥有明确的剧情推进总开关默认值。
  - step_3: 修改 src/service/settings/settings-service.ts，新增 normalize/apply 剧情推进全局开关的小函数：当 globalMeta_ACU.plotEnabledGlobal 是 boolean 时投影到 settings_ACU.plotSettings.enabled；当缺失时从 settings_ACU.plotSettings.enabled 迁移，只有旧值明确 false 才迁移 false，否则迁移 true，并保存 globalMeta。然后在已保存配置分支和无保存配置分支的 plotSettings 兜底后调用。
  - step_4: 修改 src/presentation/pages/popup-bindings-plot.ts，剧情推进 UI 总开关 change 时同时写 settings_ACU.plotSettings.enabled、globalMeta_ACU.plotEnabledGlobal，并调用 saveGlobalMeta_ACU() 与 saveSettingsAndNotify_ACU()。
  - step_5: 执行 npm run build，验证 Rollup 与架构护栏通过。
  - step_6: 读取/搜索关键文件，确认 DEFAULT_PLOT_SETTINGS_ACU.enabled=true、globalMeta 默认 true、加载投影函数存在、UI 写 globalMeta、预设/聊天快照仍删除 enabled、plot-logic preservedEnabled 仍存在。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。该方案不是只改默认值，而是建立 globalMeta 单一全局状态源并在 settings 中投影，三维评分 5/5、5/5、4/5。
  - 是否遗漏了已知边界条件？ → NO。已覆盖新安装默认开、旧用户明确关迁移为关、缺失字段默认开、UI 开/关持久化、预设/快照不覆盖、刷新/新对话投影。
  - 是否因改动量大而想缩减方案？ → NO。没有只改 defaults 这种半修复；按状态源头、加载、UI 写入三条链路完整处理。
  - 是否打算跳过某些文件？ → NO。profile-repo、settings-service、popup-bindings-plot、defaults-json 都在 affected_scope；plot-logic 和 chat-scope-plot 已侦察确认保留现有删除/保护逻辑即可。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。step_1 到 step_4 覆盖源码，step_5 到 step_6 覆盖构建和验证，.analysis-cache.md 已写入本 analysis。
  - context是否充分？是否有未读但可能相关的文件？ → YES。已读默认值、globalMeta、settings 加载、UI 绑定、预设/快照恢复、运行时门禁。未读完整 dist 不影响方案，构建后再验证。
  - 是否有发现了但被我判断为“无关紧要”而跳过的问题？ → NO。上一轮 preservedEnabled 逻辑不是问题，保留是正确的；真正缺口是全局状态源和默认值，已纳入计划。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO。源码修改使用 apply_diff/edit_file；shell 仅用于构建和只读验证。
</analysis>

<decision_point>
issue: 构建在 Rollup 成功后被架构护栏拦截，具体违规是 src/presentation/pages/popup-bindings-plot.ts:5 直接 import src/data/repositories/profile-repo.ts 的 globalMeta_ACU 和 saveGlobalMeta_ACU，形成 presentation → data 依赖。这个问题不是格式洁癖，而是分层边界被刺穿：presentation 层一旦直接掌握 data repository，就会让 UI 事件绕过 service 业务边界，后续任何全局状态迁移、兼容投影、保存策略调整都可能被 UI 侧散落调用破坏。
options:
  - option_1: 在违规 import 行末尾加 // arch-ok 让护栏通过。可维护性 1/5，健壮性 1/5，可扩展性 1/5。理由：这是用豁免掩盖错误依赖方向，不解决 presentation 越层写 data 的根因；后续剧情推进全局开关如果需要迁移或联动，UI 侧仍会直接污染 data 状态。结论：拒绝。
  - option_2: 保持 presentation 直接 import data，并期望架构脚本未来放宽。可维护性 1/5，健壮性 1/5，可扩展性 1/5。理由：项目已有明确三层护栏，且现有 0TK/向量混合交火增强方案全局开关就是通过 service/settings/settings-service.ts 暴露 setter 给 presentation 调用；剧情推进开关应遵守同一模式。结论：拒绝。
  - option_3: 在 src/service/settings/settings-service.ts 新增 setGlobalPlotEnabled_ACU(enabled: boolean)，由 service 层封装 settings_ACU.plotSettings.enabled、globalMeta_ACU.plotEnabledGlobal 与 saveGlobalMeta_ACU() 的同步；src/presentation/pages/popup-bindings-plot.ts 只 import 这个 service setter，并继续调用 saveSettingsAndNotify_ACU()。可维护性 5/5，健壮性 5/5，可扩展性 5/5。理由：presentation → service 是合法依赖，service → data 是合法依赖；全局状态写入集中在 settings service，与 setZeroTkOccupyMode_ACU 和 setSummaryVectorIndexMode_ACU 的既有设计一致；之后若剧情推进开关需要额外兼容迁移或副作用，只需改 service，不让 UI 直接碰 data。
selected: option_3。
execution_update:
  - 在 src/service/settings/settings-service.ts 增加导出的 setGlobalPlotEnabled_ACU，确保 plotSettings 对象缺失时先用 DEFAULT_PLOT_SETTINGS_ACU 补齐，再同步运行时投影和 globalMeta 全局状态，并保存 globalMeta。
  - 在 src/presentation/pages/popup-bindings-plot.ts 删除 presentation → data 的违规 import，改为 import setGlobalPlotEnabled_ACU from settings-service；checkbox change handler 调用 setter 后再 saveSettingsAndNotify_ACU。
validation_update:
  - 修改后必须重新执行 npm run build，确认架构护栏 presentation → data 恢复 0 条。
  - 必须确认 popup-bindings-plot.ts 不再出现 profile-repo import。
  - 必须确认 DEFAULT_PLOT_SETTINGS_ACU.enabled=true、plotEnabledGlobal=true、预设/聊天快照删除 enabled、plot-logic preservedEnabled 保护仍在。
</decision_point>

<decision_point>
issue: 关键逻辑验证命令使用 findstr 串联时返回 exit code 1，但输出已经显示前几项验证命中：src/shared/defaults-json.js 存在 "enabled": true；src/data/repositories/profile-repo.ts 和 src/service/settings/settings-service.ts 存在 plotEnabledGlobal；src/presentation/pages/popup-bindings-plot.ts 和 src/service/settings/settings-service.ts 存在 setGlobalPlotEnabled_ACU。失败原因来自最后一个验证子命令 findstr /C:"profile-repo" src\presentation\pages\popup-bindings-plot.ts 未命中，这在本次验证目标里反而是期望结果：popup-bindings-plot.ts 不应再 import profile-repo。把“无命中”视为整条串联命令失败，是验证命令设计不严谨，不是源码缺陷。
options:
  - option_1: 因 findstr exit code 1 就继续改源码。可维护性 1/5，健壮性 1/5，可扩展性 1/5。理由：源码没有暴露新的错误，盲目修改只会制造噪音；这就是没理解验证命令语义就动手的典型事故入口。结论：拒绝。
  - option_2: 忽略该失败并直接完成。可维护性 2/5，健壮性 2/5，可扩展性 2/5。理由：虽然根据输出能判断失败来自预期无命中，但如果不做分项复核，就无法证明预设/聊天快照删除 enabled 和 preservedEnabled 保护仍存在。结论：拒绝。
  - option_3: 保持源码不变，改用分项读取/搜索验证关键语义：读取或搜索 popup-bindings-plot.ts 确认不含 profile-repo；搜索 delete snapshot.enabled、delete currentSettings.enabled、preservedEnabled；必要时读取相关片段。可维护性 5/5，健壮性 5/5，可扩展性 5/5。理由：这是对验证命令问题的精确处理，不把验证工具的非零退出码误判为实现错误，同时补足关键链路复核。
selected: option_3。
execution_update:
  - 不修改源码。
  - 继续用 read_file/search_files 对 src/presentation/pages/popup-bindings-plot.ts、src/service/template/chat-scope/chat-scope-plot.ts、src/presentation/pages/popup-helpers.ts、src/service/plot/plot-logic.ts 做分项验证。
</decision_point>

<output_quality_review>
verification:
  - npm run build 已通过：Rollup 成功生成 dist/index.bundle.js，三层架构护栏总计违规 0 条，presentation → data 恢复 0 条。
  - src/shared/defaults-json.js:143 确认为 "enabled": true，满足剧情推进默认打开。
  - src/data/repositories/profile-repo.ts:20 和 src/data/repositories/profile-repo.ts:31 确认新增 plotEnabledGlobal: true，globalMeta 默认值具备剧情推进全局开关。
  - src/service/settings/settings-service.ts:26-38 确认 applyGlobalPlotEnabledSetting_ACU 会在 globalMeta_ACU.plotEnabledGlobal 缺失时从旧 settings_ACU.plotSettings.enabled 迁移：旧值明确 false 才迁移 false，否则默认 true；之后强制把 globalMeta 投影回 settings_ACU.plotSettings.enabled。
  - src/service/settings/settings-service.ts:180-190 和 src/service/settings/settings-service.ts:242-249 确认已保存配置分支与无保存配置分支都会调用 applyGlobalPlotEnabledSetting_ACU。
  - src/service/settings/settings-service.ts:640-650 确认 setGlobalPlotEnabled_ACU 由 service 层封装 UI 开关写入，更新 settings_ACU.plotSettings.enabled 与 globalMeta_ACU.plotEnabledGlobal，并保存 globalMeta。
  - src/presentation/pages/popup-bindings-plot.ts:4-22 确认不再 import data/repositories/profile-repo，而是 import service/settings/settings-service 的 setGlobalPlotEnabled_ACU，符合 presentation → service 分层。
  - src/presentation/pages/popup-bindings-plot.ts:30-35 确认 checkbox change handler 调用 setGlobalPlotEnabled_ACU 后 saveSettingsAndNotify_ACU。
  - src/service/template/chat-scope/chat-scope-plot.ts:20-22 确认聊天快照仍 delete snapshot.enabled，不让总开关进入对话快照。
  - src/presentation/pages/popup-helpers.ts:491-493 确认保存剧情预设时仍 delete currentSettings.enabled，不让总开关进入预设。
  - src/service/plot/plot-logic.ts:423-498 确认 applyPlotPresetToSettings_ACU、resetPlotSettingsToDefault_ACU、replaceCurrentPlotSettingsWithSnapshot_ACU 三条链路仍 preservedEnabled，预设应用、重置、快照恢复不会覆盖当前全局投影后的总开关。
quality_assessment:
  - 正确性：满足“默认打开，但用户全局关就刷新/新对话都关，用户全局开就刷新/新对话都开”。状态源头从散落的 settings 字段提升为 globalMeta_ACU.plotEnabledGlobal，settings_ACU.plotSettings.enabled 只作为运行时门禁兼容投影。
  - 架构性：修复中一度出现 presentation 直接 import data 的错误，这个错误低级得像是故意喂给架构护栏的。已改为 service setter 后，依赖方向恢复正常；没有使用 // arch-ok 逃避问题。
  - 兼容性：旧用户如果曾在 settings_ACU.plotSettings.enabled 明确关闭剧情推进，会迁移为全局关闭；缺失字段或新安装默认开启，符合助手纠正的产品语义。
  - 可维护性：新增 setGlobalPlotEnabled_ACU 与 setZeroTkOccupyMode_ACU、setSummaryVectorIndexMode_ACU 的全局开关封装模式一致。后续剧情推进总开关若增加联动，只需维护 service 层。
  - 风险：saveSettingsAndNotify_ACU 后续若调用 saveSettings_ACU，不会反向覆盖 globalMeta_ACU.plotEnabledGlobal；正式 UI 写入入口已经先调用 setGlobalPlotEnabled_ACU。非 UI 代码若直接改 settings_ACU.plotSettings.enabled 而不调用 setter，不会改变全局开关，这是设计上允许的，因为总开关变更必须走 service setter。
remaining_limitations:
  - 本次没有做浏览器 UI 点击验证，只做了构建、架构护栏与源码链路验证。对这种设置状态修复而言，构建与源码路径已经覆盖核心风险；若后续出现 UI 渲染 checkbox 状态不同步，需要再查 loadPlotSettingsToUI_ACU 的读取路径。
</output_quality_review>
