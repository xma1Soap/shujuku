<analysis>
context: 已从以下文件获取完整上下文：
- src/data/sqlite/schema-mapper.ts（296 行）：generateDDL() 在第 42 行直接 trim() 返回用户 DDL；generateInserts() 在第 117 行，遍历 content 数据行，对每个值调用 escapeValue()（仅做 NULL/数字/引号转义，第 261-267 行）；sanitizeIdentifier() 仅做标识符合法化。当前没有全角字符兼容化，没有受约束值规范化。
- src/service/table/sql-table-service.ts（548 行）：applyEdits() 在第 177 行，清理 HTML 注释后用 splitSqlStatements() 拆分，直接 engine.runBatch()；executeMutation() 在第 241 行直接 engine.run()；splitSqlStatements() 在第 483 行已有字符串感知的分号拆分逻辑。两个写入入口都没有 SQL 预处理。
- src/data/sqlite/sync-bridge.ts（246 行）：_loadSheet() 在第 118 行调用 generateDDL/generateInserts 后直接执行，不做字符清洗。
- YO改-骰子-SQL版v3.json 第 505 行 sheet_inventory.sourceData.ddl 中有全角等号 `>＝`，这是导致 `near "0": syntax error` 的确切根因。
- 已有测试目录结构：tests/data/sqlite/ 下有 schema-mapper.test.ts、sync-bridge.test.ts、sqlite-engine.test.ts。

needs: 用户需要的是：1）让 SQLite 输入链路能自动兼容全角符号、大小写不规范等常见问题；2）修复模板文件中的确定错误；3）不能误改正文文本。

key_challenges: 核心难点是实现"只改 SQL 结构字符，不改正文字符"。最稳妥的做法是扫描式替换——逐字符判断是否在字符串字面量内（单引号包裹），只在结构位置做全角转半角。splitSqlStatements() 已经实现了字符串感知的分号拆分（第 489-514 行），可以复用类似的扫描逻辑。

confidence: HIGH
  - 所有调用链入口、函数签名、已有代码逻辑都已读取确认
  - 模板中的脏数据已精确定位到行和字符
  - 无未验证假设

approach: 三维评估综合最优的方案是创建 src/data/sqlite/sql-normalizer.ts 统一模块，提供 normalizeSqlStructure() 和 normalizeConstrainedValue() 两个函数，然后在四个接入点调用。
  三维评分：
  - 可维护性: 5/5 — 规范化逻辑集中在独立模块，所有入口复用，测试围绕单一模块展开
  - 健壮性: 5/5 — 扫描式替换区分字符串内外，白名单字段值规范化不会越权，规范后仍非法则保留失败
  - 可扩展性: 4/5 — 后续可扩展更多约束字段的规范化规则；扣 1 分因为值级规范化需要列白名单维护

edge_cases:
  - 全角运算符 `＞＝` 出现在 CHECK 约束中（结构位置）→ 应转换为 `>=`
  - 全角运算符出现在字符串字面量中（如 `'消耗品＞＝材料'`）→ 不应转换
  - code_index 值为 `'am0001'` → 规范为 `'AM0001'`
  - code_index 值为 `'ＡＭ０００１'`（全角字母数字）→ 规范为 `'AM0001'`
  - code_index 值为 `' AM0001 '`（有首尾空格）→ 规范为 `'AM0001'`
  - code_index 值为 `'AM12'` → 规范后仍不匹配 GLOB 模式，必须继续报错
  - chronicle_text / description 等正文中的全角标点保留原样
  - DDL 注释（-- ...）中的全角字符不转换（注释不影响 SQLite 解析，且可能包含用户中文说明）
  - 连续转义引号 `''` 不应中断字符串边界判断

affected_scope:
  - src/data/sqlite/sql-normalizer.ts（新增）
  - src/data/sqlite/schema-mapper.ts（修改 generateDDL、generateInserts）
  - src/service/table/sql-table-service.ts（修改 applyEdits、executeMutation）
  - YO改-骰子-SQL版v3.json（修复模板中的全角等号）
  - tests/data/sqlite/sql-normalizer.test.ts（新增）

execution_plan:
  - step_1: 新增 src/data/sqlite/sql-normalizer.ts，实现 normalizeSqlStructure(sql) 和 normalizeConstrainedValue(columnName, value) 两个导出函数。normalizeSqlStructure 使用扫描式逐字符处理，区分字符串内/外和注释内/外，在字符串外且非注释内的全角运算符/括号/逗号转为 ASCII。normalizeConstrainedValue 按 columnName 白名单执行值规范化（当前白名单只有 code_index，做 trim + NFKC + 全角数字转半角 + 大写）
  - step_2: 修改 src/data/sqlite/schema-mapper.ts 的 generateDDL()，在第 47 行 return ddl 之前调用 normalizeSqlStructure(ddl)。修改 generateInserts()，在第 147-148 行 escapeValue 之前，对白名单字段调用 normalizeConstrainedValue
  - step_3: 修改 src/service/table/sql-table-service.ts 的 applyEdits()，在第 188 行 splitSqlStatements 之后对每条语句调用 normalizeSqlStructure。修改 executeMutation()，在第 245 行 engine.run 之前调用 normalizeSqlStructure(sql)
  - step_4: 修复 YO改-骰子-SQL版v3.json 第 505 行的 `>＝` 为 `>=`
  - step_5: 新增 tests/data/sqlite/sql-normalizer.test.ts，覆盖全部边界条件测试用例
  - step_6: 运行测试验证

degradation_check:
  - 方案是否是三维评估综合最优的？ → YES + 可维护性 5/5（集中模块）、健壮性 5/5（扫描式替换 + 白名单值规范化）、可扩展性 4/5（白名单可扩展）
  - 是否遗漏了已知边界条件？ → NO + 已列出 9 个边界条件，涵盖结构字符、字符串保护、值规范化、非法值保留失败、正文保护、注释保护、转义引号
  - 是否因改动量大而想缩减方案？ → NO + 这是用户明确要求的健全化方案，不做就是留雷
  - 是否打算跳过某些文件？ → NO + affected_scope 中 5 个文件全覆盖
  - execution_plan是否覆盖affected_scope所有文件？ → YES + 6 步覆盖全部 5 个文件
  - context是否充分？是否有未读但可能相关的文件？ → YES + schema-mapper.ts 完整读取，sql-table-service.ts 完整读取，模板文件精确定位
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO
  - execution_plan中是否有步骤计划使用 shell 命令修改源代码？ → NO
</analysis>
---
<output_quality_review>
task_summary: 为 SQLite 模式建立了统一的 SQL 输入规范化机制，覆盖模板导入链路和运行时 SQL 执行链路，同时修复了模板文件中的全角等号错误。
deliverables:
  - src/data/sqlite/sql-normalizer.ts（新增 — SQL 规范化核心模块）
  - src/data/sqlite/schema-mapper.ts（修改 — generateDDL/generateInserts 接入规范化）
  - src/service/table/sql-table-service.ts（修改 — applyEdits/executeMutation 接入规范化）
  - YO改-骰子-SQL版v3.json（修复 — 全角等号 >＝ → >=）
  - tests/data/sqlite/sql-normalizer.test.ts（新增 — 47 个测试用例）

metrics:
  total_files_modified: 5
  execution_plan_coverage: 6/6 = 100%
  edge_cases_handled: 9/9 = 100%
  confidence_assessment: HIGH

substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？ → NO + sql-normalizer.ts 包含三层能力且全部可被测试击穿；删除任何一层都会导致对应测试失败
  - 产物是否能被其目标对象的变化所"击穿"？ → YES + 修改扫描逻辑/白名单/映射表都会导致对应测试立即失败
  - 实质性比率: 5/5 = 100%

completeness_check:
  - 是否存在被跳过的模块/函数/路径？ → NO + generateDDL、generateInserts、applyEdits、executeMutation 全部接入；executeQuery 保持只读符合设计
  - 产物覆盖的范围是否与 execution_plan 中 affected_scope 完全一致？ → YES
  - 核心业务逻辑是否都有直接验证？ → YES + DDL全角修复 20 个用例、INSERT值规范化 8 个用例、UPDATE值规范化 2 个用例、code_index规范化 14 个用例、边界保护 3 个用例
  - affected_scope 覆盖率: 5/5 = 100%

value_density_check:
  - 高价值:低价值 = 47:0，高价值占比 100%
  - 是否存在"用数量掩盖质量"的模式？ → NO

alignment_check:
  - 产物满足的是用户的本质需求：让全角符号/大小写不匹配这类细微问题从致命错误降级为自动修复，同时保留无法安全修复的值的明确失败
  - "如果这是别人交给我的，我会接受吗？" → YES
</output_quality_review>
