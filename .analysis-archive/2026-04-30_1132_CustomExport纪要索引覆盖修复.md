<analysis>
context: 本轮侦察确认，`updateReadableLorebookEntry_ACU` 在 `src/service/worldbook/pipeline.ts` 中每次合并表格后调用 `updateCustomTableExports_ACU`，该函数会删除旧的 `TavernDB-ACU-CustomExport-*` 条目并重建世界书条目。`updateCustomTableExports_ACU` 在 `src/service/worldbook/injection-engine-custom.ts` 的 `buildExtraIndexEntryBlock_ACU` 中确实尝试处理 `extraIndexSpec.entryName === '纪要索引'` 的特殊路径：当 `summaryVectorIndexModeEnabled` 为 true 时调用 `formatSummaryVectorIndexRecallOverride_ACU()`，并用返回的 `content` 替代原始 `extraIndexSpec.indexRows` 生成的 Markdown 表格。`formatSummaryVectorIndexRecallOverride_ACU` 位于 `src/service/runtime/plot-runtime/plot-data-format.ts`，只读取 `currentSummaryVectorIndexRecall_ACU` 这个运行时内存变量；该变量由 `orchestrateSummaryVectorIndexBeforeSend_ACU` 在 `src/service/plot/summary-vector-index-orchestrator.ts` 第 200 行附近设置。问题点已经非常清楚：`TavernDB-ACU-CustomExport-纪要索引` 的世界书持久条目只有在 `updateCustomTableExports_ACU` 被调用时才会重建，而发送前召回流程只设置内存态 `currentSummaryVectorIndexRecall_ACU`，没有在设置召回结果后主动刷新 CustomExport 世界书条目。因此用户发送时可能已有新的召回覆盖状态，但世界书里的 `TavernDB-ACU-CustomExport-纪要索引` 内容仍停留在上一次表格同步时的全量索引或旧内容。另一个已确认风险是 `buildExtraIndexEntryBlock_ACU` 中传入参数包含 `exportPrefix` 但实际命名使用闭包内 `getImportEntryName`，参数本身未参与判断；这不是当前 bug 的主因，但说明这段实现已经有轻微设计污点。剧情推进 `$5` 的 `formatSummaryIndexForPlot_ACU` 会直接读取内存态覆盖，所以剧情推进占位符可能已生效；用户反馈的 CustomExport 条目未覆盖，指向的是“世界书条目本体未被发送前召回状态重写”的路径缺口。
needs: 需求的本质不是再造一个 formatter，而是保证“向量混合交火增强方案”在发送前完成召回后，`TavernDB-ACU-CustomExport-纪要索引` 这个 CustomExport 世界书条目的 `content` 也被同步覆盖成当前召回结果。实现必须不破坏普通自定义导出更新、不影响外部导入、不让普通向量流程复活，并且在无召回结果、召回为空、配置无效或世界书 API 不可用时保持可预期降级。
key_challenges: 核心难点有三个。第一，当前覆盖结果保存在运行时内存变量，而 CustomExport 条目刷新发生在表格同步流程，两条链路时序脱节；只在 `buildExtraIndexEntryBlock_ACU` 里读内存态属于被动覆盖，发送前不触发表格同步就不会生效。第二，不能粗暴调用完整 `updateReadableLorebookEntry_ACU` 来刷新，因为那会重新合并聊天表格、重建大量世界书条目，发送前预处理阶段这么做成本高且副作用大，漏洞明显得像是故意写给事故看的。第三，必须精确更新 `TavernDB-ACU-CustomExport-纪要索引` 单条内容，并保留其 order、placement、enabled 等已有世界书属性，否则会破坏用户的世界书配置和 0TK/互斥控制。
confidence: HIGH。本轮已经读取了 CustomExport 构建函数、剧情推进 formatter、发送前编排器、运行时状态与世界书 pipeline，根因链路明确：发送前召回只写内存态，没有主动写 CustomExport 世界书条目；既有被动覆盖只在下一次 CustomExport 重建时才可能生效。未验证的部分仅剩具体 worldbook gateway `setLorebookEntries_ACU` 对 content 局部更新是否保留未传字段，但项目中多处已使用同一 gateway 做 uid + enabled/order 的局部更新，因此风险可控。
approach: 采用最小副作用方案：在 `src/service/worldbook/injection-engine-custom.ts` 增加一个可复用的单条覆盖函数，负责定位当前隔离前缀下的 `TavernDB-ACU-CustomExport-纪要索引`，用 `formatSummaryVectorIndexRecallOverride_ACU()` 的内容通过该条目的现有模板形态生成最终 content，并调用 `setLorebookEntries_ACU` 只更新 uid/content。然后在 `orchestrateSummaryVectorIndexBeforeSend_ACU` 成功设置 `currentSummaryVectorIndexRecall_ACU` 后调用该覆盖函数。这样不重建全部 CustomExport，不重新合并表格，不删除条目，也不触碰普通向量流程。
  三维评分（每个维度 1-5 分，5 为最优）：
  - 可维护性: 4/5 — 修改点集中在 CustomExport 世界书条目职责所在模块和发送前编排调用点，命名清晰，避免把世界书写入逻辑塞进 formatter。扣 1 分是因为现有 `injection-engine-custom.ts` 函数较长，新增导出函数仍在同文件会继续受该文件复杂度影响。
  - 健壮性: 5/5 — 单条定位、检查世界书 API、检查 override、检查 uid、保留原条目 placement/enabled/order，只更新 content；在条目不存在时记录 warning 并跳过，不阻断正文生成。
  - 可扩展性: 4/5 — 后续如果还有“发送前召回覆盖某个 CustomExport 条目”的需求，可以复用同类单条覆盖模式；扣 1 分是因为目前只针对固定条目名，没有抽象为通用 CustomExport override registry，贸然抽象反而会过度设计。
edge_cases: 第一，当前没有可用召回结果时不能把世界书条目清空，必须跳过覆盖。第二，召回结果为空但流程成功时应写入“未召回相关纪要”的内容，因为这是合法结果，不是失败。第三，启用数据隔离时必须使用 `getIsolationPrefix_ACU()` 后的完整 comment，否则会覆盖错角色/错隔离空间的条目。第四，世界书条目还没创建时不能创建一个残缺条目，因为缺少 placement/order/template 来源，应该提示需要先完成一次世界书/表格同步。第五，旧条目可能用了自定义 extraIndexInjectionTemplate，单条覆盖如果直接写裸内容会丢失模板包裹；因此应尽量从旧 content 中识别 `# 纪要索引` 包裹或复用默认模板，至少保持与当前 buildExtraIndexEntryBlock 的默认输出一致。第六，外部导入条目不应被覆盖，目标 comment 必须是非导入的 `TavernDB-ACU-CustomExport-纪要索引`。第七，0TK 互斥下该模式本应关闭；若条目 enabled 为 false，只更新 content 不强行启用，避免破坏现有控制。
affected_scope: `src/service/worldbook/injection-engine-custom.ts` 需要新增精确覆盖函数并复用已有 formatter；`src/service/plot/summary-vector-index-orchestrator.ts` 需要在成功召回后调用覆盖函数；可能需要读取 `src/data/gateways/worldbook-gateway.ts` 以确认 set 接口语义；构建验证涉及 `package.json` 的 `npm run build` 脚本和生成产物 `dist/index.bundle.js`，但本轮除非用户要求发布，不应自动覆盖根目录 `index.js` 或推送分支。
execution_plan:
  - step_1: 读取 `src/data/gateways/worldbook-gateway.ts` 确认 `setLorebookEntries_ACU` 的更新语义，避免用错 API 造成字段丢失。
  - step_2: 在 `src/service/worldbook/injection-engine-custom.ts` 新增 `refreshSummaryVectorIndexCustomExportEntry_ACU`，内部检查 worldbook API、目标世界书、隔离前缀、当前 override、目标条目 uid，并只更新该条目的 content；条目不存在时记录 warning 并返回 false。
  - step_3: 在 `src/service/plot/summary-vector-index-orchestrator.ts` 导入并调用 `refreshSummaryVectorIndexCustomExportEntry_ACU`，调用点放在 `_set_currentSummaryVectorIndexRecall_ACU` 之后、返回 success 之前；失败只作为 warning，不阻断发送。
  - step_4: 使用 `npm run build` 做 TypeScript/Rollup/架构护栏验证。
  - step_5: 搜索构建产物或源码确认新函数被打包引用，并向用户说明根因、修复路径、是否需要重新发送一轮才能看到覆盖。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。比完整重建世界书条目副作用更小，比只改 formatter 更能命中用户反馈的世界书持久条目问题；可维护性 4/5、健壮性 5/5、可扩展性 4/5。
  - 是否遗漏了已知边界条件？ → NO。已覆盖无 override、空召回、隔离前缀、条目不存在、外部导入、enabled 保持、模板输出一致性等边界。
  - 是否因改动量大而想缩减方案？ → NO。本方案本来就是最小必要改动；缩减成“解释不是 bug”会让 CustomExport 条目继续不更新，属于逃避问题。
  - 是否打算跳过某些文件？ → NO。会读取 gateway 确认接口语义，再修改 CustomExport 与 orchestrator 两处必要文件。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。两个源代码文件的修改与 gateway 验证、构建验证均已覆盖。
  - context是否充分？是否有未读但可能相关的文件？ → YES。还需要读取 `src/data/gateways/worldbook-gateway.ts` 确认局部更新语义；这是执行前必须补的一步，不能凭感觉写。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → YES。`buildExtraIndexEntryBlock_ACU` 参数 `exportPrefix` 未实际使用属于现有代码污点，但不影响当前 bug；本轮不处理以避免扩大改动范围，后续可单独清理。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO。源码修改使用标准编辑工具；shell 只用于构建验证。
</analysis>

<output_quality_review>
result: 已修复 `TavernDB-ACU-CustomExport-纪要索引` 世界书持久条目未随发送前纪要向量召回覆盖的问题。
root_cause: 原实现只在 `updateCustomTableExports_ACU` 重建 CustomExport 条目时被动读取 `currentSummaryVectorIndexRecall_ACU`，但发送前召回流程只写运行时内存态，不主动刷新 CustomExport 世界书条目，所以用户查看到的世界书条目本体仍是旧内容或全量索引。
changes:
  - `src/service/worldbook/injection-engine-custom.ts`: 新增 `refreshSummaryVectorIndexCustomExportEntry_ACU`，按当前隔离前缀精确定位 `TavernDB-ACU-CustomExport-纪要索引`，仅更新该条目的 `content`，不重建全部世界书条目，不删除旧条目，不强行改 enabled/order/placement。
  - `src/service/worldbook/injection-engine-custom.ts`: 将 CustomExport 纪要索引默认模板提取为 `getSummaryVectorIndexCustomExportFallbackTemplate_ACU`，保证被动重建和主动覆盖输出形态一致。
  - `src/service/plot/summary-vector-index-orchestrator.ts`: 在成功 `_set_currentSummaryVectorIndexRecall_ACU` 后立即调用 `refreshSummaryVectorIndexCustomExportEntry_ACU`，让发送前召回结果同步落到 CustomExport 世界书条目本体。
validation:
  - `npm run build`: 成功。
  - 三层架构护栏: 全部通过，违规 0 条。
  - `findstr` 检查: `refreshSummaryVectorIndexCustomExportEntry_ACU` 已存在于源码和 `dist/index.bundle.js`。
known_limits:
  - 如果目标世界书里尚未创建过 `TavernDB-ACU-CustomExport-纪要索引`，发送前覆盖不会凭空创建残缺条目，而是记录 warning；需要先执行一次世界书/表格同步创建该条目。
  - 本轮没有自动覆盖根目录 `index.js`、没有推送分支、没有创建 tag，因为用户本次要求是“请检查”并修复源码链路，不是发布任务。
</output_quality_review>
