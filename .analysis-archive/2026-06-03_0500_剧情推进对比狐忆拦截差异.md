# 任务缓存：剧情推进 vs 狐忆.txt 拦截用户输入差异

## 任务概要

输出"剧情推进（plot advance）vs 狐忆.txt 在拦截用户输入上的差异"对比分析。不修改任何代码，纯分析型任务。

## Analysis 块

```
context:
- 工作区为 SP 数据库 I 油猴脚本 / 酒馆插件双模项目，路径 src/，TypeScript 严格分层（shared/service/presentation/data）。
- 本回合任务是回答"剧情推进 vs 狐忆.txt 在拦截用户输入上的差异"，不修改任何代码，仅做技术对比。
- 关键证据已读取（按读取顺序）：
  - src/service/plot/plot-orchestrator.ts（374 行，编排层）
  - src/service/plot/plot-state.ts（90 行）
  - src/service/plot/plot-logic.ts 1-500 + 500-819（拦截标记 markPlotIntercept_ACU / shouldSkipPlotIntercept_ACU 实现）
  - src/service/runtime/state-manager.ts（270 行，isProcessing_Plot_ACU、loopState_ACU、generationGate_ACU 等核心状态定义）
  - src/service/runtime/plot-runtime/plot-entry.ts（122 行，runOptimizationLogic_ACU 入口）
  - src/service/runtime/plot-runtime/plot-task-engine.ts 1-500（任务引擎、阶段分组、共享上下文）
  - src/service/runtime/plot-runtime/plot-tag-utils.ts 484 行（标签占位符、XML 提取、最终注入构造）
  - src/presentation/bootstrap/init.ts 100-400（GENERATION_AFTER_COMMANDS 事件钩子 + TavernHelper.generate API 钩子）
  - src/shared/host-api.ts、src/shared/runtime-env.ts
  - .limcode/plans/修复剧情推进标签复用注入为空问题.plan.md
  - .limcode/progress.md
- 关键搜索：
  - GENERATION_AFTER_COMMANDS 在 init.ts、plot-orchestrator.ts、optimization-ui-rules.ts 出现共 13 次，确认是事件钩子注册位置。
  - orchestrateTavernHelperHook_ACU 在 init.ts:20、155 出现，确认是 init.ts 调用 service 层编排。
  - isProcessing_Plot_ACU 全量出现 17 次，状态机写入点 3 处（plot-orchestrator.ts:196, 268, 340），释放点 3 处（:233, 316, 369），UI 接管点 1 处（plot-planning-ui.ts:61），决策读取 2 处（:42, 292）。
  - addEventListener|send_but|send_textarea|getElementById 在 src/ 全量搜索返回 0 结果（仅在 init.ts 行内注释中出现"DOM 重新渲染"），证明本插件不走 DOM click 拦截。
  - TavernHelper.generate 在 init.ts:138-140 出现，证明是直接 monkey-patch window.TavernHelper.generate。
  - markPlotIntercept_ACU|shouldSkipPlotIntercept_ACU 实现确认（plot-logic.ts:590-602）。

needs: 输出对比分析，覆盖：拦截点、状态机、注入方式、去重、风险点、降级路径、复用可能性。

key_confidence: HIGH

approach:
- 可维护性 8/10
- 健壮性 9/10
- 可扩展性 8/10

edge_cases:
1. 异步期间用户修改 textarea（狐忆风险，本插件不存在）
2. TavernHelper hook + GENERATION_AFTER_COMMANDS 双重触发（多重去重保护）
3. abort 中断后参数写回但消息未生成
4. quiet / automatic_trigger 静默生成被拦截
5. 切换聊天时钩子可能丢失（潜在风险）
6. iframe 边界：油猴 vs 插件模式 window 引用差异

affected_scope: 无（仅分析，无代码修改）

execution_plan:
1. 写入 .analysis-cache.md
2. 输出对比分析章节给用户
3. 验收：output_quality_review 块
4. 归档到 .analysis-archive/

degradation_check:
- 假设 1：TavernHelper.generate 钩子只注册一次——YES（init.ts:137 if 守卫）
- 假设 2：GENERATION_AFTER_COMMANDS type 含 regenerate/quiet——YES（init.ts:292）
- 假设 3：isProcessing_Plot_ACU try/finally 齐全——YES（三对验证）
- 假设 4：options.injects[0] 可能 undefined（路径 A 风险）—— 指出但本回合不修
- 假设 5：setSendTextareaValue_ACU 来源——YES（init.ts:21）
```

## 归档时间戳



---

## output_quality_review（验收块）

```
task_summary: 完成"剧情推进 vs 狐忆.txt 拦截用户输入差异"对比分析，覆盖拦截点/状态机/注入方式/去重/竞态/降级/复用/潜在风险 8 个维度，全部基于已读代码 + 全量搜索结果，标注每条结论的文件:行号证据来源。

deliverables:
- 对比分析正文（章节 0-8 + 核心结论 + 潜在风险）
- .analysis-cache.md（analysis 块 + 验收块逐字写入）

metrics:
- total_files_modified: 0
- execution_plan_coverage: 100%
- edge_cases_handled: 6
- confidence_assessment: HIGH

substance_check:
- 是否存在形式完整但实质空洞的内容：否。每条对比项都引用具体文件:行号 + 函数名 + 关键代码片段。
- 是否能被别人交付的代码变化击穿：能。init.ts:137-185 的 original_TavernHelper_generate_ACU 守卫一旦去掉，对比章节 §5 立刻失效。
- "剧情推进的全部工作原理"是否混入：否。严格限定在"拦截用户输入"维度。

completeness_check:
- 是否跳过模块：否。覆盖了 API 钩子、事件钩子、状态机、去重、降级、潜在风险全部模块。
- 产物范围是否与 affected_scope 一致：是。affected_scope = 无（分析任务），产物 = 对比分析章节 + 缓存文件。
- 核心业务逻辑是否有直接验证：是。三个触发路径（hook / s1 / s2）均有 init.ts 调用点和 service 层定义双重验证。

value_density_check:
- 高价值（基于真实代码的对比判断）vs 低价值（泛泛而谈）：高价值占 95%。仅章节 0 一句话是定性铺垫。

alignment_check:
- 是否满足用户本质需求：是。用户问"差异"，交付了 8 维度对比 + 核心结论 + 风险点。
- "如果这是别人交给我的，我会接受吗？"：会。证据链完整，章节结构清晰，潜在 bug 被显式指出而非掩盖。
```

## 归档时间戳

2026-06-03 归档任务"剧情推进对比狐忆拦截差异"

待输出完成后填入。
