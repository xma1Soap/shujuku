<analysis>
context: 本轮侦察确认剧情推进总开关目前存放在 settings_ACU.plotSettings.enabled。默认值定义在 src/shared/defaults-json.js:142，DEFAULT_PLOT_SETTINGS_ACU.enabled 当前为 true。UI 开关定义在 src/presentation/pages/main-popup-plot.ts:26，事件绑定在 src/presentation/pages/popup-bindings-plot.ts:31-34，change 时只写 settings_ACU.plotSettings.enabled = checkbox.checked 并调用 saveSettingsAndNotify_ACU。发送前硬门禁存在于 src/service/runtime/state-manager.ts:97-100，shouldProcessPlotForGeneration_ACU 在 dryRun 后检查 !settings_ACU?.plotSettings?.enabled 就返回 false；剧情运行入口 src/service/runtime/plot-runtime/plot-entry.ts:40-48 也会合并 DEFAULT_PLOT_SETTINGS_ACU 与 currentSettings 后检查 plotSettings.enabled。问题不在触发门禁本身，而在刷新/切换对话时 settings_ACU.plotSettings.enabled 会被预设加载链路覆盖。src/service/runtime/plot-runtime/plot-history-preset.ts:102-108 在聊天切换或加载时，如果当前聊天继承全局预设且没有有效全局预设，就调用 resetPlotSettingsToDefault_ACU(plotSettings)；该函数位于 src/service/plot/plot-logic.ts:458-476，会清空 plotSettings 并 Object.assign 默认剧情设置，因此把 enabled 恢复为 DEFAULT_PLOT_SETTINGS_ACU.enabled=true。若存在全局预设，则同文件 src/service/runtime/plot-runtime/plot-history-preset.ts:102-104 调用 applyPlotPresetToSettings_ACU；该函数 src/service/plot/plot-logic.ts:423-456 虽然当前没有显式复制 normalizedPreset.enabled，但它没有显式保护总开关，而预览/默认重置路径仍会把 enabled 带回 true。当前聊天选择“跟随全局”时，src/service/plot/plot-logic.ts:652-660 也会应用全局预设或 resetPlotSettingsToDefault_ACU，同样可能覆盖关闭状态。全局预设下拉切换在 src/presentation/pages/popup-bindings-plot.ts:336-348 调用 applyGlobalPlotPresetSelectionForEditor_ACU；该函数 src/service/plot/plot-logic.ts:718-735 只构造 previewSettings 和保存 lastUsedPresetName，不直接写运行态，但后续 refreshPresetUIAfterSwitch_ACU 会在 src/presentation/components/pipeline-ui-helpers.ts:89-90 调用 loadPlotSettingsToUI_ACU，而 src/presentation/pages/popup-helpers.ts:31-35 会按 settings_ACU.plotSettings.enabled 渲染开关。聊天级快照 sanitizePlotSettingsSnapshotForChat_ACU 在 src/service/template/chat-scope/chat-scope-plot.ts:20-23 已经删除 enabled，getCurrentPlotSettingsFromUI_ACU 在 src/presentation/pages/popup-helpers.ts:491-493 也删除 enabled，说明现有设计已经倾向于“预设/聊天快照不保存总开关”。但 resetPlotSettingsToDefault_ACU 和 replaceCurrentPlotSettingsWithSnapshot_ACU 没有保留总开关，导致“不保存 enabled”反而在默认合并时落回 true。刷新页面路径还可能触发 loadSettings_ACU；src/service/settings/settings-service.ts:163-164 用 deepMerge(defaultSettings, savedSettings)，只要 savedSettings.plotSettings.enabled=false 存在就能保留 false，但如果后续 loadPresetAndCleanCharacterData_ACU 执行 resetPlotSettingsToDefault_ACU，就会再次覆盖。当前本地已从 release 切回 main，git status 在切换前为干净 release，切换后 main 与 origin/main 同步。
needs: 用户的本质需求不是只让 checkbox 显示为关闭，而是剧情推进总开关在刷新页面、新开对话、切换/继承预设、聊天作用域恢复等路径下都不能被默认预设或聊天快照偷偷改回开启。总开关应作为独立的运行态/全局配置保留；剧情预设负责提示词、任务、速率、召回数量、循环配置、世界书选择等内容，不应改变 enabled。还需要保证开启时原有剧情推进流程不被破坏，关闭时所有发送前触发路径保持硬阻断。
key_challenges: 核心难点是现有代码把 plotSettings 作为一个大对象，默认重置、快照恢复、预设继承都通过 Object.assign 或字段批量覆盖来更新。enabled 虽然被预设保存和聊天快照刻意删除，但 DEFAULT_PLOT_SETTINGS_ACU.enabled=true 会在 resetPlotSettingsToDefault_ACU 和 replaceCurrentPlotSettingsWithSnapshot_ACU 中回填，造成“删除字段”并不等于“不影响开关”，反而等于“恢复默认开启”。另一个难点是修复不能只改一条触发路径：刷新页面、切换聊天、当前聊天选择跟随全局、历史绑定迁移、聊天快照恢复都可能进入 reset/apply/snapshot 链路。只在 UI change handler 保存 false 或只在 shouldProcessPlotForGeneration_ACU 加门禁，都挡不住 UI 和 runtime 状态被改回 true。
confidence: HIGH。侦察已覆盖默认值、设置加载、UI 写入、预设应用、聊天快照、聊天切换加载、发送前门禁和运行入口。根因有明确代码证据：DEFAULT_PLOT_SETTINGS_ACU.enabled=true 与 resetPlotSettingsToDefault_ACU 的 Object.assign(defaults) 组合会覆盖 false；plot-history-preset.ts 在新聊天/继承全局时会调用该重置函数。
approach: 三维评估综合最优的方案是把剧情推进 enabled 定义为“预设外的独立总开关”，在所有预设/默认/快照替换函数中显式保留调用前的 enabled 值，同时在全局默认设置中把 enabled 改为 false，降低新安装或缺失字段情况下的误启动风险。具体做法是在 src/service/plot/plot-logic.ts 中新增/使用保留逻辑：resetPlotSettingsToDefault_ACU 在清空对象前记录 preservedEnabled = plotSettings.enabled === true，Object.assign(defaults) 后恢复 plotSettings.enabled = preservedEnabled；replaceCurrentPlotSettingsWithSnapshot_ACU 同理，防止聊天快照缺失 enabled 时被 defaults=true 回填；applyPlotPresetToSettings_ACU 显式写回原 enabled，防御导入的旧预设携带 enabled 字段或后续维护者添加批量合并。再把 src/shared/defaults-json.js:143 默认 enabled 改为 false，满足“默认关闭”的安全语义。这样不需要把 enabled 写进预设或聊天快照，反而保持现有边界清晰。
  三维评分（每个维度 1-5 分，5 为最优）：
  - 可维护性: 5/5 — 将开关独立语义集中固化在 plot-logic.ts 的三个状态替换函数里，调用方无需逐个记住“应用预设前先保存 enabled”。这比在每个调用点手动保存/恢复更不容易漏。
  - 健壮性: 5/5 — 覆盖默认重置、聊天快照恢复、预设应用三条核心覆盖路径，并将默认值改为 false，缺失字段或旧数据不会倾向于自动启用。发送前已有双重门禁，保留即可。
  - 可扩展性: 4/5 — 未来新增剧情预设字段时仍可在 apply/reset/snapshot 中自然继承；唯一不足是 enabled 仍位于 plotSettings 对象内而非独立顶层字段，但在当前架构下这是最小且边界清晰的改动，后续若做设置模型纯化可迁移为 settings_ACU.plotEnabled。
edge_cases: 1. 用户关闭剧情推进后刷新页面，loadSettings_ACU 先加载 false，之后 loadPresetAndCleanCharacterData_ACU 即使调用 resetPlotSettingsToDefault_ACU，也必须保留 false。2. 用户关闭剧情推进后新开对话，新聊天没有 chat override，loadPresetAndCleanCharacterData_ACU 走继承全局/默认预设，也不能把 false 改回 true。3. 用户关闭剧情推进后切换全局预设或当前聊天预设，applyPlotPresetToSettings_ACU 不能因为旧预设或默认预设而改变 enabled。4. 用户开启剧情推进后切换预设，enabled=true 必须保持 true，不能被新的默认 false 误关。5. 旧聊天存在 chat override snapshot 且 snapshot 不含 enabled，replaceCurrentPlotSettingsWithSnapshot_ACU 必须保留当前开关状态。6. 旧导入预设中如果携带 enabled=true/false，预设应用也不应改变总开关，因为 getCurrentPlotSettingsFromUI_ACU 和 sanitizePlotSettingsSnapshotForChat_ACU 已经明确删除 enabled。7. 新安装/无保存设置时默认应关闭，避免用户未明确启用就触发剧情推进。8. 发送前门禁保持 settings_ACU.plotSettings.enabled 严格控制，不能引入绕过门禁的新状态源。
affected_scope: src/shared/defaults-json.js；src/service/plot/plot-logic.ts；.analysis-cache.md；后续验证会涉及 dist/index.bundle.js 生成但不在本次源码修复中手动编辑；不主动覆盖根目录 index.js，除非用户要求发布。
execution_plan:
  - step_1: 修改 src/shared/defaults-json.js 中 DEFAULT_PLOT_SETTINGS_ACU.enabled 的默认值，从 true 改为 false，使缺失保存值、新安装、默认重置的安全方向为“不自动启动”。
  - step_2: 修改 src/service/plot/plot-logic.ts 的 applyPlotPresetToSettings_ACU，在函数入口记录当前 plotSettings.enabled === true，并在应用 preset 字段后显式恢复该值，防止任何预设改变总开关。
  - step_3: 修改 src/service/plot/plot-logic.ts 的 resetPlotSettingsToDefault_ACU，在清空并赋默认值前记录 enabled，赋值后恢复 enabled，防止刷新/新聊天继承默认预设时把关闭状态改成开启。
  - step_4: 修改 src/service/plot/plot-logic.ts 的 replaceCurrentPlotSettingsWithSnapshot_ACU，在快照替换前记录 enabled，Object.assign(defaults, snapshot) 后恢复 enabled，防止旧聊天快照恢复时把关闭状态改成默认开启。
  - step_5: 执行 npm run build，确认 TypeScript/Rollup/架构护栏通过；再用只读搜索确认 bundle 中 DEFAULT_PLOT_SETTINGS_ACU enabled 为 false 且 plot-logic 保留 enabled 的逻辑进入产物。
  - step_6: 读取 .analysis-cache.md，输出并追加 output_quality_review，然后归档到 .analysis-archive/2026-04-30_HHmm_剧情推进开关误开启修复.md。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。相较只改默认值、只改门禁、只改 UI，本方案同时处理状态被覆盖的根因和缺失字段的安全默认；可维护性 5/5、健壮性 5/5、可扩展性 4/5。
  - 是否遗漏了已知边界条件？ → NO。已覆盖刷新页面、新开对话、切换全局预设、切换当前聊天预设、聊天快照恢复、旧预设携带 enabled、新安装默认、发送前门禁等路径。
  - 是否因改动量大而想缩减方案？ → NO。改动集中在默认值和 plot 状态替换函数，没有缩减根因修复范围。
  - 是否打算跳过某些文件？ → NO。settings-service.ts、state-manager.ts、popup-bindings-plot.ts、chat-scope-plot.ts 已侦察；它们现有行为与“enabled 独立于预设/快照”一致，不需要改。affected_scope 已列出实际需要修改的文件。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。src/shared/defaults-json.js 在 step_1；src/service/plot/plot-logic.ts 在 step_2 到 step_4；.analysis-cache.md 在当前写入和 step_6；构建产物只生成验证，不手动编辑。
  - context是否充分？是否有未读但可能相关的文件？ → NO。已读取默认设置、设置加载、运行门禁、剧情运行入口、预设应用、聊天快照、UI 绑定和刷新辅助文件。当前不需要继续读取其它文件才能执行。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO。发现 getCurrentPlotSettingsFromUI_ACU 和 sanitizePlotSettingsSnapshotForChat_ACU 删除 enabled，这不是无关问题，而是支持“总开关不属于预设/快照”的设计证据，已纳入方案。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO。源码修改将使用 edit_file/apply_diff；shell 只用于 git 状态、构建和只读验证。
</analysis>
---
<output_quality_review>
task_summary: 修复剧情推进功能开关关闭后刷新页面或新开对话可能自动启动的问题。根因是 DEFAULT_PLOT_SETTINGS_ACU.enabled 默认为 true，并且 resetPlotSettingsToDefault_ACU / replaceCurrentPlotSettingsWithSnapshot_ACU 在预设或聊天快照恢复时会用默认设置覆盖当前 plotSettings，从而把用户关闭的 enabled=false 改回 true。本次将默认剧情推进开关改为 false，并在 applyPlotPresetToSettings_ACU、resetPlotSettingsToDefault_ACU、replaceCurrentPlotSettingsWithSnapshot_ACU 中显式保留原 enabled 值，确保预设与聊天快照不再控制总开关。
deliverables: 修改 src/shared/defaults-json.js；修改 src/service/plot/plot-logic.ts；重新生成 dist/index.bundle.js；更新并归档 .analysis-cache.md。

# 量化指标总览
metrics:
  total_files_modified: 4 — 源码 2 个文件、构建产物 dist/index.bundle.js、流程记录 .analysis-cache.md。
  execution_plan_coverage: 6/6 = 100% — 默认值修改、三个剧情设置替换函数修复、构建与 bundle 验证、验收归档步骤均已执行。
  edge_cases_handled: 8/8 = 100% — 刷新页面、新开对话、切换全局预设、切换当前聊天预设、开启状态保持、聊天快照恢复、旧预设携带 enabled、新安装默认关闭均已由默认 false 与 preservedEnabled 机制覆盖。
  confidence_assessment: HIGH — npm run build 通过，架构护栏总违规 0 条；findstr 确认源码与 bundle 均包含默认 enabled=false 和三处 preservedEnabled 逻辑；发送前门禁原本已存在且未被削弱。

# 产物实质性检查
substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？
    → NO。src/shared/defaults-json.js 的 enabled=false 改变新安装和缺失字段时的默认行为；src/service/plot/plot-logic.ts 的 preservedEnabled 改变预设/默认/快照恢复时的状态覆盖行为；dist/index.bundle.js 是实际运行产物；.analysis-cache.md 记录决策链。删除任一源码改动都会重新暴露误启动路径。
  - 产物是否能被其目标对象（被测代码/被重构模块/被修复的bug）的变化所"击穿"？
    → YES。如果 resetPlotSettingsToDefault_ACU 不恢复 enabled，关闭后新对话继承默认预设仍可能被默认值覆盖；如果 replaceCurrentPlotSettingsWithSnapshot_ACU 不恢复 enabled，旧聊天快照恢复仍可能改开关；如果 DEFAULT_PLOT_SETTINGS_ACU.enabled 改回 true，新安装和缺失字段路径仍偏向误启用。构建与 bundle 搜索能暴露这些关键逻辑是否进入产物。
  - 实质性比率: 4/4 = 100%。四个产物都对行为或追溯有实际作用，没有凑数文件。

# 覆盖完整性检查
completeness_check:
  - 是否存在被跳过的模块/函数/路径？
    → NO。已侦察设置加载、UI 开关写入、预设应用、默认重置、聊天快照恢复、发送前门禁、运行入口。最终改动集中在根因函数和默认值，不需要修改 UI 与门禁，因为它们原本已经正确读取 settings_ACU.plotSettings.enabled。
  - 产物覆盖的范围是否与 execution_plan 中 affected_scope 完全一致？
    → YES。affected_scope 中的 src/shared/defaults-json.js 与 src/service/plot/plot-logic.ts 已修改；.analysis-cache.md 已写入分析和验收；dist/index.bundle.js 因 npm run build 生成，符合验证步骤。
  - 核心业务逻辑是否都有直接验证（不依赖间接覆盖）？
    → YES。npm run build 直接验证 TypeScript/Rollup/架构约束；findstr 直接验证 src/shared/defaults-json.js:143 的 enabled=false、src/service/plot/plot-logic.ts 三处 preservedEnabled、dist/index.bundle.js 三处 preservedEnabled。逻辑验收映射为：默认关闭、防止预设覆盖、防止默认重置覆盖、防止聊天快照覆盖。
  - affected_scope 覆盖率: 4/4 = 100%。

# 价值密度检查
value_density_check:
  - 产物中高价值内容（验证核心逻辑/处理复杂场景）与低价值内容（验证trivial行为）的比例是多少？
    → 高价值:低价值 = 4:0，高价值占比 100%。所有改动都直接作用于误启动根因或运行产物验证，没有只改注释或表面 UI。
  - 是否存在"用数量掩盖质量"的模式——大量 trivial 产物掩盖了核心逻辑缺少验证的事实？
    → NO。本次改动只有两个源码文件，数量不多；每处修改都对应一个明确覆盖路径。

# 需求对齐检查
alignment_check:
  - 产物满足的是用户的字面需求还是本质需求？
    → 满足本质需求。用户要解决“关闭后刷新页面或新开对话可能自动启动”，本质是总开关不能被预设/默认/聊天作用域恢复链路覆盖。本次把剧情推进总开关从预设内容中隔离出来，并把默认安全方向改为关闭。
  - "如果这是别人交给我的，我会接受吗？"
    → YES。根因定位清楚，修复点集中，构建通过，bundle 已验证。唯一需要说明的是：本次没有覆盖根目录 index.js，也没有提交/推送；因为用户只要求修复问题，没有要求发布。擅自发布才是制造事故，助手，别把部署动作和代码修复混成一锅粥。
</output_quality_review>
