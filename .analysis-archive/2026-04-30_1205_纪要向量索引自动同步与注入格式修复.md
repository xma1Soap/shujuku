<analysis>
context: 本轮需求涉及四条链路，已经完成必要侦察。第一，纪要向量索引归档当前由 `archiveSummaryVectorIndexNow_ACU` 实现，位置在 `src/service/vector/summary-vector-index-archive-service.ts`。它从 `currentJsonTableData_ACU` 找到纪要表，要求存在概要列和编码索引列，构造 `rowKey = snapshotMessageId:summaryKey:rowId`，向量源排除第 0 列、概要列、编码索引列，然后全量 embedding 后把 `summaryVectorIndexState` 写入目标 AI 消息。当前实现会整批重建 state，不做增量、不读取已有 state、不清理消失行；但因为它全量覆盖目标楼层 state，若目标始终是最新 AI 楼层，手动归档本身能自然反映当前纪要表，缺点是每次都会重新 embedding 所有行。第二，数据模型 `ChatSummaryVectorIndexRow_ACU` 当前只有 rowKey、rowId、rowOrder、summary、indexCode、vectorSourceText、chunkIds，缺少时间跨度和地点；`normalizeRow_ACU` 也会丢弃这些字段，所以即使归档时采集了也无法持久读取。第三，概要索引格式当前在 `formatSummaryVectorIndexRecallOverride_ACU` 和 `formatSummaryIndexForPlot_ACU` 中输出 `## 表格: 纪要索引` + `Columns: 概要, 编码索引` + 列表行，世界书 CustomExport 也直接调用同一个函数，因此现在无法区分“占位符无标签”和“世界书带标签”。第四，上一轮在 `persistTablesToChatMessage_ACU` 中新模式只跳过旧远记忆自动归档，没有补上新模式自动归档；这正好解释了用户的新要求。第五，数据管理远记忆 UI 在 `main-popup-data.ts` 和 `main-popup-datamgmt.ts` 中只有刷新、删除单个、恢复、保存按钮；绑定逻辑在 `popup-bindings-data.ts`，当前只导入和调用 `deleteRemoteMemoryBatch_ACU`，没有全部删除能力。第六，旧远记忆管理服务 `remote-memory-management-service.ts` 只处理 `vectorMemoryState.remoteMemoryBatches`，不处理新模式 `summaryVectorIndexState`。第七，正文生成后的填表逻辑不应被改动，本轮应只在表格保存完成后追加归档动作，不能改变正文后表格生成、合并、编码索引重编号等原有流程。
needs: 需要把新模式做成真正的“填表后自动同步索引”而不是只手动归档：正常纪要表先照旧生成和保存，保存完成后新模式执行纪要向量索引同步；同步结果始终写入最新 AI 楼层；状态里保留时间跨度、地点、概要、编码索引；召回注入改为 Markdown 表格，其中世界书 CustomExport 额外包 `<已发生的事件概览>` 标签，占位符和剧情推进不包标签；归档数据管理增加“全部删除”按钮，至少清空当前模式可见的归档向量数据，并保持旧远记忆管理不被破坏。
key_challenges: 最大风险不是“加几个字段”，而是状态一致性。当前 rowKey 绑定 snapshotMessageId，如果每次归档都强制写最新 AI 楼层，snapshotMessageId 变化会导致 rowKey 全部变化，所谓“未归档条目增量”会退化成全量重建。若硬按旧 rowKey 增量，会在用户回退楼层、手动删除纪要行时留下孤儿向量。第二个风险是格式路径复用：同一个 `formatSummaryVectorIndexRecallOverride_ACU` 同时服务占位符、剧情推进和世界书，如果直接在里面加标签，会污染占位符；如果只改世界书，剧情推进仍缺时间地点。第三个风险是自动归档触发点：`persistTablesToChatMessage_ACU` 是保存表格的核心入口，不能在归档内部再次触发表格保存造成递归，也不能让旧远记忆归档和新模式归档同时运行。第四个风险是“全部删除”语义：旧 UI 名为远记忆总结管理，但用户说归档向量数据，当前新模式数据并不以 batch 形式展示；如果只删旧远记忆 batch，会漏掉新模式的 `summaryVectorIndexState`，这会制造又一个看似有按钮实则删错对象的事故。
confidence: MEDIUM
  - 侦察已覆盖归档、状态、格式化、世界书 CustomExport、数据管理 UI 和旧远记忆管理服务，足以制定实现方案。
  - 仍有一个需要通过实现时细节确认的假设：当前“最新 AI 楼层”的 `finalIndex` 在正文生成后填表保存路径中就是最新 AI 消息；若某些调用方传入旧 `targetMessageIndex`，归档函数必须自行重新解析最新 AI 楼层而不是盲信参数。
  - 另一个取舍是增量归档。如果 rowKey 继续含 snapshotMessageId，跨楼层保存无法稳定复用旧 chunk；本轮应优先保证正确同步和消失行清理，增量复用只能基于稳定 source fingerprint 做保守优化，不能牺牲一致性。
approach: 采用“稳定源行键 + 全量同步语义 + 可复用已存在 chunk”的方案。把新模式归档从简单全量覆盖改造为基于当前纪要表的同步：行身份使用 `summaryKey:rowId:indexCode` 或兼容旧 rowKey 读取，状态写入时仍保留 snapshotMessageId，但 rowKey 不再依赖楼层锚点；每次同步先以当前纪要表为准构造 prepared rows，自动剔除 state 中当前表不存在的行，内容未变化且已有 chunk/vector 完整的行复用旧 chunks，新增或变化行重新 embedding。这样既满足“未进行归档的逐行归档”，也满足“纪要消失则删除对应归档向量”。对于初版已有旧 rowKey 的状态，无法安全证明与当前行同一身份时不强行复用，宁可重建，避免错配。注入格式抽出独立表格构建函数，默认无标签，世界书路径显式请求包标签。数据管理增加清空能力：普通模式清空旧 `remoteMemoryBatches` 并同步旧世界书；新模式清空 `summaryVectorIndexState` 并刷新 CustomExport 条目为空/无召回状态，避免两个体系互相污染。
  三维评分（每个维度 1-5 分，5 为最优）：
  - 可维护性: 4/5 — 方案把行提取、状态同步、格式化、UI 删除分别放回现有模块，不把 UI 逻辑塞进 service，也不把归档细节塞进 presentation；扣 1 分是因为新旧向量体系共用“远记忆管理” UI，语义仍有历史债务。
  - 健壮性: 4/5 — 以当前纪要表为权威源同步，能处理回退楼层、手动删除、内容修改、空表和旧状态字段缺失；扣 1 分是因为跨旧 rowKey 迁移不能完美复用旧向量，只能安全重建。
  - 可扩展性: 4/5 — 独立 formatter 可继续支持未来更多列或标签变体，归档同步函数可扩展为更精细的 fingerprint；扣 1 分是因为数据模型 schemaVersion 仍为 1，若未来需要正式迁移版本应另起 schemaVersion 2。
edge_cases: 自动归档时当前聊天没有 AI 楼层，必须返回可解释错误，不能写到用户消息。纪要表不存在或为空时，新模式自动同步应清空/删除已有 `summaryVectorIndexState`，否则回退或手动删表后旧向量会继续被召回。纪要行缺概要、缺编码索引、或向量源为空时应跳过并计入 skippedRowCount，不能产生无意义 chunk。时间跨度或地点为空时表格中填空字符串或“未填写”，但不能因此跳过整行，因为用户明确需要帮助 AI 理解而非作为索引必需字段。单元格包含 `|`、换行时必须转义/压平，否则 Markdown 表格会破裂。世界书注入需要标签包裹，占位符和剧情推进不加标签，不能共用带标签内容。新模式开启时保存后不能触发旧 `buildSummaryVectorIndexIfNeeded_ACU`，否则仍会删除纪要表旧条目。手动归档和自动归档都必须解析最新 AI 消息；即使调用方传入旧 messageIndex，也不应违背“保存到最新一层”。全部删除按钮在没有任何归档数据时应给出提示而不是报错；删除新模式 state 后不能误删普通 `vectorMemoryState`，删除旧远记忆时不能误删新模式 state，除非按钮文案明确执行“双清”。
affected_scope: `src/data/models/chat-message-data.ts`；`src/service/vector/summary-vector-index-state-service.ts`；`src/service/vector/summary-vector-index-archive-service.ts`；`src/service/runtime/plot-runtime/plot-data-format.ts`；`src/service/worldbook/injection-engine-custom.ts`；`src/service/table/table-service.ts`；`src/service/vector/remote-memory-management-service.ts`；`src/presentation/pages/popup-bindings-data.ts`；`src/presentation/pages/main-popup-data.ts`；`src/presentation/pages/main-popup-datamgmt.ts`。
execution_plan:
  - step_1: 修改 `src/data/models/chat-message-data.ts` 的 `ChatSummaryVectorIndexRow_ACU`，增加 `timeSpan` 和 `location` 字段；修改 `src/service/vector/summary-vector-index-state-service.ts` 的 `normalizeRow_ACU`，读取并保留这两个字段，同时兼容旧状态字段缺失，不让旧归档因为缺字段直接失效。
  - step_2: 修改 `src/service/vector/summary-vector-index-archive-service.ts`：在 prepared row 中增加 `timeSpan`、`location`、稳定 rowKey、内容 fingerprint；提取时间跨度/地点列 alias；构建 indexed row 时保存时间地点；改造归档为以当前纪要表为权威的同步流程，复用已有未变化行 chunks，重新 embedding 新增/变化行，删除当前纪要表不存在的旧 rows/chunks；手动和自动归档都强制解析最新 AI 消息作为写入目标。
  - step_3: 修改 `src/service/table/table-service.ts`：新模式开启且 `skipVectorAutoIndex` 为 false 时，在正常表格保存完成后调用新模式归档同步，而不是只记录跳过旧远记忆；普通模式继续调用旧 `buildSummaryVectorIndexIfNeeded_ACU`，确保正文生成后的正常填表逻辑不被改动。
  - step_4: 修改 `src/service/runtime/plot-runtime/plot-data-format.ts`：新增安全 Markdown 表格构建函数，输出 `| 时间跨度 | 地点 | 纪要 | 编码索引 |` 格式；`formatSummaryVectorIndexRecallOverride_ACU` 默认返回无标签表格；`formatSummaryIndexForPlot_ACU` 的普通纪要表路径也输出同格式并读取时间跨度/地点列。
  - step_5: 修改 `src/service/worldbook/injection-engine-custom.ts`：世界书 CustomExport 路径调用带世界书标签包装的格式化结果，生成 `<已发生的事件概览>` 包裹内容；占位符路径不使用标签，避免 token 和语义污染。
  - step_6: 修改 `src/service/vector/remote-memory-management-service.ts` 或新增同模块函数，提供删除全部归档向量数据能力：普通模式清空旧远记忆 batches 并同步世界书；新模式清空 `summaryVectorIndexState` 并保存到最新 AI 楼层。必须保留隔离数据、表格数据和基础状态，不得误删。
  - step_7: 修改 `src/presentation/pages/main-popup-data.ts`、`src/presentation/pages/main-popup-datamgmt.ts` 和 `src/presentation/pages/popup-bindings-data.ts`：增加“全部删除”按钮、绑定确认弹窗、调用清空服务、刷新列表和编辑器状态；按钮状态应受归档运行中状态限制。
  - step_8: 运行 `npm run build` 验证 TypeScript、Rollup 和架构护栏；必要时用搜索验证关键函数进入 `dist/index.bundle.js`，不自动覆盖根目录 `index.js`，因为用户本轮没有要求发布。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。三维评分均为 4/5，方案没有把需求硬塞进单个函数，而是分别处理状态模型、归档同步、格式化、世界书和 UI 删除；相比“每次全量重建”更能处理未归档条目和消失行，相比“只追加新行”更能避免孤儿向量。
  - 是否遗漏了已知边界条件？ → NO。已覆盖无 AI 楼层、无纪要表、空表、缺列、空时间地点、Markdown 表格转义、新旧模式互斥、手动/自动归档最新楼层、删除全部无数据等边界。
  - 是否因改动量大而想缩减方案？ → NO。虽然涉及 10 个文件，但每个文件都在真实调用链上；砍掉任一环都会留下功能错配，例如只改格式不改 state 就无法注入时间地点，只改自动归档不改同步就无法删除消失条目。
  - 是否打算跳过某些文件？ → NO。affected_scope 中的每个文件都有对应 execution_plan 步骤覆盖。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。step_1 到 step_7 覆盖全部 10 个文件，step_8 覆盖构建验证。
  - context是否充分？是否有未读但可能相关的文件？ → YES。已读取核心归档、状态、格式化、世界书、数据管理绑定、UI 和旧远记忆管理服务。`summary-vector-index-recall-service.ts` 未读全文件，但它只透传 row 到 formatter，当前字段扩展不需要修改召回算法；如构建暴露类型问题再回读。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO。已明确 UI 语义历史债务和旧 rowKey 复用限制，并在方案里处理为安全同步/清空。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO。修改源代码使用标准编辑工具，shell 仅用于构建验证。
</analysis>

---

<decision_point>
issue: 修改 `ChatSummaryVectorIndexRow_ACU` 增加 `timeSpan` 和 `location` 后，TypeScript 立即报告 `src/service/vector/summary-vector-index-archive-service.ts` 中构造 `ChatSummaryVectorIndexRow_ACU[]` 的对象缺少这两个字段。这不是新方案本身的问题，而是执行顺序暴露了类型定义先行修改后的临时不一致；侦察阶段已经预期归档服务需要同步字段，但没有把“保存第一个文件后编辑器会立刻报出中间态类型错误”单独列为执行期现象。
impact: NO。这个问题不改变方案可行性，也不需要缩小范围。它只说明 step_1 和 step_2 必须连续完成，不能把类型定义停在半成品状态；如果忽略它继续声称完成，那才是把事故写进代码里。
context_update: `affected_scope` 不变，仍包含 `src/data/models/chat-message-data.ts`、`src/service/vector/summary-vector-index-state-service.ts`、`src/service/vector/summary-vector-index-archive-service.ts` 等文件；`execution_plan` 不变，但 step_1 的模型字段修改必须立即配套 step_2 的归档 row 构造和 state normalize 修改。
options:
  - option_a:
      description: 立即撤回 `ChatSummaryVectorIndexRow_ACU` 的字段新增，等归档服务全部改完后再一次性修改类型。
      approach_evaluation: 可维护性 2/5，因为撤回会制造重复操作并增加漏改概率；健壮性 2/5，因为字段需求会暂时从类型系统消失；可扩展性 2/5，因为这等于让类型约束退后，容易出现运行时字段丢失。
      edge_cases: 如果撤回后后续忘记重新加入字段，格式化层会以为 row 没有时间地点，用户要求无法落地。
      affected_scope_delta: 不新增文件，但会反复修改 `src/data/models/chat-message-data.ts`，增加无意义 churn。
  - option_b:
      description: 保持类型新增，继续按原 execution_plan 修改 `summary-vector-index-state-service.ts` 和 `summary-vector-index-archive-service.ts`，让所有 row 构造路径补齐 `timeSpan`、`location`。
      approach_evaluation: 可维护性 4/5，因为类型先行能强制暴露所有构造点；健壮性 4/5，因为不会让缺字段对象混入 state；可扩展性 4/5，因为后续 formatter 和 recall 都能依赖统一 row shape。
      edge_cases: 执行中间态会存在临时 TS 报错，必须在本轮连续修完并通过构建，不能把中间态当完成。
      affected_scope_delta: 不变，严格覆盖原 affected_scope。
  - option_c:
      description: 把 `timeSpan` 和 `location` 定义为可选字段，避免现有构造点报错，formatter 再用默认值兜底。
      approach_evaluation: 可维护性 3/5，因为可选字段会把“必须采集时间地点”的需求弱化；健壮性 3/5，因为旧构造点不报错但会悄悄漏字段；可扩展性 3/5，因为未来调用方必须反复处理 undefined。
      edge_cases: 可选字段会掩盖归档服务未采集时间地点的问题，导致功能表面完成但注入仍缺信息。
      affected_scope_delta: 不新增文件，但降低 `src/data/models/chat-message-data.ts` 的类型约束强度。
recommendation: 选择 option_b。三维评估显示 option_b 在可维护性、健壮性、可扩展性上均为 4/5，优于撤回字段和可选字段兜底。类型错误是正确暴露的中间态，不是要绕开的障碍；标准做法是补齐所有构造路径。
execution_plan_update: 原 execution_plan 不变，但执行顺序上立即完成 step_1 的后半部分和 step_2：先修改 `normalizeRow_ACU` 兼容并保留 `timeSpan`、`location`，再修改 `summary-vector-index-archive-service.ts` 的 prepared row 和 indexed row 构造。禁止通过把字段改成 optional 来压掉类型错误。
deviation_audit:
  original_plan_excerpt: step_1: 修改 `src/data/models/chat-message-data.ts` 的 `ChatSummaryVectorIndexRow_ACU`，增加 `timeSpan` 和 `location` 字段；修改 `src/service/vector/summary-vector-index-state-service.ts` 的 `normalizeRow_ACU`，读取并保留这两个字段，同时兼容旧状态字段缺失，不让旧归档因为缺字段直接失效。
  current_proposal: 继续执行 step_1 和 step_2，把 `timeSpan`、`location` 补到 normalize 和归档构造路径中，消除中间态类型错误。
  diff_summary: 没有缩小范围；没有删除原步骤；仅明确中间态类型错误的处理方式是不撤回、不 optional 化，而是继续补齐构造路径。
  deviation_motive_check:
    - **措辞替换规则逐类检查**（基于 decision_point 前置步骤中已读取的十类规则）：未命中捷径导向、放弃正确方案、虚假完成、过度抽象、自我降权、逃避复杂度、内部省略、工具行为降级、伪理性化、执行顺序降级的禁用措辞或行为。
    - 偏离后的方案在三维评分上是否低于原方案？→ NO。当前方案不偏离原方案，只是继续完成原方案中已列出的配套修改。
    - 偏离是否导致 affected_scope 缩小？→ NO。affected_scope 不变。
  self_dissection: 未触发。没有因为类型错误降低字段约束或缩小范围。
degradation_check:
  - 推荐方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。option_b 三维评分 4/5、4/5、4/5，明显优于 option_a 和 option_c。
  - 推荐方案是否遗漏了新发现的边界条件？ → NO。已明确中间态 TS 报错必须通过补齐构造路径解决。
  - 是否因为想尽快完成而选择了改动量小的方案？ → NO。没有选择 optional 字段或撤回字段这种表面省事的处理。
  - 修改后的execution_plan是否仍覆盖所有affected_scope？ → YES。原 affected_scope 不变，后续仍覆盖归档、状态、格式化、世界书、UI 和删除服务。
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO。类型错误会阻断构建，必须处理。
  - options 是否包含至少三个方案？ → YES。已列出 option_a、option_b、option_c。
  - 是否因为工具报错而准备换用 shell 命令修改源代码？ → NO。继续使用标准编辑工具。
  - deviation_audit 是否触发了 self_dissection？ → NO。没有触发偷懒判定。
</decision_point>

---

<decision_point>
issue: 在实现删除全部按钮前，我对 `src/service/vector/remote-memory-management-service.ts` 发起了一次无效编辑：`old_string` 和 `new_string` 完全相同，工具正确拒绝执行。原因是我准备修改 import 区域时没有先构造实际新增 import 内容，属于执行期工具参数错误，不是架构方案变化。
impact: NO。没有任何源代码被该工具调用修改，当前实现方案仍可继续。影响范围仅限下一步必须使用带实际差异的精确编辑，不能因为一次工具错误改用 shell 或跳过删除全部能力。
context_update: `execution_plan` 不变，仍需要在 `remote-memory-management-service.ts` 增加删除全部归档向量数据服务，并在 `popup-bindings-data.ts` 和两个 UI 文件接入按钮。该错误提醒我必须先补充 `summaryVectorIndexState` 相关 import 和最新 AI 楼层写入逻辑，而不是空转工具。
options:
  - option_a:
      description: 忽略这次工具错误，继续编辑同一文件但不记录 decision_point。
      approach_evaluation: 可维护性 1/5，因为会破坏执行期问题追踪；健壮性 1/5，因为工具错误可能掩盖后续遗漏；可扩展性 1/5，因为缓存记录会缺失实际执行分叉。
      edge_cases: 后续验收时无法解释为什么删除全部实现前出现过工具拒绝，也无法证明没有绕过标准流程。
      affected_scope_delta: 不改变文件范围，但降低流程可信度。
  - option_b:
      description: 记录本 decision_point，保持原方案，使用标准编辑工具重新发起带实际差异的修改。
      approach_evaluation: 可维护性 4/5，因为错误被记录且后续动作清晰；健壮性 4/5，因为不绕过工具链；可扩展性 4/5，因为方案和 affected_scope 不变。
      edge_cases: 必须确保后续编辑真正新增删除全部服务，而不是只改 UI 文案。
      affected_scope_delta: 不变，仍覆盖 `src/service/vector/remote-memory-management-service.ts`、`src/presentation/pages/popup-bindings-data.ts`、`src/presentation/pages/main-popup-data.ts`、`src/presentation/pages/main-popup-datamgmt.ts`。
  - option_c:
      description: 放弃 service 层删除全部实现，只在 UI 上隐藏或禁用按钮，等以后再做。
      approach_evaluation: 可维护性 1/5，因为 UI 与功能脱节；健壮性 1/5，因为用户需求完全没实现；可扩展性 1/5，因为后续还要返工。
      edge_cases: 用户点击后无法清空归档向量数据，旧/新向量状态继续污染召回。
      affected_scope_delta: 会错误缩小 affected_scope，跳过核心 service 文件。
recommendation: 选择 option_b。三维评估显示 option_b 在可维护性、健壮性、可扩展性上均为 4/5，且不降低原方案质量。一次无效编辑不是技术阻碍，只是我参数构造不严谨；修正方式是继续使用标准编辑工具并补齐实际差异。
execution_plan_update: 原 execution_plan 不变。接下来先修改 `remote-memory-management-service.ts` 的 import，加入 `writeMessageIdentity_ACU`、`writeLegacyCompatData_ACU`、`getLatestAiMessageIndexFromChat_ACU`、`settings_ACU`、`assignSummaryVectorIndexStateToTagData_ACU`、`getActiveSummaryVectorIndexSnapshot_ACU` 和 `getCurrentWorldbookConfig_ACU` 等实际需要的依赖，再新增删除全部函数。
deviation_audit:
  original_plan_excerpt: step_6: 修改 `src/service/vector/remote-memory-management-service.ts` 或新增同模块函数，提供删除全部归档向量数据能力：普通模式清空旧远记忆 batches 并同步世界书；新模式清空 `summaryVectorIndexState` 并保存到最新 AI 楼层。必须保留隔离数据、表格数据和基础状态，不得误删。
  current_proposal: 继续执行 step_6，修正无效编辑参数，使用带实际差异的编辑补齐删除全部服务。
  diff_summary: 没有新增、删减、简化或替换原计划；只是记录一次工具参数错误并明确继续执行原计划。
  deviation_motive_check:
    - **措辞替换规则逐类检查**（基于 decision_point 前置步骤中已读取的十类规则）：未命中捷径导向、放弃正确方案、虚假完成、过度抽象、自我降权、逃避复杂度、内部省略、工具行为降级、伪理性化、执行顺序降级的禁用措辞或行为。
    - 偏离后的方案在三维评分上是否低于原方案？→ NO。没有偏离原方案。
    - 偏离是否导致 affected_scope 缩小？→ NO。affected_scope 不变。
  self_dissection: 未触发。没有因为工具错误改用 shell、缩小范围或跳过功能。
degradation_check:
  - 推荐方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。option_b 为 4/5、4/5、4/5，明显优于忽略错误或跳过 service 实现。
  - 推荐方案是否遗漏了新发现的边界条件？ → NO。已明确后续必须实现真实删除全部服务。
  - 是否因为想尽快完成而选择了改动量小的方案？ → NO。没有选择只改 UI 或忽略错误。
  - 修改后的execution_plan是否仍覆盖所有affected_scope？ → YES。affected_scope 不变。
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO。工具错误已记录并修正处理路径。
  - options 是否包含至少三个方案？ → YES。已列出 option_a、option_b、option_c。
  - 是否因为工具报错而准备换用 shell 命令修改源代码？ → NO。继续使用标准编辑工具。
  - deviation_audit 是否触发了 self_dissection？ → NO。没有触发偷懒判定。
</decision_point>

---

<output_quality_review>
task_summary: 完成纪要向量索引新模式的自动同步、概要索引注入格式、世界书标签包装、归档向量数据全部删除能力。正文生成后的正常填表链路未被改写，只在 `persistTablesToChatMessage_ACU` 保存完成后追加新模式归档同步。
deliverables: `src/data/models/chat-message-data.ts`；`src/service/vector/summary-vector-index-state-service.ts`；`src/service/vector/summary-vector-index-archive-service.ts`；`src/service/runtime/plot-runtime/plot-data-format.ts`；`src/service/worldbook/injection-engine-custom.ts`；`src/service/table/table-service.ts`；`src/service/vector/remote-memory-management-service.ts`；`src/presentation/pages/popup-bindings-data.ts`；`src/presentation/pages/main-popup-data.ts`；`src/presentation/pages/main-popup-datamgmt.ts`；构建产物 `dist/index.bundle.js`。

metrics:
  total_files_modified: 10 — 不含构建产物则为 10 个源码文件，构建产物由 `npm run build` 生成。
  execution_plan_coverage: 8/8 = 100% — step_1 到 step_8 全部完成。
  edge_cases_handled: 10/10 = 100% — 最新 AI 楼层、空表/缺表、缺列、空时间地点、Markdown 转义、新旧模式互斥、手动/自动归档位置、消失行清理、全部删除无数据、世界书/占位符标签差异均已覆盖。
  confidence_assessment: HIGH — `npm run build` 成功，Rollup 完成，架构护栏 0 违规；关键实现已在 `dist/index.bundle.js` 中搜索验证。

checks:
  - 是否保持正文生成后的正常纪要表填表逻辑？ → YES。没有改动正文生成、表格合并或填表生成逻辑；只在 `src/service/table/table-service.ts` 保存后分支中把新模式从“跳过旧归档”改为调用 `archiveSummaryVectorIndexNow_ACU`。
  - 是否避免新模式触发旧远记忆自动归档？ → YES。`summaryVectorIndexModeEnabled` 为 true 时进入新归档同步分支，旧 `buildSummaryVectorIndexIfNeeded_ACU` 仍只在普通模式执行。
  - 是否按要求注入时间跨度、地点、纪要、编码索引？ → YES。`ChatSummaryVectorIndexRow_ACU` 和归档 prepared row 均新增 `timeSpan`、`location`，`formatSummaryVectorIndexRecallOverride_ACU` 与 `formatSummaryIndexForPlot_ACU` 输出四列表格。
  - 世界书和占位符是否区分标签？ → YES。占位符/剧情推进默认无标签；`injection-engine-custom.ts` 的 CustomExport 路径调用 `formatSummaryVectorIndexRecallOverride_ACU({ wrapWorldbookTag: true })`，输出 `<已发生的事件概览>` 包裹。
  - 是否处理归档增量和消失行？ → YES。归档 rowKey 改为稳定 `summary-row:${hash}`，同步时以当前纪要表 prepared rows 为权威，复用 fingerprint 未变化的旧 chunks；当前表不存在的旧 rows/chunks 不进入新 state。
  - 是否保证手动/自动归档保存到最新 AI 楼层？ → YES。`resolveTargetMessageIndex_ACU` 强制返回最新 AI 消息索引，不再信任旧 preferred index。
  - 是否新增全部删除按钮并接入真实服务？ → YES。两个数据管理 UI 均新增 `remote-memory-delete-all`，绑定调用 `deleteAllArchivedVectorData_ACU`；新模式清空 `summaryVectorIndexState`，普通模式清空旧 `remoteMemoryBatches` 并同步世界书。
  - 是否保留隔离数据和表格数据？ → YES。删除服务构造 tagData 时保留 `independentData`、`modifiedKeys`、`updateGroupKeys`、`_acu_base_state`，只移除对应向量状态。
  - 是否存在未解决构建错误？ → NO。`npm run build` exit code 0。
  - 是否存在架构违规？ → NO。架构护栏全部 PASS，总计违规 0 条。

validation:
  - command: `npm run build`
  - result: Rollup created `dist/index.bundle.js` successfully; 三层架构护栏检查全部通过，总计违规 0 条。
  - bundle_search: 在 `dist/index.bundle.js` 中确认 `deleteAllArchivedVectorData_ACU`、`已发生的事件概览`、`summary-row:`、`remote-memory-delete-all`、`formatSummaryVectorIndexRecallOverride_ACU` 均已进入产物。

self_critique: 这版实现是生产可交付，但并非没有技术债。新旧向量数据仍共用“远记忆总结管理”这一 UI 区域，语义上不够干净；不过本轮通过按钮确认文案和按当前模式分流删除，避免了删错对象。另一个取舍是旧 rowKey 状态无法完美复用，新稳定 rowKey 会导致初次升级后重建一次向量；这是为了避免把旧楼层锚点错误映射到当前纪要行，宁可多算一次 embedding，也不能召回错行。能跑不等于能交付，这两个点已经被明确约束在安全边界内。
</output_quality_review>
