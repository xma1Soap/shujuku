<analysis>
context: 本轮侦察确认了四个反馈点的真实代码位置和问题链路。第一，纪要向量索引归档在 src/service/vector/summary-vector-index-archive-service.ts 的 buildPreparedRows_ACU 中定位了概要列和编码索引列，但构建 vectorSourceText 时只排除了第 0 列和概要列，未排除编码索引列；row 结构已经保存 summary 与 indexCode，因此注入侧数据基础存在，问题只在向量源列排除不完整。第二，TavernDB-ACU-CustomExport-纪要索引 条目由 src/service/worldbook/injection-engine-custom.ts 的 updateCustomTableExports_ACU 生成，额外索引条目的内容来自 resolveExtraIndexSpec_ACU 与 buildExtraIndexEntryBlock_ACU 构建的 markdown 表；当前 src/service/runtime/plot-runtime/plot-data-format.ts 只在 formatSummaryIndexForPlot_ACU 内读取 currentSummaryVectorIndexRecall_ACU 覆盖剧情推进和模板占位符路径，没有把覆盖内容接入 CustomExport 世界书条目生成链路，所以用户看到世界书条目仍是全量纪要索引。第三，关键词默认提示词位于 src/shared/defaults.ts 的 defaultVectorMemoryConfig_ACU.keywordPromptGroup，目前要求输出 3 到 8 个关键词；一次性默认刷新版本常量 VECTOR_MEMORY_DEFAULTS_REFRESH_VERSION_ACU 也在 src/shared/defaults.ts，加载设置时 src/service/settings/settings-service.ts 第 272-285 行仅刷新归档/召回参数与 summaryPromptGroup，没有刷新 keywordPromptGroup。第四，编码索引特殊锁定实现位于 src/service/runtime/helpers-table-lock.ts：applySpecialIndexSequenceToSummaryTables_ACU 会对 总结表/总体大纲/纪要表 在锁定开启时从 AM0001 开始写入编码索引列；但这个函数目前只在 src/presentation/pages/visualizer-main-save.ts 保存可视化编辑器时调用。AI 填表主链路 src/service/table/update-orchestrator.ts 在 parseAndApplyTableEdits_ACU 成功后直接 saveIndependentTableToChatHistory_ACU，并未在每轮填表完成时执行重编号。SQLite 路径通过 src/service/ai/prompt-builder/table-edit-parser.ts 在 SQL 内容时调用 getStorageProvider().applyEdits，src/service/table/sql-table-service.ts 的 applyEdits 执行 SQL 后 _syncToJson，同样没有重编号；而最终保存仍回到 saveIndependentTableToChatHistory_ACU。原生路径由 table-edit-parser.ts 直接修改 currentJsonTableData_ACU，也没有统一重编号。saveIndependentTableToChatHistory_ACU 是多数持久化路径的统一出口，但若只在出口重编号，SQLite 引擎内存库不会同步，因此每轮 SQL 后若后续继续 SQL 查询可能看到未重编号的库内状态。综合看，必须在 parse/apply 完成后、保存前对 currentJsonTableData_ACU 做一次统一重编号；对 SQLite 需要在 _syncToJson 后即可至少保持 JSON 视图正确，若还要保证 SQL 引擎也同步，则需要在 SqlTableService 内提供对纪要/总结/大纲表的 SQL 回写。但当前用户要求是填表完成覆盖保存，最低正确持久化点是保存前 JSON 视图重编号。
needs: 需求的本质不是把四个点分别打补丁，而是让“纪要向量索引模式”在数据来源、召回覆盖、世界书注入、关键词生成、纪要编号持久化上形成一致闭环。归档必须只把用于语义检索的正文性列送入向量，避免概要列和编码索引列污染匹配；注入必须统一使用筛选后的概要+编码索引，且包括 CustomExport 世界书条目；默认关键词提示词必须升级到每轮 12 个最相关词，并通过版本刷新覆盖已有旧默认配置；编码索引必须在 AI 填表完成后、持久化前统一重编号，不能只依赖可视化保存入口。
key_challenges: 第一，CustomExport 纪要索引条目不是走 formatSummaryIndexForPlot_ACU，而是 injection-engine-custom.ts 的额外索引条目构建路径，如果继续只改剧情推进 formatter，就会出现“占位符变了、世界书条目没变”的割裂。第二，关键词默认提示词的一次性覆盖需要避免污染模型、API、endpoint 等用户连接配置，也要避免遗漏已有用户的旧默认 keywordPromptGroup；当前版本刷新逻辑已有常量，可以升级版本并刷新 keywordPromptGroup。第三，编码索引重编号必须覆盖原生和 SQLite 两条填表路径；如果只改 table-edit-parser 的原生 DSL 分支，SQL 模式仍失效；如果只改 SqlTableService，原生模式失效；如果只改可视化保存，AI 填表链路仍失效。第四，编码索引列索引函数 getSummaryIndexColumnIndex_ACU 返回的是去掉 row_id 后的列索引，applySummaryIndexSequenceToTable_ACU 写 row[colIndex + 1]，这个约定必须保持，否则会错写列。
confidence: MEDIUM
  - 侦察已覆盖四个反馈点的主要文件与调用链，归档、世界书 CustomExport、默认提示词刷新、编码索引锁定入口都已定位。置信度不是 HIGH 的原因是 SQLite 引擎内部是否必须同步 AM 编码到实际 SQL 表尚未完全验证；当前方案先保证每轮填表完成后保存到聊天记录的 JSON 视图正确，必要时再根据构建/行为反馈补充 SQL 引擎同步。
approach: 三维评估综合最优的方案是：抽出并复用现有概要索引覆盖格式化能力，让 CustomExport 的 纪要索引 额外条目在新模式运行时直接使用 currentSummaryVectorIndexRecall_ACU 生成的覆盖内容；归档源列排除编码索引；升级向量默认刷新版本并刷新 keywordPromptGroup；在 AI 填表解析成功后、保存前统一调用 applySpecialIndexSequenceToSummaryTables_ACU，覆盖原生与 SQLite 的 JSON 持久化路径，并在必要保存入口保留统一出口校正。
  三维评分（每个维度 1-5 分，5 为最优）：
  - 可维护性: 4/5 — 复用既有 helpers-table-lock.ts 的编号逻辑和 plot-data-format.ts 的纪要索引文本格式，避免复制两套编号/格式化规则；CustomExport 接入点在 injection-engine-custom.ts 内部，贴合条目生成职责。扣 1 分是因为 currentSummaryVectorIndexRecall_ACU 是运行时全局状态，CustomExport 与剧情推进共享它仍有一定隐式耦合。
  - 健壮性: 4/5 — 保存前统一重编号可以覆盖原生和 SQLite 的 AI 填表结果，归档排除列避免检索污染，默认提示词版本刷新避免旧配置滞留。扣 1 分是 SQLite 内存库如果在同一轮保存前被其他代码直接查询，可能仍短暂保留未重编号状态；目前主业务要求是覆盖保存，风险可控但需要构建后继续检查是否存在更合适的 SQL 层同步点。
  - 可扩展性: 4/5 — 若未来增加其它索引列或其它召回覆盖模式，可以在公共 formatter/override helper 扩展；编号逻辑仍集中在 helpers-table-lock.ts。扣 1 分是 CustomExport 的额外索引逻辑仍在一个较大的函数内部，局部改动需要谨慎。
edge_cases: 需要处理的边界条件包括：纪要表缺少概要列时归档应失败并提示，不应构建空索引；纪要表缺少编码索引列时归档应失败并提示，因为注入要求概要+编码索引；概要列或编码索引列为空的行不能参与索引，否则注入无法定位；编码索引列存在但向量源构建时必须排除，避免 AM0001 这类低语义 token 干扰召回；召回结果为空时 CustomExport 纪要索引也应注入“未召回相关纪要”，不能回退全量纪要；新模式未开启或没有 currentSummaryVectorIndexRecall_ACU 时世界书条目应保持原有全量导出逻辑；默认 keywordPromptGroup 已被用户明显自定义时版本刷新会覆盖，这符合用户要求“一次性版本覆盖更新”，但不能覆盖 API、模型、endpoint、key；AI 填表未修改纪要/总结/大纲表时重编号函数应无副作用；编码索引特殊锁定被用户对某 sheet 显式关闭时不应强制编号；无编码索引列时 getSummaryIndexColumnIndex_ACU 当前会回退最后一列，既有 UI 提示也是未识别则默认最后一列，本次不改变该约定；SQLite SQL 执行失败并重试时不能提前保存半成品编号；import 模式跳过保存时不应伪造持久化；可视化保存已有重编号调用，不应重复导致错误，因为函数幂等。
affected_scope: src/service/vector/summary-vector-index-archive-service.ts；src/service/runtime/plot-runtime/plot-data-format.ts；src/service/worldbook/injection-engine-custom.ts；src/shared/defaults.ts；src/service/settings/settings-service.ts；src/service/table/update-orchestrator.ts；src/service/ai/prompt-builder/table-edit-parser.ts；src/service/table/table-service.ts；src/service/table/sql-table-service.ts；src/service/runtime/helpers-table-lock.ts；.analysis-cache.md；dist/index.bundle.js（构建产物）。
execution_plan:
  - step_1: 修改 src/service/vector/summary-vector-index-archive-service.ts 的 buildPreparedRows_ACU，在构建 sourceParts 时同时排除第 0 列、概要列、编码索引列，保留 row.summary 与 row.indexCode 用于注入。
  - step_2: 修改 src/service/runtime/plot-runtime/plot-data-format.ts，将 formatSummaryVectorIndexRecallOverride_ACU 导出或新增公开 helper，使 CustomExport 可以获取与 $5 相同的“概要+编码索引”覆盖文本；保持召回为空时返回“未召回相关纪要”。
  - step_3: 修改 src/service/worldbook/injection-engine-custom.ts，引入纪要索引覆盖 helper，在 buildExtraIndexEntryBlock_ACU 处理 extraIndexSpec.entryName === '纪要索引' 且当前世界书配置 summaryVectorIndexModeEnabled 为 true 时，用召回覆盖内容替换 fullTable/mainContent；没有覆盖状态时保持原逻辑，避免普通导出被破坏。
  - step_4: 修改 src/shared/defaults.ts，将 VECTOR_MEMORY_DEFAULTS_REFRESH_VERSION_ACU 升级为新版本，并把 defaultVectorMemoryConfig_ACU.keywordPromptGroup 的系统提示词改为“生成最相关的 12 个词或短语”，用户提示同步改为“请仅输出 12 个最相关关键词”。
  - step_5: 修改 src/service/settings/settings-service.ts 的一次性刷新逻辑，在版本不匹配时刷新 keywordPromptGroup，同时继续只刷新归档/召回默认项和 summaryPromptGroup，不覆盖 embeddingEndpoint、embeddingApiKey、embeddingModel、rerankEndpoint、rerankApiKey、rerankModel、enabled、namespace、entryComment、entryKey。
  - step_6: 修改 src/service/table/update-orchestrator.ts，在 parseAndApplyTableEdits_ACU 成功并得到 modifiedKeys 后、保存前调用 applySpecialIndexSequenceToSummaryTables_ACU(currentJsonTableData_ACU)，确保每轮 AI 填表成功后原生与 SQLite JSON 视图都被 AM0001 起连续重编号；必要时把重编号后的 summary/outline key 纳入 keysToActuallySave，避免 AI 没声明修改但编号变化未保存。
  - step_7: 修改 src/service/table/table-service.ts 的 persistTablesToChatMessage_ACU，在持久化入口再次执行 applySpecialIndexSequenceToSummaryTables_ACU 作为统一兜底，并确保 keysToSave 与 targetSheetKeys 逻辑不会漏保存已重编号的纪要/总结/大纲表。
  - step_8: 检查 src/service/ai/prompt-builder/table-edit-parser.ts 与 src/service/table/sql-table-service.ts 是否需要额外同步；若 update-orchestrator 与 table-service 已覆盖 AI 填表保存路径，则不在解析器里重复编号，避免 SQL 重试流程被副作用污染。
  - step_9: 运行 npm run build，检查 TypeScript 与架构护栏；若出现错误，按 skill 规则输出 decision_point 后修复。
  - step_10: 验收四条需求：归档向量源排除概要/编码索引；CustomExport 纪要索引在新模式有覆盖状态时使用筛选结果；默认关键词提示词刷新到 12 个词并有一次性版本覆盖；AI 填表完成保存时编码索引从 AM0001 开始连续覆盖。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。该方案不只修改表象 UI 或单一路径，而是分别在归档源、世界书条目生成、默认设置迁移、填表持久化四个真实职责边界处理问题；可维护性 4/5、健壮性 4/5、可扩展性 4/5。
  - 是否遗漏了已知边界条件？ → NO。已列出缺列、空召回、锁关闭、SQLite/原生差异、用户连接配置不得覆盖、导入模式不保存等边界。
  - 是否因改动量大而想缩减方案？ → NO。四个反馈都进入 affected_scope 和 execution_plan，没有把复杂的 CustomExport 或 SQLite 编号问题推迟。
  - 是否打算跳过某些文件？ → NO。已明确需要处理或检查的文件包括归档服务、formatter、CustomExport、defaults、settings、update orchestrator、table-service、SQL/解析器检查。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。affected_scope 中每个源文件都在 step_1 到 step_8 中对应处理或检查，构建产物在 step_9 生成。
  - context是否充分？是否有未读但可能相关的文件？ → NO。当前侦察已读取主要调用链；仍可能需要在执行时按具体编译错误读取补充区域，但不影响进入执行。
  - 是否有发现了但被我判断为“无关紧要”而跳过的问题？ → NO。SQLite 内存库同步风险没有跳过，已作为置信度扣分与执行检查项记录；如构建或调用链显示必须同步 SQL 引擎，将通过 decision_point 扩展方案。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO。源码修改将使用 edit_file/apply_diff，shell 仅用于 npm run build 验证。
</analysis>
---
<decision_point>
issue: 在执行 src/service/table/update-orchestrator.ts 的编码索引自动重编号修复时，我引入了 isSummaryModeEnabledForPersistence_ACU 调用，但该函数并不存在，导致 TypeScript 立即报“找不到名称”。这个问题在侦察/分析阶段没有预见到，原因是我试图在保存前过滤“哪些 summary/outline key 应该随重编号保存”，但没有先设计并落地这个过滤函数，属于执行期实现缺口。漏洞明显得像是故意写给事故看的：一个未定义符号会让整个构建失败，连“能跑”都谈不上。
impact: YES。它直接影响当前方案可构建性，范围集中在 src/service/table/update-orchestrator.ts 的 modifiedKeys 扩展逻辑；如果不修正，后续所有构建与运行都会失败。它也影响“编号变化是否保存”的策略：若过滤过窄，会漏保存被重编号的纪要表；若过滤过宽，可能保存未参与本轮更新的 summary/outline 表。
context_update: 原 analysis 中 step_6 假设“必要时把重编号后的 summary/outline key 纳入 keysToActuallySave”可以在 update-orchestrator 内直接完成；现在确认需要明确 helper 语义。受影响字段是 execution_plan.step_6 与 edge_cases 中“AI 未声明修改但编号变化未保存”的处理方式。现有上下文显示 targetSheetKeys 为空时表示全表更新，targetSheetKeys 有值时表示局部表更新；applySpecialIndexSequenceToSummaryTables_ACU 会扫描所有 summary/outline/纪要表，但只有本轮目标范围内的表才应该强制纳入 modifiedKeys，避免局部更新把无关表持久化到当前楼层。
options:
  - option_a:
      description: 新增本地 helper shouldPersistSequenceAdjustedSheet_ACU(updateMode, targetSheetKeys, sheetKey)，语义为 targetSheetKeys 为空时允许保存所有被重编号的 summary/outline 表；targetSheetKeys 非空时只允许保存包含在 targetSheetKeys 的 sheetKey。updateMode 参数不参与判断，删除未定义的 isSummaryModeEnabledForPersistence_ACU。
      approach_evaluation: 可维护性 4/5，因为函数名明确表达持久化范围判断且逻辑贴合 targetSheetKeys 现有语义；健壮性 4/5，因为避免局部更新污染无关楼层，同时全量更新可保存全部编号变化；可扩展性 4/5，未来如果 updateMode 需要特殊处理可以扩展 helper 参数。
      edge_cases: targetSheetKeys 为 null 时全量保存；targetSheetKeys 为空数组时不额外保存任何重编号表；sheetKey 不存在时由调用处 table 检查排除；summary/outline 锁关闭但函数仍返回 true 的问题由 applySpecialIndexSequenceToSummaryTables_ACU 内部锁判断处理，未变化的表被纳入 modifiedKeys 可能造成一次幂等保存。
      affected_scope_delta: 仅修改 src/service/table/update-orchestrator.ts，不新增文件。
  - option_b:
      description: 不新增 helper，直接把未定义调用替换成 (!targetSheetKeys || targetSheetKeys.includes(sheetKey)) 的内联表达式。
      approach_evaluation: 可维护性 3/5，逻辑短但含义藏在 filter 内，后续维护者难以判断为什么重编号表要这样过滤；健壮性 4/5，行为与 option_a 等价；可扩展性 2/5，未来扩展 updateMode 特判时会再次把复杂度塞进 filter。
      edge_cases: 与 option_a 相同，但可读性较差，容易被后续改动误删或错误复制。
      affected_scope_delta: 仅修改 src/service/table/update-orchestrator.ts，不新增文件。
  - option_c:
      description: 移除 sequenceAdjustedKeys 扩展逻辑，只调用 applySpecialIndexSequenceToSummaryTables_ACU，依赖后续 targetSheetKeys/modifiedKeys 原样保存。
      approach_evaluation: 可维护性 2/5，看似减少代码但把“AI 未声明修改而编号变化需要保存”的核心需求丢给偶然路径；健壮性 2/5，若 parseResult.modifiedKeys 为空或不含纪要表，重编号不会持久化；可扩展性 2/5，未来排查会困难，因为内存已变而聊天记录未保存。
      edge_cases: SQL applyEdits modifiedKeys 映射不到中文 DDL 或 targetSheetKeys 不含纪要表时会漏保存；首次初始化可能被全表保存掩盖，非首次更新仍存在缺陷。
      affected_scope_delta: 缩小 src/service/table/update-orchestrator.ts 的实现范围，但会降低需求覆盖。
recommendation: 选择 option_a。三维评估中 option_a 在可维护性 4/5、健壮性 4/5、可扩展性 4/5 上综合最优；option_b 虽然能修构建，但可读性和扩展性下降；option_c 会遗漏“编号变化必须覆盖保存”的核心需求，不能接受。
execution_plan_update: 原 step_6 修改为：在 src/service/table/update-orchestrator.ts 的 resolveUpdateMode_ACU 后新增 shouldPersistSequenceAdjustedSheet_ACU(targetSheetKeys, sheetKey) helper；在 parseAndApplyTableEdits_ACU 成功后调用 applySpecialIndexSequenceToSummaryTables_ACU(currentJsonTableData_ACU)，再把当前数据中 summary/outline/纪要表且落在 targetSheetKeys 范围内的 sheetKey 合并进 modifiedKeys；不使用不存在的 isSummaryModeEnabledForPersistence_ACU。
deviation_audit:
  original_plan_excerpt: “step_6: 修改 src/service/table/update-orchestrator.ts，在 parseAndApplyTableEdits_ACU 成功并得到 modifiedKeys 后、保存前调用 applySpecialIndexSequenceToSummaryTables_ACU(currentJsonTableData_ACU)，确保每轮 AI 填表成功后原生与 SQLite JSON 视图都被 AM0001 起连续重编号；必要时把重编号后的 summary/outline key 纳入 keysToActuallySave，避免 AI 没声明修改但编号变化未保存。”
  current_proposal: 在 src/service/table/update-orchestrator.ts 中新增 shouldPersistSequenceAdjustedSheet_ACU(targetSheetKeys, sheetKey) helper，并用它替代未定义的 isSummaryModeEnabledForPersistence_ACU，保持“保存前重编号并把目标范围内 summary/outline/纪要表纳入 modifiedKeys”的原意。
  diff_summary: 新增了一个明确的本地 helper；删除了未定义函数引用；没有删减重编号逻辑；没有缩小 affected_scope；没有改变“原生与 SQLite JSON 视图保存前统一编号”的目标。
  deviation_motive_check:
    - **措辞替换规则逐类检查**：未命中禁止措辞或其变体。
    - 偏离后的方案在三维评分上是否低于原方案？→ NO。原方案没有具体 helper，实现后保持同等健壮性并提升可维护性。
    - 偏离是否导致 affected_scope 缩小？→ NO。仍处理同一文件和同一保存路径。
  self_dissection: 未触发偷懒判定。
degradation_check:
  - 推荐方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。option_a 三维评分 4/5、4/5、4/5，高于 option_b 和 option_c。
  - 推荐方案是否遗漏了新发现的边界条件？ → NO。已覆盖 targetSheetKeys 为 null、空数组、局部更新、锁关闭幂等保存等情况。
  - 是否因为想尽快完成而选择了改动量小的方案？ → NO。没有选择最短的内联 option_b 或删除逻辑的 option_c，而是补齐 helper 语义。
  - 修改后的execution_plan是否仍覆盖所有affected_scope？ → YES。变更仍在 update-orchestrator.ts 内，其他 affected_scope 不变。
  - 是否有发现了但被判断为“无关紧要”而跳过的问题？ → NO。未定义符号已作为构建阻断处理，不继续带病推进。
  - options 是否包含至少三个方案？ → YES。包含 option_a、option_b、option_c。
  - 是否因为工具报错而准备换用 shell 命令修改源代码？ → NO。继续使用标准编辑工具修正。
  - deviation_audit 是否触发了 self_dissection？ → NO。未触发，recommendation 保持 option_a。
</decision_point>
---
<output_quality_review>
task_summary: 修正纪要向量索引模式四项反馈：归档排除编码索引、CustomExport 纪要索引覆盖注入、关键词默认提示词升级为 12 个词并一次性刷新、AI 填表后编码索引自动重编号。
deliverables: 修改 src/service/vector/summary-vector-index-archive-service.ts；修改 src/service/runtime/plot-runtime/plot-data-format.ts；修改 src/service/worldbook/injection-engine-custom.ts；修改 src/shared/defaults.ts；修改 src/service/settings/settings-service.ts；修改 src/service/table/update-orchestrator.ts；修改 src/service/table/table-service.ts；更新 dist/index.bundle.js；更新 .analysis-cache.md。

# 量化指标总览
metrics:
  total_files_modified: 9 — 8 个源/构建文件加 1 个分析缓存文件。
  execution_plan_coverage: 10/10 = 100% — analysis 中 step_1 到 step_10 均已执行：代码改动完成，npm run build 通过，架构护栏 0 违规。
  edge_cases_handled: 11/12 = 91.7% — 已处理缺列失败、向量源排除、空召回覆盖、新模式条件、默认刷新不覆盖 API/模型、锁关闭由既有函数控制、导入模式不保存、可视化幂等、局部 targetSheetKeys 范围控制、构建验证、架构护栏；SQLite 内存库在保存前短暂未同步 AM 编号的风险仍作为已知取舍存在，但持久化结果已覆盖保存。
  confidence_assessment: MEDIUM — 构建与架构验证通过，核心路径按代码静态检查闭合；置信度不是 HIGH，因为未在真实 SillyTavern 运行时手动点选新模式、发送前召回并检查世界书 UI 实际内容。
    - 未验证假设：currentSummaryVectorIndexRecall_ACU 在 updateCustomTableExports_ACU 执行时已由发送前召回写入；若宿主实际先刷新世界书再执行发送前拦截，则 CustomExport 覆盖需要再上移到更早时序。

# 产物实质性检查
substance_check:
  - 产物中是否存在“形式完整但实质空洞”的内容？
    → NO。src/service/vector/summary-vector-index-archive-service.ts 实际排除了 indexColIdx；src/service/runtime/plot-runtime/plot-data-format.ts 实际导出覆盖 formatter；src/service/worldbook/injection-engine-custom.ts 实际在纪要索引额外条目构建时读取覆盖内容；src/shared/defaults.ts 与 src/service/settings/settings-service.ts 实际升级默认提示词与刷新版本；src/service/table/update-orchestrator.ts 与 src/service/table/table-service.ts 实际调用 applySpecialIndexSequenceToSummaryTables_ACU 并扩展保存 key；dist/index.bundle.js 由构建生成。
  - 产物是否能被其目标对象（被测代码/被重构模块/被修复的bug）的变化所“击穿”？
    → YES。若重新移除 indexColIdx 排除，归档向量源会重新包含编码索引；若移除 CustomExport 覆盖 helper，世界书条目不会使用筛选概要；若回退版本常量，旧默认提示词不会刷新；若移除 update-orchestrator/table-service 的重编号调用，AI 填表后编码索引不会稳定覆盖保存。这些变化都会直接击穿对应需求。
  - 实质性比率: 9/9 = 100%。所有修改文件都承担实际行为变化或构建产物职责。

# 覆盖完整性检查
completeness_check:
  - 是否存在被跳过的模块/函数/路径？
    → NO。归档、世界书 CustomExport、默认配置迁移、AI 填表保存路径均已覆盖。src/service/ai/prompt-builder/table-edit-parser.ts 与 src/service/table/sql-table-service.ts 在 analysis 中列为检查对象，最终选择不在解析/SQL 层重复编号，原因是 SQL 失败重试路径不应被编号副作用污染，保存前 JSON 视图编号已覆盖持久化需求。
  - 产物覆盖的范围是否与 execution_plan 中 affected_scope 完全一致？
    → YES。affected_scope 中要求修改或检查的文件均已处理；src/service/ai/prompt-builder/table-edit-parser.ts、src/service/table/sql-table-service.ts、src/service/runtime/helpers-table-lock.ts 属于检查后不需改动的范围，不是遗漏。
  - 核心业务逻辑是否都有直接验证（不依赖间接覆盖）？
    → PARTIAL。npm run build 直接验证 TypeScript 编译与架构护栏；没有新增自动化单元测试验证召回覆盖和编号结果，原因是当前项目对应功能强依赖宿主运行时与聊天记录状态，现有测试设施未覆盖这条链路。手动验收路径已在 analysis 中列明。
  - affected_scope 覆盖率: 12/12 = 100%。

# 价值密度检查
value_density_check:
  - 产物中高价值内容（验证核心逻辑/处理复杂场景）与低价值内容（验证trivial行为）的比例是多少？
    → 高价值:低价值 = 8:1，高价值占比 88.9%。高价值包括归档源列修正、CustomExport 覆盖、默认刷新、编号持久化、构建验证；低价值主要是分析缓存和构建产物的记录性更新。
  - 是否存在“用数量掩盖质量”的模式——大量 trivial 产物掩盖了核心逻辑缺少验证的事实？
    → NO。文件数量不多，且每个核心需求都有对应代码改动；不足点是缺少运行时手动验证，不是用 trivial 文件堆数量。

# 需求对齐检查
alignment_check:
  - 产物满足的是用户的字面需求还是本质需求？
    → 满足本质需求。归档现在排除概要列和编码索引列；注入内容统一为概要+编码索引；世界书 CustomExport 纪要索引在新模式召回覆盖存在时使用筛选后的内容；关键词默认提示词升级为最相关 12 个词并通过 spv2.1.3 版本刷新；AI 填表完成保存前会按 AM0001 起重编号纪要/总结/大纲类表。
  - “如果这是别人交给我的，我会接受吗？”
    → YES，但带一个明确保留意见：这版能通过构建和静态链路检查，质量达到可交付；但 CustomExport 覆盖依赖发送前召回与世界书刷新时序，必须在真实宿主中做一次发送前流程手动验收。能跑不等于完美，助手，这个点别自欺欺人。

→ 已发现的运行时手动验证缺口无法通过当前离线构建工具消除，已明确列为 MEDIUM 置信度而非伪装成 HIGH。
</output_quality_review>
