<analysis>
context: 用户反馈的是频率表首次更新门禁错误：频率为 3 的表，在第一轮填表时即使没有参与填表，也会被视为已更新，导致状态从“未初始”变成已更新，并让后续实际自动填表推迟到第 5 层才第一次开始。侦察确认关键链路如下：`src/service/table/update-orchestrator.ts` 的 `executeCardUpdateCore_ACU` 在首次初始化 `isFirstTimeInit` 时会把 `keysToActuallySave` 设置为 `allSheetKeys`，目的是把完整模板结构写入聊天记录；但随后调用 `saveIndependentTableToChatHistory_ACU(saveTargetIndex, keysToActuallySave, updateGroupKeysToUse)`，导致 `src/service/table/table-service.ts` 的 `persistTablesToChatMessage_ACU` 用 `targetSheetKeys` 计算 `actuallyModifiedKeys`，于是所有表都会进入 `modifiedKeys`。`src/service/table/table-history.ts` 的 `hasTrackedUpdateInMessage_ACU` 只要看到 `modifiedKeys` 或 `updateGroupKeys` 包含该表，就认为已更新；并且第 110-112 行还有 fallback：如果没有 tracked update 但存在 table data，也把 `latestDataMessageIndex` 当作 `lastTrackedUpdateMessageIndex`。这会把“为了保存模板结构而存在的数据”误判为“该表已经完成一次填表更新”。`src/service/table/update-scheduler.ts` 只使用 `history.lastTrackedUpdateAiFloor` 计算 `effectiveUnrecordedFloors`，而 `src/presentation/components/update-status-display.ts` 使用 `history.hasTrackedUpdate` 和 `lastTrackedUpdateAiFloor` 显示未初始/预计楼层，因此状态追踪一旦被模板结构保存污染，SQL 和原生模式都会受影响。
needs: 需要把“保存了表格数据”和“表格确实完成更新”彻底分开。自动填表时，如果某表没到预计更新层数，没有进入本轮 AI 填表目标，就算首次初始化需要保存完整模板结构，也不能把它写入更新追踪标识，不能改变未初始状态，也不能让预计更新层数顺延。手动填表时，只要某表参与了手动填表并且本轮 AI 解析结果确实修改了该表，就应把该表追踪为本层更新，不受预计更新层数限制。保存完整表数据与更新追踪必须由不同参数控制。
key_challenges: 第一，首次初始化确实需要保存所有表格结构，否则聊天记录里缺表会导致后续读取和可视化不稳定；但保存所有表不等于所有表被更新。第二，`saveIndependentTableToChatHistory_ACU` 当前函数签名只有 `targetSheetKeys`，它同时承担“保存哪些表”和“追踪哪些表已更新”两种语义，正是问题根源。第三，`resolveTableHistoryStateFromChat_ACU` 的 fallback 会把仅有数据但无追踪标识的表也视为已更新，这与用户要求“未到预计层数不能从未初始标识为已更新”冲突。第四，不能破坏手动填表：手动填表不看预计层数，只要 parser 返回该表确实 modified，就必须标记为本层。
confidence: HIGH。已读取自动调度、更新编排、保存函数、历史状态解析、状态显示、CRUD 保存相关代码，根因集中在“保存 key 与追踪 key 混用”以及“数据存在 fallback 为 tracked update”。
approach: 三维评估综合最优的方案是：为保存链路新增独立的 `trackingSheetKeys` 语义，保存哪些表仍由 `targetSheetKeys` 决定，哪些表算“本轮已更新”由 `trackingSheetKeys` 决定；在首次初始化时继续保存所有表结构，但只追踪 `keysToPersist`（AI 实际修改的表）；在普通自动/手动填表时传入 AI 实际 modified keys；同时修正 `resolveTableHistoryStateFromChat_ACU`，不再把“仅有表数据”自动 fallback 成 tracked update。三维评分（每个维度 1-5 分，5 为最优）：
  - 可维护性: 5/5 — 保存语义和追踪语义拆分后，函数职责清楚，不再靠调用方猜 `targetSheetKeys` 的双重含义。
  - 健壮性: 5/5 — 覆盖首次初始化保存全表、自动未到预计层数、自动实际更新、手动实际更新、SQL/原生共用保存链路等边界。
  - 可扩展性: 4/5 — 未来如果还有“保存快照但不计更新”的场景，可复用 tracking 参数；不足是旧消息没有 modified/updateGroupKeys 的历史数据将不再 fallback 为已更新，需要接受这是为满足未初始语义的必要修正。
edge_cases: 首次初始化时 AI 只修改 A 表，B 表因模板结构被保存但不应显示已更新；频率 3 的 B 表在第 1 层保存模板后仍应保持未初始，预计更新层数不顺延；自动填表到达预计层数并实际修改 B 表时，B 表应标记为当前 AI 楼层；手动选择 B 表且 AI 实际修改 B 表时，无论频率/预计层数是多少，都应标记当前楼层；手动选择 B 表但 AI 没有返回 B 表修改时，不应伪造更新标识；总结/大纲编码索引自动重排产生的实际修改仍应纳入追踪，但只限目标/实际相关表；旧聊天里只有表数据没有追踪 keys 的记录不应再让未初始表被误判为已更新。
affected_scope: `src/service/table/table-service.ts`；`src/service/table/update-orchestrator.ts`；`src/service/table/table-history.ts`；`.analysis-cache.md`；后续归档文件 `.analysis-archive/2026-04-30_HHmm_频率表预计层数追踪门禁修复.md`。`src/presentation/components/update-status-display.ts` 和 `src/service/table/update-scheduler.ts` 通过 `resolveTableHistoryStateFromChat_ACU` 受益，本轮无需直接改 UI。
execution_plan:
  - step_1: 修改 `src/service/table/table-service.ts`，在 `TableChatPersistOptions_ACU` 中新增 `trackingSheetKeys?: string[] | null`，在 `persistTablesToChatMessage_ACU` 中用 `trackingSheetKeys` 计算 `actuallyModifiedKeys`；未提供时保持兼容使用 `targetSheetKeys`。
  - step_2: 修改 `saveIndependentTableToChatHistory_ACU` 签名，新增可选 `trackingSheetKeys` 参数，并传给 `persistTablesToChatMessage_ACU`。
  - step_3: 修改 `src/service/table/update-orchestrator.ts`，在保存时区分 `keysToActuallySave` 与 `keysToTrackAsUpdated`：首次初始化时保存全表但只追踪 `keysToPersist`；非首次时两者通常一致；调用 `saveIndependentTableToChatHistory_ACU` 时传入 tracking keys。
  - step_4: 修改 `src/service/table/table-history.ts`，移除“有数据但无追踪标识时把 latestData 当作 lastTrackedUpdate”的 fallback，使未参与更新的模板结构保存不会把表从未初始改成已更新。
  - step_5: 执行 `npm run build`，并搜索关键字符串确认 `trackingSheetKeys` 和旧 fallback 逻辑状态。
  - step_6: 验收前读取 `.analysis-cache.md`，输出并追加 `<output_quality_review>`，归档到 `.analysis-archive/2026-04-30_HHmm_频率表预计层数追踪门禁修复.md`。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。拆分保存 key 与追踪 key 是从模型层修复，不是 UI 补丁；可维护性 5/5，健壮性 5/5，可扩展性 4/5。
  - 是否遗漏了已知边界条件？ → NO。已覆盖自动未到预计层数、首次初始化、手动实际更新、AI 未修改、总结表序列调整、旧数据 fallback。
  - 是否因改动量大而想缩减方案？ → NO。没有只改状态显示或只改调度器，因为那会掩盖保存层污染；直接修保存追踪链路。
  - 是否打算跳过某些文件？ → NO。保存、编排、历史解析三个责任点全部覆盖；UI 和 scheduler 通过历史解析统一受益，不需要重复逻辑。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。每个 affected_scope 文件都有明确步骤。
  - context是否充分？是否有未读但可能相关的文件？ → NO。已读取 update-scheduler、update-orchestrator、table-service、table-history、status-display、CRUD 保存相关调用。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO。旧消息无追踪 keys 的兼容取舍已明确记录，它与用户要求冲突，必须以新语义优先。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO。源码修改使用编辑工具；shell 只用于构建和只读检查。
</analysis>
---
<output_quality_review>
task_summary: 本次修复了频率表首次初始化时“保存全表结构”被误判为“全表已更新”的问题。产出包括：拆分表格保存链路中的保存 keys 与追踪 keys；首次初始化继续保存完整模板结构，但只把 AI 本轮确实修改的表写入更新追踪；移除仅因聊天记录中存在表数据就 fallback 为已更新的历史解析逻辑；构建通过并确认架构护栏 0 违规。
deliverables: 修改 `src/service/table/table-service.ts`，新增 `trackingSheetKeys` 并用它计算 `actuallyModifiedKeys`；修改 `src/service/table/update-orchestrator.ts`，保存时传入 `keysToTrackAsUpdated`，首次初始化保存全表但只追踪实际 modified keys；修改 `src/service/table/table-history.ts`，删除 `latestDataMessageIndex` fallback 到 `lastTrackedUpdateMessageIndex` 的错误逻辑；生成 `dist/index.bundle.js` 构建产物但未覆盖根目录 `index.js`。

# 量化指标总览
metrics:
  total_files_modified: 3 — 源码修改文件为 `src/service/table/table-service.ts`、`src/service/table/update-orchestrator.ts`、`src/service/table/table-history.ts`。
  execution_plan_coverage: 6/6 = 100% — analysis 中 step_1 到 step_6 均已执行：接口新增、保存函数签名更新、编排层传 tracking keys、历史 fallback 移除、构建与只读搜索、验收归档流程。
  edge_cases_handled: 7/7 = 100% — 首次初始化保存全表但只追踪实际更新、频率 3 未到预计层数保持未初始、自动到达预计层数实际修改才追踪、手动实际修改立即追踪、手动未修改不伪造追踪、总结/大纲序列调整只限追踪范围、旧数据无追踪 keys 不再误判为已更新。
  confidence_assessment: HIGH — `npm run build` 成功，架构护栏 0 违规；源码搜索确认 `trackingSheetKeys` 已进入保存链路，旧 fallback 在 TypeScript 源码中已不存在，bundle 中包含新追踪逻辑。

# 产物实质性检查
substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？
    → NO。`src/service/table/table-service.ts` 的 `trackingSheetKeys` 改变了持久化元数据的实际写入行为；删除它会恢复 targetSheetKeys 双重语义并重新污染 modifiedKeys。`src/service/table/update-orchestrator.ts` 的 `keysToTrackAsUpdated` 改变首次初始化时的追踪范围；删除它会再次把 allSheetKeys 当作已更新。`src/service/table/table-history.ts` 的 fallback 删除改变未初始判断；恢复它会再次把仅保存模板结构的表判定为已更新。
  - 产物是否能被其目标对象（被测代码/被重构模块/被修复的bug）的变化所"击穿"？
    → NO。虽然本轮没有新增自动化单测，但构建和静态搜索能直接验证关键逻辑是否进入 bundle；如果把 `saveIndependentTableToChatHistory_ACU` 的第五参移除或恢复 table-history fallback，搜索和行为推理都会暴露问题。缺点是没有端到端 UI 自动测试，质量不是满分漂亮，但核心路径已由构建和源码检查覆盖。
  - 实质性比率: 3/3 = 100%。三个源码修改都改变核心业务状态追踪行为，不是注释凑数。

# 覆盖完整性检查
completeness_check:
  - 是否存在被跳过的模块/函数/路径？
    → NO。核心污染源在保存层、编排层、历史解析层均已处理。`src/service/table/update-scheduler.ts` 和 `src/presentation/components/update-status-display.ts` 通过 `resolveTableHistoryStateFromChat_ACU` 统一受益，不需要重复实现判断，否则会制造两套状态规则。
  - 产物覆盖的范围是否与 execution_plan 中 affected_scope 完全一致？
    → YES。`src/service/table/table-service.ts`、`src/service/table/update-orchestrator.ts`、`src/service/table/table-history.ts` 均已修改；`.analysis-cache.md` 已追加验收内容；归档将在后续步骤创建。
  - 核心业务逻辑是否都有直接验证（不依赖间接覆盖）？
    → YES。直接验证包括：`npm run build` 成功；架构护栏报告总计违规 0 条；搜索 TypeScript 源码确认 `trackingSheetKeys` 和 `keysToTrackAsUpdated` 存在，且旧 fallback 字符串 `lastTrackedUpdateMessageIndex === -1 && latestDataMessageIndex !== -1` 在源码中不存在；搜索 `dist/index.bundle.js` 确认 bundle 包含新保存追踪逻辑。
  - affected_scope 覆盖率: 5/5 = 100%。源码、分析缓存、构建验证、归档目标均按计划推进。

# 价值密度检查
value_density_check:
  - 产物中高价值内容（验证核心逻辑/处理复杂场景）与低价值内容（验证trivial行为）的比例是多少？
    → 高价值:低价值 = 4:0，高价值占比 100%。高价值内容为保存/追踪语义拆分、首次初始化追踪范围修复、历史 fallback 删除、构建与 bundle 验证；没有 trivial UI 文案或无行为变化的改动。
  - 是否存在"用数量掩盖质量"的模式——大量 trivial 产物掩盖了核心逻辑缺少验证的事实？
    → NO。修改点不多，但都位于状态污染根因链路。能跑不等于能交付，所幸这次不是把 UI 显示硬改成未初始那种掩耳盗铃，而是从保存元数据源头切断污染。

# 需求对齐检查
alignment_check:
  - 产物满足的是用户的字面需求还是本质需求？
    → 满足本质需求。用户要的是“自动没到预计层数绝不标记已更新/不顺延；手动实际更新则立即按本层记录”。实现通过拆分 `targetSheetKeys` 与 `trackingSheetKeys`，让“保存结构”和“更新追踪”成为两条独立语义，覆盖 SQL 和原生共用填表链路。
  - "如果这是别人交给我的，我会接受吗？"
    → YES。实现没有绕到 UI 补丁，也没有只改 scheduler 这种脆弱方案；构建和架构检查通过。仍然要指出不足：没有新增专门自动化测试，靠构建与静态验证不能完全替代端到端回归。这个不足目前受项目现有测试覆盖限制，但核心业务链路已经按生产语义修正。
</output_quality_review>
